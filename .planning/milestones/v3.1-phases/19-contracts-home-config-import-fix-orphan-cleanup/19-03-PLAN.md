---
phase: 19-contracts-home-config-import-fix-orphan-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/run/no-leak.ts
  - knip.jsonc
  - .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
  - .dependency-cruiser.cjs
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "src/run/no-leak.ts is deleted (genuine dead code: no production export, no src/ importer)"
    - "knip.jsonc no longer ignores src/run/no-leak.ts, and its explanatory comment block is removed"
    - "pnpm run knip reports zero orphan modules and warns about no unmatched ignore pattern"
    - "src/run/no-leak.test.ts (the real T-11-09 redaction contract) is retained and still passes"
    - "The conventions skill §5 + depcruise preset comment name src/types/ as the leaf contracts band (documentation only — no fence enforcement turned on)"
  artifacts:
    - path: "knip.jsonc"
      provides: "knip config with no lingering ignore for a deleted file"
  key_links:
    - from: "src/run/no-leak.test.ts"
      to: "src/logging/create-logger.ts + src/run/summary.ts"
      via: "the redaction contract (REDACT_PATHS, toCompactSummary) the test actually guards — survives deletion of the doc module"
      pattern: "REDACT_PATHS|toCompactSummary"
---

<objective>
Resolve the `no-leak.ts` orphan (ARCH-03). `src/run/no-leak.ts` exports only a doc-type `NoLeakSurface` and is imported by nothing in `src/` — it is knip-green ONLY because `knip.jsonc:16` ignores it [src: knip.jsonc#L16]; drop the ignore and knip immediately reports it as `Unused files (1)` [src: 19-RESEARCH.md#Summary]. Remove the dead module AND its knip ignore in the same change, keeping `no-leak.test.ts` (the real T-11-09 cross-surface redaction contract). Also refresh the now-stale conventions-skill §5 wording and the depcruise preset comment to name `src/types/` as the leaf contracts band — documentation only, NO band-fence enforcement (that is Phase 23 / ARCH-06).

Purpose: Zero orphans is ARCH-03's literal success oracle (`pnpm run knip`). The skill/preset wording update closes the code↔skill gap this milestone exists to fix.
Output: deleted `no-leak.ts`, cleaned `knip.jsonc`, refreshed conventions §5 + depcruise comment.
</objective>

<artifacts_this_phase_produces>
This plan REMOVES a file rather than adding one. Net artifact changes:
- DELETED: `src/run/no-leak.ts` (dead doc module; only export was the `NoLeakSurface` doc-type)
- MODIFIED: `knip.jsonc` (ignore entry + its comment block removed)
- MODIFIED (documentation only): `.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md` §5, `.dependency-cruiser.cjs` comment — encode `src/types/` as the leaf contracts band; NO `forbidden` fence added/enabled.
- RETAINED (not touched): `src/run/no-leak.test.ts` — the executable T-11-09 contract.
</artifacts_this_phase_produces>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-contracts-home-config-import-fix-orphan-cleanup/19-RESEARCH.md
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# The orphan, its config ignore, and the test that stays:
@src/run/no-leak.ts
@knip.jsonc
</context>

<premises>
premises:
  - claim: no-leak.ts exports only the doc-type NoLeakSurface and no production symbol
    src: src/run/no-leak.ts#L23
    verify: grep -n 'export' src/run/no-leak.ts
  - claim: no-leak.ts is imported by NOTHING in src/ except the test and comment references — genuine orphan
    src: 19-RESEARCH.md#Summary
    verify: grep -rln "no-leak" src/ | grep -v 'no-leak.test.ts' | grep -v 'no-leak.ts'
  - claim: knip is green ONLY because knip.jsonc ignores src/run/no-leak.ts; dropping the ignore makes knip report Unused files (1)
    src: knip.jsonc#L16
    verify: grep -n 'no-leak' knip.jsonc
  - claim: the real redaction contract lives in create-logger.ts (REDACT_PATHS) + summary.ts (toCompactSummary) and is asserted by no-leak.test.ts — independent of the deleted doc module
    src: src/run/no-leak.ts#L11-17
    verify: grep -rln 'REDACT_PATHS\|toCompactSummary' src/
  - claim: conventions skill §5 wording is stale — it says "Today RunSummary is in run/types.ts" which is already fixed (RunSummary lives in src/types/run-summary.ts)
    src: .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md#L149-154
    verify: sed -n '149,154p' .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
  - claim: .dependency-cruiser.cjs has no band-fences today (only no-circular + no-orphans) — band-fences are Phase 23 / ARCH-06, NOT this phase
    src: .dependency-cruiser.cjs#L3
    verify: grep -c 'downward-only\|band-skip\|write-scope\|no-parser' .dependency-cruiser.cjs
carried_forward:
  - claim: verify the file is actually deleted on disk before trusting the executor summary (v3.0 fabrication incident)
    src: STATE.md#Blockers-Concerns
</premises>

<tasks>

<task type="auto">
  <name>Task 1: Justify removal vs wiring against the in-code comment, then delete no-leak.ts and drop its knip ignore</name>
  <files>src/run/no-leak.ts, knip.jsonc</files>
  <read_first>
    - src/run/no-leak.ts (the whole file — note its head-comment claims it is the "production companion" and the knip comment claims it "is intentionally kept; the export ... must not be removed"; this plan overrides that claim with live evidence)
    - knip.jsonc:11-16 (the ignore entry + its explanatory comment block to remove)
    - src/run/no-leak.test.ts (the T-11-09 contract that STAYS — read enough to confirm it asserts redaction behavior, not the NoLeakSurface doc-type)
  </read_first>
  <action>
**Wire-vs-remove decision (mandatory justification against the existing comment).** The CONTEXT grants discretion and prefers removal for genuine dead code [src: 19-CONTEXT.md#decisions]. The in-code comment in both `no-leak.ts` and `knip.jsonc` claims the file is "intentionally kept" and "must not be removed". Override that claim with live evidence, recorded in the SUMMARY:
- `no-leak.ts` exports a single doc-type `NoLeakSurface` and NO production symbol [src: src/run/no-leak.ts#L23].
- It is imported by nothing in `src/` (only its own test and comment prose reference the name) [src: 19-RESEARCH.md#Summary].
- The contract it *documents* — no secret/byte leak across the three output surfaces — is actually enforced by `create-logger.ts` `REDACT_PATHS`, `run-once.ts` `sanitizeSourceUrl`, and `summary.ts` `toCompactSummary`, and is *asserted* by `no-leak.test.ts` (T-11-09) [src: src/run/no-leak.ts#L11-17]. The doc module carries no executable contract; deleting it loses no guard.

Therefore: **remove**, not wire. (Wiring would mean importing a doc-type somewhere just to satisfy knip — unrequested surface with no consumer benefit.)

Steps:
1. Delete `src/run/no-leak.ts`.
2. In `knip.jsonc`: remove the `"ignore": ["src/run/no-leak.ts"]` entry AND its explanatory comment block (the PROG-04 / "intentionally kept" lines at `knip.jsonc:11-16` [src: knip.jsonc#L16]). Leave the rest of `knip.jsonc` (entry, project, `ignoreExportsUsedInFile`, and the unrelated 17-unused-exports comment block) untouched.
3. KEEP `src/run/no-leak.test.ts` — it asserts the redaction behavior and is independent of the deleted file. Confirm it does not import `no-leak.ts` (if it imports `NoLeakSurface`, repoint the test to inline the union locally or drop the unused import — the test's subject is the redaction code, not the doc-type). Verify the test still passes.

Confirm on disk that `src/run/no-leak.ts` no longer exists before trusting completion [src: STATE.md#Blockers-Concerns].
  </action>
  <verify>
    <automated>test ! -f src/run/no-leak.ts && pnpm run knip && pnpm test src/run/no-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/run/no-leak.ts` does not exist: `test ! -f src/run/no-leak.ts` exits 0
    - `knip.jsonc` no longer references no-leak: `grep -c 'no-leak' knip.jsonc` returns 0
    - `pnpm run knip` exits 0 with zero `Unused files` and no unmatched-ignore warning
    - `src/run/no-leak.test.ts` still exists and passes: `pnpm test src/run/no-leak.test.ts` exits 0
    - The redaction guards survive: `grep -rl 'REDACT_PATHS' src/logging/create-logger.ts` and `grep -rl 'toCompactSummary' src/run/summary.ts` both resolve
  </acceptance_criteria>
  <done>no-leak.ts deleted; knip ignore + comment removed; knip reports zero orphans; the T-11-09 redaction test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh conventions §5 + depcruise comment to name src/types/ as the leaf contracts band (documentation only)</name>
  <files>.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md, .dependency-cruiser.cjs</files>
  <read_first>
    - .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md:149-154 (§5 stale wording: "Today RunSummary is in run/types.ts and evidence/ imports it upward") and :88 (the Cross-cutting band-table cell that already names types/)
    - .dependency-cruiser.cjs:1-50 (the forbidden array — currently only no-circular + no-orphans; band-fences are NOT here yet)
  </read_first>
  <action>
Documentation-only update — encode `src/types/` as the leaf contracts band per the resolved pre-plan decision [src: 19-CONTEXT.md#decisions]. Do NOT add or enable any depcruise `forbidden` band-fence — ARCH-06 (Phase 23) owns enforcement [src: .dependency-cruiser.cjs#L3].

1. In `.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md` §5 (lines 149-154) [src: SKILL.md#L149-154]: replace the stale present-tense claim ("Today `RunSummary` is in `run/types.ts` and `evidence/s3-evidence-store.ts` imports it **upward** (a layer violation); moving the *type* to `types/` fixes it…") with text reflecting the completed move: `RunSummary`/`CompactRunSummary` and the four cross-band DTOs (`ReplayCandidate`, `RawReplayStorageEvidence`, `IngestStagingPayload`, plus `SourceTransport`) now live in `src/types/` (one file per contract, matching the `run-summary.ts` precedent; no barrel); the *builders* stay in their owning bands; each band `types.ts` keeps band-local types only and re-exports the moved contract downward via shim. Keep the §A band-table cell at :88 (already names `types/`) consistent — adjust its phrasing only if it still implies the move is pending. Do not change any other section.

2. In `.dependency-cruiser.cjs`: add a short comment (near the top of the `forbidden` array or a header block) noting that `src/types/` is the leaf cross-cutting contracts band — imported downward by any band, imports nothing upward — and that the eight five-band `forbidden` fences are deferred to Phase 23 (ARCH-06) and intentionally NOT enabled here. This is a comment ONLY; the `forbidden` rules array gains no new enforced rule in this phase.

Self-check: `grep -c 'downward-only\|band-skip\|write-scope\|no-parser' .dependency-cruiser.cjs` must still return 0 (no fence rule names added) — only a comment changed.
  </action>
  <verify>
    <automated>pnpm run depcruise && pnpm run knip && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md §5 no longer carries the stale present-tense claim that the RunSummary move is still pending: `grep -cF 'Today `RunSummary` is in' .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md` returns 0
    - SKILL.md §5 names the leaf contracts home: `grep -c 'types/' .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md` is non-zero
    - `.dependency-cruiser.cjs` gained NO new enforced fence: `grep -c 'downward-only\|band-skip\|write-scope\|no-parser' .dependency-cruiser.cjs` returns 0
    - `pnpm run depcruise` exits 0 (config still valid, no new rule firing)
    - `pnpm run knip` + `pnpm run typecheck` exit 0
  </acceptance_criteria>
  <done>§5 reflects the completed src/types/ move; depcruise comment names the leaf band; no fence enforcement turned on; gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletes a doc-only module with no runtime role and edits config-comment/skill prose. No data flow, input parsing, secret path, network call, or write scope is altered. The redaction guards (`REDACT_PATHS`, `sanitizeSourceUrl`, `toCompactSummary`) the deleted file *documented* remain in place and remain asserted by `no-leak.test.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information disclosure | deleting no-leak.ts weakening the secret/byte-leak guard | mitigate | The executable guard is `REDACT_PATHS` + `toCompactSummary` + `sanitizeSourceUrl`, NOT the deleted doc module; `no-leak.test.ts` (T-11-09) is retained and must pass post-deletion, proving the redaction contract still holds. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (zero external packages). Lockfile untouched. |

**Security verdict:** Removing a pure-documentation module and updating prose. No new external surface, no input-parsing change, no auth/secret/network change. The only security-adjacent file deleted carries no runtime role; its guarded contract stays enforced by the redaction code + `no-leak.test.ts`. No new high-severity threats.
</threat_model>

<verification>
- `test ! -f src/run/no-leak.ts` — file gone on disk.
- `pnpm run knip` — zero orphans, no unmatched-ignore warning (ARCH-03 oracle).
- `pnpm test src/run/no-leak.test.ts` — the T-11-09 redaction contract still green.
- `pnpm run depcruise` — config still valid; `grep -c 'downward-only\|band-skip\|write-scope\|no-parser' .dependency-cruiser.cjs` returns 0 (no fence enabled).
- Phase gate: `pnpm run verify` fully green + golden oracle green via `pnpm run test:integration`.
</verification>

<success_criteria>
- `no-leak.ts` is removed and `pnpm run knip` reports zero orphan modules with no lingering ignore.
- `no-leak.test.ts` is retained and passes — the redaction contract is intact.
- Conventions §5 + depcruise preset comment name `src/types/` as the leaf contracts band; no band-fence enforcement is turned on (deferred to Phase 23).
- `pnpm run verify` (incl. depcruise + knip) green.
</success_criteria>

<output>
Create `.planning/phases/19-contracts-home-config-import-fix-orphan-cleanup/19-03-SUMMARY.md` when done.
</output>
