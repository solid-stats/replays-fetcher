---
phase: 19-contracts-home-config-import-fix-orphan-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/types/source-transport.ts
  - src/discovery/types.ts
  - src/config.ts
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "SourceTransport is declared in src/types/source-transport.ts (leaf home)"
    - "config.ts imports SourceTransport from ./types/source-transport.js — never from ./discovery/types.js"
    - "config.ts imports nothing from any capability band (no ./discovery/, ./storage/, ./staging/ import)"
    - "discovery/types.ts re-exports SourceTransport downward so its existing internal/external references still resolve"
    - "pnpm run typecheck + depcruise + knip stay green; config parsing behavior unchanged"
  artifacts:
    - path: "src/types/source-transport.ts"
      provides: "SourceTransport literal union (\"direct\" | \"ssh\") cross-cutting home"
      contains: "SourceTransport"
  key_links:
    - from: "src/config.ts"
      to: "src/types/source-transport.ts"
      via: "import type { SourceTransport } from \"./types/source-transport.js\""
      pattern: "types/source-transport"
    - from: "src/discovery/types.ts"
      to: "src/types/source-transport.ts"
      via: "export type { SourceTransport } from \"../types/source-transport.js\" (downward re-export shim)"
      pattern: "types/source-transport"
---

<objective>
Remove the only currently-existing upward import in the codebase: `config.ts` (cross-cutting Band 5) imports `SourceTransport` from `discovery/types.js` (capability Band 3) [src: src/config.ts#L5]. Move `SourceTransport` — a self-contained literal union `"direct" | "ssh"` [src: src/discovery/types.ts#L16] — into `src/types/`, so `config.ts` depends on nothing upward (ARCH-02).

Purpose: This is THE upward import the milestone build order calls out as the only currently-existing one. Removing it means the leaf contracts band is correct before later phases.
Output: `src/types/source-transport.ts` + a downward re-export shim in `discovery/types.ts` + `config.ts` repointed.
</objective>

<artifacts_this_phase_produces>
| New file | Symbol moved in | Moved from |
|----------|-----------------|------------|
| `src/types/source-transport.ts` | `SourceTransport` (literal union `"direct" \| "ssh"`) | `src/discovery/types.ts:16` |

Modified-in-place: `src/config.ts` (import repointed `./discovery/types.js` → `./types/source-transport.js`), `src/discovery/types.ts` (downward shim added, original declaration removed).
</artifacts_this_phase_produces>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-contracts-home-config-import-fix-orphan-cleanup/19-RESEARCH.md
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# The shim precedent and the two files this plan touches:
@src/run/types.ts
@src/config.ts
@src/discovery/types.ts
</context>

<premises>
premises:
  - claim: config.ts imports SourceTransport upward from ./discovery/types.js at line 5, used at 193/198/218
    src: src/config.ts#L5
    verify: grep -n 'SourceTransport\|discovery/types' src/config.ts
  - claim: SourceTransport is a self-contained literal union with no cross-band reference, safe to move with zero back-import
    src: src/discovery/types.ts#L16
    verify: sed -n '16p' src/discovery/types.ts
  - claim: only config.ts and discovery/types.ts reference SourceTransport across src/ — small blast radius
    src: graph:source-config
    verify: grep -rln 'SourceTransport' src/
  - claim: SourceTransport is already `type` (not interface), so no MECH-01 interaction — move verbatim
    src: src/discovery/types.ts#L16
    verify: grep -n 'export type SourceTransport' src/discovery/types.ts
carried_forward:
  - claim: verify the file actually changed on disk before trusting the executor summary (v3.0 fabrication incident)
    src: STATE.md#Blockers-Concerns
</premises>

<tasks>

<task type="auto">
  <name>Task 1: Move SourceTransport into src/types/ and repoint config.ts</name>
  <files>src/types/source-transport.ts, src/discovery/types.ts, src/config.ts</files>
  <read_first>
    - src/discovery/types.ts:16 (the single-line literal union to move; line 18 ReplayCandidate is plan 01's concern and is already a downward shim by this wave)
    - src/config.ts:5,193,198,218 (the upward import at :5 and its three use sites)
    - src/run/types.ts (the downward re-export shim pattern to copy)
  </read_first>
  <action>
1. Create `src/types/source-transport.ts` containing the `SourceTransport` declaration moved verbatim from `src/discovery/types.ts:16` [src: src/discovery/types.ts#L16]: the literal union `export type SourceTransport = "direct" | "ssh";`. Keep it `type` (it is already `type`, no MECH-01 interaction).

2. In `src/discovery/types.ts`: DELETE the original `SourceTransport` declaration and add a downward re-export shim copying the `src/run/types.ts` form [src: src/run/types.ts#L1-11]: `export type { SourceTransport } from "../types/source-transport.js";`. Any reference to `SourceTransport` within `discovery/types.ts` itself (e.g. `SourceFetchOptions`/`SourceClient` fields, if any) still resolves through the re-export.

3. In `src/config.ts`: repoint line 5 from `import type { SourceTransport } from "./discovery/types.js";` to `import type { SourceTransport } from "./types/source-transport.js";` [src: src/config.ts#L5]. The three use sites (`:193`, `:198`, `:218` — `sourceTransportOrUndefined` + `readSourceConfigInput` + the `SourceConfigInput.sourceTransport` field type) are unchanged; only the import source moves. The `SOURCE_TRANSPORT` env var and its Zod parsing are untouched [src: 19-RESEARCH.md#Runtime-State-Inventory] — this is a pure type-import move, zero runtime change.

After this, `config.ts` has no import from any capability band — confirm with the acceptance grep.
  </action>
  <verify>
    <automated>pnpm run typecheck && pnpm run depcruise && pnpm run knip && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/source-transport.ts` exists and declares the literal union: `grep -c 'SourceTransport = "direct" | "ssh"' src/types/source-transport.ts` returns 1
    - `config.ts` no longer imports from discovery: `grep -c 'discovery/types' src/config.ts` returns 0
    - `config.ts` imports SourceTransport from the leaf home: `grep -c 'types/source-transport' src/config.ts` returns 1
    - `config.ts` imports nothing from any capability band: `grep -cE 'from "\./(discovery|storage|staging)/' src/config.ts` returns 0
    - `src/discovery/types.ts` re-exports SourceTransport downward and no longer declares it: `grep -c 'export type SourceTransport = ' src/discovery/types.ts` returns 0
    - `pnpm run typecheck` + `pnpm run depcruise` + `pnpm run knip` + `pnpm test` all exit 0
  </acceptance_criteria>
  <done>SourceTransport lives in src/types/source-transport.ts; config.ts depends on nothing upward; discovery shims it down; full gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Compile-time type-import relocation only. `SourceTransport` is a config *value type* (`"direct" \| "ssh"`); the `SOURCE_TRANSPORT` env var and its Zod parsing in `config.ts` are unchanged — only the type's import path moves. No data flow, secret path, or write scope is altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | config validation behavior changing during the import move | accept | Zod config schema and `SourceTransport` parsing are untouched; only the import source line changes. 100% V8 coverage + golden oracle assert config behavior is unchanged. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (zero external packages). Lockfile untouched. |

**Security verdict:** Pure compile-time type-import move in an ingest CLI. No new external surface, no input-parsing change, no auth/secret/network change. No new high-severity threats. Ingest-boundary invariants unchanged.
</threat_model>

<verification>
- `pnpm run typecheck` — config.ts and discovery references resolve through the new home/shim.
- `pnpm run depcruise` — no upward import remains (config.ts → leaf only).
- `pnpm run knip` — `source-transport.ts` is imported (no new orphan).
- `pnpm test` — config unit tests green (parsing behavior unchanged).
- Manual: `grep -E 'from "\./(discovery|storage|staging)/' src/config.ts` returns nothing.
- Phase gate: `pnpm run verify` fully green + golden oracle green via `pnpm run test:integration`.
</verification>

<success_criteria>
- `config.ts` no longer imports `SourceTransport` from `discovery/`; `config.ts` depends on nothing upward.
- `SourceTransport` lives in `src/types/source-transport.ts`; `discovery/types.ts` re-exports it downward.
- `pnpm run typecheck` + `depcruise` + `knip` + `pnpm test` all green; config parsing behavior byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-contracts-home-config-import-fix-orphan-cleanup/19-02-SUMMARY.md` when done.
</output>
