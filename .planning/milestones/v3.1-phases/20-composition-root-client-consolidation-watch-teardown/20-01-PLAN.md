---
phase: 20-composition-root-client-consolidation-watch-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/clients.test.ts
autonomous: true
requirements:
  - ARCH-04
user_setup: []

must_haves:
  truths:
    - "Grep proves exactly one `new S3Client(` and exactly one `new Pool(` in production src/ (excluding *.test.ts)."
    - "`pnpm run knip` reports zero unused exports — no surviving `*FromConfig` convenience factory."
    - "A guard test mechanically asserts the single-constructor invariant by reading clients.ts source, so a future second constructor fails `pnpm test`."
  artifacts:
    - path: "src/commands/clients.test.ts"
      provides: "ARCH-04 single-constructor invariant guard (reads clients.ts source, asserts exactly one S3Client + one Pool constructor)"
      min_lines: 25
  key_links:
    - from: "src/commands/clients.test.ts"
      to: "src/commands/clients.ts"
      via: "readFile of clients.ts source + assertion that constructor count == 1"
      pattern: "new S3Client\\(|new Pool\\("
  prohibitions:
    - "No production code is deleted in this plan — the *FromConfig factories already do not exist (research §2); this plan LOCKS the invariant, it does not delete code."
    - "The guard test must NOT read *.integration.test.ts harness files (they legitimately build their own pool) — scope the read to src/commands/clients.ts only."
    - "No new `v8 ignore` coverage suppression."

premises:
  - claim: Exactly one prod `new S3Client(` exists, at src/commands/clients.ts:14
    src: 20-RESEARCH.md#1-client-construction-sites
    verify: "test \"$(grep -rn 'new S3Client(' src --include='*.ts' | grep -vc '.test.ts')\" = 1"
  - claim: Exactly one prod `new Pool(` exists, at src/commands/clients.ts:25
    src: 20-RESEARCH.md#1-client-construction-sites
    verify: "test \"$(grep -rn 'new Pool(' src --include='*.ts' | grep -vc '.test.ts')\" = 1"
  - claim: No *FromConfig/*FromDatabaseUrl factory exists in production src/
    src: 20-RESEARCH.md#2-fromconfig-convenience-factories
    verify: "test -z \"$(grep -rn 'FromConfig\\|FromDatabaseUrl' src --include='*.ts' | grep -v '.test.ts')\""
  - claim: knip is wired into verify and currently green
    src: package.json#scripts.knip
    verify: "pnpm run knip"
  - claim: The existing GUARD-04 guard is scoped to contract-check.ts only, not a global single-constructor guard
    src: 20-RESEARCH.md#2-fromconfig-convenience-factories
    verify: "grep -n 'contractCheckSourceFiles' src/contract-check/contract-check.test.ts"

carried_forward:
  - claim: Phase 19 left adapter signatures stable (contracts settled in src/types/); the composition root is unchanged since the v2 watch daemon shipped
    src: ../19-composition?#none
---

<objective>
Lock in the ARCH-04 single-constructor invariant. The live tree already satisfies it — exactly
one `new S3Client(` (`clients.ts:14`) and one `new Pool(` (`clients.ts:25`) in production code,
and zero `*FromConfig`/`*FromDatabaseUrl` factories (a prior refactor already collapsed them; the
existing GUARD-04 in `contract-check.test.ts:272-273` forbids their name strings forever). There
is nothing to delete.

This plan PROVES and LOCKS the invariant so it cannot silently regress: a new guard test that reads
`clients.ts` source and asserts exactly one S3Client constructor and exactly one Pool constructor,
plus a re-confirmation that `pnpm run knip` flags no surviving convenience factory.

Purpose: ARCH-04 — one S3Client + one pg.Pool, built at the composition root and injected; convenience
factories gone. The behavioral half (ARCH-05 watch teardown) is Plan 02.
Output: `src/commands/clients.test.ts` (new guard) + green `verify` (incl. knip).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-composition-root-client-consolidation-watch-teardown/20-RESEARCH.md
@.planning/phases/20-composition-root-client-consolidation-watch-teardown/20-VALIDATION.md

# The single composition root — both prod constructors live here (the thing the guard reads):
@src/commands/clients.ts
# Existing guard-style test that reads src files (pattern to mirror — readFile + import.meta.url URL):
@src/contract-check/contract-check.test.ts

# Graph context: clients.ts/shared.ts/commands/ are community 1 "CLI Command Registration"
# [src: graph:CLI Command Registration]. No upper band depends on clients.ts internals — it is the
# composition root, the leaf of the runtime DI graph.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the ARCH-04 single-constructor guard test</name>
  <files>src/commands/clients.test.ts</files>
  <read_first>
    - src/commands/clients.ts (the file under guard — exactly one `new S3Client(` :14, one `new Pool(` :25)
    - src/contract-check/contract-check.test.ts:277-290 (mirror this exact pattern: `readFile(new URL("../../<path>", import.meta.url), "utf8")` then assert on the source text)
  </read_first>
  <behavior>
    - Test 1: reading `src/commands/clients.ts` source, the count of `new S3Client(` occurrences is exactly 1.
    - Test 2: reading `src/commands/clients.ts` source, the count of `new Pool(` occurrences is exactly 1.
    - Test 3 (invariant intent): the source contains no `FromConfig` and no `FromDatabaseUrl` substring (no convenience factory re-introduced in the composition root).
  </behavior>
  <action>
    Create `src/commands/clients.test.ts` mirroring the GUARD-04 source-read pattern at
    `contract-check.test.ts:277-290` [src: 20-RESEARCH.md#2-fromconfig-convenience-factories]: read
    `src/commands/clients.ts` via `readFile(new URL("../../src/commands/clients.ts", import.meta.url), "utf8")`
    and assert the invariant against the source text.

    Count occurrences of the two constructor literals and assert each equals exactly 1: build the
    counts with `String.prototype.split(literal).length - 1` (or a global-flag `matchAll`) — do NOT
    `grep -c` from inside the test. Assert the S3 client constructor count is 1 and the pool
    constructor count is 1. Add a third assertion that the source contains neither the
    `FromConfig` nor the `FromDatabaseUrl` substring, so the collapsed convenience factories
    [src: 20-RESEARCH.md#2-fromconfig-convenience-factories] can never reappear at the composition root.

    CRITICAL — comment-text discipline: do NOT write the constructor literals (`new S3Client(`,
    `new Pool(`) or the factory substrings (`FromConfig`, `FromDatabaseUrl`) as bare tokens inside
    comments or test names — they would echo into this very file and inflate the counts the test
    reads from `clients.ts` only, but a reviewer-style global guard could trip. Build each searched
    literal from split-string parts the way GUARD-04 does (`["new S3", "Client("].join("")`,
    `["new ", "Pool("].join("")`, `["From", "Config"].join("")`, `["From", "DatabaseUrl"].join("")`)
    so no whole literal appears verbatim in the test file. Name the test by concept
    ("composition root constructs exactly one S3 client and one pool"), not by the literal.

    Use Vitest `describe`/`test`/`expect` and AAA structure per
    [src: AGENTS.md#Project-Skills solidstats-fetcher-ts-tests]. No real clients, no network — this
    is a pure static source-read guard.
  </action>
  <verify>
    <automated>pnpm test -- src/commands/clients.test.ts</automated>
  </verify>
  <done>
    `pnpm test -- src/commands/clients.test.ts` passes; the test reads clients.ts source and asserts
    exactly one S3 client constructor, exactly one pool constructor, and no FromConfig/FromDatabaseUrl
    substring. No whole searched literal appears verbatim in the test file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm the ARCH-04 invariant across the full gate (grep + knip + coverage)</name>
  <files>src/commands/clients.test.ts</files>
  <read_first>
    - package.json (scripts.verify, scripts.knip, scripts.test:coverage)
  </read_first>
  <action>
    Run the full behavior-preservation gate to confirm ARCH-04 is locked with zero collateral
    [src: ROADMAP.md#Phase-20 behavior-preservation gate]:

    1. Confirm the two production constructor counts are each exactly 1 via the premise verify
       commands (`grep -rn 'new S3Client(' src --include='*.ts' | grep -vc '.test.ts'` == 1, same
       for `new Pool(`).
    2. Confirm zero `*FromConfig`/`*FromDatabaseUrl` in production src
       (`grep -rn 'FromConfig\|FromDatabaseUrl' src --include='*.ts' | grep -v '.test.ts'` is empty).
    3. Run `pnpm run knip` — must report zero unused exports (no surviving convenience factory)
       [src: 20-RESEARCH.md#2-fromconfig-convenience-factories].
    4. Run `pnpm run verify` — format, lint, typecheck, unit (incl. the new guard), 100% V8
       coverage, build, depcruise, knip all green. The new guard test must not drop coverage below
       100% and must add no `v8 ignore`.

    Do NOT modify production code in `clients.ts`/`shared.ts` — the invariant is already satisfied;
    this task only proves it. If any grep count is not 1, STOP and surface it (research expected 1).
  </action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>
    `pnpm run verify` exits 0 (incl. knip green + 100% coverage); the three grep checks confirm one
    S3Client constructor, one Pool constructor, and zero FromConfig/FromDatabaseUrl in production src.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a static source-read guard test only. No new input crosses any boundary; no client is constructed at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | reintroduction of a duplicate `S3Client`/`Pool` constructor or a `*FromConfig` factory bypassing the single-injected-client rule | mitigate | `clients.test.ts` guard reads `clients.ts` source and fails `pnpm test` on a second constructor or any FromConfig/FromDatabaseUrl substring; GUARD-04 already forbids the old factory name strings |
| T-20-02 | Information Disclosure | guard test inadvertently logging S3/DB credentials | accept | the guard reads source text only — it never constructs a client, never touches `config.s3`/`databaseUrl`; no secret is in scope |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan (pure test addition); RESEARCH.md §Standard Stack confirms zero new dependencies — Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `pnpm test -- src/commands/clients.test.ts` green (the new guard).
- `grep -rn 'new S3Client(' src --include='*.ts' | grep -vc '.test.ts'` == 1.
- `grep -rn 'new Pool(' src --include='*.ts' | grep -vc '.test.ts'` == 1.
- `grep -rn 'FromConfig\|FromDatabaseUrl' src --include='*.ts' | grep -v '.test.ts'` empty.
- `pnpm run knip` green (no surviving convenience factory).
- `pnpm run verify` exits 0 (100% V8 coverage, no new `v8 ignore`).
</verification>

<success_criteria>
- ARCH-04 invariant is mechanically locked: a future second `S3Client`/`Pool` constructor or a
  re-introduced `*FromConfig` factory fails `pnpm test`.
- No production code changed; the behavior-preservation gate (golden oracles unaffected, 100%
  coverage, depcruise + knip) stays green.
</success_criteria>

## Artifacts this phase produces (Plan 01)

- `src/commands/clients.test.ts` — ARCH-04 single-constructor invariant guard (new).

<output>
Create `.planning/phases/20-composition-root-client-consolidation-watch-teardown/20-01-SUMMARY.md` when done
</output>
