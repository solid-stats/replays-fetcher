---
phase: 21-mechanical-convention-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - .oxfmtrc.json
  - "src/**/*.ts"
autonomous: true
requirements: [MECH-02]
must_haves:
  truths:
    - "oxfmt --check . exits 0 after the import-sort write (all import blocks are sorted)"
    - "An unsorted import block FAILS pnpm run format:check"
    - "tsc --noEmit stays exit 0 after the reorder"
    - "Unit test count and pass results are unchanged by the reorder"
  artifacts:
    - path: ".oxfmtrc.json"
      provides: "sortImports import-order enforcement (MECH-02 lock-in)"
      contains: "sortImports"
  key_links:
    - from: ".oxfmtrc.json"
      to: "src/**/*.ts"
      via: "oxfmt format:check enforces import order across all source on every verify"
      pattern: "sortImports"
  prohibitions:
    - "No logic change in any reordered file — only import-statement order changes"
    - "No semantic-audit findings (Phase 26) pulled in"
    - "No edit to the external shared @solid-stats/ts-toolchain preset (LOCAL .oxfmtrc.json only)"
    - "The broad ~56-file reordering diff is ACCEPTED, not narrowed away — it is pure, zero-logic, diff-reviewable reordering"
---

<objective>
Enable `oxfmt` import sorting LOCALLY (one key in `.oxfmtrc.json`), normalize import order across the tree with `oxfmt --write .`, and lock it in so an unsorted import block fails `verify`. This is the MECH-02 mechanical lane. The spike confirmed import sorting is currently entirely off (neither the inherited preset nor the local config sets `sortImports`), so `format:check` does zero import validation today.

Purpose: Close the import-order convention gap and make it non-regressable. Per RESEARCH and CONTEXT, this lands LOCALLY (the shared `@solid-stats/ts-toolchain` preset is an external pinned git dep — propagating there is a deferred cross-app task, not this phase).
Output: Updated `.oxfmtrc.json` (one key) + reordered source files (~56 files, pure import-block reordering); one isolated mechanical commit, separate from the Plan 01 conversion commit.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md
@.oxfmtrc.json
@package.json
</context>

<artifacts_this_phase_produces>
- `.oxfmtrc.json` key addition: `sortImports` set to `true` (this plan; the only addition to the local oxfmt config).
- (Plan 01 added the `consistent-type-definitions` rule to `.oxlintrc.json`.)
- No new source modules. No new dependency.
</artifacts_this_phase_produces>

<dependency_note>
This plan is wave 2, depends_on 21-01. Plan 01's `oxlint --fix` conversion touches the same import regions this plan reorders; running the conversion first then the sort avoids re-churning the diff (RESEARCH Pitfall 3 — keep them as two separate, individually-reviewable commits). Both plans declare `src/**/*.ts` in `files_modified`, so the file-overlap rule forces this plan into the next wave — which is exactly the intended sequencing.
</dependency_note>

<tasks>

<task type="auto">
  <name>Task 1: Enable sortImports locally and normalize import order</name>
  <files>.oxfmtrc.json, src/**/*.ts</files>
  <read_first>
    @.oxfmtrc.json — the local config currently has the 5 keys printWidth/useTabs/semi/singleQuote/trailingComma and NO sortImports (default Disabled), so format:check does zero import checking today.
    @.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md — §"MECH-02 — Local Import-Sort Mechanism" gives the exact one-key change and the verified blast radius; §"Blast-radius surprise — 56 files, not ~17" explains the broad pure-reordering diff is expected and is the recommended ACCEPT path; §"Comment-anchoring interaction with Phase 22" notes the 14 max-lines disable comments may be reshuffled (transient — Phase 22 removes 4 of them anyway).
  </read_first>
  <action>
    Add the single key `sortImports` with value `true` to `.oxfmtrc.json` (per the CONTEXT cross-app decision: LOCAL repo config only — do NOT touch the external `@solid-stats/ts-toolchain` preset). The `format`/`format:check` scripts auto-discover this local config (they run bare `oxfmt --write .` / `oxfmt --check .` with no `-c` flag), so no script change is needed. Then run `pnpm run format` (which is `oxfmt --write .`) to normalize import order across the tree. The spike verified this reorders imports in about 56 files (not the ~17 the audit predicted — oxfmt's default perfectionist algorithm sorts the whole import block by module specifier): ACCEPT this broad diff per the research recommendation — it is pure, behavior-free, `tsc`-green import reordering and the enforcement is the point. Do NOT reach for the `partitionByComment`/`groups` object-form narrowing (RESEARCH Open Question 1 recommends the default; A1 narrowing is unspiked) unless review explicitly objects to the max-lines-comment reshuffle. Do NOT hand-edit import blocks, do NOT introduce any logic change, and do NOT pull in any Phase 26 semantic-audit finding. If `oxfmt --check .` does not pass after the write, or `tsc` reports any error, STOP and report rather than hand-fixing.
  </action>
  <acceptance_criteria>
    - `grep -c sortImports .oxfmtrc.json` is at least 1.
    - `pnpm run format:check` (`oxfmt --check .`) exits 0 after the write.
    - `pnpm run typecheck` exits 0 (the reorder is behavior-free).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && grep -q sortImports .oxfmtrc.json && pnpm run format:check && pnpm run typecheck</automated>
  </verify>
  <done>sortImports is enabled in .oxfmtrc.json, format:check is green on the reordered tree, and tsc is green.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the import-order lock-in and behavior preservation</name>
  <files>(no edits — verification only)</files>
  <read_first>
    @.planning/phases/21-mechanical-convention-cleanup/21-VALIDATION.md — Per-Task Verification Map row 21-02-02 (an unsorted import block must fail format:check) and the behavior-preservation gate.
    @.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md — §"Behavior-Preservation Gate" (tsc, golden oracle + 100% V8 coverage, depcruise, knip confirmed unaffected) and §"Redundant Suppressions — NONE" (the only import-related disable is src/cli.ts import/no-unassigned-import for Sentry side-effect ordering — unrelated to MECH-02, it stays).
  </read_first>
  <action>
    Prove the lock-in fires: pick any one `src/**/*.ts` file with two or more imports, temporarily swap two import lines into a non-alphabetical order, run `pnpm run format:check`, confirm it now FAILS, then revert that temporary edit (leave the tree exactly as Task 1 produced it). Run `pnpm run verify` to confirm the full gate is green (format:check → lint → typecheck → test → coverage → build → depcruise → knip), and `pnpm run test:integration` to confirm the golden run-once and golden watch oracles are unaffected. Confirm the `src/cli.ts` `import/no-unassigned-import` Sentry-ordering disable is untouched (it is unrelated to MECH-02 and stays — RESEARCH §"Redundant Suppressions — NONE"). Note in the SUMMARY that the broad ~56-file diff is pure import reordering (tsc-green, zero logic change) per Pitfall 2, and record the DEFERRED cross-app follow-up (propagate `sortImports` into the shared `@solid-stats/ts-toolchain` preset so server-2/web inherit it) so the milestone audit sees it — do NOT implement it.
  </action>
  <acceptance_criteria>
    - An unsorted import block makes `pnpm run format:check` exit non-zero (lock-in proven), then the tree is reverted to the Task 1 state.
    - `pnpm run verify` exits 0 (golden coverage + depcruise + knip green).
    - `pnpm run test:integration` exits 0 (golden run-once + golden watch oracles unaffected).
    - `src/cli.ts` still contains its `import/no-unassigned-import` disable (untouched).
    - SUMMARY records the broad-diff rationale and the deferred shared-preset propagation.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm run format:check && pnpm run verify && grep -q "no-unassigned-import" src/cli.ts</automated>
    <human-check>Integration oracle gate: `pnpm run test:integration` exits 0 (Docker-backed golden run-once + golden watch); confirm test count/pass unchanged from before the reorder.</human-check>
  </verify>
  <done>An unsorted import fails format:check (then reverted), verify is green, the Sentry-ordering disable is intact, and the golden integration oracles pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a compile-time-only source-text refactor (import-statement reordering). No new external surface, no runtime input path, no auth/secret/network boundary is added or moved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | `oxfmt --write .` import reorder across ~56 files | accept | Pure import-block reordering; ESM import order is semantically irrelevant except for the one side-effect import in `src/cli.ts`, which is guarded by its `import/no-unassigned-import` disable and verified untouched. `tsc` exit 0 + golden oracle + 100% V8 coverage + depcruise + knip confirm zero behavior change. |
| T-21-04 | Tampering | npm/pip/cargo installs | accept | This phase installs NO external packages. No package-legitimacy surface. |

No new threats introduced: the change is compile-time only (import order). No input validation, auth, secret handling, or external service surface is added, moved, or removed.
</threat_model>

<verification>
- Enablement: `grep -c sortImports .oxfmtrc.json` >= 1.
- Normalization: `pnpm run format:check` exit 0 after `oxfmt --write .`.
- Lock-in: an unsorted import block fails `pnpm run format:check`.
- Behavior preservation: `pnpm run typecheck` exit 0; `pnpm run verify` green (coverage 100% unchanged, depcruise + knip green) + `pnpm run test:integration` green (golden oracles).
- Untouched: `src/cli.ts` Sentry-ordering `import/no-unassigned-import` disable intact.
</verification>

<success_criteria>
- Import order normalized across the tree and enforced locally so an unsorted import fails `verify`.
- `tsc` green; golden oracle + 100% V8 coverage + depcruise + knip unaffected.
- One isolated mechanical commit (the import-sort), separate from the Plan 01 conversion commit.
- Broad ~56-file pure-reordering diff accepted (not narrowed).
- No edit to the external shared toolchain preset; cross-app propagation recorded as deferred.
</success_criteria>

<output>
Create `.planning/phases/21-mechanical-convention-cleanup/21-02-SUMMARY.md` when done.
</output>
