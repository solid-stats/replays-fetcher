---
phase: 21-mechanical-convention-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .oxlintrc.json
  - "src/**/*.ts"
autonomous: true
requirements: [MECH-01]
must_haves:
  truths:
    - "Zero interface declarations remain in src/ (all 156 converted to type)"
    - "A reintroduced interface declaration FAILS pnpm run lint"
    - "tsc --noEmit stays exit 0 after conversion"
    - "Unit test count and pass results are unchanged by the conversion"
  artifacts:
    - path: ".oxlintrc.json"
      provides: "typescript/consistent-type-definitions error rule (MECH-01 lock-in)"
      contains: "consistent-type-definitions"
  key_links:
    - from: ".oxlintrc.json"
      to: "src/**/*.ts"
      via: "oxlint enforces type-over-interface across all source on every verify"
      pattern: "consistent-type-definitions"
  prohibitions:
    - "No logic change in any converted file — only the interface/type keyword (and extends to intersection) changes"
    - "No semantic-audit findings (Phase 26) pulled in"
    - "No edit to the external shared @solid-stats/ts-toolchain preset (LOCAL .oxlintrc.json only)"
    - "src/commands/shared.ts stays at exactly 300 lines (the conversion is a line-count wash)"
---

<objective>
Convert all 156 `interface` declarations in `src/` (137 prod + 19 test, across 53 files) to `type` via `oxlint --fix`, then lock the convention in so a future `interface` fails `verify`. This is the MECH-01 mechanical lane — near-100% precision, proven behavior-preserving by the spike (`tsc` exit 0, golden oracle + 100% V8 coverage unaffected).

Purpose: Close the largest mechanical convention gap (type-over-interface, conventions skill §A) and make it non-regressable, before the Phase 22 god-file splits land large structural diffs.
Output: Updated `.oxlintrc.json` (one rule) + 53 converted source files; one isolated mechanical commit.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md
@.oxlintrc.json
@package.json
</context>

<artifacts_this_phase_produces>
- `.oxlintrc.json` rule addition: `typescript/consistent-type-definitions` set to `["error","type"]` (this plan).
- (Plan 02 adds the `sortImports` key to `.oxfmtrc.json`.)
- No new source modules. No new dependency — ts-morph is NOT needed (spike converted 156/156 with `tsc` green).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add consistent-type-definitions rule and run the oxlint conversion</name>
  <files>.oxlintrc.json, src/**/*.ts</files>
  <read_first>
    @.oxlintrc.json — local rules block currently overrides only no-await-in-loop, typescript/require-await, typescript/no-magic-numbers; the type-definitions rule is NOT set locally.
    @.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md — §"The Spike (MECH-01)" gives the exact verified reproduction and the mandatory oxfmt re-canonicalize step; §"Interface-semantics exemptions — NONE" confirms zero sites need interface semantics (zero declaration merging, zero declare-global/module, zero implements, 5 extends chains all rewritten as intersections including one extends Error).
  </read_first>
  <action>
    Add the rule `typescript/consistent-type-definitions` with value `["error","type"]` to the `rules` object in `.oxlintrc.json` (per the CONTEXT cross-app decision: LOCAL repo config only — do NOT touch the external `@solid-stats/ts-toolchain` preset). Then run `pnpm exec oxlint --fix --config .oxlintrc.json src` to convert all 156 interface declarations to type aliases across the 53 files. The spike verified this converts 156/156 with no refusals: extends chains become intersection types and `tsc` stays green. CRITICAL ordering per RESEARCH §"Important sequencing note": the autofix output is NOT oxfmt-canonical (it leaves about 54 files with non-canonical formatting such as a trailing intersection), so immediately run `pnpm run format` (which is `oxfmt --write .`) after the conversion and before committing, or `format:check` in verify will fail. Do NOT hand-edit any converted site, do NOT introduce any logic change, and do NOT pull in any Phase 26 semantic-audit finding — this is the mechanical lane only. If the autofix unexpectedly leaves any interface declaration in `src/` or `tsc` reports any error, STOP and report rather than hand-fixing — the spike says this cannot happen, so a deviation is a real signal worth surfacing.
  </action>
  <acceptance_criteria>
    - `grep -rhE '^\s*(export )?interface ' src | grep -vc '^#'` yields `0` (zero interface declarations remain).
    - `pnpm run typecheck` exits 0.
    - `pnpm run format:check` exits 0 (the post-conversion oxfmt re-canonicalize ran).
    - `grep -c consistent-type-definitions .oxlintrc.json` is at least 1.
    - `wc -l < src/commands/shared.ts` equals 300 (Pitfall 4 — the conversion is a line-count wash).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && test "$(grep -rhE '^\s*(export )?interface ' src | grep -vc '^#')" = "0" && pnpm run typecheck && grep -q consistent-type-definitions .oxlintrc.json && test "$(wc -l < src/commands/shared.ts)" = "300" && pnpm run format:check</automated>
  </verify>
  <done>Zero interface declarations remain in src/, the type-definitions rule is in .oxlintrc.json, tsc and format:check are green, and shared.ts is still 300 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the lock-in and behavior preservation</name>
  <files>(no edits — verification only)</files>
  <read_first>
    @.planning/phases/21-mechanical-convention-cleanup/21-VALIDATION.md — Per-Task Verification Map row 21-01-02 (a reintroduced interface must fail lint) and the behavior-preservation gate.
    @.planning/phases/21-mechanical-convention-cleanup/21-RESEARCH.md — §"Behavior-Preservation Gate" (tsc, golden oracle + 100% V8 coverage, depcruise, knip all confirmed unaffected) and §"Redundant Suppressions — NONE" (nothing to remove for this rule pair).
  </read_first>
  <action>
    Run `pnpm run lint` — it must pass clean on the converted tree (the rule no longer finds any interface). Then prove the lock-in fires: temporarily reintroduce a single `interface Tmp { a: number }` into any one `src/**/*.ts` file, run `pnpm run lint`, confirm it now FAILS with the consistent-type-definitions error, then revert that temporary edit (leave the tree exactly as Task 1 produced it). Run `pnpm run verify` to confirm the full gate is green (format:check → lint → typecheck → test → coverage → build → depcruise → knip), and `pnpm run test:integration` to confirm the golden run-once and golden watch oracles are unaffected. Note explicitly in the SUMMARY that there are NO redundant suppressions to remove for this rule pair (RESEARCH §"Redundant Suppressions — NONE": zero consistent-type-definitions suppressions exist; the `src/cli.ts` import/no-unassigned-import disable is unrelated and stays), so Success-Criterion 3's "redundant suppressions removed" has nothing to remove here and is not an unmet gap. Also record the DEFERRED cross-app follow-up (propagate the rule into the shared `@solid-stats/ts-toolchain` preset so server-2/web inherit it) in the SUMMARY so the milestone audit sees it — do NOT implement it.
  </action>
  <acceptance_criteria>
    - `pnpm run lint` exits 0 on the converted tree.
    - A reintroduced `interface` makes `pnpm run lint` exit non-zero (lock-in proven), then the tree is reverted to the Task 1 state.
    - `pnpm run verify` exits 0 (golden coverage + depcruise + knip green).
    - `pnpm run test:integration` exits 0 (golden run-once + golden watch oracles unaffected).
    - SUMMARY states "no redundant suppressions to remove for this rule pair" and records the deferred shared-preset propagation.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm run lint && pnpm run verify</automated>
    <human-check>Integration oracle gate: `pnpm run test:integration` exits 0 (Docker-backed golden run-once + golden watch); confirm test count/pass unchanged from before the conversion.</human-check>
  </verify>
  <done>lint is green on the converted tree, a reintroduced interface fails lint (then reverted), verify is green, and the golden integration oracles pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a compile-time-only source-text refactor (interface→type keyword change). No new external surface, no runtime input path, no auth/secret/network boundary is added or moved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | `oxlint --fix` codemod output across 53 files | accept | Pure keyword/intersection rewrite; `tsc` exit 0 + golden oracle + 100% V8 coverage + depcruise + knip all confirm zero behavior change. No identifier, object key, env var, or schema changes (RESEARCH §"Runtime State Inventory" — nothing found in any runtime category). |
| T-21-02 | Tampering | npm/pip/cargo installs | accept | This phase installs NO external packages — ts-morph fallback ruled out by the spike. No package-legitimacy surface. |

No new threats introduced: the change is compile-time only (interface→type). No input validation, auth, secret handling, or external service surface is added, moved, or removed.
</threat_model>

<verification>
- Conversion: `grep -rhE '^\s*(export )?interface ' src | grep -vc '^#'` == 0; `pnpm run typecheck` exit 0.
- Lock-in: rule present in `.oxlintrc.json`; a reintroduced `interface` fails `pnpm run lint`.
- Formatting: `pnpm run format:check` exit 0 (post-conversion oxfmt re-canonicalize done).
- Behavior preservation: `pnpm run verify` green (coverage 100% unchanged, depcruise + knip green) + `pnpm run test:integration` green (golden oracles).
- Carry-forward guard: `src/commands/shared.ts` still 300 lines.
</verification>

<success_criteria>
- All 156 interface declarations converted to type; zero remain in `src/`.
- `consistent-type-definitions: ["error","type"]` enforced locally so a new `interface` fails `verify`.
- `tsc` green; golden oracle + 100% V8 coverage + depcruise + knip unaffected.
- One isolated mechanical commit, zero logic change.
- No edit to the external shared toolchain preset; cross-app propagation recorded as deferred.
</success_criteria>

<output>
Create `.planning/phases/21-mechanical-convention-cleanup/21-01-SUMMARY.md` when done.
</output>
