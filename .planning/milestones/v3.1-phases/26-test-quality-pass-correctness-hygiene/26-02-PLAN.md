---
phase: 26-test-quality-pass-correctness-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/staging/payload.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "payload.test.ts no longer carries an inline `eslint-disable max-lines` — the multi-behavior file is split into one-behavior-per-test (RITE) so the suppression is unnecessary, not disabled"
    - "The duplicated RawReplayStorageEvidence literal (repeated in arrange AND assert) is factored into a single typed builder with overrides; expected payload derives from it, no copy-paste literal"
    - "Date-parse precedence/range scenarios in payload.test.ts use a `test.each` parameterized table where the assertion logic is identical"
    - "Every existing assertion is preserved — no behavior assertion is weakened or dropped; coverage stays 100%"
  artifacts:
    - path: "src/staging/payload.test.ts"
      provides: "RITE-split, builder-based, test.each date-parse staging-payload suite with no inline lint suppression"
      contains: "createStoredEvidence"
  key_links:
    - from: "src/staging/payload.test.ts"
      to: "src/staging/payload.ts"
      via: "toIngestStagingPayload called with builder output, asserted against derived expected"
      pattern: "toIngestStagingPayload"
---

<objective>
TEST-01 + TEST-02 (+ TEST-03 для date-parse) на `src/staging/payload.test.ts` — самом дублирующем suite фазы. Три правки в одном файле:

1. **TEST-01** — дублированный `RawReplayStorageEvidence`-литерал (повторён inline в arrange И в `toStrictEqual`) → typed builder `createStoredEvidence(overrides?)`; expected payload derive из него `[std: §G typed builders]`.
2. **TEST-02 (RITE)** — файл несёт inline `eslint-disable max-lines` «precedence + range-validation kept together» — это сигнал смешения поведений `[src: 26-RESEARCH.md#Test-Quality-Inventory]`. Split на one-behavior-per-test; suppression исчезает естественно через split (НЕ disable — split, per `solidstats-shared-ts-standards §C`).
3. **TEST-03** — date-parse precedence/range сценарии → `test.each`-таблица где assertion-логика идентична `[std: §G parameterized tables]`.

Purpose: поднять test-rigor без потери ни одной assertion и без изменения источника (`payload.ts` не трогается).
Output: RITE-split, builder-based, table-driven payload suite; coverage 100% held.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-test-quality-pass-correctness-hygiene/26-RESEARCH.md

Skill rules this plan must honor (read in full before editing tests):
@.agents/skills/solidstats-fetcher-ts-tests/SKILL.md
@.agents/skills/solidstats-shared-ts-standards/SKILL.md
</context>

<premises>
premises:
  - claim: payload.test.ts carries an inline `eslint-disable max-lines` justified as "precedence + range-validation scenarios kept together" — a TEST-02 mixing signal, not an allowed exception
    src: src/staging/payload.test.ts#L1
    verify: "head -1 src/staging/payload.test.ts"
  - claim: the storedEvidence RawReplayStorageEvidence literal is duplicated — built in arrange AND repeated field-by-field in the toStrictEqual expected (checksum, sourceFilename, sourceUrl, byteSize, discoveredAt)
    src: src/staging/payload.test.ts#L11
    verify: "grep -c 'sourceFilename\\|byteSize\\|discoveredAt' src/staging/payload.test.ts"
  - claim: test files have max-lines off via shared eslint config (so the inline disable is redundant once behaviors are split), and the right tool for a mixed file is split, never an inline structural-limit disable
    src: .agents/skills/solidstats-shared-ts-standards/SKILL.md#C
    verify: "grep -n 'max-lines.*off\\|\\*\\*/\\*.test.ts' .agents/skills/solidstats-shared-ts-standards/SKILL.md"
  - claim: html.test.ts and components-to-utc-iso.test.ts already use test.each for date parsing — the compliant pattern to mirror for payload date-parse
    src: src/discovery/html.test.ts
    verify: "grep -ln 'test.each' src/discovery/html.test.ts src/time/components-to-utc-iso.test.ts"

carried_forward:
  - claim: test-quality is never a standalone BLOCK unless a test masks a real bug; this plan must NOT weaken any assertion while refactoring
    src: .agents/skills/solidstats-fetcher-ts-tests/SKILL.md#Not-owned-here
</premises>

<!-- planner-discipline-allow: eslint-disable -->

<tasks>

<task type="auto">
  <name>Task 1: Factor a typed createStoredEvidence builder + derive expected payload (TEST-01)</name>
  <read_first>
    - src/staging/payload.test.ts (full — current storedEvidence const at lines 11-25, the toStrictEqual expected block 28-60+, all subsequent tests)
    - src/staging/payload.ts (full — toIngestStagingPayload signature + what fields it maps, so the builder's defaults are correct)
    - src/storage/types.ts (RawReplayStorageEvidence type definition)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §G (typed builders/factories with overrides)
  </read_first>
  <action>
    Add a typed builder `createStoredEvidence(overrides?: Partial<RawReplayStorageEvidence>): RawReplayStorageEvidence` at the top of `payload.test.ts` with the current `storedEvidence` field values as sensible defaults, merged with `overrides` (es-toolkit/spread). Every test that needs evidence calls the builder (with overrides only for the field under test) instead of an inline literal. Where the `toStrictEqual` expected currently repeats the same field values inline `[src: src/staging/payload.test.ts#L28]`, derive the expected payload's promotionEvidence/checksum/etc from the builder output (or from named constants the builder also uses) so the literal exists in exactly one place — no copy-paste between arrange and assert.

    Keep assertions byte-equivalent: the refactor changes WHERE the values come from, not WHAT is asserted. Do NOT change `payload.ts`. Do NOT introduce a magic value that diverges from the original literal — the existing exact strings (checksum, sourceFilename "2026_05_09__00_32_44__1_ocap", sourceUrl, discoveredAt) stay identical.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/staging/payload.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'createStoredEvidence' src/staging/payload.test.ts` >= number of evidence-using tests (builder used, not inline literals).
    - The full RawReplayStorageEvidence literal appears in exactly one place (the builder defaults) — no second field-by-field copy in a `toStrictEqual` arrange/assert pair.
    - `pnpm test -- src/staging/payload.test.ts` passes with the same number of (or more) assertions; `pnpm run test:coverage` 100%.
  </acceptance_criteria>
  <done>Duplicated evidence literal factored into one typed builder; expected derived from it; assertions unchanged; coverage 100%.</done>
</task>

<task type="auto">
  <name>Task 2: RITE split + date-parse test.each; remove the inline eslint-disable max-lines (TEST-02 + TEST-03)</name>
  <read_first>
    - src/staging/payload.test.ts (full — line 1 eslint-disable; all tests, identifying which assert >1 behavior, esp. precedence-mapping vs range-validation date scenarios)
    - src/discovery/html.test.ts (the compliant test.each shape for date parsing — mirror it)
    - src/time/components-to-utc-iso.test.ts (second compliant test.each date sample)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §C (suppression policy: split, never disable structural limits) and §G (test.each)
  </read_first>
  <action>
    Split every test in `payload.test.ts` that asserts more than one behavior into one-behavior-per-test (RITE) — precedence-mapping (filename-primary vs listing-fallback replayTimestamp) separated from range-validation, separated from field-mapping. Each resulting test has a single clear behavior in its name.

    Convert the date-parse precedence/range scenarios — where the arrange differs only by input and the assertion shape is identical — into a `test.each([...cases])` table mirroring `html.test.ts` / `components-to-utc-iso.test.ts` (a `cases` array of `{ name/input, expected }` rows + one runner) `[src: src/discovery/html.test.ts]`. Scenarios with genuinely different setup/assertion stay as standalone tests — do not force-fit them into the table (RESEARCH: "оставить отдельными те, где setup радикально различается").

    Remove the inline `/* eslint-disable max-lines ... */` on line 1 `[src: src/staging/payload.test.ts#L1]`. The shared eslint config already sets `max-lines: off` for `**/*.test.ts` `[src: .agents/skills/solidstats-shared-ts-standards/SKILL.md#C]`, so the inline disable is redundant; and even were it not, the correct response to a mixed file is the RITE split above, never a structural-limit disable. After removal, `pnpm run lint` must stay green.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/staging/payload.test.ts && pnpm run lint && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'eslint-disable' src/staging/payload.test.ts` == 0.
    - `grep -c 'test.each\|it.each' src/staging/payload.test.ts` >= 1 (date-parse precedence/range table present).
    - Each test asserts a single behavior (no test mixing precedence AND range validation in one `test(...)`).
    - `pnpm run lint` exits 0 (no max-lines violation reintroduced); `pnpm run test:coverage` 100%; total assertions preserved (no behavior dropped).
  </acceptance_criteria>
  <done>payload.test.ts is RITE-split, date-parse uses test.each, the inline suppression is gone via split (not disable); lint + coverage green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only change) | No production trust boundary crossed — this plan edits a single `*.test.ts` file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | test refactor weakening an assertion | mitigate | RITE split preserves every assertion; coverage gate (100%) + golden oracle catch any silently-dropped behavior — `<verification>` |
| T-26-02-SC | Tampering | npm installs | accept | No package installs — existing Vitest toolchain only |

Phase adds no endpoints, no write-scope change, no production-code edit in this plan — security-light.
</threat_model>

<verification>
After EACH task commit:
- `pnpm test -- src/staging/payload.test.ts` green
- `pnpm run test:coverage` — 100% maintained, NO new `v8 ignore`
After the plan (wave-merge gate):
- `pnpm run verify` exits 0 (lint proves the inline disable removal did not reintroduce a violation)
- `pnpm run test:integration` — golden oracle green (behavior unchanged)
</verification>

<success_criteria>
- payload.test.ts: zero inline eslint-disable, one typed builder, date-parse test.each table, one-behavior-per-test.
- Every prior assertion preserved; coverage 100%; lint + golden oracle green.
</success_criteria>

<output>
Create `.planning/phases/26-test-quality-pass-correctness-hygiene/26-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New test-helper symbols (test-infra only, not production public surface):
- `createStoredEvidence(overrides?)` — typed builder in `src/staging/payload.test.ts`.
- A `cases`-style const array for the date-parse `test.each` table in `src/staging/payload.test.ts`.

No production symbols are created or renamed by this plan.
