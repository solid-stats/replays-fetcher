---
phase: 26-test-quality-pass-correctness-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/staging/postgres-staging-repository.test.ts
  - src/staging/postgres-staging-repository.integration.test.ts
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The postgres-staging dedup/conflict scenarios that share identical assertion logic (benign-already_staged vs conflict classification) are driven by a `test.each` parameterized table"
    - "Scenarios with materially different setup (db-error structured failure, unmatched-violation fail, existsBySourceIdentity lean query) stay as standalone tests — not force-fit into the table"
    - "The integration conflict-vs-benign pair is evaluated for a test.each table and converted where the assertion shape is identical"
    - "Every existing scenario and its assertion is preserved; coverage stays 100%; integration golden behavior unchanged"
  artifacts:
    - path: "src/staging/postgres-staging-repository.test.ts"
      provides: "table-driven dedup/conflict classification unit suite"
      contains: "test.each"
  key_links:
    - from: "src/staging/postgres-staging-repository.test.ts"
      to: "src/staging/postgres-staging-repository.ts"
      via: "stage()/existsBySourceIdentity asserted across the parameterized matrix"
      pattern: "\\.stage\\(|existsBySourceIdentity"
---

<objective>
TEST-03: dedup/conflict matrix в `postgres-staging-repository.test.ts` (10 отдельных `test(...)` с общей assertion-формой) → `test.each`-таблица для веток с идентичной классификационной логикой (benign already_staged / conflict), оставив отдельными те, где setup радикально различается (db-error, unmatched-violation, existsBySourceIdentity) `[src: 26-RESEARCH.md#Test-Quality-Inventory]`. Плюс оценить и сконвертировать conflict-vs-benign пару в integration-suite.

Purpose: убрать дублирование классификационных тестов через параметризацию `[std: §G parameterized tables]` без потери ни одного сценария.
Output: table-driven dedup/conflict unit suite + (где уместно) integration pair; coverage 100% held, integration behavior byte-stable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-test-quality-pass-correctness-hygiene/26-RESEARCH.md

Skill rules this plan must honor (read in full before editing tests):
@.agents/skills/solidstats-fetcher-ts-tests/SKILL.md
@.agents/skills/solidstats-shared-ts-standards/SKILL.md
</context>

<premises>
premises:
  - claim: postgres-staging-repository.test.ts has ~10 standalone tests covering insert / benign-empty-RETURNING / 23505-match / changed-identity-conflict / cross-source-conflict / unmatched-violation-fail / db-error / existsBySourceIdentity×2 — several share an identical assertion form
    src: src/staging/postgres-staging-repository.test.ts#L118
    verify: "grep -c '^test(' src/staging/postgres-staging-repository.test.ts"
  - claim: the integration suite has benign re-stage / same-source-diff-checksum conflict / existsBySourceIdentity scenarios — a candidate conflict-vs-benign test.each pair
    src: src/staging/postgres-staging-repository.integration.test.ts#L60
    verify: "grep -c 'test(\\|it(' src/staging/postgres-staging-repository.integration.test.ts"
  - claim: ingest-page-prefetch-dedup.test.ts already uses test.each (cannotMissCases) — the in-repo compliant sample to mirror
    src: src/run/ingest-page-prefetch-dedup.test.ts#L98
    verify: "grep -n 'test.each' src/run/ingest-page-prefetch-dedup.test.ts"
  - claim: the staging repository is the write-scope adapter — these tests must NOT change any production code, only the test structure
    src: .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md#B
    verify: "git diff --name-only -- src/staging/postgres-staging-repository.ts | wc -l"

carried_forward:
  - claim: ON CONFLICT (checksum, object_key) DO NOTHING benign-skip + (source_system, source_replay_id) conflict-throw classification is the Phase 24 contract these tests assert — preserve the exact classification semantics
    src: .planning/STATE.md#Decisions
</premises>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the dedup/conflict classification matrix with test.each (unit)</name>
  <read_first>
    - src/staging/postgres-staging-repository.test.ts (full — all ~10 tests at lines 118-289; identify which share the stage()→{status} assertion shape vs which have unique setup)
    - src/staging/postgres-staging-repository.ts (stage() + existsBySourceIdentity signatures + the classifyExistingStaging branches, so the table cases map to real branches)
    - src/run/ingest-page-prefetch-dedup.test.ts (lines ~98+ — the cannotMissCases test.each shape to mirror)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §G (test.each for identical-assertion matrices)
  </read_first>
  <action>
    In `postgres-staging-repository.test.ts`, identify the subset of tests whose body is "arrange a fake pg result → call stage() → assert the returned `{ status }` classification" with an IDENTICAL assertion shape (the benign already_staged variants and the conflict variants). Convert that subset into a single `test.each([...cases])` table mirroring `ingest-page-prefetch-dedup.test.ts` `[src: src/run/ingest-page-prefetch-dedup.test.ts#L98]`: a `cases` array of `{ name, ...query-stub-inputs, expectedStatus }` rows + one runner that arranges the stub from the row and asserts the classification.

    KEEP as standalone tests (do NOT force into the table) the scenarios with materially different setup or assertion `[src: 26-RESEARCH.md#Test-Quality-Inventory]`:
    - insert pending records (different assertion — inserted row shape)
    - fall-through-to-classify when benign empty rows resolve no existing row (multi-step)
    - unmatched-violation → structured fail
    - structured failure for database errors
    - existsBySourceIdentity lean-query×2 (different method, different assertion)

    Do NOT modify `postgres-staging-repository.ts` (production). Do NOT weaken any assertion — every status/branch currently asserted must be a row in the table or a retained standalone test. The classification semantics (already_staged vs conflict vs failed) must be byte-identical to today.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/staging/postgres-staging-repository.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test.each\|it.each' src/staging/postgres-staging-repository.test.ts` >= 1.
    - Every classification outcome asserted before (insert, already_staged via empty RETURNING, 23505-match already_staged, changed-identity conflict, cross-source conflict, unmatched-violation fail, db-error fail, existsBySourceIdentity true/false) is still asserted — as a table row or a retained standalone test.
    - `git diff --name-only` shows NO change to `src/staging/postgres-staging-repository.ts`.
    - `pnpm test -- src/staging/postgres-staging-repository.test.ts` passes; `pnpm run test:coverage` 100%.
  </acceptance_criteria>
  <done>Identical-assertion dedup/conflict scenarios are table-driven; distinct-setup scenarios retained standalone; production untouched; coverage 100%.</done>
</task>

<task type="auto">
  <name>Task 2: Evaluate + convert the integration conflict-vs-benign pair to test.each where assertion is identical</name>
  <read_first>
    - src/staging/postgres-staging-repository.integration.test.ts (full — benign re-stage / same-source-diff-checksum conflict / existsBySourceIdentity at lines 60-162)
    - src/staging/postgres-staging-repository.test.ts (the unit table from Task 1 — reuse the case-row idiom for consistency)
    - .agents/skills/solidstats-fetcher-ts-tests/SKILL.md (integration harness: isolated DB per test via testcontainers; no shared mutable state)
  </read_first>
  <action>
    Evaluate the integration suite's benign-re-stage vs same-source-different-checksum-conflict pair. IF their assertion shape is identical (arrange first stage → re-stage with a variant → assert `{ status }`) AND the testcontainer setup can be shared per-row without breaking per-test isolation, convert that pair to a `test.each` table consistent with the unit-table idiom from Task 1.

    IF conversion would force-share mutable DB state across rows or obscure the conflict-vs-benign distinction, LEAVE them as standalone tests and record a one-line rationale in the SUMMARY (this is a legitimate "evaluate → leave" outcome, not a skipped requirement). The integration determinism rule (isolated resource per test) outranks the test.each tidiness goal `[src: .agents/skills/solidstats-fetcher-ts-tests/SKILL.md#Integration-harness]`. Do NOT touch existsBySourceIdentity integration tests (distinct method). Do NOT modify production code.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - Either the conflict-vs-benign integration pair is a `test.each` table (assertion shape identical, isolation preserved), OR they remain standalone with a recorded rationale in the SUMMARY — both are acceptable; the requirement is the explicit evaluation, not a forced conversion.
    - `git diff --name-only` shows NO change to `src/staging/postgres-staging-repository.ts`.
    - `pnpm run test:integration` passes (golden oracle + staging integration green); per-test DB isolation preserved (no order dependency introduced).
  </acceptance_criteria>
  <done>Integration conflict-vs-benign pair evaluated; converted to test.each only if isolation+assertion allow, else retained with rationale; integration green; production untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only change) | No production trust boundary crossed — edits two `*.test.ts` files; staging adapter source untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | parameterization dropping a conflict-classification case | mitigate | Acceptance criteria require every prior classification outcome to remain asserted (table row or standalone); coverage 100% gate catches a dropped branch |
| T-26-03-02 | Tampering | integration test losing per-test DB isolation | mitigate | Isolation rule outranks test.each conversion; convert only when isolation preserved, else leave with rationale — Task 2 |
| T-26-03-SC | Tampering | npm installs | accept | No package installs — existing Vitest + testcontainers toolchain only |

Phase adds no endpoints, no write-scope change; production staging adapter is not edited — security-light.
</threat_model>

<verification>
After Task 1 commit:
- `pnpm test -- src/staging/postgres-staging-repository.test.ts` green
- `pnpm run test:coverage` — 100% maintained, NO new `v8 ignore`
After Task 2 / plan (wave-merge gate):
- `pnpm run verify` exits 0
- `pnpm run test:integration` — golden oracle + staging integration green (behavior byte-stable)
</verification>

<success_criteria>
- Identical-assertion dedup/conflict scenarios parameterized with test.each; distinct-setup ones retained.
- Integration pair explicitly evaluated (converted or rationalized).
- Every prior scenario preserved; production untouched; coverage + integration green.
</success_criteria>

<output>
Create `.planning/phases/26-test-quality-pass-correctness-hygiene/26-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New test-infra symbols only:
- A `cases`-style const array for the dedup/conflict `test.each` table in `src/staging/postgres-staging-repository.test.ts` (and possibly the integration file).

No production symbols are created or renamed by this plan.
