---
phase: 13-shared-solid-stats-ts-toolchain-bootstrap
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - "package.json"
  - "pnpm-lock.yaml"
  - "tsconfig.json"
autonomous: true
requirements: [CFG-03, CFG-04]
must_haves:
  truths:
    - "Fetcher package.json несёт devDependency @solid-stats/ts-toolchain пинованную тегом github:solid-stats/ts-toolchain#v0.1.0"
    - "pnpm-lock.yaml резолвит git-dep в фиксированный commit SHA и воспроизводим под --frozen-lockfile"
    - "Fetcher tsconfig.json extends @solid-stats/ts-toolchain/tsconfig/base.json вместо дублирования strict-флагов"
    - "pnpm run typecheck остаётся зелёным после extends (поведение TS не изменилось)"
    - "pnpm run verify остаётся зелёным end-to-end (format/lint/typecheck/test/integration/coverage 100%/build)"
  artifacts:
    - path: "package.json"
      provides: "tag-pinned git-dep на shared toolchain"
      contains: "github:solid-stats/ts-toolchain#v0.1.0"
    - path: "tsconfig.json"
      provides: "extends shared base + только repo-specific compilerOptions"
      contains: "@solid-stats/ts-toolchain/tsconfig/base.json"
    - path: "pnpm-lock.yaml"
      provides: "воспроизводимая резолюция git-dep → commit SHA"
      contains: "solid-stats/ts-toolchain"
  key_links:
    - from: "tsconfig.json"
      to: "node_modules/@solid-stats/ts-toolchain/tsconfig/base.json"
      via: "extends bare package specifier → exports map shared package.json"
      pattern: "@solid-stats/ts-toolchain/tsconfig/base.json"
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm add -D резолвит тег v0.1.0 в 40-char SHA, записанный в lockfile"
      pattern: "solid-stats/ts-toolchain"
---

<objective>
Подключить фетчер к shared toolchain как tag-пинованный pnpm git-dep и доказать end-to-end, что `tsconfig.json` extends shared base без изменения поведения. Закрывает CFG-03 (tag-pinned git-dep + воспроизводимый frozen lockfile) и CFG-04 (tsconfig extends shared preset + `pnpm verify` зелёный).

Purpose: финальное звено фазы — фетчер реально потребляет пресет (tsconfig subpath), сохраняя toolchain-only инвариант: `src/`, CLI, пятибэндовый ingest-пайплайн ЗАМОРОЖЕНЫ, поведение сохранено, 100% coverage держится. В этой фазе потребляется ТОЛЬКО tsconfig-пресет; swaps formatter (Phase 15) / linter (Phase 16) / build (Phase 17) / hooks (Phase 18) НЕ делаются — скрипты `lint`/`format`/`build` фетчера остаются нетронутыми.

Output: изменённые `package.json` (+git-dep), `pnpm-lock.yaml` (резолвленный SHA), `tsconfig.json` (extends), зелёный `pnpm verify` и зелёный `pnpm install --frozen-lockfile` от чистого состояния.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md
@.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md
@.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-02-SUMMARY.md
@package.json
@tsconfig.json
@tsconfig.build.json
@Dockerfile
</context>

<critical_constraints>
- Тег `v0.1.0` ДОЛЖЕН существовать в origin (создан планом 13-02) перед началом — иначе `pnpm add` не зарезолвит. Подтверди из 13-02-SUMMARY.md.
- Pin ТОЛЬКО тегом: `github:solid-stats/ts-toolchain#v0.1.0`. НИКОГДА веткой (`#master` → нерепродуцируемый lockfile, Pitfall P13-4/5).
- Добавляй dep через `pnpm add -D "github:solid-stats/ts-toolchain#v0.1.0"` — это автоматически обновит lockfile (НЕ редактируй package.json вручную без обновления lockfile — `--frozen-lockfile` упадёт с ERR_PNPM_OUTDATED_LOCKFILE).
- НЕ добавляй `@solid-stats/ts-toolchain` в `pnpm.onlyBuiltDependencies` — у него нет build step.
- В `tsconfig.json` ВСЕГДА явно задавай `include` и `types` — не полагайся на наследование из base (base их намеренно НЕ содержит, Pitfall P13-5). Сохрани `include` как есть (`src/**/*.ts`, `vitest.config.ts`, `eslint.config.js`) — удаление `eslint.config.js` из include отложено на Phase 16.
- НЕ трогай `tsconfig.build.json` (он `extends ./tsconfig.json` и наследует базу транзитивно), скрипты `lint`/`format`/`build`, `Dockerfile` (он уже делает `pnpm install --frozen-lockfile` — публичный git-dep скачается без auth; менять не нужно — см. 13-PATTERNS.md §Dockerfile).
- Это работа над фетчером через GSD-execute — никаких правок `src/`.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Добавить tag-пинованный git-dep и зафиксировать воспроизводимый lockfile</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-02-SUMMARY.md (подтверждение что тег v0.1.0 запушен; peeled SHA)
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md §Git-dep Consumption (точная механика specifier, lockfile-запись, frozen-lockfile)
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md §`replays-fetcher/package.json` + §`pnpm-lock.yaml` (что менять, что НЕ трогать)
    - package.json (текущий — git-dep добавляется в devDependencies; не трогать dependencies/scripts/engines/pnpm.onlyBuiltDependencies)
  </read_first>
  <action>
  1. Подтверди, что тег существует в remote: `git ls-remote --tags git@github.com:solid-stats/ts-toolchain.git v0.1.0` (или `gh api repos/solid-stats/ts-toolchain/git/refs/tags/v0.1.0`) возвращает ref. Если нет — стоп, план 13-02 не завершён.

  2. В корне фетчера добавь git-dep: `pnpm add -D "github:solid-stats/ts-toolchain#v0.1.0"`. Это:
     - впишет `"@solid-stats/ts-toolchain": "github:solid-stats/ts-toolchain#v0.1.0"` в `devDependencies` package.json;
     - зарезолвит тег → 40-char commit SHA и запишет резолюцию в `pnpm-lock.yaml`;
     - распакует пакет в `node_modules/@solid-stats/ts-toolchain/`.

  3. Подтверди резолюцию SHA в lockfile: `grep -A2 "ts-toolchain" pnpm-lock.yaml` показывает запись со specifier `github:solid-stats/ts-toolchain#v0.1.0` и version-строкой, содержащей 40-символьный SHA (codeload tarball URL). Сверь, что SHA совпадает с peeled SHA тега из 13-02-SUMMARY.md (Assumption A1).

  4. Подтверди, что пакет установлен с пятью пресетами: `test -f node_modules/@solid-stats/ts-toolchain/tsconfig/base.json` (+ oxlint/base.oxlintrc.json, oxfmt/base.oxfmtrc.json, vitest/base.ts, lefthook.yml).

  5. Проверь воспроизводимость от чистого состояния: `rm -rf node_modules && pnpm install --frozen-lockfile` проходит без ERR_PNPM_OUTDATED_LOCKFILE.

  6. НЕ редактируй package.json вручную помимо того, что сделал `pnpm add`. НЕ добавляй пакет в `pnpm.onlyBuiltDependencies`.

  Закоммить `package.json` + `pnpm-lock.yaml` ВМЕСТЕ.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.devDependencies['@solid-stats/ts-toolchain']!=='github:solid-stats/ts-toolchain#v0.1.0')process.exit(1)" && grep -q "solid-stats/ts-toolchain" pnpm-lock.yaml && test -f node_modules/@solid-stats/ts-toolchain/tsconfig/base.json && rm -rf node_modules && pnpm install --frozen-lockfile && test -f node_modules/@solid-stats/ts-toolchain/tsconfig/base.json && echo OK</automated>
  </verify>
  <done>`package.json` devDependencies несёт git-dep пинованную `#v0.1.0`; `pnpm-lock.yaml` резолвит её в фиксированный SHA; `pnpm install --frozen-lockfile` от чистого `node_modules` проходит и распаковывает все пять пресетов; пакет НЕ в onlyBuiltDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Переключить tsconfig.json на extends shared base и подтвердить зелёный verify</name>
  <files>tsconfig.json</files>
  <read_first>
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md §`replays-fetcher/tsconfig.json` (current shape + target shape: что уходит в base, что остаётся)
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md §Preset Consumption Patterns §1 + Pitfall P13-3/P13-5 (exports subpath resolution; явные include/types)
    - tsconfig.json (current — full shape)
    - tsconfig.build.json (transitively extends ./tsconfig.json — НЕ менять, убедиться что build всё ещё видит базу)
  </read_first>
  <action>
  Перепиши `tsconfig.json` на target-форму из 13-PATTERNS.md §`replays-fetcher/tsconfig.json`:
  - Добавь `"extends": "@solid-stats/ts-toolchain/tsconfig/base.json"` (bare specifier — TypeScript резолвит через exports map shared package.json, Pitfall P13-3).
  - В `compilerOptions` ОСТАВЬ ТОЛЬКО repo-specific поля: `outDir` `"dist"`, `rootDir` `"."`, `types` `["node", "vitest"]`. УДАЛИ все strict-флаги и `target`/`module`/`moduleResolution`/`skipLibCheck` — они теперь наследуются из base (это и есть CFG-04: ссылка на пресет вместо дублирования контента).
  - СОХРАНИ `include` `["src/**/*.ts", "vitest.config.ts", "eslint.config.js"]` и `exclude` `["dist", "node_modules"]` ВЕРБАТИМ (явно — не наследуй; удаление `eslint.config.js` из include отложено на Phase 16, Open Question 2).

  Затем докажи отсутствие изменения поведения (toolchain-only инвариант):
  1. `pnpm run typecheck` (`tsc -p tsconfig.json --noEmit`) — зелёный. Если TS теряет глобалы vitest/node или файлы из include — проверь, что `types` и `include` заданы явно (Pitfall P13-5), и что extends зарезолвился (Assumption A3); НЕ ослабляй strict-настройки.
  2. `pnpm run build` (`tsc -p tsconfig.build.json`) — зелёный (build-tsconfig транзитивно наследует базу через ./tsconfig.json).
  3. `pnpm run verify` end-to-end зелёный: format → lint → typecheck → test → test:integration (Docker testcontainers) → test:coverage (100% statements/branches/functions/lines) → build.
  4. Финальный gate: `rm -rf node_modules && pnpm install --frozen-lockfile && pnpm run verify` зелёный от чистого состояния (CFG-03 + CFG-04 совместно).

  НЕ трогай scripts `lint`/`format`/`build`, `tsconfig.build.json`, `Dockerfile`, `src/`. Закоммить `tsconfig.json`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=JSON.parse(fs.readFileSync('tsconfig.json','utf8'));if(t.extends!=='@solid-stats/ts-toolchain/tsconfig/base.json')process.exit(1);if(t.compilerOptions.strict!==undefined)process.exit(1);if(!t.compilerOptions.outDir||!t.compilerOptions.types||!t.include)process.exit(1)" && pnpm run typecheck && pnpm run verify && echo OK</automated>
  </verify>
  <done>`tsconfig.json` extends `@solid-stats/ts-toolchain/tsconfig/base.json`, в его `compilerOptions` нет унаследованных strict-флагов (только outDir/rootDir/types), `include`/`exclude` заданы явно; `pnpm run typecheck`, `pnpm run build` и полный `pnpm run verify` (100% coverage) зелёные; `pnpm install --frozen-lockfile` от чистого состояния + verify зелёные. `src/`, скрипты lint/format/build, Dockerfile не тронуты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetcher build/CI/Docker → GitHub git-dep | `pnpm install` тянет внешний публичный config-пакет |
| external config preset → fetcher tsconfig | strict-настройки TS теперь приходят из внешнего пресета |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | git-dep resolution (mutable ref) | mitigate | pin тегом `#v0.1.0` (не веткой); `pnpm add` резолвит тег → 40-char SHA в lockfile; `--frozen-lockfile` в CI + Docker верифицирует SHA → mutable ref не может тихо переехать; acceptance: lockfile содержит SHA, frozen-install зелёный от чистого состояния |
| T-13-08 | Tampering | reproducibility of install | mitigate | `pnpm-lock.yaml` коммитится вместе с package.json; финальный gate `rm -rf node_modules && pnpm install --frozen-lockfile` проходит; Dockerfile уже использует `--frozen-lockfile` (без auth для public repo) |
| T-13-09 | Tampering / Elevation | git-dep lifecycle scripts | mitigate | shared repo не имеет build/prepare; пакет НЕ в `pnpm.onlyBuiltDependencies`; pnpm 11 `strictDepBuilds` блокирует git-dep lifecycle-скрипты по умолчанию |
| T-13-10 | Denial of Service | поведенческий регресс toolchain | accept | extends только tsconfig, поведение проверяется `pnpm verify` 100% coverage; swaps lint/format/build отложены на Phases 15-17 → blast radius минимален |
</threat_model>

<verification>
- `package.json` devDependencies несёт `@solid-stats/ts-toolchain: github:solid-stats/ts-toolchain#v0.1.0`.
- `pnpm-lock.yaml` резолвит git-dep в фиксированный 40-char SHA (совпадает с peeled SHA тега v0.1.0).
- `pnpm install --frozen-lockfile` от чистого `node_modules` проходит (CFG-03 reproducibility).
- `tsconfig.json` extends `@solid-stats/ts-toolchain/tsconfig/base.json`; strict-флаги не дублируются; include/types явные.
- `pnpm run typecheck`, `pnpm run build`, полный `pnpm run verify` (100% coverage) зелёные (CFG-04, поведение не изменилось).
- `src/`, скрипты lint/format/build, tsconfig.build.json, Dockerfile не тронуты (toolchain-only инвариант).
</verification>

<success_criteria>
- CFG-03: фетчер потребляет `@solid-stats/ts-toolchain` как git-dep пинованную тегом `v0.1.0`; `pnpm-lock.yaml` воспроизводим (frozen-lockfile install работает в CI + Docker).
- CFG-04: `tsconfig.json` extends shared base вместо дублирования strict-контента; `pnpm verify` остаётся зелёным end-to-end.
</success_criteria>

<output>
Create `.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-03-SUMMARY.md` when done. Запиши: резолвленный 40-char SHA в lockfile, tsconfig keys removed (список strict-флагов ушедших в base) / added (`extends`), подтверждение зелёного `pnpm verify` + frozen-lockfile install, подтверждение что `src/`/scripts/Dockerfile не тронуты.
</output>

## Artifacts this phase produces (plan 13-03)

| Artifact | Change | Note |
|----------|--------|------|
| `package.json` | modified | +devDependency `@solid-stats/ts-toolchain: github:solid-stats/ts-toolchain#v0.1.0` |
| `pnpm-lock.yaml` | regenerated | git-dep резолвлен в фиксированный commit SHA; воспроизводим под `--frozen-lockfile` |
| `tsconfig.json` | modified | +`extends @solid-stats/ts-toolchain/tsconfig/base.json`; removed strict-флаги ( → base); kept outDir/rootDir/types/include/exclude |

New devDependency: `@solid-stats/ts-toolchain` (git-dep). Tag consumed: `v0.1.0`. Not touched: `src/`, lint/format/build scripts, `tsconfig.build.json`, `Dockerfile`.
