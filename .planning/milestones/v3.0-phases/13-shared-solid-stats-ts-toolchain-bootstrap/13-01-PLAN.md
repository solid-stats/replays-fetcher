---
phase: 13-shared-solid-stats-ts-toolchain-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "EXTERNAL:solid-stats/ts-toolchain/package.json"
  - "EXTERNAL:solid-stats/ts-toolchain/tsconfig/base.json"
  - "EXTERNAL:solid-stats/ts-toolchain/tsconfig.json"
  - "EXTERNAL:solid-stats/ts-toolchain/oxlint/base.oxlintrc.json"
  - "EXTERNAL:solid-stats/ts-toolchain/oxfmt/base.oxfmtrc.json"
  - "EXTERNAL:solid-stats/ts-toolchain/vitest/base.ts"
  - "EXTERNAL:solid-stats/ts-toolchain/lefthook.yml"
  - "EXTERNAL:solid-stats/ts-toolchain/.github/workflows/ci.yml"
  - "EXTERNAL:solid-stats/ts-toolchain/.gitignore"
  - "EXTERNAL:solid-stats/ts-toolchain/README.md"
  - "EXTERNAL:solid-stats/ts-toolchain/pnpm-lock.yaml"
autonomous: true
requirements: [CFG-01, CFG-02]
must_haves:
  truths:
    - "Репозиторий solid-stats/ts-toolchain содержит пять пресетов (tsconfig/oxlint/oxfmt/vitest) + lefthook.yml на ветке master"
    - "package.json пресета имеет exports map с подключом ./tsconfig/base.json"
    - "Самовалидирующий CI workflow (.github/workflows/ci.yml) запускается на push в master и гоняет lint/format/typecheck"
    - "Все файлы пресета закоммичены и запушены в origin/master через git/gh"
  artifacts:
    - path: "EXTERNAL:solid-stats/ts-toolchain/package.json"
      provides: "config-only пакет с files/exports/scripts/devDependencies"
      contains: "@solid-stats/ts-toolchain"
    - path: "EXTERNAL:solid-stats/ts-toolchain/tsconfig/base.json"
      provides: "strict TypeScript base preset"
      contains: "exactOptionalPropertyTypes"
    - path: "EXTERNAL:solid-stats/ts-toolchain/oxlint/base.oxlintrc.json"
      provides: "spike-locked oxlint preset (plugins typescript/unicorn/import/oxc)"
      contains: "unicorn/no-null"
    - path: "EXTERNAL:solid-stats/ts-toolchain/oxfmt/base.oxfmtrc.json"
      provides: "flat oxfmt reference (printWidth 80)"
      contains: "printWidth"
    - path: "EXTERNAL:solid-stats/ts-toolchain/vitest/base.ts"
      provides: "shared vitest coverage defaults (v8, thresholds 100)"
      contains: "vitestBaseConfig"
    - path: "EXTERNAL:solid-stats/ts-toolchain/lefthook.yml"
      provides: "pre-commit/pre-push hook preset"
      contains: "pre-commit"
    - path: "EXTERNAL:solid-stats/ts-toolchain/.github/workflows/ci.yml"
      provides: "self-validating CI (lint/format/typecheck)"
      contains: "pnpm run typecheck"
  key_links:
    - from: "EXTERNAL:solid-stats/ts-toolchain/package.json"
      to: "EXTERNAL:solid-stats/ts-toolchain/tsconfig/base.json"
      via: "exports map подключ ./tsconfig/base.json → ./tsconfig/base.json"
      pattern: "\"./tsconfig/base.json\""
    - from: "EXTERNAL:solid-stats/ts-toolchain/tsconfig.json"
      to: "EXTERNAL:solid-stats/ts-toolchain/tsconfig/base.json"
      via: "self-validation tsconfig extends ./tsconfig/base.json для typecheck в CI"
      pattern: "\"extends\""
---

<objective>
Авторить начальное содержимое **внешнего** репозитория `solid-stats/ts-toolchain` (он уже существует, публичный, пустой, default-ветка `master`) и запушить его в `origin/master`: пять пресетов (tsconfig / oxlint / oxfmt / vitest) + `lefthook.yml` + config-only `package.json` (с `exports` для tsconfig subpath) + самовалидирующий GitHub Actions CI + собственный воспроизводимый `pnpm-lock.yaml`.

Purpose: закрыть CFG-01 (репо с пятью пресетами + lefthook.yml) и заложить CFG-02 (самовалидирующий CI), создав консумируемую основу для tag-cut в плане 13-02 и git-dep в плане 13-03. Эта фаза — toolchain-only bootstrap: `src/`, CLI и пятибэндовый ingest-пайплайн фетчера ЗАМОРОЖЕНЫ и НЕ затрагиваются этим планом.

Output: запушенная ветка `master` репозитория `solid-stats/ts-toolchain` со всеми файлами пресета и зелёным (или запускающимся) CI.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md
@.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md
@.planning/spikes/001-oxlint-preset-port/oxlintrc.candidate.json
@.planning/spikes/CONVENTIONS.md
@tsconfig.json
@vitest.config.ts
</context>

<critical_constraints>
- **Внешний репо НЕ внутри воркдерева фетчера.** Авторь его в sibling temp-директории (например `$(mktemp -d)/ts-toolchain` или `/tmp/ts-toolchain-bootstrap`), клонируя через `git clone git@github.com:solid-stats/ts-toolchain.git`. НЕ добавляй preset-файлы в дерево `replays-fetcher`. `git status --short` в воркдереве фетчера ДОЛЖЕН остаться чистым после этого плана (этот план не меняет ни одного файла фетчера).
- **Только TAG/SHA pin** в downstream (план 13-03) — но сам этот план просто пушит `master`; tag режется в плане 13-02 ПОСЛЕ зелёного CI.
- Репо публичный ⇒ `pnpm install` в чужом CI должен работать БЕЗ auth. Не вводи приватные зависимости.
- Версии инструментов **spike-locked**: oxlint `1.69.0`, oxfmt `0.54.0`, typescript `^6.0.3` (следует фетчеру), node `>=25 <26`, pnpm `>=11 <12`, packageManager `pnpm@11.0.9`.
- Не добавляй `build`/`prepare` скрипты в `package.json` пресета — config-only пакет, pnpm 11 блокирует prepare у git-deps по умолчанию.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Клонировать пустой внешний репо и заавторить файлы пресетов в sibling-директории</name>
  <files>EXTERNAL:solid-stats/ts-toolchain/package.json, EXTERNAL:solid-stats/ts-toolchain/tsconfig/base.json, EXTERNAL:solid-stats/ts-toolchain/tsconfig.json, EXTERNAL:solid-stats/ts-toolchain/oxlint/base.oxlintrc.json, EXTERNAL:solid-stats/ts-toolchain/oxfmt/base.oxfmtrc.json, EXTERNAL:solid-stats/ts-toolchain/vitest/base.ts, EXTERNAL:solid-stats/ts-toolchain/lefthook.yml, EXTERNAL:solid-stats/ts-toolchain/.gitignore, EXTERNAL:solid-stats/ts-toolchain/README.md</files>
  <read_first>
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md §`@solid-stats/ts-toolchain` Repo Structure + §Preset Consumption Patterns (точные shape'ы package.json, base.json, exports map, scripts)
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md §Pattern Assignments — External Repo Files (готовые pattern-блоки для каждого файла)
    - .planning/spikes/001-oxlint-preset-port/oxlintrc.candidate.json (полный spike-locked oxlint ruleset — портируется в oxlint/base.oxlintrc.json ВЕРБАТИМ, см. action)
    - tsconfig.json (источник значений compilerOptions для tsconfig/base.json — переносятся ВСЕ кроме outDir/rootDir/types)
    - vitest.config.ts (источник coverage thresholds для vitest/base.ts)
  </read_first>
  <action>
  Создай sibling рабочую директорию ВНЕ воркдерева фетчера: `WORKDIR=$(mktemp -d)/ts-toolchain` и `git clone git@github.com:solid-stats/ts-toolchain.git "$WORKDIR"`. Все правки делай ТОЛЬКО в `$WORKDIR`. (Репо публичный и пустой; clone даст пустое дерево с remote `origin` и default-веткой `master` — если локальной ветки нет, `git switch -c master`.)

  Авторь файлы (значения бери ВЕРБАТИМ из 13-RESEARCH.md §Repo Structure и 13-PATTERNS.md):

  1. `package.json` — config-only пакет per 13-RESEARCH.md §Repo Structure: `name` `@solid-stats/ts-toolchain`, `version` `0.1.0`, `type` `module`, `private` `false`, `description`, `files` массив `["tsconfig","oxlint","oxfmt","vitest","lefthook.yml"]`, `exports` map с пятью подключами (`./tsconfig/base.json`, `./oxlint/base.oxlintrc.json`, `./oxfmt/base.oxfmtrc.json`, `./vitest/base` → `./vitest/base.ts`, `./lefthook.yml`), `scripts` `{lint: "oxlint vitest/", format: "oxfmt --check .", "format:write": "oxfmt --write .", typecheck: "tsc --noEmit"}`, `devDependencies` `{oxlint: "1.69.0", oxfmt: "0.54.0", typescript: "^6.0.3"}`, `engines` `{node: ">=25 <26", pnpm: ">=11 <12"}`, `packageManager` `pnpm@11.0.9`. НЕТ `main`/`module`/`build`/`prepare`. Не добавляй `@solid-stats/ts-toolchain` в `pnpm.onlyBuiltDependencies` — у него нет build step.

  2. `tsconfig/base.json` — strict база per 13-PATTERNS.md: `$schema` json.schemastore.org/tsconfig, и в `compilerOptions` ВСЕ флаги из текущего фетчер-`tsconfig.json` КРОМЕ `outDir`/`rootDir`/`types` (т.е. target ES2023, module/moduleResolution NodeNext, strict, exactOptionalPropertyTypes, noFallthroughCasesInSwitch, noImplicitOverride, noImplicitReturns, noPropertyAccessFromIndexSignature, noUncheckedIndexedAccess, noUncheckedSideEffectImports, noUnusedLocals, noUnusedParameters, skipLibCheck). НЕ включай `include`/`exclude`/`types` в base — они repo-specific (Pitfall P13-5).

  3. `tsconfig.json` (в корне shared repo, для self-typecheck в CI) — `extends` `./tsconfig/base.json`, `compilerOptions.types` `["node"]`, `include` `["vitest/base.ts"]`. Это позволяет `pnpm run typecheck` (`tsc --noEmit`) проверить vitest/base.ts.

  4. `oxlint/base.oxlintrc.json` — портируй spike-locked ruleset из `.planning/spikes/001-oxlint-preset-port/oxlintrc.candidate.json` ВЕРБАТИМ (plugins `["typescript","unicorn","import","oxc"]` + полный rules-блок), с двумя fetcher-релевантными overrides уже заложенными в кандидате: `unicorn/no-null` off, `no-await-in-loop` остаётся как есть в кандидате. ВАЖНО: замени поле `$schema` со spike-пути `./node_modules/oxlint/configuration_schema.json` на тот же относительный путь — он валиден, т.к. oxlint — devDep shared repo. (Этот пресет авторится сейчас, но ПОТРЕБЛЯЕТСЯ фетчером только в Phase 16 — не в этой фазе.)

  5. `oxfmt/base.oxfmtrc.json` — flat reference per 13-RESEARCH.md: `{printWidth: 80, useTabs: false, semi: true, singleQuote: false, trailingComma: "all"}`. oxfmt НЕ поддерживает extends (issue #16394) — это образец, который consumers дублируют.

  6. `vitest/base.ts` — экспортируй `vitestBaseConfig` через `defineConfig` из `vitest/config` с `test.coverage.provider "v8"` и `thresholds {statements:100, branches:100, functions:100, lines:100}` (значения из фетчер-`vitest.config.ts`). Named export `export const vitestBaseConfig`.

  7. `lefthook.yml` — preset per 13-RESEARCH.md §lefthook: `pre-commit.commands.format` (`glob "*.{ts,tsx,js,mjs,json}"`, `run: oxfmt --check {staged_files}`), `pre-commit.commands.lint` (`glob "*.{ts,tsx}"`, `run: oxlint {staged_files}`), `pre-push.commands.typecheck` (`run: pnpm run typecheck`), `pre-push.commands.test` (`run: pnpm test`). Только shipping — wiring в фетчер будет в Phase 18.

  8. `.gitignore` — минимум: `node_modules/`, `dist/`, `*.log`.

  9. `README.md` — короткое описание: shared TypeScript toolchain presets для Solid Stats сервисов; как потребляется (tsconfig extends через bare specifier, oxlint через relative node_modules path, oxfmt дублирование значений, vitest mergeConfig, lefthook extends node_modules path); что pin делается ТОЛЬКО тегом/SHA. Укажи, что это pilot, который позже потребят server-2 и web — структура пресетов должна оставаться reusable, не fetcher-specific.

  Используй Write-инструмент для каждого файла (НЕ heredoc). Все строки/идентификаторы/ключи — verbatim английский.
  </action>
  <verify>
    <automated>WORKDIR=$(git -C "$WORKDIR" rev-parse --show-toplevel) && test -f "$WORKDIR/package.json" && test -f "$WORKDIR/tsconfig/base.json" && test -f "$WORKDIR/tsconfig.json" && test -f "$WORKDIR/oxlint/base.oxlintrc.json" && test -f "$WORKDIR/oxfmt/base.oxfmtrc.json" && test -f "$WORKDIR/vitest/base.ts" && test -f "$WORKDIR/lefthook.yml" && node -e "const p=require('$WORKDIR/package.json'); if(!p.exports['./tsconfig/base.json'])process.exit(1); if(p.scripts.build||p.scripts.prepare)process.exit(1); if(p.private!==false)process.exit(1)" && git -C "$(pwd)" status --short | grep -q . && echo "FETCHER TREE DIRTY — FAIL" || echo OK</automated>
  </verify>
  <done>В `$WORKDIR` существуют все девять файлов; `package.json` имеет `exports['./tsconfig/base.json']`, `private:false`, нет `build`/`prepare` скриптов; воркдерево фетчера осталось чистым (план не тронул ни одного файла фетчера).</done>
</task>

<task type="auto">
  <name>Task 2: Сгенерировать собственный lockfile shared repo, добавить self-validating CI, закоммитить и запушить master</name>
  <files>EXTERNAL:solid-stats/ts-toolchain/.github/workflows/ci.yml, EXTERNAL:solid-stats/ts-toolchain/pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-RESEARCH.md §Self-validating CI (минимальный workflow: checkout/pnpm/setup-node/install --frozen-lockfile/lint/format/typecheck)
    - .planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-PATTERNS.md §`.github/workflows/ci.yml` (role-match с фетчер cd.yml; action-версии)
    - .github/workflows/cd.yml (фетчер CI как структурный аналог: pnpm/action-setup, actions/setup-node node-version 25 cache pnpm)
  </read_first>
  <action>
  В `$WORKDIR` (sibling-директория из Task 1):

  1. Сгенерируй собственный воспроизводимый lockfile shared repo: `pnpm install` (без `--frozen-lockfile` — lockfile ещё не существует). Это поставит devDeps (oxlint 1.69.0, oxfmt 0.54.0, typescript ^6.0.3) и создаст `pnpm-lock.yaml`. Этот lockfile нужен, чтобы CI shared repo мог делать `pnpm install --frozen-lockfile`.

  2. Перед commit прогони пресет-валидацию ЛОКАЛЬНО (то же, что будет в CI), чтобы master запушился уже зелёным: `pnpm run lint` (oxlint на vitest/), `pnpm run format` (oxfmt --check .), `pnpm run typecheck` (tsc --noEmit на vitest/base.ts). Если `oxfmt --check` находит расхождения — прогони `pnpm run format:write` и пересними значения; цель — zero-diff. Если `oxlint` ругается на vitest/base.ts — поправь preset-файл (НЕ ослабляй ruleset). Если `tsc` падает — поправь vitest/base.ts. Все три ДОЛЖНЫ пройти локально перед push.

  3. Создай `.github/workflows/ci.yml` per 13-RESEARCH.md §Self-validating CI: `name: CI`, триггеры `on.push.branches:[master]` + `on.pull_request.branches:[master]`, job `validate` на `ubuntu-latest` с `timeout-minutes: 15`, шаги: `actions/checkout@v4`, `pnpm/action-setup@v4` (with `version: 11`), `actions/setup-node@v4` (with `node-version: '25'`, `cache: 'pnpm'`), `pnpm install --frozen-lockfile`, `pnpm run lint`, `pnpm run format`, `pnpm run typecheck`. (Это CFG-02 self-validation: CI линтит/форматит/тайпчекит пресеты ДО того, как режется консумируемый тег в плане 13-02.)

  4. Закоммить ВСЁ содержимое (`git add -A && git commit`) с сообщением вроде `feat: initial shared toolchain presets (tsconfig/oxlint/oxfmt/vitest + lefthook) + self-validating CI`, добавив co-author trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Запушь: `git push -u origin master`.

  5. Подтверди, что push прошёл: `git ls-remote origin master` возвращает SHA, совпадающий с локальным `git rev-parse HEAD`.

  НЕ режь тег в этом плане — tagging gate в плане 13-02 после подтверждения зелёного CI. Воркдерево фетчера должно остаться чистым.
  </action>
  <verify>
    <automated>test -f "$WORKDIR/pnpm-lock.yaml" && test -f "$WORKDIR/.github/workflows/ci.yml" && grep -q "pnpm run typecheck" "$WORKDIR/.github/workflows/ci.yml" && grep -q "branches:" "$WORKDIR/.github/workflows/ci.yml" && (cd "$WORKDIR" && pnpm install --frozen-lockfile && pnpm run lint && pnpm run format && pnpm run typecheck) && [ "$(git -C "$WORKDIR" rev-parse HEAD)" = "$(git -C "$WORKDIR" ls-remote origin master | cut -f1)" ] && echo OK</automated>
  </verify>
  <done>`pnpm-lock.yaml` существует в shared repo; локальные lint/format/typecheck зелёные; `.github/workflows/ci.yml` гоняет lint/format/typecheck на push в master; `origin/master` указывает на тот же SHA, что и локальный HEAD (push успешен). Тег НЕ создан.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → GitHub (`solid-stats/ts-toolchain`) | авторинг и push содержимого пресетов во внешний публичный репо |
| GitHub Actions runner → npm registry | shared repo CI ставит devDeps при `pnpm install --frozen-lockfile` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | shared repo preset files | mitigate | self-validating CI (lint/format/typecheck) гоняется на push в master ДО того, как режется консумируемый тег (план 13-02) — сломанный/враждебный пресет не доходит до фетчера; acceptance: ci.yml содержит все три шага |
| T-13-02 | Tampering | shared repo `pnpm-lock.yaml` | mitigate | воспроизводимый lockfile коммитится; CI использует `--frozen-lockfile` — devDeps не дрейфуют между push'ами; acceptance: pnpm-lock.yaml существует и закоммичен |
| T-13-SC | Tampering | npm/pnpm installs (oxlint/oxfmt/typescript devDeps) | mitigate | все три инструмента spike-proven (Package Legitimacy Audit в 13-RESEARCH.md: все OK); версии pinned (oxlint 1.69.0, oxfmt 0.54.0); pnpm 11 `strictDepBuilds` блокирует lifecycle-скрипты git-deps |
| T-13-03 | Tampering | fetcher worktree | accept | план авторит ВНЕ дерева фетчера (sibling temp dir); acceptance-проверка подтверждает `git status --short` фетчера чист |
</threat_model>

<verification>
- Все девять preset/config файлов существуют в sibling shared-repo дереве.
- `package.json` валиден как config-only пакет (exports map, private:false, нет build/prepare).
- oxlint/base.oxlintrc.json портирован verbatim из spike-кандидата (plugins typescript/unicorn/import/oxc).
- Локальные `pnpm run lint && pnpm run format && pnpm run typecheck` зелёные в shared repo.
- `origin/master` запушен; SHA совпадает с локальным HEAD.
- Воркдерево фетчера `replays-fetcher` осталось чистым (`git status --short` пуст) — план не тронул фетчер.
</verification>

<success_criteria>
- CFG-01: репо `solid-stats/ts-toolchain` на `master` содержит пять пресетов (tsconfig/oxlint/oxfmt/vitest) + `lefthook.yml` + config-only `package.json` с `exports`.
- CFG-02 (authoring): самовалидирующий `.github/workflows/ci.yml` существует и запускается на push в `master`, гоняя lint/format/typecheck (gate-cut тега — в плане 13-02).
</success_criteria>

<output>
Create `.planning/phases/13-shared-solid-stats-ts-toolchain-bootstrap/13-01-SUMMARY.md` when done. Запиши в summary: путь к sibling рабочей директории `$WORKDIR`, SHA запушенного `master` HEAD, и подтверждение что фетчер-дерево осталось чистым.
</output>

## Artifacts this phase produces (plan 13-01)

| Artifact | Repo | Note |
|----------|------|------|
| `package.json` (config-only, exports map) | solid-stats/ts-toolchain | new |
| `tsconfig/base.json` | solid-stats/ts-toolchain | new — strict base preset |
| `tsconfig.json` (self-validation) | solid-stats/ts-toolchain | new |
| `oxlint/base.oxlintrc.json` | solid-stats/ts-toolchain | new — spike-locked ruleset (consumed Phase 16) |
| `oxfmt/base.oxfmtrc.json` | solid-stats/ts-toolchain | new — flat reference (consumed Phase 15) |
| `vitest/base.ts` | solid-stats/ts-toolchain | new — coverage defaults (consumed Phase 14+) |
| `lefthook.yml` | solid-stats/ts-toolchain | new — hook preset (wired Phase 18) |
| `.github/workflows/ci.yml` | solid-stats/ts-toolchain | new — self-validating CI |
| `pnpm-lock.yaml` | solid-stats/ts-toolchain | new — own reproducible lockfile |
| `.gitignore`, `README.md` | solid-stats/ts-toolchain | new |

Pushed branch: `origin/master`. Tag: NOT cut here (plan 13-02). No fetcher devDependency / tsconfig change here (plan 13-03).
