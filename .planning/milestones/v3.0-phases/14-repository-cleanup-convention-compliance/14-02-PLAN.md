---
phase: 14-repository-cleanup-convention-compliance
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/errors/config-validation-error.ts
  - src/config.ts
  - src/index.ts
  - src/cli.ts
  - src/config.test.ts
  - src/cli.test.ts
autonomous: true
requirements: [CLN-04]
must_haves:
  truths:
    - "Config validation failure throws a typed AppError subclass (code config_invalid), not a raw Error"
    - "Every externally-sourced string/URL config field is length-bounded with .max()"
    - "Invalid config still surfaces the same issue strings and the same exit-code-2 behavior"
    - "pnpm verify stays green at 100% coverage; CLI behavior byte-identical"
  artifacts:
    - path: "src/errors/config-validation-error.ts"
      provides: "ConfigValidationError extends AppError<\"config_invalid\"> carrying issues in details"
      contains: "extends AppError"
    - path: "src/config.ts"
      provides: "configSchema with .max() bounds on all 11 unbounded string/URL fields; throws ConfigValidationError"
      contains: ".max("
  key_links:
    - from: "src/config.ts"
      to: "src/errors/config-validation-error.ts"
      via: "loadConfig/loadSourceConfig throw new ConfigValidationError(...)"
      pattern: "ConfigValidationError"
    - from: "src/cli.ts"
      to: "src/errors/config-validation-error.ts"
      via: "instanceof ConfigValidationError guards in catch blocks"
      pattern: "instanceof ConfigValidationError"
---

<objective>
CLN-04a + CLN-04b: привести типизацию ошибок конфига и валидацию полей к `solidstats-fetcher-ts-conventions`. (a) `ConfigError extends Error` → `ConfigValidationError extends AppError<"config_invalid">` (§B типизированная иерархия ошибок; §C показывает именно `ConfigValidationError`); (b) добавить `.max()` к 11 неограниченным строковым/URL-полям configSchema (§C/std §D «unbounded externally-sourced field is a DoS vector»).

Purpose: Закрыть два реальных отклонения от skill в одном плане, потому что оба правят `src/config.ts` (один владелец файла — одна волна). Поведение сохраняется: те же issue-строки, тот же exit code 2, те же accepted значения config.
Output: Новый `src/errors/config-validation-error.ts`; обновлённые `config.ts`, `index.ts`, `cli.ts` и тесты.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-repository-cleanup-convention-compliance/14-RESEARCH.md

# Skill — планка соответствия
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# Источники, которые задание правит / на которые опирается
@src/config.ts
@src/errors/app-error.ts
@src/errors/checkpoint-conflict-error.ts
@src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLN-04a — ConfigError → ConfigValidationError extends AppError</name>
  <files>src/errors/config-validation-error.ts, src/config.ts, src/index.ts, src/cli.ts, src/config.test.ts, src/cli.test.ts</files>
  <read_first>
    - src/errors/app-error.ts (abstract base: protected constructor `(code, message, options?)`; generic `<Code extends string>`; есть `details`, `isOperational`, `cause`; НЕТ httpStatus — не восстанавливать)
    - src/errors/checkpoint-conflict-error.ts (эталонный паттерн concrete-подкласса AppError: public constructor, `super("checkpoint-conflict", "<msg>", { cause, details, isOperational: true })`, flatten-details helper)
    - src/config.ts:122-130 (текущий `class ConfigError extends Error` с `readonly issues: string[]`), :153, :169 (два throw-site)
    - src/index.ts:1 (реэкспорт `ConfigError`)
    - src/cli.ts:20 (import), :250, :729, :750 (три `instanceof ConfigError`)
    - src/config.test.ts (импорт + `.toThrow(ConfigError)` на строках 70, 276, 345, 374)
    - 14-RESEARCH.md §CLN-04 DEV-01 + Pitfall 3 (полный список call sites)
    - .agents/skills/solidstats-fetcher-ts-conventions §B/§C; "config_invalid" уже существует как литерал RunFailureCategory в run/types.ts
  </read_first>
  <behavior>
    - Невалидный config (отсутствуют обязательные поля) → выбрасывается `ConfigValidationError`, и `error instanceof AppError === true`, `error.code === "config_invalid"`.
    - `error.issues` остаётся доступен как массив строк формата `"<path>: <message>"` (как раньше у ConfigError) — call sites в cli.ts читают `error.issues`.
    - `loadConfig({})` и `loadSourceConfig({})` бросают `ConfigValidationError` (тесты `.toThrow(ConfigValidationError)`).
    - CLI exit-code остаётся 2 для config-ошибки; JSON-вывод `check`/`discover` с `issues: [...]` байт-идентичен (тот же массив строк).
  </behavior>
  <action>
    Создать `src/errors/config-validation-error.ts`: `export class ConfigValidationError extends AppError<"config_invalid">` по эталону `checkpoint-conflict-error.ts`. Public constructor принимает `issues: string[]`; вызывает `super("config_invalid", \`Invalid configuration: ${issues.join("; ")}\`, { details: { issues }, isOperational: true })`. Сохранить публичное readonly-поле `issues: readonly string[]` на инстансе, чтобы существующие call sites (`error.issues`) продолжали работать без чтения из `details`. НЕ добавлять httpStatus (app-error.ts §9-12).
    В `src/config.ts`: удалить класс `ConfigError`; заменить оба `throw new ConfigError(...)` (строки 153, 169) на `throw new ConfigValidationError(...)` с тем же массивом issue-строк; импортировать `ConfigValidationError` из `./errors/config-validation-error.js`. Per DEV-01: переименование, не изменение логики — issue-строки собираются тем же `.map((issue) => \`${issue.path.join(".")}: ${issue.message}\`)`.
    В `src/index.ts`: заменить реэкспорт `ConfigError` на `ConfigValidationError` (источник теперь `./errors/config-validation-error.js`; `loadConfig`/`redactConfig`/типы остаются из `./config.js`).
    В `src/cli.ts`: заменить импорт `ConfigError` и все три `error instanceof ConfigError` (строки 250, 729, 750) на `ConfigValidationError` (импорт из `./errors/config-validation-error.js`, убрать из импорта `./config.js`).
    В `src/config.test.ts` и `src/cli.test.ts`: обновить импорт и все `.toThrow(ConfigError)` / упоминания типа на `ConfigValidationError`. Тесты проверяют поведение по типу — заменить идентификатор, не ослаблять проверки. НЕ делать `ConfigError` обратносовместимым alias — это pilot-репо без внешних потребителей (RESEARCH Open Question 1); полное переименование. Grep по `ConfigError` после правок должен вернуть 0.
  </action>
  <verify>
    <automated>grep -rn "ConfigError" src/ && echo "FAIL: ConfigError reference remains" || (pnpm run typecheck && pnpm test -- --run src/config.test.ts src/cli.test.ts)</automated>
  </verify>
  <done>`src/errors/config-validation-error.ts` существует; `ConfigValidationError extends AppError<"config_invalid">`; grep по `ConfigError` в `src/` = 0; `loadConfig({})`/`loadSourceConfig({})` бросают `ConfigValidationError` (тесты зелёные); `error.issues` доступен; CLI exit-code-2 и JSON-issues байт-идентичны; typecheck зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLN-04b — добавить .max() bounds к 11 неограниченным config-полям</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts:37-100 (sourceConfigSchema + configSchema; поля с `.min(1)`/`z.url()` без `.max()`)
    - 14-RESEARCH.md §CLN-04 DEV-03 (таблица 11 полей + рекомендованные .max() значения)
    - .agents/skills/solidstats-fetcher-ts-conventions §C («Bound every field: strings get .max(); unbounded externally-sourced field is a DoS vector [🟡]»); std §D
  </read_first>
  <behavior>
    - Каждое из 11 строковых/URL-полей принимает значения текущей валидной длины без изменений (config из тестов и реальных env проходит).
    - Поле, переполняющее свой `.max()`, отвергается с issue по тому же path — но это новая защитная ветка, не меняющая успешный путь.
    - Все существующие config-тесты остаются зелёными без изменения тестовых значений (значения короче лимитов).
  </behavior>
  <action>
    Добавить `.max(n)` к каждому из 11 полей configSchema per DEV-03: `sourceSshHost` `.max(253)`, `sourceSshCommand` `.max(2048)`, `region` `.max(64)`, `bucket` `.max(63)`, `accessKeyId` `.max(128)`, `secretAccessKey` `.max(256)`, `checkpointPrefix` `.max(256)`, `evidencePrefix` `.max(256)`; URL-поля `sourceUrl` / `endpoint` / `databaseUrl` (`z.url()`) — `.max(2048)`. Цепочки: для строк `z.string().min(1).max(n)` (с сохранением `.optional()`/`.default(...)` где есть); для URL `z.url().max(n)`. Лимиты вынести именованными константами рядом с существующими bound-константами (`MIN_CONCURRENCY` и т.п.) — `.min/.max` аргументы НЕ освобождены от no-magic-numbers (см. STATE Phase 10-01 решение); назвать, напр., `MAX_HOSTNAME_LEN = 253`, `MAX_URL_LEN = 2048`, `MAX_S3_BUCKET_LEN = 63` и т.д. Это ТОЛЬКО схема — никакой логики loadConfig/redactConfig не менять. НЕ менять `.min()`, defaults, `.optional()`, `booleanFromEnvironment`, numeric-поля (они уже bounded). Если какой-то config-тест передаёт строку длиннее нового лимита (маловероятно) — НЕ ослаблять лимит; скорректировать тестовое значение на реалистичное в пределах лимита и отметить в SUMMARY.
  </action>
  <verify>
    <automated>test "$(grep -c '\.max(' src/config.ts)" -ge 11 && pnpm run typecheck && pnpm test -- --run src/config.test.ts || echo "FAIL: fewer than 11 .max() bounds or tests/typecheck red"</automated>
  </verify>
  <done>`src/config.ts` содержит `.max()` на всех 11 строковых/URL-полях (>=11 вхождений `.max(`); лимиты — именованные константы; `pnpm run typecheck` зелёный; `src/config.test.ts` зелёный без ослабления проверок; успешный путь loadConfig неизменён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → loadConfig | Внешне-управляемые значения окружения парсятся Zod-схемой до любого side-effect (§C: validate at boot). CLN-04b добавляет верхние границы длины на этой границе. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Denial of Service | неограниченные строковые config-поля (V5 Input Validation) | mitigate | Zod `.max()` на 11 полях (DEV-03); граница длины на boot-границе. Severity: low (single-writer scheduled job, env-источник доверенный, но §C/std §D предписывают bound всё равно). |
| T-14-03 | Tampering | поведенческая регрессия от переименования/bounds | mitigate | `pnpm verify` зелёный + 100% coverage; переименование ConfigError — типобезопасное (grep=0 остаточных), bounds не трогают успешный путь. Severity: low. |
</threat_model>

<verification>
- `grep -rn "ConfigError" src/` → 0 (полное переименование).
- `grep -c '\.max(' src/config.ts` → >= 11.
- `pnpm run typecheck && pnpm test` — зелёные; config/cli тесты проходят с переименованным типом.
- Полный гейт волны: `pnpm run verify` зелёный, coverage 100%, файловый набор НЕ сокращён (добавлен `src/errors/config-validation-error.ts` + его покрытие сохраняется тестами config).
</verification>

<success_criteria>
CLN-04a + CLN-04b закрыты: `ConfigValidationError extends AppError<"config_invalid">` заменил `ConfigError` во всех call sites; все 11 config-полей length-bounded именованными `.max()` константами; невалидный config даёт тот же exit-code-2 и те же issue-строки; `pnpm verify` зелёный при 100% coverage; CLI-поведение байт-идентично.
</success_criteria>

<output>
Create `.planning/phases/14-repository-cleanup-convention-compliance/14-02-SUMMARY.md` when done
</output>