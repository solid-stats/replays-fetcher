---
phase: 14-repository-cleanup-convention-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/cli.ts
  - src/run/run-once.ts
  - src/discovery/discover.ts
  - src/staging/stage-raw-replay.test.ts
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]
must_haves:
  truths:
    - "pnpm install no longer prints the 'pnpm field … no longer read' deprecation warning"
    - "Repo-wide search confirms 0 TODO/FIXME/XXX/HACK markers in code"
    - "Every retained eslint-disable carries a -- reason justification comment"
    - "pnpm verify stays green at 100% coverage; behavior unchanged"
  artifacts:
    - path: "package.json"
      provides: "Cleaned manifest with the deprecated pnpm.onlyBuiltDependencies block removed"
      contains: "\"verify\""
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "build-allowlist now sourced solely from allowBuilds: in the workspace file"
      pattern: "allowBuilds"
---

<objective>
Низкорисковая механическая уборка: CLN-01 (удалить устаревшее поле `pnpm.onlyBuiltDependencies` из `package.json`), CLN-02 (подтвердить отсутствие TODO/FIXME — действий по коду не требуется), CLN-03 (добавить `-- reason` к 9 `no-await-in-loop` suppress без обоснования).

Purpose: Привести репозиторий к чистому, документированному baseline на ТЕКУЩЕМ ESLint-инструментарии перед портом на Oxlint (Phase 16). Поведение НЕ меняется — только удаление дубля конфига и комментарии-обоснования.
Output: Очищенный `package.json`, 9 дополненных suppress-комментариев, подтверждённый чистый grep по TODO/FIXME.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-repository-cleanup-convention-compliance/14-RESEARCH.md

# Skill — планка соответствия (cite §refs in acceptance criteria)
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLN-01 — удалить deprecated pnpm.onlyBuiltDependencies из package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (блок `"pnpm": { "onlyBuiltDependencies": [...] }`, строки 51-59 — пять пакетов: cpu-features, esbuild, protobufjs, ssh2, unrs-resolver)
    - pnpm-workspace.yaml (уже содержит `allowBuilds:` с теми же пятью пакетами — актуальный дом настройки для pnpm 11+)
  </read_first>
  <action>
    Удалить из `package.json` весь верхнеуровневый блок `"pnpm": { "onlyBuiltDependencies": [...] }` целиком (это объект `pnpm` после `engines`), включая запятую перед ним если она остаётся висячей. Per CLN-01 / RESEARCH §CLN-01 F-01: pnpm 11.0.9 выдаёт `"The pnpm field in package.json is no longer read by pnpm"` при каждом install/CI; пять пакетов уже продублированы в `pnpm-workspace.yaml` `allowBuilds:`, поэтому удаление безопасно и не меняет, какие нативные сборки разрешены. НЕ трогать `pnpm-workspace.yaml`. НЕ трогать `engines`, `packageManager`, scripts, dependencies, devDependencies. Это единственная находка dead-config в репозитории (RESEARCH F-02..F-06: все остальные скрипты/файлы используются — KEEP).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.pnpm!==undefined){console.error('FAIL: pnpm field still present');process.exit(1)} console.log('ok: no pnpm field')" && pnpm install --frozen-lockfile 2>&1 | grep -i "no longer read" && echo "FAIL: warning still printed" || echo "ok: no deprecation warning"</automated>
  </verify>
  <done>`package.json` не содержит ключа `pnpm`; `pnpm install --frozen-lockfile` не печатает строку "no longer read"; `pnpm-workspace.yaml` неизменён и всё ещё перечисляет пять `allowBuilds`.</done>
</task>

<task type="auto">
  <name>Task 2: CLN-02 — подтвердить 0 TODO/FIXME repo-wide (confirm-only, без изменений кода)</name>
  <files>(none — confirm-only; no source files modified)</files>
  <read_first>
    - 14-RESEARCH.md §CLN-02 (repo-wide grep вернул 0 совпадений в коде; единственное совпадение — hex-строка integrity в pnpm-lock.yaml, не комментарий)
  </read_first>
  <action>
    Подтверждающая задача — НЕ изобретать работу (RESEARCH §CLN-02: CLN-02 уже выполнен). Прогнать repo-wide grep по `TODO|FIXME|XXX|HACK` в исходниках/конфигах/скриптах, исключив сгенерированные/вендоренные пути (`node_modules/`, `dist/`, `coverage/`, `.agents/`, `.claude/`, `.planning/`, `.git/`, `pnpm-lock.yaml`). Ожидаемый результат — пусто. Зафиксировать вывод в SUMMARY как evidence закрытия CLN-02. Если внезапно найден реальный маркер (не ожидается) — НЕ удалять молча: завести его как tracked-задачу или сообщить как blocker.
  </action>
  <verify>
    <automated>! grep -rIn --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=coverage --exclude-dir=.agents --exclude-dir=.claude --exclude-dir=.planning --exclude-dir=.git --exclude=pnpm-lock.yaml -E "TODO|FIXME|XXX|HACK" src/ *.ts *.js *.json *.yaml *.yml 2>/dev/null && echo "ok: 0 markers" || echo "FAIL: marker found — do NOT delete silently, report"</automated>
  </verify>
  <done>Grep по TODO/FIXME/XXX/HACK в коде/конфиге возвращает 0 совпадений; CLN-02 подтверждён закрытым с зафиксированным evidence; никакой код не изменён.</done>
</task>

<task type="auto">
  <name>Task 3: CLN-03 — добавить -- reason к 9 no-await-in-loop suppress без обоснования</name>
  <files>src/cli.ts, src/run/run-once.ts, src/discovery/discover.ts, src/staging/stage-raw-replay.test.ts</files>
  <read_first>
    - 14-RESEARCH.md §CLN-03 Категория B (таблица: файл/строка/предлагаемый текст для всех 9 строк)
    - src/cli.ts:568, 578 (sequential raw storage + staging follow)
    - src/run/run-once.ts:199, 203, 236 (pacer floor / page sequence / checkpoint before next page)
    - src/discovery/discover.ts:112, 116, 334 (sequential source requests / page detail / avoid aggressive polling)
    - src/staging/stage-raw-replay.test.ts:103 (sequential loop keeps assertion close to result)
    - .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md §A "Lint-suppression policy" + solidstats-shared-ts-standards §C (каждый retained suppress несёт обоснование)
  </read_first>
  <action>
    К каждой из 9 строк `// eslint-disable-next-line no-await-in-loop`, у которых СЕЙЧАС нет `-- <reason>`, дописать суффикс ` -- <reason>` ровно в той же строке директивы (формат `// eslint-disable-next-line no-await-in-loop -- <reason>`). Тексты обоснований — из таблицы RESEARCH §CLN-03 Категория B (sequential raw storage / staging follows raw evidence / RANGE-04 pacer floor awaited / sequential page processing / checkpoint before next page / source requests intentionally sequential / page detail same source-order sequence / avoid aggressive polling / sequential loop keeps assertion close). Все 14 `no-await-in-loop` LOAD-BEARING (фетчер намеренно sequential — CAS loop, pacing, discovery page loop, retry) — НИ ОДИН не удалять, только дополнить недостающие 9. Пять уже имеющих обоснование (retry.ts ×2, s3-checkpoint-store.ts ×3) — НЕ трогать. НЕ менять логику циклов, не переписывать await на Promise.all (это изменило бы поведение и нарушило source-order инвариант). Это правка ТОЛЬКО комментариев. ВАЖНО: cli.ts уже несёт `/* eslint-disable max-lines */` на строке 2 — НЕ трогать его в этом плане (он исчезнет после split в 14-04); здесь меняем только две `no-await-in-loop` строки внутри cli.ts.
  </action>
  <verify>
    <automated>grep -rn "eslint-disable-next-line no-await-in-loop" src/ | grep -v -- "-- " && echo "FAIL: bare no-await-in-loop disable without reason remains" || echo "ok: all no-await-in-loop disables justified"</automated>
  </verify>
  <done>Все вхождения `eslint-disable-next-line no-await-in-loop` в `src/` несут суффикс `-- <reason>` (0 голых); `pnpm run lint` зелёный; логика циклов не изменена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Чисто внутренняя уборка конфига и комментариев; новой внешней поверхности атаки не добавляется. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | поведенческая регрессия от уборки | mitigate | `pnpm verify` зелёный + 100% coverage после плана; правки ограничены package.json (дубль) и комментариями — логика не меняется. Severity: low. |
| T-14-SC | Tampering | npm/pnpm installs | accept | Phase 14 не устанавливает новых пакетов (RESEARCH §Package Legitimacy Audit: не применимо). |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` — без предупреждения "no longer read".
- `pnpm run lint` — зелёный; нет голых `no-await-in-loop` без `-- reason`.
- `pnpm run typecheck && pnpm test` — зелёные (поведение не изменено).
- Полный гейт волны: `pnpm run verify` зелёный, coverage 100%, файловый набор НЕ сокращён.
</verification>

<success_criteria>
CLN-01 закрыт (нет `pnpm` поля, нет deprecation warning); CLN-02 подтверждён закрытым (0 маркеров, evidence в SUMMARY); CLN-03 закрыт для 9 строк (все `no-await-in-loop` обоснованы); `pnpm verify` зелёный при 100% coverage; поведение фетчера не изменилось.
</success_criteria>

<output>
Create `.planning/phases/14-repository-cleanup-convention-compliance/14-01-SUMMARY.md` when done
</output>
