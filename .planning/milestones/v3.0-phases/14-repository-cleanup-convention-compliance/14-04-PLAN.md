---
phase: 14-repository-cleanup-convention-compliance
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - src/cli.ts
  - src/commands/check.ts
  - src/commands/contract-check.ts
  - src/commands/discover.ts
  - src/commands/run-once.ts
autonomous: true
requirements: [CLN-04]
must_haves:
  truths:
    - "cli.ts contains only buildCli + resolveDependencies + the four program.command().action() registrations"
    - "Each command's option parsing, dependency assembly, and orchestrator dispatch lives in src/commands/<command>.ts"
    - "The /* eslint-disable max-lines */ suppression in cli.ts is removed (file is now thin)"
    - "Public CLI surface is byte-identical: same commands, flags, exit codes, and JSON summary shape"
    - "pnpm verify stays green at 100% coverage with the file set NOT reduced"
  artifacts:
    - path: "src/commands/run-once.ts"
      provides: "run-once handler: option parsing, resource assembly, runOnce dispatch (moved out of cli.ts)"
      contains: "runOnce"
    - path: "src/cli.ts"
      provides: "Thin command band — registration only, no eslint-disable max-lines"
      min_lines: 30
  key_links:
    - from: "src/cli.ts"
      to: "src/commands/run-once.ts"
      via: "program.command(\"run-once\").action(one-liner delegating to the handler)"
      pattern: "commands/run-once"
    - from: "src/cli.test.ts"
      to: "src/cli.ts"
      via: "tests still drive the public CLI via buildCli — unchanged import surface"
      pattern: "buildCli"
---

<objective>
CLN-04d (ВЫСОКИЙ РИСК, структурный): разбить 822-строчный god-file `src/cli.ts` на per-command модули в `src/commands/` (skill §A Command-band: `cli.ts` = `buildCli` + `resolveDependencies` + ровно четыре `program.command().action()` wiring-вызова, каждый action — однострочная делегация в `commands/<command>.ts`). После split `/* eslint-disable max-lines */` в `cli.ts` исчезает.

Purpose: Закрыть последнее и самое крупное отклонение от skill §A. Это ЧИСТО структурный рефакторинг — логика перемещается, не меняется. Изолирован в отдельную последнюю волну, потому что HIGH-risk: безопасные планы 14-01..14-03 от него НЕ зависят.
Output: `src/commands/{check,contract-check,discover,run-once}.ts`; тонкий `src/cli.ts` без max-lines suppress.

ВАЖНО (gate для исполнителя): если split НЕ удаётся удержать `pnpm verify` зелёным И coverage 100% И публичное CLI-поведение байт-идентичным (набор команд, флаги, exit codes, форма JSON-summary) — НЕ форсировать. Откатить частичные правки и сообщить как blocker для возможного deferral, а не ломать гейт.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-repository-cleanup-convention-compliance/14-RESEARCH.md

# Skill — планка соответствия (§A Command-band + god-file constraint)
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# Источник, который задание режет
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLN-04d — извлечь обработчики check + contract-check + discover в src/commands/</name>
  <files>src/commands/check.ts, src/commands/contract-check.ts, src/commands/discover.ts, src/cli.ts</files>
  <read_first>
    - src/cli.ts полностью (текущая структура: `buildCli` :159, `resolveDependencies` :176, `registerCheckCommand` :205, `registerContractCheckCommand` :267, `registerDiscoverCommand` :303 + хелперы `runDryRunDiscovery` :356, `runStoreRawDiscovery` :537, `discoverForStoreRaw` :623, `createStoreRawResources` :639, `createStagingRepository` :657, `storeRawMode`/`storeRawCounts`/`stageRawEvidence`/`loadDryRunSourceConfig`/`loadStoreRawConfig`/`countRawStorage`/`countStaging`/`writeJson` + типы `DiscoverOptions`, `RawStorageCounts`, `StagingCounts`, `StoreRawResources`, `SourceConfigResult`, `AppConfigResult`)
    - src/cli.test.ts (импортирует ТОЛЬКО `buildCli` из `./cli.js` и `* as configModule` из `./config.js` — НЕ импортирует внутренние функции; тест гоняет публичный контракт через `buildCli().parseAsync` и парсит stdout JSON)
    - 14-RESEARCH.md §CLN-04 DEV-02 + Pitfall 2 (проверить, что cli.test.ts импортирует только из ./cli.js; прогнать pnpm test сразу после split)
    - .agents/skills/solidstats-fetcher-ts-conventions §A Command-band + "god-file constraint" (cli.ts тонкий; option parsing/dependency assembly/orchestrator dispatch — в commands/<command>.ts)
  </read_first>
  <action>
    Завести `src/commands/` и перенести по одному модулю на команду. `src/commands/check.ts`: экспортирует `registerCheckCommand(program, dependencies)` (или хэндлер-функцию + тонкую регистрацию) с телом текущего `registerCheckCommand` — loadConfig, createSourceClient, три connectivity-probe, writeJson, exit-2 на ConfigValidationError/!ok. `src/commands/contract-check.ts`: тело текущего `registerContractCheckCommand` + зависимые `loadDryRunSourceConfig`. `src/commands/discover.ts`: тело `registerDiscoverCommand` + ВЕСЬ store-raw/dry-run кластер (`runDryRunDiscovery`, `runStoreRawDiscovery`, `discoverForStoreRaw`, `createStoreRawResources`, `createStagingRepository`, `storeRawMode`, `storeRawCounts`, `stageRawEvidence`, `countRawStorage`, `countStaging` + типы `DiscoverOptions`, `RawStorageCounts`, `StagingCounts`, `StoreRawResources`).
    Общие утилиты, нужные нескольким командам (`writeJson`, `buildRetryWarnEmitter`, `loadStoreRawConfig`, `loadDryRunSourceConfig`, `createRunId`, `SourceConfigResult`/`AppConfigResult` типы, `BuildCliDependencies` интерфейс) — вынести в общий модуль (напр. `src/commands/shared.ts` или `src/commands/types.ts`), импортируемый каждым command-модулем и `cli.ts`. НЕ дублировать их. Перемещать ТЕЛА функций дословно — никакой переработки логики, тех же сообщений, тех же exit-кодов, той же формы JSON (T-14-05). Сохранить inline-комментарии (включая две `no-await-in-loop -- reason` строки из 14-01 и `v8 ignore` директивы на их строках — они должны мигрировать ВМЕСТЕ с кодом, иначе coverage упадёт). Каждый command-action в `cli.ts` после переноса — однострочная делегация (`.action(async (opts) => handler(dependencies, opts))`). НЕ трогать `run-once` в этой задаче (он в Task 2). Прогнать `pnpm test` сразу после переноса каждого модуля. Если `cli.test.ts` сломался на импорте — значит что-то тянулось из cli.ts напрямую (не ожидается per read); решать переэкспортом из cli.ts или правкой пути, НЕ ослаблением теста.
  </action>
  <verify>
    <automated>test -f src/commands/check.ts && test -f src/commands/contract-check.ts && test -f src/commands/discover.ts && pnpm run typecheck && pnpm test -- --run src/cli.test.ts || echo "FAIL: command modules missing or cli contract test red"</automated>
  </verify>
  <done>Существуют `src/commands/check.ts`, `contract-check.ts`, `discover.ts` с перенесёнными телами; их action-вызовы в `cli.ts` — однострочные делегации; `pnpm test src/cli.test.ts` зелёный без правки утверждений теста; типы и `v8 ignore`/`no-await-in-loop` директивы мигрировали вместе с кодом; логика и JSON-форма не изменены.</done>
</task>

<task type="auto">
  <name>Task 2: CLN-04d — извлечь run-once в src/commands/run-once.ts и убрать max-lines suppress из cli.ts</name>
  <files>src/commands/run-once.ts, src/cli.ts</files>
  <read_first>
    - src/cli.ts:387-475 (`registerRunOnceCommand` — самый тяжёлый: option parsing, loadStoreRawConfig, createStoreRawResources, runOnce dispatch, toCompactSummary stdout, flushLogger, exitCode) + :477-535 (`flushLogger`, `evidenceFileOption`, `buildRetryWarnEmitter`, `requireStagingRepository`, `maxPagesOption`) + :2 (`/* eslint-disable max-lines */`) + :813-822 (binary entrypoint block с двумя `v8 ignore`)
    - src/cli.test.ts (run-once сценарии: парсит compact JSON summary из stdout, проверяет exitCode, runId, evidence-file запись)
    - 14-RESEARCH.md §CLN-04 DEV-02 (после split max-lines suppress в cli.ts ДОЛЖЕН исчезнуть — файл становится коротким)
  </read_first>
  <action>
    Создать `src/commands/run-once.ts` с телом `registerRunOnceCommand` + сопутствующими хелперами, используемыми только run-once (`flushLogger`, `evidenceFileOption`, `maxPagesOption`, `requireStagingRepository`, `RunOnceOptions` тип). Сохранить ВСЕ inline-комментарии (CORE-02 substrate, D-02/D-16 PROG объяснения) и `v8 ignore next` на `requireStagingRepository` — они мигрируют вместе. Action в `cli.ts` — однострочная делегация. `buildRetryWarnEmitter`/`createRunId`/`writeJson` берутся из общего shared-модуля (Task 1), не дублируются.
    После переноса `cli.ts` содержит только: shebang, импорты, `BuildCliDependencies` (если оставлен в cli.ts — иначе из shared), `buildCli`, `resolveDependencies`, четыре `register*Command` тонкие регистрации ИЛИ четыре `.action()` делегации, и binary-entrypoint блок (:813-822 с его `v8 ignore`). УДАЛИТЬ строку 2 `/* eslint-disable max-lines -- ... */` — файл теперь намного короче лимита, suppress больше не нужен (skill §A: max-lines split, never disabled). Прогнать `pnpm run lint` — он должен быть зелёным БЕЗ этого suppress (если max-lines всё ещё срабатывает — файл недостаточно тонкий, доперенести оставшуюся orchestration-логику в commands/, НЕ возвращать suppress). Binary-entrypoint `await buildCli().parseAsync(process.argv)` и его `v8 ignore` директивы оставить в `cli.ts`.
    Финальная проверка байт-идентичности: stdout JSON-summary `run-once` (compact projection), exit codes (0/1/2), набор команд и флагов — без изменений; `cli.test.ts` зелёный без правок утверждений.
  </action>
  <verify>
    <automated>test -f src/commands/run-once.ts && ! grep -q "eslint-disable max-lines" src/cli.ts && pnpm run lint && pnpm run typecheck && pnpm test -- --run src/cli.test.ts || echo "FAIL: run-once not extracted, max-lines suppress remains, or lint/typecheck/contract test red"</automated>
  </verify>
  <done>`src/commands/run-once.ts` существует с перенесённым run-once; `cli.ts` НЕ содержит `eslint-disable max-lines`; `pnpm run lint` зелёный без этого suppress; `cli.test.ts` зелёный без правок утверждений; binary entrypoint + его `v8 ignore` остались в cli.ts; CLI-поверхность (команды/флаги/exit codes/JSON-форма) байт-идентична.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Перемещение кода между модулями; новой внешней поверхности атаки нет. CLI error-boundary (§D exit codes + run summary) остаётся в command band, только переразложен по файлам. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | поведенческая регрессия от структурного split (изменение exit-кодов / формы JSON / coverage) | mitigate | Тела функций переносятся дословно; `v8 ignore`/`no-await-in-loop` директивы мигрируют вместе с кодом; `cli.test.ts` гоняет публичный контракт через `buildCli` и парсит stdout — зелёный без правок утверждений доказывает байт-идентичность; `pnpm verify` + 100% coverage — гейт. Если гейт не держится → откат + blocker, НЕ форсировать. Severity: low (mitigated by verify+coverage gate; risk самого изменения — HIGH, поэтому изолирован в свою волну). |
</threat_model>

<verification>
- `src/commands/{check,contract-check,discover,run-once}.ts` существуют; тела перенесены дословно.
- `! grep "eslint-disable max-lines" src/cli.ts` — suppress удалён.
- `pnpm run lint` зелёный БЕЗ max-lines suppress на cli.ts (доказывает, что файл стал тонким).
- `pnpm test src/cli.test.ts` зелёный без правок утверждений (доказывает байт-идентичность публичного CLI-контракта: команды, флаги, exit codes, JSON-summary форма).
- Полный гейт волны: `pnpm run verify` зелёный, coverage 100%, файловый набор НЕ сокращён (cli.ts сохранён + добавлены commands/*).
- Gate-условие отката: если verify/coverage/байт-идентичность не держатся — откатить и сообщить blocker для deferral, НЕ форсировать split.
</verification>

<success_criteria>
CLN-04d закрыт: `cli.ts` — тонкий command band (registration only, без `max-lines` suppress); вся option-parsing/dependency-assembly/orchestrator-dispatch логика в `src/commands/<command>.ts`; публичная CLI-поверхность байт-идентична (команды, флаги, exit codes 0/1/2, форма JSON-summary); `pnpm verify` зелёный при 100% coverage с НЕсокращённым файловым набором. ЛИБО — если split не держит гейт — задокументированный blocker для deferral без форсирования.
</success_criteria>

<output>
Create `.planning/phases/14-repository-cleanup-convention-compliance/14-04-SUMMARY.md` when done
</output>
