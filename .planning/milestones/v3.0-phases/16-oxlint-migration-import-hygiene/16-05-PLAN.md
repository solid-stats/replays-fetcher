---
phase: 16-oxlint-migration-import-hygiene
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .dependency-cruiser.cjs
autonomous: true
requirements: [IMP-01, IMP-02]
must_haves:
  truths:
    - "`.dependency-cruiser.cjs` сгенерирован через `depcruise --init` (не вручную)"
    - "`pnpm run depcruise` exit 0 на чистом дереве (0 циклов, 0 unresolvable)"
    - "Цикл на throwaway probe-паре ловится depcruise (exit != 0); probe удалены, дерево чистое, реальные src/ не тронуты"
    - "Boundary-правило commands→storage/staging присутствует с severity warn (DI-факт)"
    - "`eslint-plugin-import-x` + resolver отсутствуют (tsc покрывает no-unresolved)"
  artifacts:
    - path: ".dependency-cruiser.cjs"
      provides: "no-circular + boundary rules; NodeNext-совместимая резолюция через --init"
      contains: "no-circular"
    - path: "package.json"
      provides: "depcruise script + dependency-cruiser devDep"
      contains: "depcruise"
  key_links:
    - from: "package.json"
      to: ".dependency-cruiser.cjs"
      via: "depcruise script invokes dependency-cruiser --config"
      pattern: "dependency-cruiser.*--config"
    - from: ".dependency-cruiser.cjs"
      to: "tsconfig.json"
      via: "tsConfig integration для NodeNext .js→.ts резолюции"
      pattern: "tsConfig"
---

<objective>
Подключить dependency-cruiser: добавить devDep, СГЕНЕРИРОВАТЬ конфиг через `depcruise --init`
(КРИТИЧНО — рукописный конфиг даёт 220 false `not-to-unresolvable` на NodeNext `.js`→`.ts`,
Pitfall 2), добавить no-circular + ingest-boundary правила, скрипт `depcruise`, и ДОКАЗАТЬ что
посаженный import-цикл ловится (IMP-02 planted-cycle proof). `eslint-plugin-import-x` уже удалён
в 16-01 — здесь подтверждается, что no-unresolved покрыт tsc (IMP-01).

Покрывает: IMP-01 (depcruise no-cycle + boundary в verify-готовом виде; tsc покрывает
no-unresolved) и IMP-02 (planted-cycle proof). Включение `depcruise` в `verify`-цепочку
делает план 16-06 (вместе с knip — там же общий package.json).

Purpose: восстановить import-cycle/boundary защиту, потерянную с eslint-plugin-import.
Output: `.dependency-cruiser.cjs`, `depcruise` скрипт, planted-cycle доказательство в SUMMARY.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-RESEARCH.md

# Эмпирический эталонный конфиг + результат spike (0 violations, 73 модуля)
@.planning/spikes/004-depcruise-knip-import-gap/.dependency-cruiser.cjs
@.planning/spikes/004-depcruise-knip-import-gap/depcruise-out.txt
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: depcruise --init + boundary rule + depcruise script</name>
  <files>.dependency-cruiser.cjs, package.json, pnpm-lock.yaml</files>
  <read_first>
    - RESEARCH «Рецепт IMP-01»: ОБЯЗАТЕЛЬНО `depcruise --init` (auto настраивает NodeNext
      tsConfig integration; рукописный = 220 false positives). Spike-результат: 0 violations,
      73 модуля, 108 зависимостей, 0 циклов.
    - `.planning/spikes/004-depcruise-knip-import-gap/.dependency-cruiser.cjs` — эталон правил:
      no-circular (error), no-orphans (warn, pathNot cli.ts), not-to-unresolvable (error),
      not-to-dev-dep (error), not-to-undeclared (error); options:
      tsPreCompilationDeps:true, tsConfig.fileName:tsconfig.json, doNotFollow node_modules.
    - RESEARCH «Про commands/discover.ts fence #2»: discover.ts вызывает storeRaw/stageRaw через
      DI (BuildCliDependencies), НЕ прямым импортом — реального нарушения нет; boundary-правило
      добавляется как severity:warn (информационное), задокументировано в RULE-DELTA.md (16-04).
  </read_first>
  <action>
    Добавить devDep: `pnpm add -D dependency-cruiser`. Сгенерировать конфиг командой
    `npx dependency-cruiser --init` (выбрать TypeScript, NodeNext, src/). НЕ писать конфиг вручную.
    Результат — `.dependency-cruiser.cjs`. После генерации привести его к эталону spike 004:
    убедиться что присутствуют `no-circular` (severity error), `not-to-unresolvable` (error),
    и options `tsPreCompilationDeps:true` + `tsConfig.fileName:'tsconfig.json'` +
    `doNotFollow.path:['node_modules']`. Добавить кастомное boundary-правило в `forbidden`:
    name `no-commands-to-storage-direct`, severity `warn` (НЕ error — discover.ts использует DI,
    нет реального нарушения; фиксирует fence #2 backlog как информационное), comment с указанием
    на DI-паттерн, `from.path: '^src/commands'`, `to.path: '^src/(storage|staging)'`.
    Игнорировать `**/*.test.ts`/`.integration.`/`.fixtures.` от orphan-шума (через pathNot в
    no-orphans или doNotFollow). Добавить в package.json scripts:
    `"depcruise": "dependency-cruiser src --config .dependency-cruiser.cjs"`. НЕ включать
    depcruise в verify здесь — это план 16-06 (общий package.json, последовательная волна).
  </action>
  <verify>
    <automated>test -f .dependency-cruiser.cjs && node -e "const c=require('./.dependency-cruiser.cjs'); const names=c.forbidden.map(r=>r.name); if(!names.includes('no-circular')) throw new Error('no-circular missing'); if(c.options.tsPreCompilationDeps!==true) throw new Error('tsPreCompilationDeps must be true'); const b=c.forbidden.find(r=>r.name==='no-commands-to-storage-direct'); if(!b||b.severity!=='warn') throw new Error('boundary rule must exist with severity warn'); console.log('depcruise config OK')" && node -e "const p=require('./package.json'); if(!/dependency-cruiser .*--config .dependency-cruiser.cjs/.test(p.scripts.depcruise)) throw new Error('depcruise script wrong'); console.log('script OK')"</automated>
  </verify>
  <done>
    `.dependency-cruiser.cjs` сгенерирован через --init; содержит no-circular (error),
    not-to-unresolvable (error), boundary-правило commands→storage/staging (warn),
    tsPreCompilationDeps+tsConfig; `depcruise` скрипт добавлен; `pnpm run depcruise` exit 0 на
    чистом дереве (0 violations, как spike).
  </done>
</task>

<task type="auto">
  <name>Task 2: Planted-cycle proof (IMP-02, throwaway probe-файлы) + подтверждение IMP-01 (tsc покрывает no-unresolved)</name>
  <files>src/__cycle-probe-a.ts, src/__cycle-probe-b.ts</files>
  <read_first>
    - RESEARCH «Proof of IMP-02: planted import cycle» — посадить цикл, запустить depcruise
      (должна быть ошибка no-circular), убрать, прогнать снова (чисто).
    - VALIDATION.md Manual-Only: «Plant an import cycle → non-zero; remove → zero. Record both
      in SUMMARY.»
    - IMP-01: eslint-plugin-import-x + eslint-import-resolver-typescript уже удалены в 16-01;
      no-unresolved покрывается `tsc --noEmit` (typecheck).
    - КРИТИЧНО про contamination: НЕ мутировать реальные src/ файлы для proof — крах посередине
      оставит «грязный» рабочий src/. Цикл сажается на ВЫБРАСЫВАЕМОЙ паре probe-файлов.
  </read_first>
  <action>
    Доказать IMP-02 на ВРЕМЕННОЙ throwaway-паре (НИ ОДИН реальный src/ файл не редактируется):
    создать `src/__cycle-probe-a.ts` и `src/__cycle-probe-b.ts`, импортирующие друг друга
    (a импортирует b, b импортирует a — обоюдная зависимость = цикл). Использовать
    try/finally-семантику, чтобы probe-файлы удалялись ДАЖЕ если шаг-ассерт падает:
    (1) создать обе probe; (2) `pnpm run depcruise` — ОЖИДАЕТСЯ ненулевой exit + `no-circular`,
    ЗАФИКСИРОВАТЬ вывод; (3) В finally-ветке (выполняется независимо от исхода шага 2) УДАЛИТЬ обе
    `src/__cycle-probe-*.ts`; (4) повторно `pnpm run depcruise` — exit 0 (чисто).
    Оба прогона (на цикле / после удаления) записать в SUMMARY — это proof IMP-02.
    ФИНАЛ: assert `git status --short` ЧИСТ (probe-файлы удалены, ни один трекаемый src/ файл не
    изменён) — probe были untracked и стёрты. Так как реальные файлы не трогались, отдельного
    `git checkout` исходников не требуется.
    Подтвердить IMP-01: `! grep -qE 'eslint-plugin-import|import-resolver' package.json`
    (импорт-пакеты удалены) и `pnpm run typecheck` зелёный (tsc покрывает no-unresolved).
  </action>
  <verify>
    <automated>! grep -qE 'eslint-plugin-import|import-resolver' package.json && test ! -f src/__cycle-probe-a.ts && test ! -f src/__cycle-probe-b.ts && pnpm run typecheck && pnpm run depcruise && test -z "$(git status --short)" && echo "IMP-01 ok + probe files removed + clean tree + depcruise clean"</automated>
  </verify>
  <done>
    Planted-cycle proof выполнен на throwaway probe-паре и записан в SUMMARY (depcruise упал на
    цикле, прошёл после удаления probe); `src/__cycle-probe-a.ts`/`-b.ts` удалены (finally),
    ни один реальный src/ файл не редактировался; `git status --short` чист; eslint-import пакеты
    отсутствуют; `pnpm run typecheck` зелёный (no-unresolved покрыт tsc); `pnpm run depcruise`
    exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Установка dependency-cruiser |
| src module graph → architecture | depcruise — главный контроль фазы против архитектурного дрейфа (циклы, ingest-boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | dependency-cruiser install | mitigate | RESEARCH audit: [OK] зрелый инструмент; lockfile коммитится |
| T-16-08 | Elevation of Privilege | accidental architecture drift (cycles / commands→storage прямой импорт) | mitigate | depcruise no-circular (error) + boundary-правило (warn) ловят дрейф; planted-cycle proof доказывает работоспособность — это реальный security-контроль фазы (ingest-boundary defense) |
</threat_model>

<verification>
- `.dependency-cruiser.cjs` через --init; no-circular + boundary + tsConfig.
- `pnpm run depcruise` exit 0 чисто; ловит цикл на throwaway probe-паре (proof в SUMMARY).
- eslint-import пакеты удалены; tsc покрывает no-unresolved.
- Probe-файлы удалены (finally); `git status --short` чист; реальные src/ файлы не тронуты.
</verification>

<success_criteria>
- IMP-01: depcruise no-cycle + boundary правила готовы; tsc покрывает no-unresolved.
- IMP-02 (cycle часть): planted-cycle proof выполнен и зафиксирован.
</success_criteria>

<output>
Create `.planning/phases/16-oxlint-migration-import-hygiene/16-05-SUMMARY.md` when done.
Записать ОБА прогона planted-cycle proof на throwaway probe-паре (вывод/exit на цикле и после
удаления probe) — обязательное manual-verification из VALIDATION.md; подтвердить чистое дерево.
</output>
