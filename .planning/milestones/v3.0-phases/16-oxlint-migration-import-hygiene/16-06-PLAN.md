---
phase: 16-oxlint-migration-import-hygiene
plan: 06
type: execute
wave: 6
depends_on: ["16-05"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - knip.jsonc
  - src/index.ts
autonomous: true
requirements: [IMP-02, LNT-04]
must_haves:
  truths:
    - "`pnpm run knip` exit 0 (находки устранены КОНСЕРВАТИВНО, без слепого удаления)"
    - "`verify` = format:check && lint && typecheck && test && test:integration && test:coverage && depcruise && knip && build"
    - "`lint:types` НЕ входит в verify"
    - "`sg docker -c \"pnpm run verify\"` зелёный целиком"
    - "Coverage остаётся 100%; file-set не уменьшён сверх задокументированного dead code"
  artifacts:
    - path: "knip.jsonc"
      provides: "knip config: entry/project/ignore (из spike 004)"
      contains: "entry"
    - path: "package.json"
      provides: "knip script + полная verify-цепочка с depcruise+knip"
      contains: "knip"
  key_links:
    - from: "package.json"
      to: "knip.jsonc"
      via: "knip script invokes knip --config knip.jsonc"
      pattern: "knip --config knip.jsonc"
    - from: "package.json"
      to: "depcruise + knip scripts"
      via: "verify chain includes depcruise && knip"
      pattern: "depcruise.*knip.*build"
---

<objective>
Подключить knip (unused modules/exports/deps) КОНСЕРВАТИВНО, собрать финальную `verify`-цепочку
(добавить depcruise + knip в правильном порядке, оставить lint:types ВНЕ verify), и прогнать
полный gate `sg docker -c "pnpm run verify"` зелёным при 100% coverage. Это финальная волна —
закрывает связанную замену.

Knip-находки spike 004 (РЕАЛЬНЫЕ) с ФИКСИРОВАННЫМ решением на каждую: 2 unused files
(`src/index.ts` public-API barrel → объявить knip `entry`, НЕ удалять; `src/run/no-leak.ts`
lone PROG-04 type → keep + knip `ignore`, НЕ удалять), 1 unused export (`toSourceSlug` →
unexport если provably-unused и coverage 100%, иначе ignore), 17 unused exported types
(→ unexport где provably-unused и coverage 100%, иначе knip-ignore; evidence-типы контракта НЕ
unexport). Консервативный bias (RESEARCH + VALIDATION): config/ignore > deletion; НИ ОДИН файл не
удаляется в этой фазе; любое unexport обязано держать `pnpm verify` зелёным при 100% coverage.

Purpose: закрыть IMP-02 (knip в verify), финализировать LNT-04 (полный green gate, lint:types
non-blocking) и собрать verify-цепочку.
Output: `knip.jsonc`, `knip`+verify скрипты, зелёный full gate.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-RESEARCH.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-VALIDATION.md

# Эмпирические источники knip
@.planning/spikes/004-depcruise-knip-import-gap/knip.jsonc
@.planning/spikes/004-depcruise-knip-import-gap/knip-out.txt
@src/index.ts
@src/run/no-leak.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: knip.jsonc + knip script + КОНСЕРВАТИВНОЕ устранение находок</name>
  <files>knip.jsonc, package.json, pnpm-lock.yaml, src/index.ts</files>
  <read_first>
    - `.planning/spikes/004-depcruise-knip-import-gap/knip.jsonc` — базовый конфиг
      (entry:["src/cli.ts"], project:["src/**/*.ts"], ignore тест/fixtures).
    - `.planning/spikes/004-depcruise-knip-import-gap/knip-out.txt` — точный список находок.
    - `src/index.ts` (3 строки: re-export ConfigValidationError/loadConfig/redactConfig + типы —
      public-API barrel; package.json `bin`=dist/cli.js, НЕТ main/exports → barrel НЕ package
      entry).
    - `src/run/no-leak.ts` (PROG-04 no-leak contract module — только `export type NoLeakSurface`,
      документирует leak-prevention surface; companion к no-leak.test.ts).
    - RESEARCH «Рецепт IMP-02 knip» + VALIDATION coverage note (консервативная обработка).
  </read_first>
  <action>
    Создать `knip.jsonc` в корне (из spike): `$schema`, `entry`, `project: ["src/**/*.ts"]`,
    `ignore: ["**/*.test.ts","**/*.integration.test.ts","**/*.fixtures.ts"]`. Учесть knip
    configuration hints (можно убрать `src/cli.ts` из entry — он уже в project). Добавить
    devDep `pnpm add -D knip` и script `"knip": "knip --config knip.jsonc"`.
    Затем устранить находки по ФИКСИРОВАННЫМ per-finding решениям (консервативный bias:
    config/ignore > deletion; НИКАКОГО слепого удаления). Решения зафиксированы, исполнитель
    НЕ импровизирует:
    (1) `src/index.ts` (public-API barrel, НЕ package entry — bin=cli.js, нет main/exports):
    РЕШЕНИЕ — объявить его knip `entry` (добавить "src/index.ts" в `entry`). НЕ удалять. Это
    намеренная публичная поверхность; документировать в SUMMARY как «intentional public surface».
    (2) `src/run/no-leak.ts` (lone `export type NoLeakSurface`, PROG-04 no-leak contract,
    companion к no-leak.test.ts): РЕШЕНИЕ — НЕ удалять (тип кодирует cross-surface контракт).
    Оставить файл и снять knip-находку через knip `ignore` записи на `src/run/no-leak.ts` (или
    `ignoreExportsUsedInFile`) с комментарием «PROG-04 no-leak contract module — companion к
    no-leak.test.ts». Документировать.
    (3) `toSourceSlug` unused export (src/checkpoint/object-key.ts:52): РЕШЕНИЕ — unexport
    (сделать локальной) ТОЛЬКО если provably unused вне файла И `pnpm run test:coverage` остаётся
    100%; иначе — knip-ignore с однострочной причиной. Не удалять функцию.
    (4) 17 unused exported types: для КАЖДОГО — unexport (сделать локальным) ТОЛЬКО где provably
    unused вне файла И coverage остаётся 100%; иначе — knip-ignore (`ignoreExportsUsedInFile` для
    типов, используемых только внутри своего файла) с однострочной причиной. НЕ unexport типы
    cross-service контракта (RawReplaySourceEvidence и пр. — evidence-типы first-class audit per
    conventions skill): если экспорт нужен потребителю — оставить и снять находку через knip
    entry/ignore.
    Общая инвариант-планка: любое удаление/unexport обязано держать `pnpm verify` зелёным при
    100% coverage; при малейшем сомнении выбирать knip-ignore, а не deletion.
    Цель: `pnpm run knip` exit 0 БЕЗ `--no-exit-code` хака.
    КАЖДОЕ решение (entry-decl / ignore / unexport) задокументировать в SUMMARY с однострочной
    причиной.
  </action>
  <verify>
    <automated>test -f knip.jsonc && node -e "const p=require('./package.json'); if(!/knip --config knip.jsonc/.test(p.scripts.knip)) throw new Error('knip script wrong'); console.log('knip script OK')" && pnpm run knip && echo "KNIP exit 0"</automated>
  </verify>
  <done>
    `knip.jsonc` существует; `knip` скрипт добавлен; `pnpm run knip` exit 0 без --no-exit-code;
    находки устранены по фиксированным per-finding решениям: src/index.ts → knip entry (НЕ
    удалён); src/run/no-leak.ts → keep + knip ignore (НЕ удалён); toSourceSlug + 17 типов →
    unexport только где provably-unused и coverage 100%, иначе knip-ignore; ни один файл не
    удалён; public-API barrel сохранён; каждое решение задокументировано в SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Собрать финальную verify-цепочку + полный gate под sg docker при 100% coverage</name>
  <files>package.json</files>
  <read_first>
    - `package.json` scripts (текущий verify после 16-01: format:check && lint && typecheck &&
      test && test:integration && test:coverage && build).
    - RESEARCH «Рецепт: цепочка verify» — порядок: format:check → lint → typecheck → test →
      test:integration → test:coverage → depcruise → knip → build. lint:types ВНЕ verify.
    - VALIDATION.md: phase gate = `sg docker -c "pnpm run verify"` зелёный; coverage 100%;
      file-set не уменьшён сверх задокументированного dead code. Session shell без docker-группы —
      verify целиком гоняется под `sg docker`.
  </read_first>
  <action>
    Обновить `verify` в package.json на:
    `pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm test && pnpm run
    test:integration && pnpm run test:coverage && pnpm run depcruise && pnpm run knip &&
    pnpm run build`. Порядок: быстрые статические гейты (format/lint/typecheck) → тесты →
    coverage → граф-анализ (depcruise) → unused (knip) → build. НЕ добавлять `lint:types` в
    verify (LNT-04 non-blocking). Прогнать полный gate: `sg docker -c "pnpm run verify"`
    (session shell не в docker-группе — поэтому через sg docker). Должен быть зелёным целиком.
    Проверить coverage отчёт = 100% reachable source И что measured file-set не уменьшён
    silently (vitest.config include не менялся; любые удаления из 16-06 Task 1 — только
    задокументированный dead code). Зафиксировать в SUMMARY: финальная verify-строка, exit 0
    полного gate, coverage %, подтверждение file-set.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const v=p.scripts.verify; for(const s of ['format:check','lint','typecheck','test','test:integration','test:coverage','depcruise','knip','build']){ if(!v.includes('pnpm run '+s) && !v.includes('pnpm '+s)) throw new Error('verify missing '+s);} if(v.includes('lint:types')) throw new Error('lint:types must NOT be in verify'); console.log('verify chain OK')"</automated>
  </verify>
  <done>
    `verify` содержит format:check, lint, typecheck, test, test:integration, test:coverage,
    depcruise, knip, build в этом порядке; lint:types отсутствует в verify;
    `sg docker -c "pnpm run verify"` зелёный целиком (зафиксировано в SUMMARY); coverage 100%;
    file-set не уменьшён silently.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Установка knip |
| dead-code removal → coverage file-set | Удаление файлов может «сыграть» на coverage — контролируется консервативной policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | knip install | mitigate | RESEARCH audit: [OK] активный проект; lockfile коммитится |
| T-16-10 | Repudiation | silent file-set reduction (gaming coverage) | mitigate | Консервативная policy: entry-decl/ignore/unexport > delete; удаление только provably-dead + coverage 100% + каждое решение задокументировано в SUMMARY; public-API barrel (src/index.ts) сохранён через entry-decl, не удалён |
| T-16-11 | Elevation of Privilege | потеря cross-service evidence-типов (unexport) | mitigate | Evidence-типы (RawReplaySourceEvidence и пр.) — first-class audit per conventions skill; если нужны потребителю — оставлены экспортом + knip entry/ignore, не unexported слепо |
</threat_model>

<verification>
- `pnpm run knip` exit 0; находки устранены консервативно, задокументированы.
- `verify` = полная цепочка с depcruise+knip; lint:types вне verify.
- `sg docker -c "pnpm run verify"` зелёный; coverage 100%; file-set не уменьшён silently.
</verification>

<success_criteria>
- IMP-02: knip в verify, exit 0, planted-cycle proof (16-05) + unused hygiene покрыты.
- LNT-04: полный gate зелёный, lint:types non-blocking вне verify.
- Связанная замена завершена: pnpm verify green at 100% coverage.
</success_criteria>

<output>
Create `.planning/phases/16-oxlint-migration-import-hygiene/16-06-SUMMARY.md` when done.
Зафиксировать: финальную verify-строку, exit 0 полного `sg docker -c "pnpm run verify"`,
coverage %, КАЖДОЕ knip-решение (entry-decl/ignore/unexport/delete) с обоснованием, подтверждение
file-set не уменьшён silently.
</output>
