---
phase: 16-oxlint-migration-import-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/**/*.ts
autonomous: true
requirements: [LNT-01, LNT-02]
must_haves:
  truths:
    - "`pnpm run lint` (oxlint) завершается с exit 0 — линтер полностью зелёный"
    - "Все 450 unit-тестов проходят без правки ассертов"
    - "Оставшиеся disable-комментарии используют oxlint-форму и ссылаются на правила, которые ON"
    - "Ни одно нарушение не скрыто blanket-suppression вместо фикса"
  artifacts:
    - path: "src/errors/app-error.ts"
      provides: "custom-error-definition-совместимый AppError базовый класс"
      contains: "AppError"
    - path: "src/staging/types.ts"
      provides: "interface с method-signature-style: property форма"
      contains: "interface"
  key_links:
    - from: "src/**/*.ts"
      to: ".oxlintrc.json"
      via: "все preset-правила (method-signature-style, explicit-member-accessibility, consistent-type-specifier-style, custom-error-definition, id-length, no-useless-assignment) проходят"
      pattern: "import type"
---

<objective>
Code-fix #2 (остаток): устранить ВСЕ оставшиеся oxlint-нарушения после func-style-фикса (16-02),
чтобы `pnpm run lint` стал полностью зелёным (exit 0). Категории из spike 001 `run-supported.txt`:
`typescript/method-signature-style` (20), `typescript/explicit-member-accessibility` (11),
`import/consistent-type-specifier-style` (41), `unicorn/custom-error-definition` (6),
`eslint(id-length)` (9), `eslint(no-useless-assignment)` (1). Плюс модернизация существующих
`eslint-disable` комментариев на `oxlint-disable` форму для правил, которые остаются ON
(`max-lines`, `require-atomic-updates`, `camelcase`, `typescript/no-useless-constructor`), и
удаление теперь-инертных `no-await-in-loop` disable-комментариев (правило выключено в 16-01).

Стилевой/декларативный рефактор: поведение идентично, 450 тестов проходят без правок.

Purpose: довести LNT-01 до конца (lint полностью green) и подтвердить LNT-02 (опции
действуют — 0 false positives на портированных правилах).
Output: финально lint-чистые `src/**/*.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-RESEARCH.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-02-SUMMARY.md
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# Файлы с классами / error-base / disable-комментариями
@src/errors/app-error.ts
@src/errors/checkpoint-conflict-error.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Устранить method-signature-style / explicit-member-accessibility / consistent-type-specifier-style / custom-error-definition</name>
  <files>src/**/*.ts</files>
  <read_first>
    - `16-02-SUMMARY.md` — остаточный список нарушений после func-style фикса.
    - `src/errors/app-error.ts` (abstract class, custom-error-definition) и
      `src/errors/checkpoint-conflict-error.ts` (concrete subclass) — целевые для
      `unicorn/custom-error-definition` (6 срабатываний по spike в этих error-классах).
    - Interface-файлы с method-shorthand (эмпирически): src/checkpoint/s3-checkpoint-store.ts,
      src/check/s3-connectivity.ts, src/discovery/types.ts, src/evidence/s3-evidence-store.ts,
      src/source/pacing.ts, src/source/throttle.ts, src/staging/postgres-staging-repository.ts,
      src/staging/stage-raw-replay.ts, src/storage/replay-byte-client.ts,
      src/storage/s3-raw-storage.ts.
    - Inline-type-import кандидаты: src/commands/check.ts, src/logging/create-logger.ts,
      src/run/run-once.ts (и др. по списку из 16-02-SUMMARY).
    - Классы с public/private членами: src/storage/replay-byte-client.ts,
      src/errors/*, src/discovery/source-client.ts.
  </read_first>
  <action>
    Прогнать oxlint и пройти по категориям:
    (1) `typescript/method-signature-style`: в interface-объявлениях переписать method-shorthand
    `foo(a: A): B` в property-форму `foo: (a: A) => B`. Не менять имена/типы.
    (2) `typescript/explicit-member-accessibility`: добавить явные `public`/`private`/`protected`
    модификаторы к членам классов, где их нет (app-error.ts поля уже readonly — добавить
    accessibility-модификатор; конструкторы и поля replay-byte-client/source-client classes).
    (3) `import/consistent-type-specifier-style`: вынести inline `type`-импорты
    `import { type Foo, bar }` в отдельный `import type { Foo }` + `import { bar }` (или
    объединить в `import type` где импорт целиком типовой).
    (4) `unicorn/custom-error-definition`: привести AppError-иерархию к ожидаемой форме правила
    (порядок `super()` → `this.name`; см. app-error.ts строки 34-44 и
    checkpoint-conflict-error.ts). Сохранить контракт: НЕ восстанавливать `httpStatus`
    (app-error.ts:9-16 — намеренно отсутствует), сохранить generic `Code`, `isOperational`,
    identifiers-only `details`. Запрещено suppress-ить вместо фикса.
  </action>
  <verify>
    <automated>node_modules/.bin/oxlint --config .oxlintrc.json src 2>&1 | grep -cE 'method-signature-style|explicit-member-accessibility|consistent-type-specifier-style|custom-error-definition' | grep -qx 0 && echo "decl rules clean"</automated>
  </verify>
  <done>
    0 нарушений по method-signature-style, explicit-member-accessibility,
    consistent-type-specifier-style, custom-error-definition; AppError-контракт сохранён (нет
    httpStatus, generic Code, isOperational, identifiers-only details).
  </done>
</task>

<task type="auto">
  <name>Task 2: id-length + no-useless-assignment + модернизация disable-комментариев → lint green</name>
  <files>src/**/*.ts</files>
  <read_first>
    - spike id-length цели (эмпирически): короткие идентификаторы в src/source/retry.ts
      (строки 33,103,112,138,159), src/discovery/source-client.ts (166,195),
      src/storage/replay-byte-client.ts (129,145). Правило `id-length:error` пришло из preset
      (в eslint.config.js явного override не было — js.configs.all давал default minLength 2).
    - Существующие disable-комментарии (через grep `eslint-disable` по src): max-lines (5 файлов:
      discovery/discover.ts, discovery/source-client.ts, storage/replay-byte-client.ts,
      run/summary.ts, run/run-once.ts), no-await-in-loop (checkpoint/s3-checkpoint-store.ts,
      commands/discover.ts, run/run-once.ts, discovery/discover.ts, source/retry.ts),
      require-atomic-updates (run/run-once.ts:250,252), camelcase (staging/payload.ts:90),
      @typescript-eslint/no-useless-constructor (replay-byte-client.ts:70, source-client.ts:54).
  </read_first>
  <action>
    (1) `eslint(id-length)`: переименовать односимвольные идентификаторы (< 2) в осмысленные имена
    (например циклические/callback-переменные `e`→`error`, `i` оставить только если массив-индекс
    разрешён правилом — иначе `index`). Согласовать с conventions skill (нейминг). Это локальные
    переменные — переименование не меняет поведение; убедиться, что тесты зелёные.
    (2) `eslint(no-useless-assignment)` (1 шт): убрать мёртвое присваивание (значение
    перезаписывается до чтения) — НЕ менять логику, только убрать избыточную инициализацию.
    (3) Модернизация disable-комментариев: oxlint ЧИТАЕТ `eslint-disable` (compat), но
    рекомендуется `oxlint-` форма. Заменить `// eslint-disable-next-line RULE` →
    `// oxlint-disable-next-line RULE` и `/* eslint-disable RULE */` → `/* oxlint-disable RULE */`
    ТОЛЬКО для правил, которые ОСТАЮТСЯ включёнными под oxlint: `max-lines`,
    `require-atomic-updates`, `camelcase`; для `@typescript-eslint/no-useless-constructor` →
    `typescript/no-useless-constructor` (oxlint prefix). УДАЛИТЬ полностью disable-комментарии для
    `no-await-in-loop` — правило выключено репо-оверрайдом (16-01), комментарии стали инертны.
    Сохранять текст обоснования в комментарии (рядом с правилом). Запрещено добавлять blanket
    `/* oxlint-disable */` без конкретного rule-id.
    Финал: `pnpm run lint` exit 0 и `pnpm test` зелёный.
  </action>
  <verify>
    <automated>pnpm run lint && echo "LINT GREEN exit 0"</automated>
  </verify>
  <done>
    `pnpm run lint` exit 0 (полностью зелёный oxlint); 0 id-length / no-useless-assignment;
    disable-комментарии на oxlint-форме для ON-правил; no-await-in-loop disable-комментарии
    удалены; нет blanket-suppressions; 450 тестов зелёные (тесты не трогались).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Декларативный/стилевой рефактор; trust boundary не меняется |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | оставшиеся code-fixes + disable-модернизация | mitigate | 450 unit-тестов как regression-guard; запрет blanket-suppressions; AppError-контракт (no httpStatus, identifiers-only details, threat T-07-01/T-09-01) сохраняется явно |
</threat_model>

<verification>
- `pnpm run lint` exit 0.
- 0 нарушений по method-signature-style / explicit-member-accessibility /
  consistent-type-specifier-style / custom-error-definition / id-length / no-useless-assignment.
- disable-комментарии: oxlint-форма для ON-правил; no-await-in-loop комментарии удалены.
- 450 тестов зелёные, тестовые файлы не изменены.
- AppError-контракт сохранён.
</verification>

<success_criteria>
- LNT-01: oxlint полностью зелёный (`pnpm lint` exit 0).
- LNT-02: портированные опции дают 0 false positives — остаток нарушений был реальными
  convention-фиксами, теперь устранёнными.
</success_criteria>

<output>
Create `.planning/phases/16-oxlint-migration-import-hygiene/16-03-SUMMARY.md` when done.
Зафиксировать: `pnpm lint` exit 0, число фиксов по каждой категории, перечень
модернизированных/удалённых disable-комментариев, подтверждение тесты-не-трогались.
</output>
