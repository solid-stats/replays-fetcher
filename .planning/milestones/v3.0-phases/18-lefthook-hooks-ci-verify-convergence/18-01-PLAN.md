---
phase: 18-lefthook-hooks-ci-verify-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - lefthook.yml
  - .github/workflows/cd.yml
  - README.md
autonomous: true
requirements:
  - HOK-01
  - HOK-02
  - HOK-03
  - VRF-01
  - VRF-02
  - VRF-03
user_setup: []

must_haves:
  truths:
    - "After a clean `pnpm install`, `.git/hooks/pre-commit` and `.git/hooks/pre-push` exist and invoke lefthook (HOK-01)"
    - "Hooks are sourced from the shared preset via `extends` — no hook bodies copied into this repo (HOK-02)"
    - "A staged ill-formatted .ts file makes `lefthook run pre-commit` fail at the format/lint step (HOK-02/V2)"
    - "Hooks are bypassable with `git commit --no-verify` / `git push --no-verify` / `LEFTHOOK=0` (HOK-03)"
    - "`pnpm run verify` runs in canonical order oxfmt → oxlint → tsc → unit → integration → coverage → tsdown(build) → depcruise → knip (VRF-02)"
    - "`pnpm run verify` is green from a clean checkout at 100% V8 coverage with no threshold/exclude relaxation (VRF-01/VRF-03)"
    - "CI rides `pnpm run verify`; Node 25 + pnpm 11 + frozen lockfile preserved; the image/GHCR publish job is intact (VRF-03)"
    - "Zero `src/` files are touched; `git status --short` clean at phase close"
  artifacts:
    - path: "lefthook.yml"
      provides: "Root lefthook config extending the shared preset (single source of truth)"
      contains: "node_modules/@solid-stats/ts-toolchain/lefthook.yml"
    - path: "pnpm-workspace.yaml"
      provides: "Build allowlist that lets lefthook's postinstall run"
      contains: "lefthook: true"
    - path: "package.json"
      provides: "lefthook exact-pinned devDep, prepare script, reordered verify script"
      contains: "\"lefthook\""
    - path: "pnpm-lock.yaml"
      provides: "Frozen-lockfile-reproducible lefthook + platform-binary resolution"
      contains: "lefthook"
    - path: ".github/workflows/cd.yml"
      provides: "CI on the new command surface with lefthook config validation"
      contains: "lefthook validate"
  key_links:
    - from: "lefthook.yml"
      to: "node_modules/@solid-stats/ts-toolchain/lefthook.yml"
      via: "extends list resolves the preset on disk (no remotes, no copied bodies)"
      pattern: "extends"
    - from: "pnpm-workspace.yaml allowBuilds"
      to: "lefthook postinstall (binary select + hook install)"
      via: "build allowlist entry `lefthook: true` un-gates the postinstall"
      pattern: "lefthook: true"
    - from: "package.json prepare"
      to: ".git/hooks/{pre-commit,pre-push}"
      via: "`lefthook install || true` writes the git hook shims"
      pattern: "lefthook install"
---

<objective>
Wire client-side lefthook git hooks into `replays-fetcher` from the shared
`@solid-stats/ts-toolchain` preset, finalize the canonical `verify` ordering, and
confirm CI on the new command surface — behavior-preserving, 100% coverage, ZERO `src/`
changes. This is the final v3.0 Track C toolchain-convergence phase.

Purpose: developers get the same fast-fail checks locally (pre-commit oxfmt+oxlint on
staged files, pre-push tsc+Vitest) that CI runs, sourced from the single shared preset so
hooks never drift from CI. The `verify` script becomes the canonical full surface.

Output: `lefthook` exact-pinned devDep + build-allowlist entry + regenerated lockfile, a
one-line root `lefthook.yml` extending the preset, a `prepare` script, a one-token
`verify` reorder, a CI `lefthook validate` assertion, and a `--no-verify` bypass note in
README. No `src/` file is modified — the measured coverage file set is untouched by
construction.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-lefthook-hooks-ci-verify-convergence/18-RESEARCH.md
@.planning/phases/18-lefthook-hooks-ci-verify-convergence/18-VALIDATION.md

# Live surfaces this plan edits (read before changing):
@package.json
@pnpm-workspace.yaml
@.github/workflows/cd.yml
@node_modules/@solid-stats/ts-toolchain/lefthook.yml
@vitest.config.ts
@Dockerfile
</context>

<interface_context>
Shared preset already on disk at `node_modules/@solid-stats/ts-toolchain/lefthook.yml`
(consumed via `extends`, NOT copied):
- pre-commit: `format` → `oxfmt --check {staged_files}` (glob `*.{ts,tsx,js,mjs,json}`);
  `lint` → `oxlint {staged_files}` (glob `*.{ts,tsx}`)
- pre-push: `typecheck` → `pnpm run typecheck`; `test` → `pnpm test`

Live `verify` script (current tail order, to be reordered) — see `package.json` line 23:
`format:check && lint && typecheck && test && test:integration && test:coverage && depcruise && knip && build`.
Target tail: `... && test:coverage && build && depcruise && knip` (move `build` before `depcruise`).

pnpm build allowlist lives in `pnpm-workspace.yaml` `allowBuilds:` (repo's equivalent of
`onlyBuiltDependencies`). Current entries: `cpu-features`, `esbuild`, `protobufjs`, `ssh2`,
`unrs-resolver`. Add `lefthook: true`.

Coverage gate: `vitest.config.ts` enforces 100% statements/branches/functions/lines over
`src/**/*.ts`. Do NOT relax thresholds or add excludes.

Docker: build stage runs full `pnpm install` then `COPY src` (no `.git` in build context),
so `prepare`'s `lefthook install` needs the `|| true` guard. Prod stage is `--prod`
(lefthook is a devDep → absent). CI (`actions/checkout@v6`) has real `.git`; GitHub sets
`CI=true` so lefthook's own postinstall skips hook install — the `prepare` slot still wires
hooks harmlessly there.

Full verify runs through `sg docker -c "..."` — the session shell lacks the docker group,
so the testcontainers integration leg, coverage leg, and any Docker build must go through
that wrapper, not bare `pnpm`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Install lefthook from the shared preset and prove hooks wire, fire, and bypass</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, lefthook.yml, README.md</files>
  <action>
Wire lefthook client-side hooks from `@solid-stats/ts-toolchain` via the four research-locked
moves (HOK-01, HOK-02, HOK-03). Do NOT copy any hook bodies — the preset is the single source
of truth.

1. Resolve the exact stable version at execute time, then add it EXACT-pinned (no `^`,
   FMT-01 supply-chain precedent). Research recorded ~2.1.9 as `latest`; confirm the current
   `latest` and that `pnpm add -D lefthook@<exact>` resolves and updates the lockfile. Run
   `pnpm add -D lefthook@<exact-version>` (e.g. the version reported by `pnpm view lefthook version`).
   The resulting `package.json` devDependency MUST be the bare version string, no caret.

2. Supply-chain disposition — record, do NOT add an interactive blocking checkpoint. The
   legitimacy seam flags `lefthook` as SUS/too-new; RESEARCH cleared this as a false positive
   (~2.58M weekly downloads, evilmartians source repo, already a transitive dep of the pinned
   shared preset, binaries ship as lockfile-pinned `optionalDependencies` with no install-time
   network fetch). Per the autonomous carte-blanche, the disposition is approve-and-proceed.
   Record this disposition (package, version, verdict, reason it is approved) in the phase
   SUMMARY supply-chain note — do NOT stall the run waiting on a human.

3. THE blocker — add `lefthook: true` under `allowBuilds:` in `pnpm-workspace.yaml`, keeping the
   existing entries and alphabetical order. Without it, pnpm 11 silently skips lefthook's
   postinstall (binary select + hook install) and hooks never wire. After the edit, run
   `pnpm install` and confirm there is NO "Ignored build scripts: lefthook" warning. If pnpm
   still warns, run `pnpm approve-builds` and reconcile so the allowlist takes effect (the
   `allowBuilds:` map is this repo's equivalent of docs' `onlyBuiltDependencies`).

4. Commit the root `lefthook.yml` as a one-line `extends` of the preset — its entire body is a
   single `extends` list whose only entry is the preset path
   `node_modules/@solid-stats/ts-toolchain/lefthook.yml`. NO `pre-commit:`/`pre-push:` keys,
   NO copied command bodies (Don't-Hand-Roll). Add a short head comment noting hooks come from
   the shared preset and bodies must not be copied here.

5. Add `"prepare": "lefthook install || true"` to `package.json` scripts (HOK-01). The
   `|| true` makes a missing `.git` (Docker build stage) non-fatal; CI is unaffected (real
   `.git`, and `CI=true` makes lefthook's own postinstall skip).

6. Regenerate + commit `pnpm-lock.yaml` in this change (run `pnpm install` so the lockfile
   records lefthook plus its `optionalDependencies` platform binaries) so CI/Docker
   `--frozen-lockfile` resolves identically. Confirm `pnpm install --frozen-lockfile` succeeds
   against the committed lockfile.

7. Prove hooks wire + fire + bypass (V1/V2/V3), leaving NO fixture committed and NO `src/`
   touched:
   - WIRE: `pnpm exec lefthook version` succeeds; `.git/hooks/pre-commit` and
     `.git/hooks/pre-push` exist and reference lefthook; `pnpm exec lefthook validate` exits 0;
     `pnpm exec lefthook dump` shows the preset's four commands (oxfmt+oxlint pre-commit,
     typecheck+test pre-push) merged via `extends`.
   - FIRE: create a deliberately mis-formatted throwaway `.ts` fixture OUTSIDE `src/` (e.g. a
     temp path under the repo root or a tmp dir), `git add` it, run `lefthook run pre-commit`
     and confirm it FAILS at the format/lint step; then `git restore --staged` and delete the
     fixture so nothing is staged or committed. The fixture MUST NOT live under `src/` (that
     would change the measured file set) and MUST NOT be committed.
   - BYPASS: confirm and document that `git commit --no-verify` / `git push --no-verify` and
     `LEFTHOOK=0 git ...` skip the hooks (native git + lefthook env). Add a one-line bypass note
     to README.md (keep README otherwise intact — only add the bypass line in the appropriate
     hooks/dev-workflow section).

Verify the working tree has no leftover fixture and no `src/` diff before considering the task
done (`git status --short` shows only the intended config/doc files).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm exec lefthook version && pnpm exec lefthook validate && test -f .git/hooks/pre-commit && test -f .git/hooks/pre-push && grep -q "node_modules/@solid-stats/ts-toolchain/lefthook.yml" lefthook.yml && grep -q "lefthook: true" pnpm-workspace.yaml && node -e "const p=require('./package.json');const v=p.devDependencies.lefthook;if(!v||v.startsWith('^')||v.startsWith('~'))process.exit(1);if(p.scripts.prepare!=='lefthook install || true')process.exit(1)" && test -z "$(git status --short -- 'src/' )"</automated>
  </verify>
  <done>
lefthook is an exact-pinned devDependency; `pnpm-workspace.yaml` allowBuilds includes
`lefthook: true` (no "Ignored build scripts" warning); root `lefthook.yml` is a one-line
`extends` of the preset with no copied bodies; `prepare` runs `lefthook install || true`;
`pnpm-lock.yaml` is regenerated and frozen-lockfile reproducible; `.git/hooks/pre-commit` and
`pre-push` exist and reference lefthook; `lefthook validate` exits 0; a mis-formatted staged
fixture blocks `lefthook run pre-commit` and `--no-verify`/`LEFTHOOK=0` bypass is documented in
README; no fixture committed; zero `src/` diff; supply-chain disposition recorded for SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reorder verify to canonical surface, add CI lefthook-validate, prove full verify green at 100% coverage</name>
  <files>package.json, .github/workflows/cd.yml</files>
  <action>
Finalize the canonical command surface and CI (VRF-01, VRF-02, VRF-03). Behavior-preserving:
the ONLY `verify` delta is moving `build` (tsdown) one position earlier.

1. Reorder the `verify` script in `package.json` so `build` runs immediately before
   `depcruise` (move it from the current last position). Final order MUST be:
   `format:check → lint → typecheck → test → test:integration → test:coverage → build →
   depcruise → knip`. The exact target string is in RESEARCH §Q5 / the package.json deltas
   block. This is a one-token move — do NOT change any other script, flag, or env. depcruise
   (`dependency-cruiser src`) and knip (`knip.jsonc`) are `src`-scoped and do not consume
   `dist/`, so building earlier changes no tool input — it only fails fast on a broken build
   before the structural gates (VRF-02 functional-risk analysis: no risk).

2. CI (`.github/workflows/cd.yml`) — keep the existing `verify` job (already
   `pnpm run verify`) and the `image`/GHCR publish job EXACTLY as-is (do NOT touch the `image`
   job, its `needs: verify`, or its `if: != pull_request` gating). Preserve Node 25, pnpm 11,
   and `pnpm install --frozen-lockfile`. Add ONE new step to the `verify` job, after "Install
   dependencies" and before "Run verification", that asserts hook-config validity via
   `pnpm exec lefthook validate` (named e.g. "Validate lefthook config"). This fails CI fast on
   a broken/typo'd `extends` path instead of on a developer's machine. No env edit is needed —
   GitHub sets `CI=true` (lefthook postinstall self-skips) and `actions/checkout@v6` provides a
   real `.git`.

3. Prove the full surface green at 100% coverage. The session shell lacks the docker group, so
   the testcontainers integration leg + coverage leg run through the docker wrapper:
   `sg docker -c "pnpm run verify"` → exit 0. Confirm `test:coverage` passes with the existing
   `vitest.config.ts` 100% thresholds (statements/branches/functions/lines) and that NO
   threshold or coverage `exclude` was relaxed (the measured `src/**/*.ts` file set is unchanged
   — VRF-03). If `sg docker -c` is unavailable in the execution environment, run the non-Docker
   legs directly and run the integration + coverage legs via the wrapper; record the command
   used in the SUMMARY.

Confirm `git status --short` is clean (only intended config/doc diffs) and zero `src/` files
changed before closing the phase.
  </action>
  <verify>
    <automated>node -e "const v=require('./package.json').scripts.verify;const o=['format:check','lint','typecheck','test','test:integration','test:coverage','build','depcruise','knip'];let last=-1;for(const s of o){const i=v.indexOf('run '+s+' ')>-1?v.indexOf('run '+s+' '):v.indexOf('run '+s);const j=s==='test'?v.indexOf('pnpm test'):i;if(j<=last){console.error('verify order wrong at '+s);process.exit(1)}last=j}" && grep -q "lefthook validate" .github/workflows/cd.yml && grep -q "image:" .github/workflows/cd.yml && grep -q "frozen-lockfile" .github/workflows/cd.yml && test -z "$(git status --short -- 'src/')"</automated>
  </verify>
  <done>
`verify` runs in canonical order (build before depcruise/knip) with no other script change; CI
`verify` job has a `lefthook validate` step after install and before "Run verification"; the
`image`/GHCR job, Node 25, pnpm 11, and frozen lockfile are unchanged; `sg docker -c "pnpm run
verify"` (or the recorded equivalent) exits 0 with 100% V8 coverage and no threshold/exclude
relaxation; zero `src/` diff; `git status --short` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → install lifecycle | A new devDependency (`lefthook`) and its postinstall script cross into the build toolchain. |
| pnpm build allowlist → postinstall execution | Allowlisting `lefthook` grants its postinstall permission to run (informed consent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering / Elevation | `lefthook` npm package (typosquat / malicious hook tool) | mitigate | Use the established evilmartians `lefthook` package — already a transitive dep of the pinned shared `@solid-stats/ts-toolchain` preset; exact-pin (no `^`) + committed lockfile so the resolved binary cannot drift. |
| T-18-02 | Tampering | `lefthook` postinstall (`node postinstall.js`) running on install | mitigate | postinstall is the documented binary-selector; it runs ONLY because we explicitly add `lefthook: true` to the pnpm build allowlist (informed consent). Binaries ship as lockfile-pinned `optionalDependencies` — no install-time network fetch. |
| T-18-03 | Tampering | Supply-chain drift via a `^`/`~` range | mitigate | Exact pin `lefthook@<version>`; bumps happen only via deliberate re-pin (FMT-01 precedent). |
| T-18-SC | Tampering | npm install of `lefthook` (seam SUS/too-new flag) | accept (record disposition) | RESEARCH cleared the SUS verdict as a false positive (seam keys "too-new" off the latest release date, not package age). Disposition is approve-and-proceed under the autonomous carte-blanche; executor records the supply-chain disposition in the SUMMARY rather than stalling on an interactive blocking checkpoint. |
</threat_model>

<verification>
Phase-level checks (map to VALIDATION.md V1–V6 + hard invariants):

- **V1 (HOK-01/02):** `pnpm install --frozen-lockfile` reproducible; `pnpm exec lefthook version`
  + `lefthook validate` exit 0; `.git/hooks/pre-commit` + `pre-push` exist and reference
  lefthook; `lefthook dump` shows the preset's 4 commands via `extends`; `lefthook: true` in
  `allowBuilds`; root `lefthook.yml` has no copied bodies.
- **V2 (HOK-02):** mis-formatted staged fixture (outside `src/`) blocks `lefthook run pre-commit`;
  fixture not committed; reverting unblocks.
- **V3 (HOK-03):** `--no-verify` / `LEFTHOOK=0` bypass documented in README.
- **V4 (VRF-01/02):** `verify` order = oxfmt → oxlint → tsc → unit → integration → coverage →
  build → depcruise → knip; `sg docker -c "pnpm run verify"` exits 0.
- **V5 (VRF-03):** `test:coverage` exits 0 at 100%; no `vitest.config.ts` threshold/exclude
  relaxation.
- **V6 (VRF-03):** CI `verify` job rides `pnpm run verify` with Node 25 + pnpm 11 + frozen
  lockfile; `image`/GHCR job intact; `lefthook validate` step added non-disruptively.
- **Hard invariants:** zero `src/` diff across the phase; `git status --short` clean at close;
  branch pushed.

End-of-phase gate: full `sg docker -c "pnpm run verify"` green at 100% coverage AND
`git status --short -- src/` empty.
</verification>

<success_criteria>
- lefthook pre-commit (oxfmt + oxlint staged) + pre-push (tsc + Vitest) installed from the
  shared `@solid-stats/ts-toolchain` preset via `extends`, mirroring CI, bypassable with
  `--no-verify` (SC1 / HOK-01, HOK-02, HOK-03).
- `pnpm verify` runs the full new surface in canonical order green from a clean checkout
  (SC2 / VRF-01, VRF-02).
- V8 coverage stays 100% reachable source; measured file set not reduced; zero `src/` changes
  (SC3 / VRF-03).
- CI rewritten/confirmed on the new command surface with a `lefthook validate` assertion; the
  `image`/GHCR publish job intact (SC4 / VRF-03).
- Supply-chain disposition for `lefthook` recorded in the SUMMARY; working tree clean.
</success_criteria>

<output>
Create `.planning/phases/18-lefthook-hooks-ci-verify-convergence/18-01-SUMMARY.md` when done.
Record in the SUMMARY: the exact lefthook version pinned, the supply-chain disposition
(approve-and-proceed, SUS false-positive rationale), the `verify` reorder, the CI step added,
and the wrapper command used for the full verify run.
</output>
