---
phase: 03-raw-replay-storage
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/cli.ts
  - src/cli.test.ts
  - README.md
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - TEST-02
user_setup:
  - service: s3-compatible-storage
    why: "The mutating raw-storage CLI path needs real S3-compatible configuration outside fake tests."
    env_vars:
      - name: S3_ENDPOINT
        source: "Object storage provider endpoint"
      - name: S3_REGION
        source: "Object storage provider region"
      - name: S3_BUCKET
        source: "Raw replay bucket"
      - name: S3_ACCESS_KEY_ID
        source: "Object storage access key"
      - name: S3_SECRET_ACCESS_KEY
        source: "Object storage secret key"
      - name: S3_FORCE_PATH_STYLE
        source: "Provider-specific path-style setting"
must_haves:
  truths:
    - "Operator can run a Phase 3 storage command path that discovers candidates and stores raw replay objects without staging."
    - "CLI output contains structured per-candidate storage evidence and counts for stored, skipped, conflict, and failed items."
    - "README documents the raw object key rule and Phase 3 boundaries."
    - "Tests guard against parser, staging/outbox, server-2 business-table, local replay-list, and run-once scope creep."
  artifacts:
    - path: "src/cli.ts"
      provides: "Phase 3 CLI path for raw storage"
    - path: "src/cli.test.ts"
      provides: "CLI storage wiring and boundary tests"
    - path: "README.md"
      provides: "Operator documentation for raw storage behavior"
  key_links:
    - from: "src/cli.ts"
      to: "src/storage/store-raw-replay.ts"
      via: "CLI maps discovered candidates into raw storage calls"
      pattern: "storeRawReplay"
    - from: "src/cli.ts"
      to: "src/config.ts"
      via: "mutating storage path loads full config including S3"
      pattern: "loadConfig"
---

<objective>
Expose Phase 3 raw storage through the CLI and document the operator boundary.

Purpose: The phase is complete only when raw storage is reachable through a user-facing command path while staging, parser, and scheduled run-once remain deferred.
Output: CLI wiring, colocated CLI tests, README updates, and final boundary guards.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@docs/integration-contract.md
@.planning/phases/03-raw-replay-storage/03-CONTEXT.md
@.planning/phases/02-source-discovery-and-dry-run/02-VERIFICATION.md
@.planning/phases/03-raw-replay-storage/03-03-SUMMARY.md

<interfaces>
From `src/cli.ts`:
```typescript
export function buildCli(): Command;
```

From Plan 03-03:
```typescript
export async function storeRawReplay(input: {
  readonly candidate: ReplayCandidate;
  readonly byteClient: ReplayByteClient;
  readonly storage: S3RawReplayStorage;
  readonly now?: () => Date;
}): Promise<RawReplayStorageEvidence>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI storage command tests and boundary guards</name>
  <files>src/cli.test.ts</files>
  <behavior>
    - Test 1: `discover --store-raw` or equivalent Phase 3 flag loads full config, discovers candidates, stores each candidate through injected fakes, and emits JSON storage evidence.
    - Test 2: `discover --dry-run` remains non-mutating and source-only.
    - Test 3: storage command output includes counts for stored, skipped, conflict, and failed statuses.
    - Test 4: static boundary guard finds no parser artifact writes, PostgreSQL/business-table writes, local replay-list writes, or `run-once` implementation in Phase 3 storage path files.
  </behavior>
  <action>Create failing CLI tests that define the exact user-facing Phase 3 command shape. Prefer `discover --store-raw` so `discover --dry-run` remains the read-only inspection mode from Phase 2 and `run-once` remains Phase 5. Tests must use injected fakes/mocks for discovery, byte fetch, and S3 storage; no live S3 or source network is required.</action>
  <verify>
    <automated>pnpm test -- src/cli.test.ts</automated>
  </verify>
  <done>CLI tests fail until storage wiring exists and boundary checks explicitly block out-of-scope subsystems.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire raw storage CLI path</name>
  <files>src/cli.ts, src/cli.test.ts</files>
  <behavior>
    - Test 1: storage command emits structured JSON report with candidate count and per-status storage counts.
    - Test 2: config failures and source/storage failures set exit code 2 with structured JSON.
    - Test 3: dry-run behavior from Phase 2 continues to pass unchanged.
  </behavior>
  <action>Update `buildCli()` to support a Phase 3 raw-storage mode, preferably `discover --store-raw`, that loads full `loadConfig()`, creates source client, replay byte client, and S3 raw storage adapter, runs discovery, stores each candidate, and writes one structured JSON report to stdout. Preserve `discover --dry-run` as source-only and non-mutating. Keep `run-once` throwing the Phase 5 planned error. Do not write staging/outbox rows, parser artifacts, local replay-list files, `server-2` business tables, or scheduled run summaries.</action>
  <verify>
    <automated>pnpm test -- src/cli.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>CLI exposes raw storage while dry-run and run-once boundaries remain intact, and failures are structured with exit code 2.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 3 storage behavior</name>
  <files>README.md</files>
  <action>Update README with the Phase 3 operator command, required S3 environment variables, raw object key rule `raw/sha256/<sha256>.ocap`, checksum-before-key behavior, HEAD-before-PUT idempotency, skip-on-match behavior, conflict-on-mismatch behavior, and explicit boundaries: no parser, no staging/outbox, no `server-2` business tables, and no scheduled `run-once` until Phase 5. Keep the AI agents plus GSD workflow statement intact.</action>
  <verify>
    <automated>pnpm run format && pnpm test -- src/cli.test.ts && grep -n "raw/sha256/<sha256>.ocap" README.md && grep -n "does not write staging" README.md</automated>
  </verify>
  <done>README documents the implemented Phase 3 raw storage command and phase boundaries with exact object-key/idempotency language.</done>
</task>

</tasks>

<source_audit>
## Coverage

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Raw replay storage reachable by fetcher | Tasks 1-2 |
| REQ | STOR-01 raw replay writes | Task 2 |
| REQ | STOR-02 checksum evidence | Task 2 via Plan 03-03 |
| REQ | STOR-03 object key/bucket/size/checksum/fetch timestamp evidence | Task 2 output |
| REQ | STOR-04 idempotent non-overwrite behavior | Task 2 via Plan 03-02 |
| REQ | STOR-05 structured failures without business state | Tasks 1-2 |
| REQ | TEST-02 mocked storage behavior | Task 1 verifies CLI with fakes; Plan 03-02 verifies adapter |
| CONTEXT | `discover --dry-run` remains read-only | Tasks 1-2 |
| CONTEXT | scheduled run-once out of scope | Tasks 1-3 |
| CONTEXT | parser/staging/server-2 business tables out of scope | Tasks 1-3 |
| RESEARCH | operator command surface includes discover/check/run-once | Task 2 adds storage under discover while keeping run-once deferred |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env -> CLI | S3/source configuration enters mutating command path. |
| CLI -> external source/S3 | Command triggers external reads and raw object writes. |
| CLI output -> operator/logs | Structured evidence is printed to stdout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Spoofing | CLI config loading | mitigate | Mutating raw-storage path must use full `loadConfig()` and reject invalid S3/source config before writing. |
| T-03-13 | Tampering | CLI storage loop | mitigate | Delegate checksum/key/idempotency to Plans 03-01 through 03-03 and never overwrite on conflict. |
| T-03-14 | Repudiation | CLI storage report | mitigate | Emit per-candidate evidence and per-status counts in one JSON report. |
| T-03-15 | Information disclosure | CLI report | mitigate | Report object/storage evidence only; do not print S3 access keys, secrets, or raw replay bytes. |
| T-03-16 | Elevation of privilege | out-of-scope writes | mitigate | Boundary tests guard against parser artifacts, staging/outbox, `server-2` business tables, local replay-list writes, and `run-once` implementation. |
</threat_model>

<verification>
Run `pnpm test -- src/cli.test.ts && pnpm run verify`.
</verification>

<success_criteria>
Operators have a documented Phase 3 command path for raw storage, test coverage proves it uses fake storage safely, and all deferred subsystems remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/03-raw-replay-storage/03-04-SUMMARY.md`.
</output>
