---
phase: 05-scheduled-operations-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/run/run-once.ts
  - src/run/run-once.test.ts
autonomous: true
requirements:
  - RUN-02
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - TEST-01
must_haves:
  truths:
    - "runOnce executes one discovery -> raw storage -> staging cycle and returns a structured summary."
    - "runOnce is bounded and does not loop forever."
    - "Expected failures are classified and reflected in summary exit code."
  artifacts:
    - path: "src/run/run-once.ts"
      provides: "Scheduled one-shot ingest orchestration"
      exports: ["runOnce"]
---

<objective>
Implement reusable run-once orchestration independent of CLI parsing.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify run-once orchestration</name>
  <files>src/run/run-once.test.ts</files>
  <action>Create colocated tests with injected fakes for successful full cycle, source failure, raw storage failure/conflict, staging failure/conflict, and empty discovery.</action>
  <verify><automated>pnpm test -- src/run/run-once.test.ts</automated></verify>
  <done>Tests lock behavior before implementation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement runOnce</name>
  <files>src/run/run-once.ts, src/run/run-once.test.ts</files>
  <action>Implement `runOnce` using injected source discovery, raw storage, staging, run ID, and clock. Reuse existing storage/staging functions. Return summary and intended exit code; do not call `process.exit` inside the orchestration.</action>
  <verify><automated>pnpm test -- src/run/run-once.test.ts && pnpm run typecheck</automated></verify>
  <done>runOnce is fully fake-tested and boundary-safe.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-scheduled-operations-and-validation/05-02-SUMMARY.md`.
</output>
