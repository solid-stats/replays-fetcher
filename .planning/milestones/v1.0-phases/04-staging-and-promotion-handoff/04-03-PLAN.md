---
phase: 04-staging-and-promotion-handoff
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/staging/stage-raw-replay.ts
  - src/staging/stage-raw-replay.test.ts
  - src/cli.ts
  - src/cli.test.ts
autonomous: true
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03
  - STAGE-04
  - STAGE-05
  - TEST-04
must_haves:
  truths:
    - "CLI can run discovery -> raw storage -> staging rows without `run-once`."
    - "Only stageable raw storage evidence creates pending staging rows."
    - "CLI output includes raw storage counts and staging counts."
    - "Expected config/source/storage/staging failures produce structured JSON and exit code 2."
  artifacts:
    - path: "src/staging/stage-raw-replay.ts"
      provides: "Raw storage evidence to staging orchestration"
      exports: ["stageRawReplay"]
    - path: "src/cli.ts"
      provides: "Phase 4 CLI path for raw storage plus staging"
  key_links:
    - from: "src/cli.ts"
      to: "src/staging/stage-raw-replay.ts"
      via: "CLI stages successful raw storage evidence"
      pattern: "stageRawReplay"
---

<objective>
Wire staging into the operator command path.

Purpose: Operators need a Phase 4 command path that discovers candidates, stores raw objects, and writes pending staging rows for `server-2` promotion.
Output: CLI/staging orchestration with injected fakes and boundary tests.
</objective>

<context>
@.planning/phases/04-staging-and-promotion-handoff/04-CONTEXT.md
@.planning/phases/04-staging-and-promotion-handoff/04-01-SUMMARY.md
@.planning/phases/04-staging-and-promotion-handoff/04-02-SUMMARY.md
@src/cli.ts
@src/storage/store-raw-replay.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify staging orchestration</name>
  <files>src/staging/stage-raw-replay.test.ts, src/cli.test.ts</files>
  <behavior>
    - Test 1: `stageRawReplay` maps stageable raw evidence to payload then repository call.
    - Test 2: fetch/storage failures skip staging and return structured skipped/failed staging evidence.
    - Test 3: CLI command emits counts for staged, already_staged, conflict, skipped, and failed staging outcomes.
    - Test 4: `discover --dry-run` remains non-mutating.
    - Test 5: `run-once` remains Phase 5 planned error.
  </behavior>
  <action>Create failing colocated tests. Prefer extending `discover --store-raw` into a raw-plus-staging command only if the command name remains clear in JSON output; otherwise add an explicit `--stage` flag. The tests define the final command shape before implementation.</action>
  <verify>
    <automated>pnpm test -- src/staging/stage-raw-replay.test.ts src/cli.test.ts</automated>
  </verify>
  <done>Tests lock Phase 4 command behavior and deferred boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI staging wiring</name>
  <files>src/staging/stage-raw-replay.ts, src/staging/stage-raw-replay.test.ts, src/cli.ts, src/cli.test.ts</files>
  <behavior>
    - Test 1: full fake path discovers candidates, stores raw objects, stages pending records, and emits structured JSON.
    - Test 2: staging conflicts/failures set exit code 2.
    - Test 3: dry-run and raw-storage-only boundaries remain intact if both modes still exist.
  </behavior>
  <action>Implement staging orchestration and CLI wiring using injected dependencies. Load full config before mutating storage/staging. Create a PostgreSQL staging repository from `DATABASE_URL`. Do not create canonical `replays`, `parse_jobs`, parser artifacts, or scheduled run summaries.</action>
  <verify>
    <automated>pnpm test -- src/staging/stage-raw-replay.test.ts src/cli.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>Operator command can write staging rows through fakes; all deferred subsystem boundaries remain explicit.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/staging/stage-raw-replay.test.ts src/cli.test.ts && pnpm run typecheck`.
</verification>

<output>
After completion, create `.planning/phases/04-staging-and-promotion-handoff/04-03-SUMMARY.md`.
</output>
