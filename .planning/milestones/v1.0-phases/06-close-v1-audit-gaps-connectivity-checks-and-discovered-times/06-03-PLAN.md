---
phase: 06-close-v1-audit-gaps-connectivity-checks-and-discovered-times
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - src/staging/types.ts
  - src/staging/payload.ts
  - src/staging/payload.test.ts
  - src/staging/postgres-staging-repository.test.ts
  - src/run/summary.test.ts
autonomous: true
requirements:
  - INT-04
  - STAGE-01
  - STAGE-03
  - OPS-02
must_haves:
  truths:
    - "D-09: staging promotion evidence preserves discoveredAt when raw evidence has it."
    - "D-10: discoveredAt is never written to replayTimestamp or the `replay_timestamp` column."
    - "D-12/D-13: run summary tests continue treating JSON summaries as the operational log surface without leaking secrets or boundary artifacts."
  artifacts:
    - path: "src/staging/types.ts"
      provides: "optional promotionEvidence.discoveredAt contract"
    - path: "src/staging/payload.ts"
      provides: "raw evidence to staging promotion evidence mapping"
    - path: "src/staging/postgres-staging-repository.test.ts"
      provides: "SQL values assertion that replayTimestamp remains undefined"
  key_links:
    - from: "src/staging/payload.ts"
      to: "src/storage/types.ts"
      via: "RawReplayStorageEvidence.discoveredAt"
      pattern: "evidence\\.discoveredAt"
    - from: "src/staging/postgres-staging-repository.ts"
      to: "ingest_staging_records.replay_timestamp"
      via: "payload.replayTimestamp remains separate"
      pattern: "payload\\.replayTimestamp"
---

<objective>
Carry discovered timestamp evidence into server-2 staging promotion evidence without overloading replay timestamp semantics.

Purpose: Close INT-04/STAGE-01/STAGE-03 by preserving source discovery evidence in JSON promotion evidence only.
Output: Updated staging payload contract, mapper, and tests proving `replay_timestamp` remains null/undefined.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md
@src/storage/types.ts
@src/staging/types.ts
@src/staging/payload.ts
@src/staging/postgres-staging-repository.ts
@src/run/summary.ts
@server-2/src/infra/db/migrations/0001_v1_domain_schema.sql
@server-2/src/modules/ingest/types.ts
@server-2/src/modules/ingest/repository.ts

<interfaces>
Server-2 staging table stores discovery evidence as JSON and trusted replay time separately:

```sql
create table ingest_staging_records (
  replay_timestamp timestamptz,
  promotion_evidence jsonb not null default '{}'::jsonb
);
```

Target staging promotion evidence addition:

```typescript
export interface IngestStagingPayload {
  readonly promotionEvidence: {
    readonly discoveredAt?: string;
    readonly fetchedAt: string;
    readonly sourceUrl: string;
  };
  readonly replayTimestamp?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map discoveredAt into promotionEvidence only</name>
  <files>src/staging/types.ts, src/staging/payload.ts, src/staging/payload.test.ts</files>
  <read_first>
    <file>src/staging/types.ts</file>
    <file>src/staging/payload.ts</file>
    <file>src/staging/payload.test.ts</file>
    <file>src/storage/types.ts</file>
    <file>server-2/src/infra/db/migrations/0001_v1_domain_schema.sql</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test 1: `toIngestStagingPayload` maps raw `discoveredAt: "2026-05-09T00:32:44.000Z"` to `payload.promotionEvidence.discoveredAt`.
    - Test 2: `toIngestStagingPayload` omits `promotionEvidence.discoveredAt` when raw evidence lacks it.
    - Test 3: `payload.replayTimestamp` remains absent when only source discovery evidence exists.
  </behavior>
  <action>Update `src/staging/types.ts` so `IngestStagingPayload["promotionEvidence"]` has `readonly discoveredAt?: string`. Update `src/staging/payload.ts` to conditionally include `discoveredAt` from `evidence.discoveredAt` in `promotionEvidence` before adding optional `sourceExternalId`. Do not assign `evidence.discoveredAt` to `payload.replayTimestamp`; `replayTimestamp` must remain absent unless another trusted replay timestamp source already exists. Update `src/staging/payload.test.ts` exact expected payloads so discovered timestamp appears only under `promotionEvidence.discoveredAt`, and add an omission assertion with `JSON.stringify(result)` for evidence without the field.</action>
  <verify>
    <automated>pnpm test -- src/staging/payload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "readonly discoveredAt\\?: string" src/staging/types.ts` finds the promotion evidence field.
    - `rg -n "discoveredAt: evidence\\.discoveredAt|evidence\\.discoveredAt" src/staging/payload.ts` finds the conditional mapper.
    - `rg -n "replayTimestamp:.*discoveredAt|replay_timestamp.*discoveredAt" src/staging src/storage` returns no matches.
    - `rg -n "not\\.toContain\\(\"discoveredAt\"\\)|promotionEvidence:.*discoveredAt" src/staging/payload.test.ts` finds presence and omission coverage.
    - `pnpm test -- src/staging/payload.test.ts` passes.
  </acceptance_criteria>
  <done>Staging payloads preserve discoveredAt as promotion evidence only and keep replay timestamp semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve repository and summary safeguards with new evidence</name>
  <files>src/staging/postgres-staging-repository.test.ts, src/run/summary.test.ts</files>
  <read_first>
    <file>src/staging/postgres-staging-repository.ts</file>
    <file>src/staging/postgres-staging-repository.test.ts</file>
    <file>src/run/summary.ts</file>
    <file>src/run/summary.test.ts</file>
    <file>docs/integration-contract.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: repository SQL values keep the sixth insert value (`replay_timestamp`) as `undefined` when promotion evidence has `discoveredAt`.
    - Test 2: inserted `promotion_evidence` JSON includes `"discoveredAt":"2026-05-09T00:32:44.000Z"`.
    - Test 3: summary JSON negative assertions cover S3 secret strings, database URL strings, SSH secret strings, raw byte marker strings, parser artifact strings, and canonical server-2 business table strings.
  </behavior>
  <action>Update `src/staging/postgres-staging-repository.test.ts` fixtures to include `promotionEvidence.discoveredAt` and assert the SQL values array still has `undefined` for `payload.replayTimestamp` at the replay timestamp position. Add or preserve an assertion that the serialized promotion evidence contains `discoveredAt`. Update `src/run/summary.test.ts` to keep the existing JSON summary as the structured operational log surface per D-12 and add negative assertions that `JSON.stringify(summary)` does not contain `secret-key`, `postgres://user:password@`, `sshpass`, `raw-replay-bytes`, `parser_artifact`, `parse_jobs`, `parse_results`, `identity`, `roles`, `requests`, or `moderation_actions`. Do not add a separate per-item logger in this plan.</action>
  <verify>
    <automated>pnpm test -- src/staging/postgres-staging-repository.test.ts src/run/summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "discoveredAt" src/staging/postgres-staging-repository.test.ts src/run/summary.test.ts` finds new evidence and leakage coverage.
    - `rg -n "undefined,\\s*\\n\\s*\\\"pending\\\"" src/staging/postgres-staging-repository.test.ts` finds the replay timestamp value still absent before status.
    - `rg -n "per-item logger|pino|logger" src/run src/staging` returns no new logger implementation matches.
    - `pnpm test -- src/staging/postgres-staging-repository.test.ts src/run/summary.test.ts` passes.
  </acceptance_criteria>
  <done>Staging repository tests prove JSON evidence storage without replay timestamp misuse, and OPS-02 summary leakage safeguards remain explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw storage evidence -> staging payload | Source metadata becomes server-2 promotion evidence. |
| Staging payload -> PostgreSQL row | JSON evidence and replay timestamp are persisted through SQL parameters. |
| Run summary -> operator stdout | Operational JSON is consumed by logs/schedulers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | `src/staging/payload.ts` | mitigate | Map source-discovered time only to `promotionEvidence.discoveredAt`; grep blocks replayTimestamp assignment from discoveredAt. |
| T-06-11 | Repudiation | `src/staging/types.ts` | mitigate | Typed optional field makes timestamp provenance explicit in promotion evidence. |
| T-06-12 | Information Disclosure | `src/run/summary.test.ts` | mitigate | Negative assertions cover secrets, raw bytes, parser artifacts, and canonical business records. |
| T-06-13 | Elevation of Privilege | `src/staging/postgres-staging-repository.test.ts` | mitigate | Preserve forbidden-write static guards; repository still writes only `ingest_staging_records`. |
</threat_model>

<verification>
Run `pnpm test -- src/staging/payload.test.ts src/staging/postgres-staging-repository.test.ts src/run/summary.test.ts` and confirm no grep evidence maps discoveredAt into replayTimestamp.
</verification>

<success_criteria>
- Staging promotion evidence includes optional `discoveredAt`.
- `replayTimestamp` stays absent/undefined for source-discovered timestamps.
- Existing source identity, checksum/object-key idempotency, and conflict tests still pass.
- Summary output leakage tests explicitly cover OPS-02 threat strings.
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-03-SUMMARY.md`
</output>
