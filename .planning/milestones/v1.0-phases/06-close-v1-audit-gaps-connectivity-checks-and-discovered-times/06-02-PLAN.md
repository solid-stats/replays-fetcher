---
phase: 06-close-v1-audit-gaps-connectivity-checks-and-discovered-times
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/storage/store-raw-replay.ts
  - src/storage/s3-raw-storage.ts
  - src/storage/store-raw-replay.test.ts
  - src/storage/s3-raw-storage.test.ts
autonomous: true
requirements:
  - INT-04
  - STAGE-01
  - OPS-02
must_haves:
  truths:
    - "D-07/D-08: raw storage evidence copies only `candidate.metadata.discoveredAt`; it never uses fetchedAt or run start as a fallback."
    - "D-11: discovered timestamp propagation does not parse replay bytes."
    - "OPS-02: raw storage evidence and tests do not include credentials, raw bytes, parser artifacts, or server-2 business records."
  artifacts:
    - path: "src/storage/types.ts"
      provides: "optional discoveredAt field on raw storage evidence"
    - path: "src/storage/s3-raw-storage.ts"
      provides: "conditional discoveredAt copy from ReplayCandidate metadata"
    - path: "src/storage/store-raw-replay.ts"
      provides: "candidate metadata handoff without timestamp synthesis"
  key_links:
    - from: "src/storage/s3-raw-storage.ts"
      to: "src/discovery/types.ts"
      via: "candidate.metadata.discoveredAt"
      pattern: "metadata\\?\\.discoveredAt"
---

<objective>
Preserve source-discovered timestamp evidence at the raw storage layer.

Purpose: Close the first half of the discovery -> storage -> staging evidence gap while keeping replay bytes opaque.
Output: Optional `discoveredAt` on raw storage evidence, populated only from discovery metadata.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md
@src/discovery/types.ts
@src/storage/types.ts
@src/storage/store-raw-replay.ts
@src/storage/s3-raw-storage.ts

<interfaces>
Existing source candidate metadata:

```typescript
export interface ReplayCandidate {
  readonly metadata?: {
    readonly discoveredAt?: string;
    readonly missionText?: string;
    readonly serverId?: number;
    readonly world?: string;
  };
}
```

Target raw evidence addition:

```typescript
export interface RawReplayStorageEvidence extends RawReplayObjectIdentity {
  readonly byteSize: number;
  readonly discoveredAt?: string;
  readonly fetchedAt: string;
  readonly source: ReplayCandidate["source"];
  readonly sourceFilename: string;
  readonly status: RawReplayStorageStatus;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional discoveredAt to raw storage evidence</name>
  <files>src/storage/types.ts, src/storage/store-raw-replay.ts, src/storage/s3-raw-storage.ts</files>
  <read_first>
    <file>src/discovery/types.ts</file>
    <file>src/storage/types.ts</file>
    <file>src/storage/store-raw-replay.ts</file>
    <file>src/storage/s3-raw-storage.ts</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test expectation before implementation: raw storage evidence includes `discoveredAt` when the input candidate has `metadata.discoveredAt`.
    - Test expectation before implementation: raw storage evidence omits the `discoveredAt` key when the input candidate has no discovered timestamp.
    - Test expectation before implementation: no code assigns `discoveredAt` from `fetchedAt`, `new Date()`, or run start time.
  </behavior>
  <action>Update `src/storage/types.ts` so `RawReplayStorageEvidence` has `readonly discoveredAt?: string`. In `src/storage/s3-raw-storage.ts`, update `toBaseEvidence` to conditionally spread `discoveredAt` from `input.candidate.metadata?.discoveredAt` only: `...(input.candidate.metadata?.discoveredAt === undefined ? {} : { discoveredAt: input.candidate.metadata.discoveredAt })`. Keep optional fields omitted instead of present with `undefined`. Do not add replay parsing, do not inspect raw bytes for timestamps, and do not change checksum or object-key logic. Leave `storeRawReplay` as a candidate handoff unless TypeScript requires a fixture/type adjustment; do not synthesize a timestamp there.</action>
  <verify>
    <automated>pnpm test -- src/storage/store-raw-replay.test.ts src/storage/s3-raw-storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "readonly discoveredAt\\?: string" src/storage/types.ts` finds the optional raw evidence field.
    - `rg -n "metadata\\?\\.discoveredAt" src/storage/s3-raw-storage.ts` finds the source metadata copy.
    - `rg -n "discoveredAt:.*fetchedAt|discoveredAt:.*new Date|discoveredAt:.*startedAt|replayTimestamp" src/storage` returns no matches.
    - `rg -n "parseReplay|replay-parser|parse_jobs|insert\\s+into" src/storage` returns no matches outside test forbidden-token arrays.
  </acceptance_criteria>
  <done>Raw storage evidence can carry source-discovered time only when discovery provided it, with no fallback and no parser/server boundary creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend raw storage tests for discoveredAt presence and omission</name>
  <files>src/storage/store-raw-replay.test.ts, src/storage/s3-raw-storage.test.ts</files>
  <read_first>
    <file>src/storage/store-raw-replay.test.ts</file>
    <file>src/storage/s3-raw-storage.test.ts</file>
    <file>src/storage/types.ts</file>
    <file>src/storage/s3-raw-storage.ts</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: a candidate with `metadata.discoveredAt: "2026-05-09T00:32:44.000Z"` produces raw evidence with exactly that value.
    - Test 2: a candidate without `metadata` produces JSON output that does not contain the string `discoveredAt`.
    - Test 3: existing HEAD-before-PUT, skip, conflict, and S3 failure tests continue to pass.
  </behavior>
  <action>Update the raw storage unit tests to include a candidate fixture with `metadata.discoveredAt: "2026-05-09T00:32:44.000Z"` and assert the returned raw evidence includes `discoveredAt: "2026-05-09T00:32:44.000Z"`. Add a separate omission assertion using `expect(JSON.stringify(result)).not.toContain("discoveredAt")` for candidates without source metadata. Keep existing fake sender tests for `HeadObjectCommand` and `PutObjectCommand`; do not replace them with container tests in this plan.</action>
  <verify>
    <automated>pnpm test -- src/storage/store-raw-replay.test.ts src/storage/s3-raw-storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "2026-05-09T00:32:44\\.000Z|not\\.toContain\\(\"discoveredAt\"\\)" src/storage/store-raw-replay.test.ts src/storage/s3-raw-storage.test.ts` finds both presence and omission assertions.
    - `pnpm test -- src/storage/store-raw-replay.test.ts src/storage/s3-raw-storage.test.ts` passes.
    - Existing tests still assert `HeadObjectCommand` before `PutObjectCommand` for raw storage writes.
  </acceptance_criteria>
  <done>Storage tests prove discovered timestamp preservation and no-fallback omission while preserving current idempotent S3 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovery candidate -> raw storage evidence | Source-provided metadata becomes operational evidence. |
| Raw bytes -> storage adapter | Replay bytes remain opaque and must not be parsed for timestamps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | `src/storage/s3-raw-storage.ts` | mitigate | Copy only `candidate.metadata.discoveredAt`; acceptance grep blocks `fetchedAt`, `new Date`, and `startedAt` fallback assignments. |
| T-06-07 | Information Disclosure | `src/storage/*.test.ts` | mitigate | Tests assert metadata presence/omission without logging raw bytes or secrets. |
| T-06-08 | Elevation of Privilege | `src/storage/*` | mitigate | No parser imports, no staging writes, no canonical server-2 business writes in storage layer. |
| T-06-09 | Repudiation | `RawReplayStorageEvidence` | mitigate | Evidence retains exact source-provided timestamp so later staging evidence can be audited. |
</threat_model>

<verification>
Run `pnpm test -- src/storage/store-raw-replay.test.ts src/storage/s3-raw-storage.test.ts` and confirm static greps in acceptance criteria are clean.
</verification>

<success_criteria>
- `RawReplayStorageEvidence` exposes optional `discoveredAt`.
- S3 raw storage conditionally includes `discoveredAt` only from `candidate.metadata.discoveredAt`.
- Tests cover present and absent discovered timestamp cases.
- No replay parsing, staging mutation, parser artifact, or server-2 business-table behavior is added.
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-02-SUMMARY.md`
</output>
