---
phase: 06-close-v1-audit-gaps-connectivity-checks-and-discovered-times
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - src/storage/s3-raw-storage.integration.test.ts
  - src/staging/postgres-staging-repository.integration.test.ts
autonomous: true
requirements:
  - RUN-04
  - TEST-02
  - TEST-03
  - STAGE-01
  - STAGE-03
must_haves:
  truths:
    - "D-15: Docker-backed Testcontainers coverage exists for both MinIO/S3-compatible storage and PostgreSQL staging behavior."
    - "D-16/D-17: `pnpm run test:integration` is a blocking verification command and Docker absence fails the command."
    - "D-18: existing fake/query-harness tests remain in place; integration tests supplement them."
  artifacts:
    - path: "vitest.config.ts"
      provides: "unit-suite exclusion for Docker-backed integration tests"
    - path: "src/storage/s3-raw-storage.integration.test.ts"
      provides: "MinIO-backed S3 adapter and read-only check coverage"
    - path: "src/staging/postgres-staging-repository.integration.test.ts"
      provides: "PostgreSQL schema-backed staging repository and connectivity coverage"
    - path: "package.json"
      provides: "test:integration script included in verify"
  key_links:
    - from: "package.json"
      to: "src/**/*.integration.test.ts"
      via: "direct test:integration file-pattern script"
      pattern: "src/\\*\\*/\\*.integration\\.test\\.ts"
    - from: "src/storage/s3-raw-storage.integration.test.ts"
      to: "src/check/s3-connectivity.ts"
      via: "HeadBucketCommand read-only probe"
      pattern: "checkS3Connectivity"
    - from: "src/staging/postgres-staging-repository.integration.test.ts"
      to: "server-2 staging schema"
      via: "ingest_staging_records table setup"
      pattern: "ingest_staging_records"
---

<objective>
Add blocking Docker-backed integration validation for S3-compatible storage and PostgreSQL staging.

Purpose: Close TEST-02/TEST-03 audit debt using real local services while retaining fast unit/fake tests.
Output: Testcontainers dev dependencies, integration Vitest config, integration tests, and verify script wiring.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-VALIDATION.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md
@package.json
@vitest.config.ts
@src/storage/s3-raw-storage.ts
@src/check/s3-connectivity.ts
@src/staging/postgres-staging-repository.ts
@src/check/postgres-connectivity.ts
@server-2/src/infra/db/migrations/0001_v1_domain_schema.sql
@server-2/src/infra/db/migrations/0002_ingest_processing_status.sql

<interfaces>
Integration dependency versions locked by research:

```bash
pnpm add -D @testcontainers/postgresql@11.14.0 @testcontainers/minio@11.14.0
```

Required scripts:

```json
{
  "test:integration": "vitest run \"src/**/*.integration.test.ts\" --no-file-parallelism --testTimeout 120000 --hookTimeout 120000",
  "verify": "pnpm run format && pnpm run lint && pnpm run typecheck && pnpm test && pnpm run test:integration && pnpm run test:coverage && pnpm run build"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add blocking integration test infrastructure</name>
  <files>package.json, pnpm-lock.yaml, vitest.config.ts</files>
  <read_first>
    <file>package.json</file>
    <file>vitest.config.ts</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-VALIDATION.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <action>Install Testcontainers modules with `pnpm add -D @testcontainers/postgresql@11.14.0 @testcontainers/minio@11.14.0`, allowing `pnpm-lock.yaml` to update. Update `vitest.config.ts` to import `configDefaults` from `vitest/config` and exclude integration tests from the fast unit suite with `exclude: [...configDefaults.exclude, "src/**/*.integration.test.ts"]`. Add direct package script `"test:integration": "vitest run \"src/**/*.integration.test.ts\" --no-file-parallelism --testTimeout 120000 --hookTimeout 120000"`. Update `"verify"` so it runs `pnpm run test:integration` after `pnpm test` and before `pnpm run test:coverage`. Per D-16, do not add Vitest projects, separate integration config files, environment guards, `test.skip`, or shell fallbacks that pass when Docker is unavailable.</action>
  <verify>
    <automated>pnpm run test:integration -- --help</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); if(!p.devDependencies['@testcontainers/postgresql']||!p.devDependencies['@testcontainers/minio']) process.exit(1); if(!p.scripts['test:integration'].includes('src/**/*.integration.test.ts')) process.exit(1); if(p.scripts['test:integration'].includes('--config')) process.exit(1); if(!p.scripts.verify.includes('pnpm run test:integration')) process.exit(1)"` exits 0.
    - `rg -n "configDefaults|src/\\*\\*/\\*.integration\\.test\\.ts" vitest.config.ts package.json` finds unit exclusion and direct integration script inclusion.
    - `! rg -n "test\\.skip|DOCKER|docker.*skip|process\\.env.*SKIP" package.json src` exits 0, proving no skip-on-Docker-absence path was introduced by this task.
  </acceptance_criteria>
  <done>Integration test command is installed, isolated from the fast unit script, and included in the blocking verify gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MinIO-backed S3 storage and check integration test</name>
  <files>src/storage/s3-raw-storage.integration.test.ts</files>
  <read_first>
    <file>src/storage/s3-raw-storage.ts</file>
    <file>src/storage/s3-raw-storage.test.ts</file>
    <file>src/check/s3-connectivity.ts</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test 1: MinIO container starts, bucket is created, and `createS3RawReplayStorageFromConfig` stores raw bytes under `raw/sha256/<checksum>.ocap`.
    - Test 2: a second store of identical bytes returns `status: "skipped"` through the real S3 adapter.
    - Test 3: `checkS3Connectivity` succeeds against the same bucket using a read-only `HeadBucketCommand` path and does not create probe objects.
  </behavior>
  <action>Create `src/storage/s3-raw-storage.integration.test.ts`. Use `MinioContainer` from `@testcontainers/minio` with the pinned deterministic image exactly as `new MinioContainer("minio/minio:RELEASE.2025-09-07T16-13-09Z")`, username `solid`, and password `solidsecret`. Use AWS SDK v3 `S3Client` plus `CreateBucketCommand` to create a test bucket named `solid-stats-replays`. Configure the existing adapter with endpoint `http://{container.getHost()}:{container.getPort()}`, region `us-east-1`, access key `solid`, secret key `solidsecret`, and `forcePathStyle: true`. Store a small `Uint8Array` through `createS3RawReplayStorageFromConfig`, assert `status: "stored"` and object key `raw/sha256/<checksum>.ocap`, store it again and assert `status: "skipped"`, then run `checkS3Connectivity({ bucket, sender: s3Client })` and assert `{ status: "passed" }`. Use a final `ListObjectsV2Command` assertion only in the test to prove no object with a probe-specific key was created. Do not skip the test when Docker is unavailable.</action>
  <verify>
    <automated>pnpm run test:integration -- src/storage/s3-raw-storage.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MinioContainer\\(\"minio/minio:RELEASE\\.2025-09-07T16-13-09Z\"\\)|createS3RawReplayStorageFromConfig|checkS3Connectivity|ListObjectsV2Command" src/storage/s3-raw-storage.integration.test.ts` finds the pinned image and required integration paths.
    - `rg -n "test\\.skip|describe\\.skip|SKIP|docker.*unavailable" src/storage/s3-raw-storage.integration.test.ts` returns no matches.
    - `pnpm run test:integration -- src/storage/s3-raw-storage.integration.test.ts` passes when Docker is available and fails if Docker cannot start the container.
  </acceptance_criteria>
  <done>MinIO integration proves real S3 adapter idempotency and read-only connectivity check behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add PostgreSQL-backed staging and check integration test</name>
  <files>src/staging/postgres-staging-repository.integration.test.ts</files>
  <read_first>
    <file>src/staging/postgres-staging-repository.ts</file>
    <file>src/staging/postgres-staging-repository.test.ts</file>
    <file>src/staging/types.ts</file>
    <file>src/check/postgres-connectivity.ts</file>
    <file>server-2/src/infra/db/migrations/0001_v1_domain_schema.sql</file>
    <file>server-2/src/infra/db/migrations/0002_ingest_processing_status.sql</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test 1: PostgreSQL container starts, minimal server-2 staging schema is applied, and `checkPostgresConnectivity` passes.
    - Test 2: `createPostgresStagingRepository` inserts a pending staging row with `promotion_evidence.discoveredAt`.
    - Test 3: inserted row has `replay_timestamp` null while `promotion_evidence->>'discoveredAt'` equals `"2026-05-09T00:32:44.000Z"`.
    - Test 4: a repeated insert of the same payload returns `already_staged`.
  </behavior>
  <action>Create `src/staging/postgres-staging-repository.integration.test.ts`. Use `PostgreSqlContainer("postgres:17-alpine")` with database `solid_stats`, username `solid`, and password `solid`. Use `pg` `Pool` with the container connection URI. Apply the minimal staging schema needed by this repo: `create extension if not exists pgcrypto;`, create enum `ingest_status` with values `'pending'`, `'processing'`, `'promoted'`, `'conflict'`, `'failed'`, and create `ingest_staging_records` with the columns and unique constraints from server-2 migration `0001_v1_domain_schema.sql`. Run `checkPostgresConnectivity({ client: pool })` and assert passed. Create a payload containing `promotionEvidence.discoveredAt: "2026-05-09T00:32:44.000Z"` and no `replayTimestamp`; stage it through `createPostgresStagingRepository(pool)`, query the row, assert `replay_timestamp is null`, assert the promotion evidence JSON includes `discoveredAt`, then stage the same payload again and assert `status: "already_staged"`. Do not create `replays`, `parse_jobs`, `parse_results`, stats, identity, role, request, or moderation tables.</action>
  <verify>
    <automated>pnpm run test:integration -- src/staging/postgres-staging-repository.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PostgreSqlContainer|checkPostgresConnectivity|createPostgresStagingRepository|ingest_staging_records" src/staging/postgres-staging-repository.integration.test.ts` finds the required integration paths.
    - `rg -n "replay_timestamp.*null|discoveredAt|already_staged" src/staging/postgres-staging-repository.integration.test.ts` finds timestamp and idempotency assertions.
    - `rg -n "create table (replays|parse_jobs|parse_results)|insert\\s+into\\s+(replays|parse_jobs|parse_results)" src/staging/postgres-staging-repository.integration.test.ts` returns no matches.
    - `rg -n "test\\.skip|describe\\.skip|SKIP|docker.*unavailable" src/staging/postgres-staging-repository.integration.test.ts` returns no matches.
    - `pnpm run test:integration -- src/staging/postgres-staging-repository.integration.test.ts` passes when Docker is available and fails if Docker cannot start the container.
  </acceptance_criteria>
  <done>PostgreSQL integration proves real staging insert/idempotency, read-only check behavior, discovered evidence JSON, and null replay timestamp semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest -> Docker daemon | Test runtime starts disposable external services. |
| Fetcher adapter -> MinIO | Real S3-compatible object writes occur in isolated local container. |
| Fetcher repository -> PostgreSQL | Real staging SQL executes in isolated local container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | `src/storage/s3-raw-storage.integration.test.ts` | mitigate | Use disposable MinIO bucket; read-only check uses `checkS3Connectivity`; no probe object writes. |
| T-06-19 | Tampering | `src/staging/postgres-staging-repository.integration.test.ts` | mitigate | Create only staging schema objects needed for tests; no server-2 business tables. |
| T-06-20 | Information Disclosure | Integration configs | mitigate | Use test-only credentials `solid`/`solidsecret`; do not log production env vars. |
| T-06-21 | Denial of Service | `pnpm run test:integration` | accept | Docker startup can be slower; command is intentionally blocking per D-16/D-17 and fails when Docker is unavailable. |
| T-06-22 | Repudiation | `package.json` verify gate | mitigate | Include `pnpm run test:integration` in `verify` so container evidence cannot be silently omitted. |
</threat_model>

<verification>
Run `pnpm run test:integration`, then `pnpm test`, then `pnpm run verify`. Docker absence must fail `pnpm run test:integration`; do not convert that failure to a skip.
</verification>

<success_criteria>
- `@testcontainers/postgresql` and `@testcontainers/minio` are dev dependencies.
- `pnpm run test:integration` runs only integration tests through the direct `src/**/*.integration.test.ts` file pattern.
- `pnpm run verify` includes `pnpm run test:integration`.
- MinIO and PostgreSQL integration tests pass with Docker and are not skipped when Docker is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-05-SUMMARY.md`
</output>
