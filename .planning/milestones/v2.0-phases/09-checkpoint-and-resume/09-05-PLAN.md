---
phase: 09-checkpoint-and-resume
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - src/run/run-once.ts
  - src/run/run-once.test.ts
  - src/cli.ts
  - src/cli.test.ts
autonomous: true
requirements: [RESUME-03, RESUME-04, RESUME-05]
must_haves:
  truths:
    - "run-once reads the checkpoint at start and begins the page loop at lastCompletedPage + 1; a missing or corrupt checkpoint logs a warning and starts at page 1 without aborting."
    - "After each completed page (never mid-page) run-once writes the checkpoint; a transient (non-412) checkpoint-write S3 error is logged and the run continues."
    - "Auto-resume triggers when a non-complete checkpoint exists for the source; an explicit --resume flag forces the resume read; --resume on a complete checkpoint starts a clean page-1 run (09-RESEARCH Open Questions Q2 RESOLVED)."
    - "Every staged row carries the same run's identity stamped as the run_id jsonb key in promotion_evidence; the final summary reports status and, when resumable, the exact --resume invocation and exit code 2."
  artifacts:
    - path: "src/run/run-once.ts"
      provides: "checkpoint read (resume start), write-after-page, final status, run_id stamped into staging"
      contains: "resumeStartPage"
    - path: "src/cli.ts"
      provides: "--resume flag, checkpoint-store DI, runId threaded to checkpoint + staging"
      contains: "--resume"
  key_links:
    - from: "src/run/run-once.ts"
      to: "src/checkpoint/s3-checkpoint-store.ts + checkpoint.ts"
      via: "read at start (resumeStartPage), write after each page"
      pattern: "checkpointStore"
    - from: "src/run/run-once.ts"
      to: "src/staging/payload.ts (run_id jsonb key)"
      via: "stageRawReplay receives input.runId → promotion_evidence.run_id"
      pattern: "runId"
    - from: "src/cli.ts"
      to: "createS3CheckpointStoreFromConfig"
      via: "DI map + createStoreRawResources"
      pattern: "createS3CheckpointStoreFromConfig"
---

<objective>
Wire the checkpoint store, resume cursor, run-status, and `run_id`-staging into the live run: `run-once.ts` reads the checkpoint at start (resume at `lastCompletedPage + 1`), writes the checkpoint after each completed page (never mid-page; transient write error → log+continue), threads the run identity into staging (stamped as the `run_id` jsonb key), and emits the final `status`/`resumeInvocation`; `cli.ts` adds the `--resume` flag, the checkpoint-store DI, and threads the single `runId` (camelCase TS variable) into both checkpoint and staging.

Purpose: the integration layer that delivers the phase goal — RESUME-03 (resume cursor + degrade), RESUME-04 (run identity into `promotion_evidence.run_id` at the call site), RESUME-05 (status + exit 2 + next-step). Builds on Plans 01-04.
Output: modified `src/run/run-once.ts` + `src/cli.ts` (with tests).
</objective>

<execution_context>
@$HOME/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@$HOME/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-checkpoint-and-resume/09-CONTEXT.md
@.planning/phases/09-checkpoint-and-resume/09-RESEARCH.md
@.planning/phases/09-checkpoint-and-resume/09-PATTERNS.md
@src/checkpoint/checkpoint.ts
@src/checkpoint/s3-checkpoint-store.ts
@src/run/summary.ts
@src/staging/payload.ts
</context>

<artifacts_this_phase_produces>
Plan 05 produces (the wiring; the pieces come from Plans 01-04):
- **resume start + write-after-page** — `src/run/run-once.ts` (checkpoint read → `resumeStartPage`; checkpoint write after each completed page; final `deriveRunStatus` + `resumeInvocation`).
- **run identity into staging at call site** — `run-once.ts` passes `input.runId` (camelCase TS variable) into `stageRawReplay` so the `run_id` jsonb key is stamped into `promotion_evidence` (RESUME-04 end-to-end; the snake_case jsonb key string itself lives in Plan 02's `payload.ts`).
- **--resume flag + checkpoint DI** — `src/cli.ts` (`.option("--resume", ...)`, `createS3CheckpointStoreFromConfig` in the DI map + `createStoreRawResources`, single `runId` threaded to checkpoint + staging).
- **--resume on a complete checkpoint → clean page-1** — auto-resume skips a `complete` checkpoint; an explicit `--resume` on a `complete` checkpoint starts a fresh full run from page 1 (09-RESEARCH Open Questions Q2 RESOLVED).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run-once — resume start, write-after-page, run_id staging, final status</name>
  <files>src/run/run-once.ts, src/run/run-once.test.ts</files>
  <read_first>
    - src/run/run-once.ts (RunOnceInput lines 17-45; page loop lines 62-115; per-candidate stage push line 98; summary assembly lines 102-109; `no-await-in-loop` disables lines 72/84/93)
    - src/checkpoint/checkpoint.ts (resumeStartPage, Checkpoint)
    - src/checkpoint/s3-checkpoint-store.ts (S3CheckpointStore.read/write)
    - src/run/summary.ts (buildRunSummary with deriveRunStatus + status/resumeInvocation from Plan 03)
    - src/staging/payload.ts (`toIngestStagingPayload` `runId` option from Plan 02 — the TS variable is camelCase `runId`; it stamps the snake_case `run_id` jsonb key)
    - 09-PATTERNS.md section "src/run/run-once.ts (MODIFY — orchestrator)" and "Never checkpoint mid-page"
    - 09-RESEARCH Open Questions (RESOLVED) Q2 — `--resume` on a complete checkpoint → clean page-1
  </read_first>
  <behavior>
    - Test: with a checkpoint whose lastCompletedPage=2 (status not complete), run-once starts the page loop at page 3 (resumeStartPage); completed pages 1-2 are NOT re-discovered (assert discoverReplays not called for p1/p2).
    - Test: with no checkpoint (store.read → {}) the loop starts at page 1.
    - Test: with a corrupt checkpoint (store.read → {} because parseCheckpoint returned undefined) the loop starts at page 1 and a warning is logged (injected logger), no throw.
    - Test: with a complete checkpoint and an explicit `--resume`/`resume: true`, run-once starts a clean page-1 run (Q2 RESOLVED); auto-resume (no flag) on a complete checkpoint also starts page 1 (does not resume).
    - Test: after each completed page, `checkpointStore.write` is called exactly once per page with lastCompletedPage = that page (assert call count == pages processed; assert NOT called mid-page — i.e. only after the per-candidate loop).
    - Test: a transient (non-412) checkpoint-write rejection is caught, logged, and the run CONTINUES (next page still processed; summary still produced).
    - Test: `stageRawReplay` receives `runId` so the staged payload's `promotion_evidence.run_id` carries it (assert the run identity threads through into the `run_id` jsonb key).
    - Test: the final summary's `status` comes from `deriveRunStatus` and a resumable run includes `resumeInvocation` + exit 2.
  </behavior>
  <action>
    Extend `RunOnceInput` (run-once.ts:17-45) with `readonly checkpointStore: S3CheckpointStore;`, optional `readonly resume?: boolean;`, and `readonly log?: Logger;` (the runId child logger — for the corrupt-degrade warning and checkpoint-write-error warning; follow the injected-function-dep style of `discoverReplays`/`stageRawReplay`). Before the loop (run-once.ts:67-69): read the checkpoint via `await input.checkpointStore.read(slug)` where `slug` is derived from the source URL (reuse the same derivation the store uses — call `toCheckpointObjectKey`'s slug logic, or have the store expose the slug; keep it consistent with Plan 04). Resume rule: resume when `input.resume === true` OR the read checkpoint exists with `status !== "complete"`; compute `startPage = resumeStartPage(checkpoint)` for the resume case, else 1. A `complete` checkpoint is NOT auto-resumed; an explicit `--resume` on a `complete` checkpoint starts a clean page-1 run (09-RESEARCH Open Questions Q2 RESOLVED — `complete` means the corpus was traversed, so `--resume` means "run the full corpus again from page 1"). On read returning no checkpoint OR a corrupt one (undefined), log a warning via `input.log?.warn` and start at page 1 — NEVER throw (RESUME-03, pitfall #1). Replace `for (let page = 1 ...)` with `for (let page = startPage ...)`. After a page's per-candidate loop COMPLETES (after run-once.ts:99, NOT inside the candidate loop — never mid-page, 09-CONTEXT specifics / pitfall #4), build the updated `Checkpoint` (lastCompletedPage=page, per-page counts, aggregate counts, lastSourceFailure from the page report, status `running`) and `await input.checkpointStore.write({ slug, checkpoint, etag })`; wrap the write in try/catch — on a non-precondition S3 error log via `input.log?.warn` and CONTINUE (transient-write-does-not-fail-run, 09-CONTEXT specifics / pitfall #5); a 412/409 is already handled inside the store (merge), so only truly transient errors reach here. Add the new `await` to the existing `no-await-in-loop` disable group. Thread `runId` into the staging call: change the `stageRawReplay` invocation (run-once.ts:94-97) so the payload builder receives `input.runId` (via `toIngestStagingPayload`'s `runId` option from Plan 02 — the camelCase variable stamps the `run_id` jsonb key; wire it through the `stageRawReplay` input or the payload options as that seam requires; RESUME-04 end-to-end). At summary assembly (run-once.ts:102-109), pass `lastCompletedPage`, `discoveredLastPage`, the derived `status` (from `deriveRunStatus`, Plan 03), and the exact `resumeInvocation` (e.g. `replays-fetcher run-once --resume`) when the status is partial/resumable. On a complete run, write a final checkpoint with `status: "complete"` so a subsequent run does NOT auto-resume. No fenced code; no `any`/`as`; no magic numbers (name the page-one sentinel and any status literals via the Plan 01/03 unions).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/run/run-once.test.ts</automated>
  </verify>
  <done>resume starts at lastCompletedPage+1; missing/corrupt → page 1 + warn (no throw); complete checkpoint + --resume → clean page-1 (Q2 RESOLVED); checkpoint written once per completed page, never mid-page; transient write error → log+continue; run identity threads into the `run_id` jsonb key in staging; final status/resumeInvocation/exit reflect deriveRunStatus; existing run-once toMatchObject assertions still pass; 100% reachable coverage on run-once changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cli — --resume flag, checkpoint-store DI, single runId to checkpoint + staging</name>
  <files>src/cli.ts, src/cli.test.ts</files>
  <read_first>
    - src/cli.ts (BuildCliDependencies lines 76-98; resolveDependencies lines 146-169; registerRunOnceCommand lines 317-376; createStoreRawResources lines 511-526; commander `.option("--dry-run", ...)` pattern lines 240-243; createRunId line 326; child logger line 328; writeJson stdout contract line 373)
    - src/checkpoint/s3-checkpoint-store.ts (createS3CheckpointStoreFromConfig)
    - 09-PATTERNS.md section "src/cli.ts (MODIFY — controller/CLI)"
  </read_first>
  <behavior>
    - Test: `run-once --resume` sets `options.resume === true` and threads `resume: true` into `dependencies.runOnce({...})`.
    - Test: `run-once` without the flag threads `resume: false`/omitted (auto-resume still works via the store read in run-once).
    - Test: the DI map exposes `createS3CheckpointStoreFromConfig` and `createStoreRawResources` builds the checkpoint store alongside `storage` (assert via an injected fake).
    - Test: the SAME `runId` (`createRunId(startedAt)`, camelCase TS variable) is passed to run-once (which feeds both the checkpoint and the staging `run_id` jsonb key) — assert one run identity value flows to the checkpoint store input and the staging payload.
    - Test: the stdout JSON summary contract is intact — `writeJson(result.summary)` unchanged; cli.test.ts `toMatchObject` summary assertions still pass with the added status/resumeInvocation fields; logs remain on stderr.
    - Test: a resumable run sets `process.exitCode = 2` (from result.exitCode).
  </behavior>
  <action>
    Add `.option("--resume", "resume from the last completed page using the source checkpoint")` to `registerRunOnceCommand` (cli.ts:321-323), mirroring `.option("--dry-run", ...)` (cli.ts:240-243); read `options.resume === true` in the action and pass `resume: options.resume === true` into `dependencies.runOnce({...})` (cli.ts:355-371). Add `createS3CheckpointStoreFromConfig?: (config: AppConfig["s3"]) => S3CheckpointStore` to `BuildCliDependencies` (cli.ts:76-98) and to `resolveDependencies` defaults (cli.ts:149-168), mirroring `createS3RawReplayStorageFromConfig` (cli.ts:84-86, 156). Build the checkpoint store in `createStoreRawResources` (cli.ts:511-526) next to `storage`, and add it to the `StoreRawResources` interface (cli.ts:123-128); pass `resources.checkpointStore` into `dependencies.runOnce({...})`. The `runId` is already `createRunId(startedAt)` (cli.ts:326, camelCase TS variable) and the child logger `log` already exists (cli.ts:328) — pass BOTH the same `runId` and `log` into `runOnce` so the checkpoint and the staging `run_id` jsonb key share one run identity (RESUME-04) and the corrupt/transient warnings log to stderr. Keep `writeJson(result.summary)` (cli.ts:373) and `process.exitCode = result.exitCode` (cli.ts:374) unchanged — the stdout contract is preserved; status/resumeInvocation are additive summary fields (CR-01). No fenced code; no `any`/`as`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/cli.test.ts</automated>
  </verify>
  <done>--resume flag threads through; checkpoint-store DI wired and built in createStoreRawResources; one runId reaches both checkpoint and the staging `run_id` jsonb key; stdout summary contract intact (toMatchObject still passes); resumable run exits 2; cli tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end verify gate — full resume cycle + boundary + contract</name>
  <files>src/run/run-once.test.ts, src/cli.test.ts</files>
  <read_first>
    - 09-RESEARCH.md "Validation Architecture → Required validations" (the full unit/contract checklist)
    - src/cli.test.ts and src/run/run-once.test.ts (existing toMatchObject contract assertions to preserve)
  </read_first>
  <behavior>
    - Test: full resume cycle — first run completes pages 1-2 then stops resumable (writes checkpoint lastCompletedPage=2, status running/resumable); a second run with the same source + fake store auto-resumes at page 3 and does not re-discover pages 1-2.
    - Test: boundary guard — run-once writes ONLY the checkpoint S3 object + raw objects + staging rows; it does NOT call any server-2 business-table path (mirror the v1 dry-run no-mutation guard style already in the suite).
    - Test: no-leak — the persisted checkpoint object (captured from the fake store) and the summary contain no secrets/bytes/HTML (allowlist assertion).
  </behavior>
  <action>
    Add the end-to-end resume-cycle and contract assertions that span run-once + cli using the existing fake-injection test style (no real S3/PG — use an in-memory fake checkpoint store and the existing fakes for discover/store/stage). Assert: (1) the two-run resume cycle skips completed pages; (2) the boundary holds — no server-2 business-table mutation, only checkpoint + raw + staging surfaces touched (assert the fakes for any server-2 path are never invoked, mirroring the v1 dry-run no-mutation guards); (3) the captured checkpoint payload and the stdout summary carry identifiers only (stringify + assert no body/secret substrings — T-09-01). These tests are the phase's nyquist gate for the resume cursor, boundary, and no-leak criteria. No `any`/`as`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/run/run-once.test.ts src/cli.test.ts</automated>
  </verify>
  <done>full resume cycle skips completed pages across two runs; boundary guard proves checkpoint+raw+staging only (no server-2 business-table writes); no-leak assertion passes; all run-once + cli tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI (--resume) → run | Flag is operator-supplied; combined with auto-resume it controls where the loop starts (and forces a clean page-1 on a complete checkpoint). |
| run-once → S3 checkpoint write | The only new write surface; must stay identifiers-only and never abort the run on transient error. |
| run-once → server-2 staging | RESUME-04 run identity stamped via the existing `promotion_evidence.run_id` jsonb key only; no business-table write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | checkpoint write payload + summary | mitigate | run-once writes only the identifiers-only `Checkpoint` (Plan 01 allowlist) and the additive `status`/`resumeInvocation` summary fields; no bytes/secrets/HTML. End-to-end no-leak test asserts it (ASVS L2 V8.3). |
| T-09-02 | Tampering | corrupt/missing checkpoint at resume | mitigate | run-once degrades to page-1 + a stderr warning on undefined checkpoint; idempotent raw/staging writes (Phase 3 HEAD-before-PUT, Phase 4 already_staged) are the correctness floor so a re-read page never double-creates (RESUME-03, pitfall #6). |
| T-09-10 | Denial of Service | a transient checkpoint-write S3 error aborting a long run | mitigate | The checkpoint write is wrapped in try/catch — a non-412 error is logged and the run continues (checkpoint is an optimization, not correctness — 09-CONTEXT specifics); the run is never failed by a checkpoint-write hiccup. |
| T-09-03 | Tampering | server-2 staging contract | mitigate | The run identity is threaded only into the existing `promotion_evidence.run_id` jsonb key (Plan 02); the boundary guard test proves no server-2 business-table path is called (mirrors v1 dry-run no-mutation guards, GUARD-pattern). Cross-app verified: server-2 has no existing run-id reader (09-RESEARCH Open Questions RESOLVED), so `run_id` safely sets the convention; ASVS L2 V1.11. |
| T-09-06 | Tampering | stdout JSON summary contract | mitigate | `writeJson(result.summary)` and `process.exitCode` unchanged; status/resumeInvocation additive; cli.test.ts `toMatchObject` re-asserted; logs stay on stderr (CR-01). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (wiring only; all deps present per 09-RESEARCH). |
</threat_model>

<verification>
- `pnpm exec vitest run src/run/run-once.test.ts src/cli.test.ts` green.
- `pnpm run verify` green (typecheck + unit + integration + 100% coverage + build) — note the pre-existing pnpm-lock.yaml / `.agents/**` aggregate-gate failures logged in 07 deferred-items are out of scope; each stage individually must be green.
- Resume cursor, boundary (no server-2 business-table write), and no-leak are covered by automated tests (nyquist gate).
- Cross-app: VERIFIED, no human gate. The run identity is stamped as `promotion_evidence.run_id` (Plan 02); server-2 currently has no run-id reader and a future correlation will read `promotion_evidence->>'run_id'` (09-RESEARCH Open Questions RESOLVED). Real-Postgres persistence is proven by Plan 02's integration test.
</verification>

<success_criteria>
- A restarted run resumes at `lastCompletedPage + 1`; missing/corrupt → page-1 start + warn, never abort (RESUME-03); a complete checkpoint + `--resume` → clean page-1 (Q2 RESOLVED).
- Every staged row carries the run's identity in `promotion_evidence.run_id` (RESUME-04).
- Final summary reports complete/partial/failed/resumable with the exact `--resume` next-step and exit 2 for resumable (RESUME-05).
- Checkpoint written after each completed page, never mid-page; transient write error → log+continue.
- Boundary intact: only checkpoint + raw + staging surfaces; no server-2 business-table writes.
</success_criteria>

<output>
Create `.planning/phases/09-checkpoint-and-resume/09-05-SUMMARY.md` when done.
</output>
