---
phase: 09-checkpoint-and-resume
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/run/types.ts
  - src/run/summary.ts
  - src/run/summary.test.ts
autonomous: true
requirements: [RESUME-05]
must_haves:
  truths:
    - "The final run summary reports a status of complete, partial, failed, or resumable."
    - "A partial or resumable run carries the exact --resume invocation as resumeInvocation and maps to exit code 2."
    - "A complete run maps to exit code 0; status and resumeInvocation are additive fields that do not break existing toMatchObject summary assertions."
  artifacts:
    - path: "src/run/types.ts"
      provides: "RunStatus union and RunSummary.status/resumeInvocation additive fields"
      contains: "RunStatus"
    - path: "src/run/summary.ts"
      provides: "deriveRunStatus + resumeInvocation wiring into buildRunSummary; exit-code mapping"
      contains: "deriveRunStatus"
  key_links:
    - from: "src/run/summary.ts (deriveRunStatus)"
      to: "RunExitCode (runExitCode)"
      via: "partial/resumable → ok:false → exit 2"
      pattern: "runExitCode"
---

<objective>
Add the run status taxonomy (`complete` / `partial` / `failed` / `resumable`), the `resumeInvocation` next-step string, and their exit-code mapping to the run summary — as additive fields that do not reshape the existing stdout summary contract. This module only touches `src/run/types.ts` and `src/run/summary.ts`, disjoint from the other Wave 1 plans; run-once consumes it in Plan 05.

Purpose: RESUME-05 — the summary states the run outcome and (when resumable) the exact `--resume` command; partial/resumable exits with code 2 so the scheduler retries.
Output: `RunStatus` union + `RunSummary.status?`/`resumeInvocation?` (types.ts), `deriveRunStatus` + summary wiring (summary.ts).
</objective>

<execution_context>
@$HOME/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@$HOME/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-checkpoint-and-resume/09-CONTEXT.md
@.planning/phases/09-checkpoint-and-resume/09-RESEARCH.md
@.planning/phases/09-checkpoint-and-resume/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Plan 03 produces:
- **run status field** — `RunSummary.status` (`RunStatus = "complete" | "failed" | "partial" | "resumable"`) in `src/run/types.ts`.
- **resume next-step** — `RunSummary.resumeInvocation?` (exact `--resume` command) + `deriveRunStatus`/status-aware exit mapping in `src/run/summary.ts`.
Plan 05 wires `deriveRunStatus` and the resume invocation into `run-once.ts` using the live checkpoint + page-loop outcome.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunStatus union and additive RunSummary fields</name>
  <files>src/run/types.ts, src/run/summary.test.ts</files>
  <read_first>
    - src/run/types.ts (`RunSummary` lines 44-58 — additive `readonly` field style; `RunExitCode = 0 | 2` line 71; `RunSourceFailure` lines 25-30)
    - 09-PATTERNS.md section "src/run/summary.ts + src/run/types.ts (MODIFY — transform/model)"
  </read_first>
  <behavior>
    - Test (type-level via a constructed object in a test): a `RunSummary` with `status: "resumable"` and `resumeInvocation: "replays-fetcher run-once --resume"` type-checks.
    - Test: a summary WITHOUT `status`/`resumeInvocation` still type-checks (both optional → backward compatible).
  </behavior>
  <action>
    Add `export type RunStatus = "complete" | "failed" | "partial" | "resumable";` to types.ts. Add two optional fields to `RunSummary` (types.ts:44-58): `readonly status?: RunStatus;` and `readonly resumeInvocation?: string;` — both optional and additive so existing `toMatchObject` assertions (cli.test.ts:364, run-once.test.ts:115) do not regress (09-CONTEXT line 40, CR-01). Do NOT change `RunExitCode` (stays `0 | 2`). Keep alphabetical/readonly ordering consistent with the existing interface. No `any`/`as`.
  </action>
  <verify>
    <automated>pnpm run typecheck</automated>
  </verify>
  <done>RunStatus exported; RunSummary has optional status + resumeInvocation; RunExitCode unchanged; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveRunStatus, resumeInvocation, and status-aware exit mapping</name>
  <files>src/run/summary.ts, src/run/summary.test.ts</files>
  <read_first>
    - src/run/summary.ts (`buildRunSummary` lines 46-75 — conditional-spread of `sourceFailure` lines 68-74; `deriveSourceFailure` lines 102-127 as the analog for `deriveRunStatus`; `runExitCode` lines 164-170)
    - 09-PATTERNS.md "Status derivation" and "Exit code 2" notes
  </read_first>
  <behavior>
    - Test: `deriveRunStatus` returns `complete` when the run is ok AND all discovered pages completed (lastCompletedPage >= discoveredLastPage).
    - Test: returns `resumable`/`partial` when at least one page completed but the run stopped early on a transient/rate_limited source failure (recoverable).
    - Test: returns `failed` when nothing is salvageable (no page completed and a permanent failure, or config-invalid path is out of scope here).
    - Test: `buildRunSummary` with a resumable input includes `status: "resumable"` and a `resumeInvocation` string ending in `--resume`; existing summary fields are unchanged (toMatchObject still passes).
    - Test: `runExitCode` returns 2 for a resumable/partial summary (ok:false) and 0 for complete (ok:true).
  </behavior>
  <action>
    Add a pure `deriveRunStatus(input)` to summary.ts modeled on `deriveSourceFailure` (summary.ts:102-127). It takes the discovery ok-ness, the source-failure classification (transient/rate_limited/permanent from `deriveSourceFailure`), `lastCompletedPage`, and `discoveredLastPage`, and returns a `RunStatus`: `complete` when ok and `lastCompletedPage >= discoveredLastPage`; `resumable` (treat as the partial-but-recoverable case) when `lastCompletedPage >= 1` and the stop cause was transient/rate_limited; `partial` for a non-recoverable mid-run stop that still completed at least one page; `failed` when no page completed and the failure is permanent (nothing salvageable). Extend `BuildRunSummaryInput` with optional `lastCompletedPage?`/`discoveredLastPage?`/`status?`/`resumeInvocation?` so run-once (Plan 05) can pass the live checkpoint cursor and the exact command; when `status` is supplied, spread it into the summary using the SAME conditional-spread pattern as `sourceFailure` (summary.ts:68-74) so the contract stays additive. Make `runExitCode` honor status: a summary with `status` in {`partial`, `resumable`, `failed`} must yield `ok:false` → exit 2, while `complete` yields exit 0 (reuse the Phase 5 exit-2 convention; `RunExitCode` type unchanged). The `resumeInvocation` string is the exact operator next-step (e.g. `replays-fetcher run-once --resume`); it is supplied by the caller and spread additively (RESUME-05). No fenced code; no `any`/`as`; no magic numbers (name the page-one sentinel).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/run/summary.test.ts</automated>
  </verify>
  <done>deriveRunStatus maps the four states; buildRunSummary spreads status/resumeInvocation additively; runExitCode maps partial/resumable/failed → 2 and complete → 0; existing summary assertions still pass; 100% reachable coverage on summary.ts changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary → stdout (scheduler/operator) | The JSON summary is a machine-read contract; status/resumeInvocation must not leak secrets and must stay additive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | stdout summary contract | mitigate | `status`/`resumeInvocation` added via conditional spread (analog: `sourceFailure`); existing `toMatchObject` assertions unchanged. Test re-asserts the prior contract still matches (ASVS L2 V13.1 stable output contract). |
| T-09-07 | Information Disclosure | resumeInvocation string | mitigate | The `--resume` command carries no secret (it is a flag, not credentials); the source URL already appears in the summary. Test asserts resumeInvocation contains only the command + flag, no env/secret. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (run/summary edits only). |
</threat_model>

<verification>
- `pnpm exec vitest run src/run/summary.test.ts` green; `pnpm run typecheck` clean.
- Existing cli.test.ts / run-once.test.ts `toMatchObject` summary assertions remain valid (additive-only) — confirmed by running `pnpm exec vitest run src/run src/cli.test.ts` if touched.
</verification>

<success_criteria>
- `deriveRunStatus` returns complete/partial/failed/resumable per the recoverability rules (RESUME-05).
- partial/resumable → exit 2, complete → exit 0.
- status + resumeInvocation are additive; stdout contract intact.
</success_criteria>

<output>
Create `.planning/phases/09-checkpoint-and-resume/09-03-SUMMARY.md` when done.
</output>
