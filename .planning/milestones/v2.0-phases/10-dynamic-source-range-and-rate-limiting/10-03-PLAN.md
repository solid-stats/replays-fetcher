---
phase: 10-dynamic-source-range-and-rate-limiting
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/source/concurrency.ts
  - src/source/concurrency.test.ts
  - src/source/throttle.ts
  - src/source/throttle.test.ts
autonomous: true
requirements: [RANGE-02, RANGE-03]
user_setup: []
must_haves:
  truths:
    - "p-limit@^7.3.0 is installed; createLimiter(concurrency) returns a p-limit limiter whose .concurrency is runtime-settable"
    - "onRateLimited halves effective concurrency (floor 1) AND raises the pacing floor by a bounded step (AIMD multiplicative decrease)"
    - "onCleanWindow, after a sustained clean window, increases effective concurrency by +1 toward max and never above max (AIMD additive increase)"
    - "The throttle reduces concurrency ONLY — it adds NO backoff (backoff stays in withRetry); pacingFloorMs is bounded"
    - "All AIMD branches (MD, AI, floor-at-1, cap-at-max, steady/no-change) are covered for the 100% V8 gate"
  artifacts:
    - path: "src/source/throttle.ts"
      provides: "createThrottleController({ now, min, max, baseConcurrency, baseSpacingMs }) → AIMD state machine"
      exports: ["createThrottleController"]
      min_lines: 30
    - path: "src/source/concurrency.ts"
      provides: "createLimiter(concurrency) thin p-limit wrapper seam"
      exports: ["createLimiter"]
    - path: "src/source/throttle.test.ts"
      provides: "parameterized AIMD branch table over injected clock"
      contains: "createThrottleController"
  key_links:
    - from: "src/source/throttle.ts onRateLimited/onCleanWindow"
      to: "effectiveConcurrency / pacingFloorMs readonly getters"
      via: "AIMD MD/AI math over injected clock"
      pattern: "effectiveConcurrency"
    - from: "src/source/concurrency.ts createLimiter"
      to: "p-limit default export"
      via: "import pLimit from \"p-limit\""
      pattern: "p-limit"
---

<objective>
Add the `p-limit` dependency, a thin `createLimiter` seam, and the pure AIMD `ThrottleController`
(RANGE-02 concurrency primitive + RANGE-03 adaptive throttle). The controller is a pure state machine
over an injected clock that halves concurrency on repeated `rate_limited` signals and additively
recovers on a clean window — the lever Wave 2 applies via `limit.concurrency = n`.

Purpose: A single shared limiter is the global in-flight governor; AIMD lets a source hiccup shrink it
globally without a retry storm (the throttle touches concurrency only — backoff stays in `withRetry`).
Output: `p-limit@^7.3.0` installed; `src/source/concurrency.ts` + `src/source/throttle.ts` with
colocated tests at 100% V8 coverage.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dynamic-source-range-and-rate-limiting/10-RESEARCH.md
@.planning/phases/10-dynamic-source-range-and-rate-limiting/10-PATTERNS.md
@src/source/backoff.ts
@src/source/classify-failure.ts
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase ledger:
- New dependency `p-limit@^7.3.0` (default import `import pLimit from "p-limit";`, ESM-only, no `.js` suffix on the bare specifier); transitive `yocto-queue` + `pnpm-lock.yaml` update.
- New file `src/source/concurrency.ts` exporting `createLimiter(concurrency: number): LimitFunction` (re-export the p-limit `LimitFunction` type for callers).
- New file `src/source/throttle.ts` exporting `createThrottleController(options): ThrottleController` with `readonly effectiveConcurrency: number`, `readonly pacingFloorMs: number`, `onRateLimited(nowMs: number): void`, `onCleanWindow(nowMs: number): void`.
- Named UPPER constants in `throttle.ts`: `MD_FACTOR` (0.5), `AI_STEP` (1), `CONCURRENCY_FLOOR` (1), the clean/rate-limited window thresholds, and the pacing-floor step (all named — `no-magic-numbers`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install p-limit and add the createLimiter seam</name>
  <files>package.json, pnpm-lock.yaml, src/source/concurrency.ts, src/source/concurrency.test.ts</files>
  <read_first>
    - .planning/phases/10-dynamic-source-range-and-rate-limiting/10-RESEARCH.md (§Standard Stack + §Package Legitimacy Audit — p-limit 7.3.0 verified OK, no postinstall, single trusted dep; §Pitfall 8 default-import shape under NodeNext)
    - package.json (existing dependency block + `"type": "module"` + `packageManager: pnpm@11.0.9`)
    - src/source/backoff.test.ts (colocated-test analog for the seam test)
  </read_first>
  <action>
    Run `pnpm add p-limit` (installs `p-limit@^7.3.0` + transitive `yocto-queue`, updates `pnpm-lock.yaml`). Create `src/source/concurrency.ts` exporting `createLimiter(concurrency: number)` that returns `pLimit(concurrency)` via `import pLimit from "p-limit";` (default import, bare specifier — NO `.js` suffix per Pitfall 8); re-export the `LimitFunction` type from p-limit for downstream typing. Create `src/source/concurrency.test.ts` asserting: (a) `createLimiter(2)` runs limited tasks and `.concurrency` reads back `2`; (b) setting `limit.concurrency = 1` at runtime is observable on a subsequent read (the AIMD lever). This is the package-legitimacy-gated install: the RESEARCH `## Package Legitimacy Audit` table already verifies `p-limit`/`yocto-queue` as `OK` (no `[ASSUMED]`/`[SUS]`/`[SLOP]`), so no blocking-human checkpoint is required.
  </action>
  <verify>
    <automated>pnpm test src/source/concurrency.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>`p-limit` in `package.json` dependencies; `pnpm-lock.yaml` updated; `createLimiter` returns a limiter with a runtime-settable `.concurrency`; seam test green.</done>
</task>

<task type="tdd">
  <name>Task 2: RED — throttle.test.ts asserts the AIMD branch table</name>
  <files>src/source/throttle.test.ts</files>
  <read_first>
    - src/source/backoff.test.ts (lines 1-55 — the exact analog: ESM `.js` import, `Number("...")` literals, parameterized branch table, injected clock)
    - .planning/phases/10-dynamic-source-range-and-rate-limiting/10-PATTERNS.md (§`src/source/throttle.ts (NEW)` — bounded-math + named-constant + injectable-clock patterns from `backoff.ts`)
    - .planning/phases/10-dynamic-source-range-and-rate-limiting/10-RESEARCH.md (§AIMD throttle example; §Open Questions Q3 — page-count windows for deterministic testing; Pitfall 4 coverage matrix; Pitfall 2 no-double-delay)
  </read_first>
  <behavior>
    - MD: starting at `effectiveConcurrency = 8`, after enough `onRateLimited` signals to trip the window, `effectiveConcurrency` becomes `4` (halve) and `pacingFloorMs` increases by the named step.
    - Repeated MD floors at `1`: successive trips drive `8→4→2→1→1` (never below `CONCURRENCY_FLOOR`).
    - AI: after a sustained clean window of `onCleanWindow` calls, `effectiveConcurrency` increases by `+1` toward `max`.
    - Cap at max: AI never raises `effectiveConcurrency` above the configured `max`.
    - Steady/no-change: a single `rate_limited` signal below the window threshold, or a clean call below the clean-window threshold, leaves `effectiveConcurrency` unchanged (no-op branch).
    - No double-delay: the controller exposes ONLY `effectiveConcurrency` + `pacingFloorMs`; assert there is no method that adds retry/backoff delay (the throttle reduces concurrency + bumps the pacing floor; it must not duplicate `withRetry`).
  </behavior>
  <action>
    Write `src/source/throttle.test.ts` with `import { expect, test } from "vitest";` and `import { createThrottleController } from "./throttle.js";`. Drive a parameterized table over a sequence of `onRateLimited`/`onCleanWindow` calls with a scripted injected `nowMs`, asserting `effectiveConcurrency` and `pacingFloorMs` transitions for MD, AI, floor-at-1, cap-at-max, and steady/no-change. Wrap numeric literals as `Number("8")` etc. Cover every branch from `<behavior>` so the 100% V8 gate passes (Pitfall 4). RED: `throttle.ts` does not yet export `createThrottleController`, so the suite must fail to compile.
  </action>
  <verify>
    <automated>pnpm test src/source/throttle.test.ts || true</automated>
  </verify>
  <done>`throttle.test.ts` exists and fails (RED); the table encodes every AIMD branch + the no-double-delay invariant.</done>
</task>

<task type="tdd">
  <name>Task 3: GREEN — implement the pure AIMD ThrottleController</name>
  <files>src/source/throttle.ts</files>
  <read_first>
    - src/source/throttle.test.ts (the RED contract from Task 2)
    - src/source/backoff.ts (the pure-math analog: top JSDoc explaining the determinism seam, hoisted UPPER constants, injectable clock, `Math.min`/`Math.max` bounded-math)
    - src/source/classify-failure.ts (`FailureKind = "permanent" | "rate_limited" | "transient"` — the `rate_limited` signal this controller is fed by in Wave 2; the controller takes the already-classified signal, it does NOT re-detect 429/403)
  </read_first>
  <behavior>
    - Same transitions as Task 2's table: MD halve floor-1, AI +1 cap-max, pacing-floor step on MD, steady no-op below thresholds.
    - `effectiveConcurrency` and `pacingFloorMs` are readonly getters reflecting current state.
    - `onRateLimited(nowMs)`/`onCleanWindow(nowMs)` advance the window using the injected millisecond clock value (never `Date.now()` internally).
  </behavior>
  <action>
    Create `src/source/throttle.ts` with a top JSDoc (mirroring `backoff.ts`) stating: pure AIMD state machine, injected clock, reduces concurrency ONLY (backoff stays in `withRetry`; Pitfall 2). Export `createThrottleController(options: { baseConcurrency: number; min: number; max: number; baseSpacingMs: number; now?: () => number })` returning `{ get effectiveConcurrency, get pacingFloorMs, onRateLimited, onCleanWindow }`. Implement MD as `Math.max(CONCURRENCY_FLOOR, Math.floor(concurrency * MD_FACTOR))` and a pacing-floor bump by a named step; AI as `Math.min(max, concurrency + AI_STEP)`. Hoist `MD_FACTOR=0.5`, `AI_STEP=1`, `CONCURRENCY_FLOOR=1`, the rate-limited/clean window thresholds, and the pacing-floor step as named UPPER_SNAKE_CASE constants (the test asserts exact transitions, so pick concrete values and document them in the JSDoc — window definition is Claude's discretion per RESEARCH Q3; page-count windows are simplest to test). No `any`/`as`; exact-optional typing. Per RANGE-03.
  </action>
  <verify>
    <automated>pnpm test src/source/throttle.test.ts && pnpm run typecheck && pnpm run lint</automated>
  </verify>
  <done>`throttle.test.ts` GREEN; `createThrottleController` exports the AIMD seam with bounded MD/AI; 100% V8 coverage on `throttle.ts`; lint clean (named constants).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classifier signal → throttle controller | The already-classified `rate_limited` kind drives AIMD state |
| npm install | `p-limit`/`yocto-queue` packages cross the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Denial of Service (against the source) | retry-storm interaction throttle ↔ withRetry | mitigate | The throttle reduces effective concurrency only and raises the pacing floor; per-request backoff stays exclusively in `withRetry`. The two never compound (Pitfall 2), so a source hiccup shrinks the global in-flight cap instead of fanning out simultaneous retries. |
| T-10-07 | Denial of Service / resource exhaustion | unbounded concurrency growth on AI recovery | mitigate | AI is bounded by `Math.min(max, …)` with `max` sourced from the Zod-capped `sourceConcurrency` (≤32); MD floors at `CONCURRENCY_FLOOR=1`. State can never leave `[1, max]`. |
| T-10-SC | Tampering | `pnpm add p-limit` supply chain | mitigate | RESEARCH `## Package Legitimacy Audit` verifies `p-limit`/`yocto-queue` as `OK` (npm registry + legitimacy seam: no postinstall, not deprecated, ~273M wk downloads, sindresorhus). Both are `OK` (not `[ASSUMED]`/`[SUS]`/`[SLOP]`), so no blocking-human legitimacy checkpoint is required; lockfile pins the resolved versions. |
</threat_model>

<verification>
- `pnpm test src/source/throttle.test.ts src/source/concurrency.test.ts` green at 100% coverage.
- `pnpm run typecheck` + `pnpm run lint` clean (p-limit default import, named AIMD constants, no `any`/`as`).
- `p-limit` present in `package.json`; `pnpm-lock.yaml` committed.
</verification>

<success_criteria>
- `createLimiter` yields a shared limiter with a runtime-settable `.concurrency` (RANGE-02 primitive; the AIMD lever).
- `createThrottleController` is a pure AIMD machine: MD halve floor-1, AI +1 cap-max, pacing-floor bump, no added backoff (RANGE-03).
- Both new modules + the install are deterministically tested; ready for Wave 2 to thread into the run loop.
</success_criteria>

<output>
Create `.planning/phases/10-dynamic-source-range-and-rate-limiting/10-03-SUMMARY.md` when done.
</output>
