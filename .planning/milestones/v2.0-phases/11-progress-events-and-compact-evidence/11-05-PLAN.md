---
phase: 11-progress-events-and-compact-evidence
plan: 05
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - src/run/no-leak.test.ts
autonomous: true
requirements: [PROG-04]
must_haves:
  truths:
    - "No secret, raw replay byte, or HTML body string appears in any lifecycle event line, the compact stdout summary, or the evidence artifact payload"
    - "The full RunSummary serialized as the evidence body contains no S3/DB/SSH secret and no userinfo in sourceUrl"
    - "pnpm run verify is green (typecheck + unit + integration + 100% V8 coverage + build)"
  artifacts:
    - path: "src/run/no-leak.test.ts"
      provides: "the PROG-04 cross-surface no-secret/body/HTML leak assertion (mirrors the DIAG-04 no-body test)"
      contains: "no-leak"
  key_links:
    - from: "src/run/no-leak.test.ts"
      to: "src/run/run-once.ts + src/run/summary.ts + src/evidence/s3-evidence-store.ts"
      via: "drives a run with secret-bearing config + userinfo sourceUrl and asserts no marker leaks into events/compact/evidence"
      pattern: "no-leak"
---

<objective>
Add the PROG-04 cross-surface no-leak guarantee: a single test that drives a
run-once cycle with a deliberately secret-bearing config (S3 keys, DB url, SSH
command) and a `https://user:pass@host/...` sourceUrl, captures every lifecycle
NDJSON event line on the injected stderr sink, the compact stdout document, and
the serialized evidence-artifact body, and asserts that NONE of them contains any
secret marker, raw replay byte, or HTML body string (D-15, mirroring the existing
DIAG-04 no-body-leak test). Then run the full `pnpm run verify` gate so the phase
ships green.

Purpose: PROG-04 requires the events, summary, and evidence artifact to preserve
secret-/boundary-safety. The prior plans each assert their slice; this plan is the
single end-to-end assertion that the THREE output surfaces together never leak a
secret, byte, or HTML body — the phase-level safety net.
Output: `src/run/no-leak.test.ts` + a green `pnpm run verify`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-progress-events-and-compact-evidence/11-CONTEXT.md
@.planning/phases/11-progress-events-and-compact-evidence/11-RESEARCH.md
@.planning/phases/11-progress-events-and-compact-evidence/11-PATTERNS.md

# The surfaces under test + the existing no-body-leak analog to mirror:
@src/run/run-once.ts
@src/run/summary.ts
@src/logging/create-logger.ts

# Read the prior SUMMARYs for the final signatures of every surface:
@.planning/phases/11-progress-events-and-compact-evidence/11-01-SUMMARY.md
@.planning/phases/11-progress-events-and-compact-evidence/11-02-SUMMARY.md
@.planning/phases/11-progress-events-and-compact-evidence/11-03-SUMMARY.md
@.planning/phases/11-progress-events-and-compact-evidence/11-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- `src/run/no-leak.test.ts`: a new colocated test file (no production symbols)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the cross-surface no-leak test over events + compact summary + evidence payload</name>
  <files>src/run/no-leak.test.ts</files>
  <read_first>
    - src/run/run-once.ts (how to drive `runOnce` with injected fakes; the event
      emission sites; `sanitizeSourceUrl`; the evidence-store + writeEvidenceFile seams)
    - src/run/summary.ts (`buildRunSummary` / `toCompactSummary` — the compact + full bodies)
    - src/logging/create-logger.ts (inject a capturing `Writable` destination to
      collect the NDJSON event lines)
    - the existing DIAG-04 no-body-leak test (locate it via
      `grep -rln 'no-body\|no.*leak\|secret' src/**/*.test.ts` — mirror its
      assertion style: build the list of forbidden marker strings, stringify each
      surface, assert none of the markers appears)
    - src/checkpoint/s3-checkpoint-store.fixtures.ts and
      src/evidence/s3-evidence-store.fixtures.ts (reuse the capturing mock sender to
      capture the serialized evidence body)
  </read_first>
  <action>
    Create `src/run/no-leak.test.ts`. Arrange a run-once cycle through injected
    fakes: a config / sourceUrl carrying unmistakable secret markers — an S3
    `accessKeyId`/`secretAccessKey`, a `DATABASE_URL` with a password, an SSH
    command, and a `https://leak-user:leak-pass@sg.zone/replays` sourceUrl — plus a
    fake byteClient/discoverReplays returning a small candidate set so at least one
    `page_complete` and the `run_complete` event fire, and a capturing evidence
    store (mock sender records the PutObject Body). Inject a capturing `Writable`
    as the logger destination to collect every NDJSON event line. Drive the run with
    `--emit-evidence` semantics (emitEvidence: true + the capturing store) and an
    `--evidence-file`-style `writeEvidenceFile` fake that records its body. Define a
    `forbiddenMarkers` array of the secret literals + `"leak-user"` + `"leak-pass"`
    + a representative raw-byte/HTML marker (e.g. `"<html"`). Assert that for the
    concatenation of (a) all captured event lines, (b) `JSON.stringify` of the
    compact stdout summary (`toCompactSummary(result.summary)`), and (c) the
    captured evidence body, NONE of the `forbiddenMarkers` appears (mirror the
    DIAG-04 assertion). Additionally assert the sourceUrl that DOES appear in the
    events/compact/evidence is the userinfo-stripped slug (host+path present,
    `leak-user`/`leak-pass` absent). Keep the test deterministic (injected clock, no
    real timers, no real S3/disk).
  </action>
  <verify>
    <automated>pnpm vitest run src/run/no-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the test fails if ANY secret marker, `leak-user`/`leak-pass`,
      or `<html` string appears in any event line, the compact summary, or the
      evidence body (it must be RED if a leak is introduced, GREEN as-is)
    - Source assertion: `grep -n 'forbiddenMarkers\|leak-user\|<html' src/run/no-leak.test.ts` matches
    - Behavior assertion: the test confirms the sourceUrl appearing in the surfaces
      is userinfo-stripped (no `leak-user`/`leak-pass`)
    - Test command: `pnpm vitest run src/run/no-leak.test.ts` passes
  </acceptance_criteria>
  <done>One cross-surface test proves no secret/byte/HTML marker reaches any event line, the compact summary, or the evidence payload, and that sourceUrl is userinfo-stripped; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full verify gate</name>
  <files>src/run/no-leak.test.ts</files>
  <read_first>
    - package.json (the `verify` script composition — typecheck + unit + integration
      + coverage + build — to know what the gate runs)
    - .planning/phases/11-progress-events-and-compact-evidence/11-VALIDATION.md
      (the sampling/sign-off contract — full suite must be green before verify-work)
  </read_first>
  <action>
    Run `pnpm run verify` and confirm it is green: typecheck passes,
    all unit tests pass, the MinIO integration tests (checkpoint + the new evidence
    store) pass, V8 coverage is 100% on reachable source, and the build succeeds. If
    coverage flags an uncovered branch introduced by Phase 11 (e.g. an evidence-flag
    branch or a catch path), add the missing test case to the relevant existing
    `*.test.ts` (do not add `/* v8 ignore */` to genuinely reachable code — Pitfall
    6) and re-run. Do not change production behavior to satisfy coverage. Note: a
    pre-existing `pnpm-lock.yaml`-format / `.agents/**` lint failure outside this
    phase's scope is a known aggregate-gate issue (logged in Phase 7 deferred-items)
    — record it in the SUMMARY if encountered, but every Phase-11 stage (typecheck,
    unit, integration, coverage, build) must be individually green.
  </action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pnpm run verify` reports typecheck, unit, integration, 100%
      coverage, and build all green for Phase-11 scope
    - Behavior assertion: no Phase-11 file shows an uncovered line/branch in the V8
      coverage report
    - Any out-of-scope pre-existing aggregate failure is documented in the SUMMARY,
      not silently absorbed
  </acceptance_criteria>
  <done>pnpm run verify is green across typecheck/unit/integration/coverage/build for Phase-11 scope; any pre-existing out-of-scope failure is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secret-bearing config + userinfo sourceUrl → three output surfaces | the test deliberately feeds secrets to prove they cannot reach events/stdout/evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Information Disclosure | events + compact summary + evidence payload (the three Phase-11 output surfaces) | mitigate | A single cross-surface no-leak test (this plan) drives a run with secret-bearing config + a `user:pass@host` sourceUrl and asserts no secret/byte/HTML marker and no userinfo reaches any of the three surfaces — the phase-level enforcement of D-15/PROG-04, mirroring the DIAG-04 no-body test. Backed by pino `REDACT_PATHS` + identifiers-only discipline + `sanitizeSourceUrl`. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed in this phase. No install task; no gate applies. |
</threat_model>

<verification>
- `pnpm vitest run src/run/no-leak.test.ts` is green and goes RED if a leak is introduced.
- `pnpm run verify` is green across typecheck / unit / integration / 100% coverage / build for Phase-11 scope.
</verification>

<success_criteria>
- A cross-surface no-leak test proves no secret/byte/HTML marker reaches any event
  line, the compact stdout summary, or the evidence payload, and that sourceUrl is
  userinfo-stripped.
- `pnpm run verify` is green for Phase-11 scope; any pre-existing out-of-scope
  aggregate failure is documented.
</success_criteria>

<output>
Create `.planning/phases/11-progress-events-and-compact-evidence/11-05-SUMMARY.md` when done.
</output>
