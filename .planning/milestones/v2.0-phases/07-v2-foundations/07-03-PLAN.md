---
phase: 07-v2-foundations
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/discovery/source-client.ts
  - src/discovery/source-client.test.ts
  - src/storage/replay-byte-client.ts
  - src/storage/replay-byte-client.test.ts
  - src/cli.ts
autonomous: true
requirements: [CORE-01, CORE-02]
tags: [error-handling, logging, dependency-injection, refactor, infrastructure]

must_haves:
  truths:
    - "SourceFetchError extends AppError while keeping its exact code union rate_limited | source_unavailable"
    - "ReplayByteFetchError extends AppError while keeping its exact code fetch_failed"
    - "All existing instanceof guards and error.code reads on both classes still work unchanged"
    - "createLogger is registered in the CLI dependency map exactly like now/createRunId/createSourceClient"
    - "A child logger keyed by runId is created in run-once after runId is computed"
    - "The final JSON summary stdout contract is byte-for-byte unchanged and cli.test.ts passes without summary-assertion edits"
  artifacts:
    - path: "src/discovery/source-client.ts"
      provides: "SourceFetchError re-parented to AppError, narrow code union preserved"
      contains: "extends AppError"
    - path: "src/storage/replay-byte-client.ts"
      provides: "ReplayByteFetchError re-parented to AppError, narrow code preserved"
      contains: "extends AppError"
    - path: "src/cli.ts"
      provides: "createLogger injected into BuildCliDependencies + resolveDependencies; child({ runId }) in run-once"
      contains: "createLogger"
  key_links:
    - from: "src/discovery/source-client.ts"
      to: "src/errors/app-error.ts"
      via: "import { AppError } from '../errors/app-error.js'"
      pattern: "from \"\\.\\./errors/app-error\\.js\""
    - from: "src/cli.ts"
      to: "src/logging/create-logger.ts"
      via: "import createLogger + resolveDependencies default + child({ runId })"
      pattern: "createLogger"
    - from: "src/cli.ts run-once"
      to: "runId child logger"
      via: "rootLogger.child({ runId })"
      pattern: "child\\(\\{ runId"
---

<objective>
Wire the two Wave-1 substrates into the existing codebase without changing any observable behavior. Re-parent `SourceFetchError` and `ReplayByteFetchError` onto the new `AppError` base (CORE-01), preserving their narrow `code` literal unions and every `instanceof`/`error.code` call site. Inject the `createLogger` factory into the CLI dependency map and create a `child({ runId })` logger in `run-once` (CORE-02 wiring).

Purpose: This completes Phase 7 — the structural refactor that gives DIAG/RESUME/RANGE/PROG/GUARD their typed-error base and logger substrate. Per RESEARCH and PATTERNS, there are currently ZERO ad-hoc `console`/log call sites to migrate; the `JSON.stringify` calls in `discover.ts` and `postgres-staging-repository.ts` are data serialization, not logging, and stay untouched. Phase 7 therefore delivers the factory + DI wiring + `child({ runId })` substrate, with no behavioral output change.

Output: re-parented error classes (+ regression-extended tests), and `src/cli.ts` with `createLogger` in `BuildCliDependencies`/`resolveDependencies` and a per-run child logger. The final JSON summary stdout contract (`writeJson`) is byte-for-byte unchanged.

Critical parity invariant: `cli.test.ts` parses joined stdout writes as one JSON object (`JSON.parse(writes.join(""))`). No pino line may be emitted to stdout that would interleave with the summary. summary stdout JSON shape unchanged; `cli.test.ts` passes.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v2-foundations/07-RESEARCH.md
@.planning/phases/07-v2-foundations/07-PATTERNS.md
@AGENTS.md
@.planning/phases/07-v2-foundations/07-01-PLAN.md
@.planning/phases/07-v2-foundations/07-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan (07-03) modifies (no new files):
- `src/discovery/source-client.ts` — `SourceFetchError` now `extends AppError<"rate_limited" | "source_unavailable">`; drops hard-coded `this.name`; adds optional `{ cause, details }` constructor options (3rd param).
- `src/storage/replay-byte-client.ts` — `ReplayByteFetchError` now `extends AppError<"fetch_failed">`; same re-parent shape.
- `src/discovery/source-client.test.ts` — extended with `instanceof AppError` + narrow-`code` regression assertions.
- `src/storage/replay-byte-client.test.ts` — extended with the same regression assertions.
- `src/cli.ts` — `createLogger?` added to `BuildCliDependencies`; `createLogger` added to `resolveDependencies` defaults (before the `...dependencies` spread); `const rootLogger = dependencies.createLogger(); const log = rootLogger.child({ runId });` added in `run-once` after line 308's `runId`.

It introduces NO new `code` union members and NO new error subclasses. It does NOT change `writeJson` or any summary payload.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-parent SourceFetchError and ReplayByteFetchError to AppError (preserve unions, instanceof, name)</name>
  <files>src/discovery/source-client.ts, src/storage/replay-byte-client.ts, src/discovery/source-client.test.ts, src/storage/replay-byte-client.test.ts</files>
  <read_first>
    - src/errors/app-error.ts (the base from plan 07-01 — constructor signature `(code, message, options?)`, `new.target.name` name derivation)
    - src/discovery/source-client.ts (lines 1-23 current SourceFetchError; throw sites 61, 73, 115, 123; instanceof guards 69, 105 — must stay valid)
    - src/storage/replay-byte-client.ts (lines 1-25 current ReplayByteFetchError; throw sites 58, 70, 102, 113; instanceof guard 66 — must stay valid)
    - src/discovery/source-client.test.ts and src/storage/replay-byte-client.test.ts (existing assertions to keep green; extend, don't rewrite)
    - .planning/phases/07-v2-foundations/07-PATTERNS.md (sections "src/discovery/source-client.ts (MODIFY)" and "src/storage/replay-byte-client.ts (MODIFY)" — before/after, do-not-touch lists, import hygiene)
    - .planning/phases/07-v2-foundations/07-RESEARCH.md (Pattern 2, Pitfall 1-3, Code Example 2)
  </read_first>
  <behavior>
    - Test (regression): `new SourceFetchError("rate_limited", msg)` still constructs; `error.code` is `"rate_limited" | "source_unavailable"` (narrow); `error.name === "SourceFetchError"`; `error instanceof SourceFetchError` AND `error instanceof AppError` AND `error instanceof Error` all true.
    - Test (regression): `new ReplayByteFetchError("fetch_failed", msg)` still constructs; `error.code === "fetch_failed"`; `error.name === "ReplayByteFetchError"`; `instanceof` chain holds (subclass, AppError, Error).
    - Test (new capability, optional cause): passing `{ cause }` preserves `error.cause`; omitting it leaves `error.cause` undefined.
  </behavior>
  <action>
    In `src/discovery/source-client.ts`: add `import { AppError } from "../errors/app-error.js";` (ESM `.js`, follows existing import hygiene at lines 4-5). Change the class to `export class SourceFetchError extends AppError<"rate_limited" | "source_unavailable">`. Constructor: `constructor(code: SourceFetchError["code"], message: string, options?: { readonly cause?: unknown; readonly details?: Readonly<Record<string, unknown>> })` delegating `super(code, message, options);`. REMOVE the hard-coded `this.name = "SourceFetchError"` and the manual `this.code = code` (the base sets both — name via `new.target.name`). Do NOT alter any throw site (lines 61, 73, 115, 123 stay `new SourceFetchError("...", msg)` — options are optional) or any `instanceof` guard (69, 105) or `error.code` read.
    In `src/storage/replay-byte-client.ts`: identical re-parent — `import { AppError } from "../errors/app-error.js";`, `export class ReplayByteFetchError extends AppError<"fetch_failed">`, constructor delegating `super(code, message, options)`, remove hard-coded `this.name`/`this.code`. Do NOT touch throw sites (58, 70, 102, 113) or the guard (66).
    Extend (do not rewrite) `src/discovery/source-client.test.ts` and `src/storage/replay-byte-client.test.ts` with the regression assertions from `<behavior>`: import `AppError` via `../errors/app-error.js`; assert `instanceof AppError`, narrow `code`, `name === "<subclass>"`, and the cause-present/absent cases. Keep all existing assertions passing.
    Lint baseline: no `any`/`as`/`console`. If TypeScript flags `this.code`/`this.name` as now-unused removals, ensure the base truly owns them (it does — verify against 07-01 source).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/discovery/source-client.test.ts src/storage/replay-byte-client.test.ts src/discovery/discover.test.ts && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/discovery/source-client.ts` contains `extends AppError<"rate_limited" | "source_unavailable">` and imports `AppError` from `"../errors/app-error.js"`.
    - `src/storage/replay-byte-client.ts` contains `extends AppError<"fetch_failed">` and imports `AppError` from `"../errors/app-error.js"`.
    - Neither file contains `this.name = "SourceFetchError"` / `this.name = "ReplayByteFetchError"` any longer (verify: `grep -n 'this.name =' src/discovery/source-client.ts src/storage/replay-byte-client.ts` returns nothing).
    - Both test files assert `instanceof AppError`, the narrow `code` value, `name === "<subclass>"`, and the cause-present + cause-absent cases.
    - `pnpm exec vitest run src/discovery/source-client.test.ts src/storage/replay-byte-client.test.ts src/discovery/discover.test.ts` passes (instanceof/code call sites intact).
    - `pnpm exec tsc -p tsconfig.json --noEmit` passes with the narrow `code` unions preserved (a `switch (error.code)` on `SourceFetchError["code"]` would still be exhaustive over the two literals).
    - No `any`/`as`/`console` introduced.
  </acceptance_criteria>
  <done>Both domain error classes extend `AppError`, keep their exact narrow `code` unions and concrete `name`, preserve all `instanceof`/`code` call sites, and gain optional `cause`/`details` — verified by extended colocated regression tests and a green type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Inject createLogger into the CLI DI map and create the runId child logger (summary contract unchanged)</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/logging/create-logger.ts (the factory + `CreateLoggerOptions` from plan 07-02 — the import target and child API)
    - src/cli.ts (lines 70-91 BuildCliDependencies; lines 139-161 resolveDependencies; line 308 `const runId = dependencies.createRunId(startedAt);` in run-once; writeJson at line 609; every writeJson summary call: 195, 209, 243, 252, 266, 276, 291, 319, 345, 371, 437, 442 — all must stay byte-for-byte)
    - src/cli.test.ts (the parity contract: `JSON.parse(writes.join(""))` parses joined stdout as ONE object — summary assertions must pass UNCHANGED)
    - .planning/phases/07-v2-foundations/07-PATTERNS.md (section "src/cli.ts (MODIFY)" — DI-map entry, default resolution with `...dependencies` spread ordering, per-command child-by-runId, CRITICAL do-NOT-change-summary list)
    - .planning/phases/07-v2-foundations/07-RESEARCH.md (Pattern 4, Pitfall 4, Code Example 6, Open Question 1 scope cut)
  </read_first>
  <action>
    In `src/cli.ts`: add `import { createLogger, type CreateLoggerOptions } from "./logging/create-logger.js";` and `import type { Logger } from "pino";` (ESM `.js` for the local import).
    Add to `interface BuildCliDependencies` (lines 70-91): `readonly createLogger?: (options?: CreateLoggerOptions) => Logger;` — placed consistently with the existing optional fn-typed deps.
    Add `createLogger` to the `resolveDependencies` return object (lines 142-160) among the real defaults, BEFORE the `...dependencies` spread so test overrides still win (preserve the spread-last ordering exactly — PATTERNS CRITICAL note).
    In the `run-once` action, immediately after `const runId = dependencies.createRunId(startedAt);` (line 308), add `const rootLogger = dependencies.createLogger();` and `const log = rootLogger.child({ runId });`. This is the CORE-02 `child({ runId })` substrate wiring. Because there are currently no log call sites, `log` may not yet be consumed by any emission; if the project's no-unused-vars lint flags `log`/`rootLogger`, prefer the minimal compliant wiring that satisfies lint without emitting to stdout — options in priority order: (a) emit a single `log.debug({ runId }, "run-once started")` record (debug is below the default `info` level, so it produces NO stdout output at default level and cannot interleave with the summary — confirm against `cli.test.ts` that stdout stays a single JSON object); (b) if (a) still risks the parity test, pass `rootLogger`/`log` down to `runOnce` as an injected dependency it accepts but does not yet use for stdout. Choose the approach that keeps `cli.test.ts` green with ZERO summary-assertion edits.
    Do NOT modify any `writeJson(...)` call or any summary payload (lines 195, 209, 243, 252, 266, 276, 291, 319, 345, 371, 437, 442). Do NOT migrate the summary to pino (that is PROG-01/02, Phase 11). Do NOT emit any pino line to stdout at or above `info` that would interleave with the summary (RESEARCH Pitfall 4). Leave the `JSON.stringify` data-serialization calls in `discover.ts`/`postgres-staging-repository.ts` untouched (they are not logging). No `any`/`as`/`console`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/cli.test.ts && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli.ts` imports `createLogger` (and `CreateLoggerOptions`/`Logger`) from the new logging module / pino.
    - `BuildCliDependencies` declares `readonly createLogger?: (options?: CreateLoggerOptions) => Logger;`.
    - `resolveDependencies` includes `createLogger` among defaults BEFORE `...dependencies` (verify the spread is still last).
    - `run-once` creates a `child({ runId })` logger after the `runId` line (verify: `grep -n 'child({ runId' src/cli.ts` returns a match).
    - Every `writeJson` summary call is unchanged — `cli.test.ts` passes with NO edits to summary assertions (parity invariant). summary stdout JSON shape unchanged; `cli.test.ts` passes.
    - `pnpm exec vitest run src/cli.test.ts` passes; `pnpm exec tsc -p tsconfig.json --noEmit` passes.
    - No `any`/`as`/`console` introduced; no pino line interleaves the stdout summary (the joined-stdout `JSON.parse` in `cli.test.ts` still yields one object).
  </acceptance_criteria>
  <done>`createLogger` is injectable via the CLI DI map exactly like the existing deps; `run-once` builds a `child({ runId })` logger; the final summary stdout contract is byte-for-byte unchanged and `cli.test.ts` passes without summary edits.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite parity gate — pnpm run verify green after the refactor</name>
  <files>src/cli.ts</files>
  <read_first>
    - package.json (the `verify` script: format → lint → typecheck → test → test:integration → test:coverage → build)
    - .planning/phases/07-v2-foundations/07-RESEARCH.md (Validation Architecture — Sampling Rate, phase gate; "no behavioral change" invariant)
    - .planning/phases/07-v2-foundations/07-VALIDATION.md (phase validation expectations)
  </read_first>
  <action>
    Run the full phase gate `pnpm run verify` and resolve any failure WITHOUT changing observable behavior. Expected-clean checks: format (Prettier), lint (ESLint `all` + typed rules — confirm no `any`/`as`/`console` slipped in, import order/hygiene clean for the new error/logging imports), typecheck (narrow `code` unions preserved), unit tests (new + extended + unchanged `cli.test.ts` summary parity), integration tests (existing MinIO/Postgres Testcontainers — Phase 7 adds none), coverage (100% reachable — the new `app-error.ts` and `create-logger.ts` and any new branches in `cli.ts` must be covered; if `cli.ts` gained an uncovered branch, add/extend a `cli.test.ts` case that drives it via an injected `createLogger` WITHOUT asserting on summary stdout shape), and build (`tsc`/bundler emits `dist/`).
    If coverage flags the new run-once logger lines as uncovered, add a `cli.test.ts` case that invokes `run-once` with an injected `createLogger` returning a logger whose `child` is exercised — assert ONLY on the injected logger / non-summary behavior, never re-baseline the summary assertions. If Docker is unavailable locally for `test:integration`, run the rest of `verify` locally and note that `test:integration` must pass in CI (RESEARCH Environment Availability).
    This task touches `src/cli.ts` only if a coverage-driven test gap requires a tiny wiring tweak; otherwise it is purely the gate run and any necessary test additions live in the colocated `*.test.ts` files from Tasks 1-2.
  </action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run verify` exits 0 (format, lint, typecheck, unit, integration, coverage, build all green).
    - The V8 coverage report shows 100% reachable coverage for `src/errors/app-error.ts`, `src/logging/create-logger.ts`, and no NEW uncovered branch in `src/cli.ts` attributable to this phase.
    - `cli.test.ts` summary assertions are unchanged from before Phase 7 (no behavioral change — if any summary assertion needed editing, that is a regression and must be fixed in source, not in the test).
    - No `any`/`as`/`console` anywhere in the phase's changed/new files (lint enforces).
  </acceptance_criteria>
  <done>`pnpm run verify` is green end-to-end; coverage gates pass for the new modules; the summary stdout contract is provably unchanged. Phase 7 ships a clean structural refactor with no behavioral change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log sink (stdout / injected stream) | The `child({ runId })` logger could emit records to stdout. Records must not interleave with the summary contract and must not carry secrets/bytes. |
| no new external surface | Re-parenting errors and wiring DI are in-process; no new network listener, no new input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | secrets reaching the new runId child logger | mitigate | The injected `createLogger` already applies `redact.paths` (plan 07-02). This plan logs NO whole `config`/`candidate`/`payload`; the run-once wiring logs at most identifiers (`runId`) at `debug` (below default `info`), so nothing reaches stdout at default level. |
| T-07-05 | Information Disclosure | `error.cause`/`error.details` of re-parented errors leaking secrets when later logged | mitigate | The re-parent adds optional `details`/`cause`, but throw sites are unchanged (no secret is passed). Any future logging of these flows through the redacting logger. `<action>` forbids passing secrets/bytes into `details`. |
| T-07-06 | Tampering | parity regression — accidental change to the summary stdout contract | mitigate | Hard constraint: every `writeJson` call byte-for-byte unchanged; `cli.test.ts` `JSON.parse(writes.join(""))` must pass with zero summary-assertion edits; the `verify` gate (Task 3) blocks merge on any drift. No pino line emitted to stdout at/above `info`. |
| T-07-SC | Tampering | npm installs | accept | No package install occurs in this plan (pino was installed under the 07-02 legitimacy checkpoint). |
</threat_model>

<verification>
- `pnpm exec vitest run src/discovery/source-client.test.ts src/storage/replay-byte-client.test.ts src/discovery/discover.test.ts` passes (re-parent regression).
- `pnpm exec vitest run src/cli.test.ts` passes with unchanged summary assertions (parity).
- `pnpm exec tsc -p tsconfig.json --noEmit` passes (narrow `code` unions preserved; DI types resolve).
- `pnpm run verify` exits 0 (full phase gate including integration + 100% reachable coverage + build).
- `grep -n 'this.name =' src/discovery/source-client.ts src/storage/replay-byte-client.ts` returns nothing (name now derived in base).
- `grep -n 'child({ runId' src/cli.ts` returns the run-once child-logger wiring.
</verification>

<success_criteria>
- `SourceFetchError extends AppError<"rate_limited" | "source_unavailable">` and `ReplayByteFetchError extends AppError<"fetch_failed">`, with all call sites and narrow unions intact.
- `createLogger` is injectable through the CLI DI map and a `child({ runId })` logger exists in `run-once`.
- The final JSON summary stdout contract is byte-for-byte unchanged; `cli.test.ts` passes without summary edits.
- `pnpm run verify` is green — no behavioral change, only structural improvement (ROADMAP success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/07-v2-foundations/07-03-SUMMARY.md` when done, confirming: both errors re-parented with unions preserved, `createLogger` DI wiring + runId child, summary contract unchanged, and `pnpm run verify` green (note CI-only if Docker integration ran in CI).
</output>
