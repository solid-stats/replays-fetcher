---
milestone: v3.0
milestone_name: Track C Toolchain Convergence
audited: "2026-06-14T09:20:00Z"
status: passed
scores:
  requirements: 24/24 satisfied (CFG-04 vitest leg closed post-audit)
  phases: 6/6 verified passed
  integration: 24/24 configs/tools wired
  flows: 1/1 (verify → green @ 100% coverage; Docker smoke; hooks fire)
gaps:
  requirements: []        # no functional blocker; CFG-04 fully closed
resolved_during_audit:
  - "CFG-04 vitest leg CLOSED (fetcher commit 5d5a23f): ts-toolchain v0.1.2 (commit d7fa16e, CI green run 27486000384) now ships the vitest preset as base.js + base.d.ts (the raw .ts was unimportable — Node won't strip types under node_modules). vitest.config.ts re-pinned #v0.1.1→#v0.1.2 and now imports vitestBaseConfig + mergeConfig() instead of duplicating thresholds. Full verify green @ 100% coverage (1797/1797) via sg docker; zero src/ change. User authorized the shared-repo master push."
  - "Stale eslint.config.js tsconfig include removed (fetcher commit 6818032)."
tech_debt:
  - phase: 18-lefthook-hooks-ci-verify-convergence
    items:
      - "oxfmt papercut: the shared lefthook pre-commit `oxfmt --check {staged_files}` exits 2 when the entire staged set is oxfmt-ignored (package.json/lockfile-only commits), false-blocking those commits (worked around with intentional --no-verify, documented per-commit). FIX: add `--no-error-on-unmatched-pattern` to the preset's format command (confirmed exit 0). Cross-repo preset patch; can ride the same v0.1.2 push."
---

# v3.0 Track C Toolchain Convergence — Milestone Audit

## Verdict: passed (CFG-04 closed during audit; one minor cross-repo follow-up remains)

All 6 phases (13–18) carry a `passed` VERIFICATION.md. All 24 requirements (CFG-01..04, CLN-01..04, FMT-01/02, LNT-01..04, IMP-01/02, BLD-01/02, HOK-01..03, VRF-01..03) are marked `[x]` in the REQUIREMENTS traceability and verified in their owning phase. `pnpm verify` runs the full new surface (oxfmt → oxlint → tsc → unit → integration → coverage → tsdown → depcruise → knip) **green at 100% V8 coverage** (1797/1797 statements, 771/771 branches, 350/350 functions, 1766/1766 lines) via `sg docker -c`. lefthook hooks wire from the shared preset and fire end-to-end (pre-commit oxfmt+oxlint staged, pre-push tsc+test); CI rides `pnpm run verify`; the Docker smoke-run of `check` passes. The ingest boundary is untouched — every Track C commit touched zero business-logic surface.

## Requirements coverage (3-source cross-reference)

24/24 requirements satisfied per VERIFICATION + traceability `[x]`. **CFG-04** ("the fetcher's config files … reference the shared presets instead of duplicating rule content") is now **fully satisfied**: `tsconfig.json`, `.oxlintrc.json`, `lefthook.yml` `extends`; `.oxfmtrc.json` byte-mirror (oxfmt has no `extends`); and `vitest.config.ts` imports `vitestBaseConfig` + `mergeConfig()`. The vitest leg was the one gap this audit surfaced and it was closed during the audit (commit `5d5a23f`, re-pin to ts-toolchain `v0.1.2`), after fixing the preset's `.ts` packaging that made the documented import unloadable under Node type-stripping.

## Phase verifications

| Phase | VERIFICATION | Notes |
|-------|--------------|-------|
| 13 Shared toolchain bootstrap | passed | tag v0.1.0→v0.1.1→**v0.1.2**; tsconfig extends; vitest preset un-consumable as shipped (.ts) — surfaced by this audit, **fixed in v0.1.2 (base.js+.d.ts) + re-pinned** |
| 14 Cleanup & conventions | passed | cli.ts split, ConfigError→AppError, RunSummary→src/types |
| 15 Oxfmt | passed | prettier→oxfmt 0.54, zero-diff reformat |
| 16 Oxlint + import hygiene | passed | eslint→oxlint, depcruise + knip, RULE-DELTA |
| 17 tsdown + Docker smoke | passed | dist/cli.mjs ESM bundle, Docker smoke PASS |
| 18 lefthook + CI verify convergence | passed | hooks from preset (extends), canonical verify order, CI on new surface |

## Integration (cross-phase wiring)

The toolchain composes end-to-end on a single pinned source of truth (`@solid-stats/ts-toolchain#v0.1.1`, no divergent pins): tsconfig/oxlint/lefthook `extends`, oxfmt byte-mirror, verify chains every installed tool, CI ⇄ local hook parity holds, no orphaned eslint/prettier/tsc-emit references (the one stale `eslint.config.js` tsconfig include was removed during this audit, commit `6818032`). The single wiring gap is the vitest preset consumption above.

## Tech debt — one tracked cross-repo preset follow-up

Lives in `@solid-stats/ts-toolchain` (out of this repo's file scope), non-blocking:

- **oxfmt unmatched-pattern papercut** (Phase 18) — the preset's pre-commit `format` command (`oxfmt --check {staged_files}`) exits 2 when the staged set is entirely oxfmt-ignored (package.json/lockfile-only commits), false-blocking those commits (worked around with intentional `--no-verify`). Fix is `oxfmt --check --no-error-on-unmatched-pattern {staged_files}` (verified exit 0). Prepared but NOT pushed — the user authorized only the CFG-04 vitest push, so this awaits a separately-authorized shared-repo push (own `v0.1.3` tag + re-pin).

The vitest packaging item (formerly #1 here) was resolved during the audit — see `resolved_during_audit` in the frontmatter.

## Nyquist

VALIDATION.md present for the phases that authored one (17, 18). Toolchain-process milestone — validation is the green verify gate at each boundary, satisfied throughout.
