---
milestone: v2.0
milestone_name: Full-Corpus Ingest Resilience
audited: 2026-06-12
status: passed
scores:
  requirements: 25/25
  phases: 6/6
  integration: clean (0 orphaned, 0 missing, 0 broken flows)
  flows: 1/1 (full-corpus run-once E2E)
gaps: {}
tech_debt: []
nyquist:
  compliant_phases: 6
  partial_phases: 0
  missing_phases: 0
  overall: compliant
verify_gate: green
---

# v2.0 Milestone Audit — Full-Corpus Ingest Resilience

**Status: PASSED.** All 25 milestone requirements satisfied, all 6 phases verified, cross-phase integration clean, the full-corpus E2E flow is complete, and `pnpm run verify` is green end-to-end (Docker integration included).

## Definition of Done

A restarted full-corpus run resumes instead of re-reading from page 1; source failures are richly diagnosed and bounded-retried; the run scope is discovered at runtime and paced/throttled to the ~1–2h target without hammering the Cloudflare-fronted source; operators follow the run via greppable per-page events and a compact summary with durable on-demand evidence; and source-parse regressions (notably "bytes from the JSON endpoint, not the HTML detail page") fail a unit test or a fast `contract-check` before they corrupt a run.

## Phase Verification Summary

| Phase | Name | Plans | VERIFICATION | Nyquist |
|-------|------|-------|--------------|---------|
| 7 | v2 Foundations | 3/3 | passed | compliant |
| 8 | Source Failure Diagnostics & Retry | 4/4 | passed | compliant |
| 9 | Checkpoint & Resume | 5/5 | passed | compliant |
| 10 | Dynamic Source Range & Rate Limiting | 5/5 | passed | compliant |
| 11 | Progress Events & Compact Evidence | 5/5 | passed | compliant |
| 12 | Source Contract Guards | 2/2 | passed | compliant |

## Requirements Coverage (3-source cross-reference)

All 25 requirements satisfied (VERIFICATION passed + SUMMARY frontmatter lists them + REQUIREMENTS.md traceability now `[x]`). No orphaned, unsatisfied, or partial requirements.

- **CORE-01, CORE-02** (P7) — AppError typed-error base; pino structured logger.
- **DIAG-01..04** (P8) — single `classifyFailure` (transient/permanent/rate_limited); enriched diagnostics; no body/secret leakage.
- **RETRY-*** (P8) — `withRetry` over the shared classifier for source + byte reads.
- **RESUME-01..05** (P9) — S3 rolling checkpoint with conditional CAS; resume at `lastCompletedPage+1`; `run_id` in `promotion_evidence`; run-status taxonomy.
- **RANGE-01..06** (P10) — runtime range discovery; `p-limit` concurrency; AIMD throttle; pacer floor; classify-before-stop-on-empty (no silent truncation).
- **PROG-01..04** (P11) — per-page pino NDJSON events; compact stdout summary; durable S3 evidence; redaction + awaited flush + boundary safety.
- **GUARD-01..04** (P12) — deterministic fixtures; `toRawReplayUrl` golden + swap regression; no-write `contract-check` CLI reusing DIAG; no-mutation guards.

## Cross-Phase Integration

Integration checker (line-cited, full-suite green) confirmed the run-once orchestration wires every phase: range discovery → paced, concurrency-limited, retried source reads → per-page checkpoint → progress events + compact summary + opt-in evidence, all on the AppError/pino substrate. `classifyFailure` is a single implementation reused by RANGE-06 stop-on-empty and the P12 `contract-check` — no divergent copies. Boundary safety holds end-to-end: only S3 raw + staging + checkpoint + opt-in evidence writes; no parser artifacts; no `server-2` business-table writes; `contract-check` writes neither S3 nor PostgreSQL.

**E2E flow:** full-corpus `run-once` — COMPLETE, 0 broken.

## Quality Gate

`pnpm run verify` exits 0: prettier ✓ · eslint ✓ · typecheck ✓ · unit (444 tests / 35 files) ✓ · integration (testcontainers MinIO + PostgreSQL) ✓ · coverage 100% (1530/1530 stmts, 764/764 branches, 350/350 funcs, 1517/1517 lines) ✓ · build ✓. Pre-existing phase-11 lint/format/coverage debt was cleared during this milestone close.

## Tech Debt

None blocking. The Node engine warning (running Node 24 vs the wanted 25) is environmental, not a milestone gap.

## Verdict

Milestone v2.0 achieved its definition of done. Ready for `complete-milestone`.
