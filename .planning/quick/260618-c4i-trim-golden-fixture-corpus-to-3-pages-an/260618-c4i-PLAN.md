---
quick_id: 260618-c4i
status: ready
mode: quick
---

# Quick 260618-c4i — Trim golden fixture corpus to 3 pages + set maxPages

## Context

The golden fixture corpus was captured at 10 pages (~105 MB) — too heavy for git
history. Per the user's decision it is trimmed to **3 pages** (~37 MB). The corpus
on disk at `src/run/fixtures/golden/` is ALREADY trimmed (3 list / 90 detail / 90
bytes / manifest) by a local trimmer that reused the real `extractReplayRows`, so
the URL keys still match the pipeline. The prior 10-page commits were `git reset`
(unpushed) so the 105 MB never enters history.

`src/run/golden-e2e.integration.test.ts` still has `maxPages: 10`, which over a
3-page corpus would make `runOnce` request page 4 → the fake source throws. The
constant must equal the corpus page count.

## Task 1 — set maxPages to 3 and commit the trimmed corpus

**files:** `src/run/golden-e2e.integration.test.ts`, `src/run/fixtures/golden/**`

**action:**
- In `golden-e2e.integration.test.ts`: change `maxPages: 10` → `maxPages: 3`, and
  update the run-2 idempotency comment that references the `maxPages: 10` cap /
  "page-11" to `maxPages: 3` / "page-4" (run 1 hits the cap on page 3 → `truncated`;
  the all-duplicate run-2 logic is unchanged). This is a corpus-coupled constant,
  not test logic.
- Commit atomically (signed): (a) the test change; (b) the trimmed corpus
  `src/run/fixtures/golden`.

**verify:** `pnpm run test:integration` GREEN 6/6 — golden run-once AND golden watch
run live (no skip); `pnpm run verify` GREEN.

**done:** both golden tests pass on the 3-page corpus; `maxPages` equals the corpus
page count; corpus committed (~37 MB); nothing pushed.

## Out of scope / findings

The discovery game-date gap (`discoveredAt`/`replay_timestamp` unpopulated) is
already recorded in `../plans/replays-fetcher/TECH-DEBT.md`.
