---
phase: quick-260623-qj5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/time/epoch-to-utc-iso.ts
  - src/time/epoch-to-utc-iso.test.ts
  - src/staging/payload.ts
  - src/staging/payload.test.ts
  - src/run/golden-e2e.integration.test.ts
autonomous: true
requirements:
  - QJ5-01

premises:
  - claim: "`evidence.source.externalId` (optional string) already reaches `toPayload`/`basePayload` in payload.ts — no threading needed; it is read at payload.ts:42 and :141"
    src: src/staging/payload.ts#L41-L51
    verify: grep -n "evidence.source.externalId" src/staging/payload.ts
  - claim: "`replayTimestamp` is currently `replayTimestampFromFilename(...) ?? evidence.discoveredAt` and `replayTimestampFromFilename` is module-private to payload.ts (no external consumers)"
    src: src/staging/payload.ts#L96-L98
    verify: grep -rn "replayTimestampFromFilename" src --include="*.ts"
  - claim: "`src/time/` is in the depcruise cross-cutting band; `staging/` (capability) importing it is downward and allowed (payload.ts already imports componentsToUtcIso)"
    src: .dependency-cruiser.cjs#L229-L241
    verify: grep -n "time" .dependency-cruiser.cjs
  - claim: "every golden-corpus id is a numeric Unix epoch (seconds); 90/90, all in range — so each corpus row's epoch-derived replay_timestamp is deterministic and equals new Date(Number(source_replay_id)*1000).toISOString()"
    src: src/run/fixtures/golden/manifest.json#expectedExternalIds
    verify: node -e "const m=require('./src/run/fixtures/golden/manifest.json'); console.log(m.expectedExternalIds.every(id=>/^\d+$/.test(id)&&Number(id)>1420070400&&Number(id)<2051222400), m.expectedExternalIds.length)"
  - claim: "the golden oracle currently asserts replay_timestamp ONLY via the discoveredAt evidence shape and a code comment claiming replay_timestamp stays filename-derived — it does not select/assert the replay_timestamp column"
    src: src/run/golden-e2e.integration.test.ts#L212-L224
    verify: grep -n "replay_timestamp" src/run/golden-e2e.integration.test.ts

carried_forward:
  - claim: "Phase 25 set replayTimestamp = replayTimestampFromFilename(...) ?? evidence.discoveredAt — filename strictly PRIMARY, listing a strict fallback; golden oracle flipped to a UTC-shape match because replay_timestamp stayed filename-derived in the corpus. This task supersedes that precedence with epoch-PRIMARY."
    src: STATE.md#Decisions
  - claim: "T-25-03 listing-timezone ship-gate was deferred (filename stays primary, bounded risk); this task closes its root cause by making true-UTC epoch primary."
    src: .planning/milestones/v3.1-MILESTONE-AUDIT.md

must_haves:
  truths:
    - "When a candidate's externalId is a numeric Unix epoch in range, the staged replayTimestamp is the epoch converted to ISO-8601 UTC (true UTC instant)."
    - "When externalId is non-numeric / out-of-range / a `derived:` id, replayTimestamp falls through to the filename parse, then to the listing game-date — in that order — and the epoch parser never throws."
    - "promotion_evidence.discoveredAt is unchanged (still the listing value, audit-only)."
    - "Across the golden corpus (all numeric-epoch ids), every staging row's replay_timestamp equals the epoch-derived ISO UTC value."
  artifacts:
    - path: src/time/epoch-to-utc-iso.ts
      provides: "Range-guarded Unix-epoch-seconds → ISO-8601 UTC parser (never throws; out-of-range/non-numeric → undefined)"
      exports: ["epochToUtcIso"]
    - path: src/time/epoch-to-utc-iso.test.ts
      provides: "Epoch parse/range matrix unit test (valid → ISO UTC; out-of-range/non-numeric → undefined)"
    - path: src/staging/payload.ts
      provides: "replayTimestamp precedence = epoch → filename → listing"
      contains: "epochToUtcIso"
  key_links:
    - from: src/staging/payload.ts
      to: src/time/epoch-to-utc-iso.ts
      via: "import epochToUtcIso; epochToUtcIso(evidence.source.externalId) is the first arm of the replayTimestamp ?? chain"
      pattern: "epochToUtcIso\\(.*externalId"
    - from: src/run/golden-e2e.integration.test.ts
      to: src/staging/payload.ts
      via: "asserts each staged row's replay_timestamp equals the epoch-derived UTC value for its source_replay_id"
      pattern: "replay_timestamp"
---

<objective>
Correct the canonical `replayTimestamp` written to staging (→ `server-2` `replays.replay_timestamp`)
to use the `externalId` Unix epoch (a true UTC instant) as the PRIMARY source, demoting the existing
filename parse and listing game-date to ordered fallbacks. The current sources are both server-local
wall-clock (≈UTC+1) stamped as UTC, and the filename one is the wrong event (file-write/game-end).
The epoch (`/replays/{id}` where `{id}` is Unix epoch seconds) is the only unambiguous UTC instant the
fetcher already captures.

This is an INTENTIONAL behavior change to the staged value. It is value-only (same field/type → NOT a
schema change); `server-2` owns server-2-side reads and any backfill (out of scope here).

Purpose: stop staging wrong-timezone, wrong-event replay timestamps so `server-2`/`web` display
corrected game times going forward.
Output: a new `src/time/epoch-to-utc-iso.ts` parser symbol (`epochToUtcIso`), the precedence change in
`payload.ts`, full unit coverage, and a flipped golden-oracle assertion.

Artifacts this phase produces:
- New symbol: `epochToUtcIso(externalId: string): string | undefined` in `src/time/epoch-to-utc-iso.ts`.
- No new evidence field — `externalId` already flows to `payload.ts` via `evidence.source.externalId`
  and is already persisted as `promotion_evidence.sourceExternalId`; `discoveredAt` audit field
  unchanged. (No staging-band threading change is required — confirmed against payload.ts:42/141.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260623-qj5-fix-replay-timestamp-source-use-external/260623-qj5-CONTEXT.md
@.planning/quick/260623-qj5-fix-replay-timestamp-source-use-external/260623-qj5-RESEARCH.md

# Skills — read EVERY file IN FULL and confirm which you read (convention-bound TS fetcher change)
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
@.agents/skills/solidstats-shared-backend-ts-standards/SKILL.md
@.agents/skills/solidstats-shared-backend-ts-standards/references/correctness-and-quality.md
@.agents/skills/solidstats-shared-ts-standards/SKILL.md
@.agents/skills/solidstats-fetcher-ts-tests/SKILL.md

# Source — exact touch points
@src/staging/payload.ts
@src/staging/payload.test.ts
@src/time/components-to-utc-iso.ts
@src/run/golden-e2e.integration.test.ts
@src/types/raw-replay.ts
@src/types/replay-candidate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add range-guarded epochToUtcIso parser in the time band</name>
  <read_first>
    - src/time/components-to-utc-iso.ts — the sibling UTC builder: doc-comment style, the
      "never throws → undefined for out-of-range" contract, file shape to mirror.
    - .agents/skills/solidstats-shared-backend-ts-standards/SKILL.md §A (naming: camelCase fn,
      kebab-case file) and §B / correctness-and-quality.md (no-throw discipline; conventions win).
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §B (no `any`/`as`; `import type`) and
      §G (test idioms: `test.each` tables, typed builders).
  </read_first>
  <files>src/time/epoch-to-utc-iso.ts, src/time/epoch-to-utc-iso.test.ts</files>
  <behavior>
    - In-range numeric-string epoch (seconds) → ISO-8601 UTC, e.g. "1781460116" → "2026-06-14T18:01:56.000Z".
    - Non-numeric input (e.g. a `derived:...` id, "abc", "", "  ") → undefined.
    - Out-of-range epoch below the lower bound (before 2015-01-01) → undefined.
    - Out-of-range epoch above the upper bound (after 2035-01-01) → undefined.
    - Inclusive lower/upper boundary values parse (just inside range), one tick past each → undefined.
    - Non-integer / fractional / leading-zero-padded / signs / whitespace-padded numeric strings →
      rejected to undefined (only a clean integer-seconds string is accepted). Decide and TEST the
      exact acceptance rule (recommend a strict integer-string check, not `parseInt`/`Number` coercion
      that silently accepts "12abc" or "1.5e9"). NEVER throws for ANY input.
  </behavior>
  <action>
    Create `epochToUtcIso(externalId: string): string | undefined` in `src/time/epoch-to-utc-iso.ts`,
    colocated with `components-to-utc-iso.ts` per the depcruise cross-cutting band [src: .dependency-cruiser.cjs#L235].
    Per the LOCKED decision [src: CONTEXT.md#decisions]: parse `externalId` as integer SECONDS,
    range-guard inside epoch seconds for 2015-01-01 .. 2035-01-01, convert in-range values to ISO-8601
    UTC ending in `Z` (`new Date(seconds*1000).toISOString()`), and return undefined for non-numeric or
    out-of-range input. NEVER throw — match the no-throw `string | undefined` contract of the sibling
    `componentsToUtcIso` [src: src/time/components-to-utc-iso.ts]. Use a strict integer-string guard so
    coercion artifacts ("12abc", "1.5e9", " 100 ", "+100", "1e3") are rejected, not silently accepted —
    they are out-of-contract source ids that must fall through to the filename/listing fallbacks, not
    ship a bogus timestamp. Define the two range bounds as named constants (UPPER_SNAKE_CASE) with a
    one-line comment explaining the chosen window [std: §C]. No `any`, no `as`, no non-null assertions;
    `no-magic-numbers` — the `1000` ms factor and the bounds are named constants. Doc-comment explains
    WHY (epoch is the only true-UTC instant; range guard rejects non-epoch `derived:` ids) — not WHAT.
    Write the unit test first (RED) covering the full <behavior> matrix as a `test.each` table
    [std: §G], including both range boundaries and the coercion-rejection cases, then implement to
    GREEN. The boundary cases must exercise BOTH branches of every guard so 100% V8 branch coverage
    holds with NO new `v8 ignore` [src: solidstats-fetcher-ts-tests#coverage-gate].
    Do NOT export the bounds or any helper beyond `epochToUtcIso` (single public symbol).
    Do NOT decode replay bytes — this reads only the source id string (no-parsing invariant intact).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/time/epoch-to-utc-iso.test.ts</automated>
  </verify>
  <done>
    `epochToUtcIso` exists in src/time/, returns the ISO-UTC string for in-range numeric epochs and
    undefined for non-numeric/out-of-range/coercion-artifact input, never throws; its unit test passes
    with both arms of every guard exercised (100% branch coverage of the new file, no `v8 ignore`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make epoch the PRIMARY replayTimestamp source in payload.ts (filename → listing fallbacks)</name>
  <read_first>
    - src/staging/payload.ts — lines 41-51 (`toSourceReplayId` reads `evidence.source.externalId`),
      96-98 (the `replayTimestamp` ?? chain), 141-149 (externalId already persisted to
      `promotion_evidence.sourceExternalId`). Confirms externalId is in scope — NO threading needed.
    - src/staging/payload.test.ts — the existing `replayTimestamp` precedence `test.each` (lines
      108-142) and absent-arm `test.each` (144-182); these must be UPDATED, not merely appended to,
      because epoch now wins over filename for the builder's default fixture.
    - .agents/skills/solidstats-fetcher-ts-conventions/SKILL.md §B (write-scope/no-parsing invariants;
      auditable evidence) and §A bands (staging→time is downward, allowed).
    - .agents/skills/solidstats-fetcher-ts-tests/SKILL.md (per-area map: payload precedence is a
      unit test; 100% reachable-source gate, no new `v8 ignore`).
  </read_first>
  <files>src/staging/payload.ts, src/staging/payload.test.ts</files>
  <behavior>
    - externalId is an in-range epoch → replayTimestamp is the epoch-derived UTC (beats filename AND listing).
    - externalId is a non-epoch/out-of-range id but filename carries a timestamp → replayTimestamp is the filename value.
    - externalId is non-epoch AND filename has no timestamp but listing discoveredAt is present → replayTimestamp is the listing value.
    - externalId is non-epoch, filename has no timestamp, no listing discoveredAt → replayTimestamp absent (payload still stageable).
    - `derived:` source identity (no externalId) → epoch arm yields undefined → filename/listing fallbacks apply exactly as today.
    - promotion_evidence.discoveredAt and sourceExternalId are unchanged (audit fields).
  </behavior>
  <action>
    Import `epochToUtcIso` from `../time/epoch-to-utc-iso.js` (mirrors the existing
    `componentsToUtcIso` import [src: src/staging/payload.ts#L3]). In `basePayload`, change the
    `replayTimestamp` derivation [src: src/staging/payload.ts#L96-L98] to the LOCKED precedence
    [src: CONTEXT.md#decisions]:
    epoch FIRST, then filename, then listing —
    `epochToUtcIso(externalId) ?? replayTimestampFromFilename(evidence.sourceFilename) ?? evidence.discoveredAt`
    — where `externalId` comes from `evidence.source.externalId` (already in scope; pass the value, and
    when it is undefined the epoch arm must be undefined too — call only with a string, e.g. guard
    `evidence.source.externalId !== undefined ? epochToUtcIso(evidence.source.externalId) : undefined`,
    matching the existing `toSourceReplayId` shape [src: src/staging/payload.ts#L42]). Keep
    `replayTimestampFromFilename` and the `?? evidence.discoveredAt` listing fallback intact — DO NOT
    delete or "fix" them with a ±offset; epoch supersedes [src: RESEARCH.md#approach]. Add a concise
    code comment on the chain documenting that filename and listing are KNOWN server-local-TZ fallbacks
    that only fire for id-less / non-epoch candidates (WHY, not WHAT) — but do NOT inline any literal
    that a negative grep gate keys on. Leave `discoveredAt` and `sourceExternalId` promotion-evidence
    handling untouched (audit). No `any`/`as`/`!`.

    UPDATE `payload.test.ts` (RED first, then GREEN):
    - The typed builder `createStoredEvidence` currently defaults `source.externalId = "1778269931"`
      (an epoch) and asserts `replayTimestamp: filenameTimestamp` — under epoch-primary this default
      now yields the EPOCH value, not the filename value. Update the affected assertions to the
      epoch-derived UTC for "1778269931" (compute it; do not hand-wave) so the default-fixture case
      proves epoch-primary, and ADD a dedicated case proving epoch beats BOTH a present filename
      timestamp and a present listing discoveredAt.
    - Restructure the precedence `test.each` into the four-rung matrix in <behavior>: epoch-wins,
      filename-fallback (non-epoch id), listing-fallback (non-epoch id + no filename ts), absent
      (non-epoch id + neither). Use a non-epoch / `derived:` id (e.g. drop externalId, or use an
      out-of-range/non-numeric one) to reach the fallback rungs — do NOT rely on a numeric id for a
      filename/listing assertion, or the epoch arm will win and the test will assert the wrong thing.
    - Keep the existing `discoveredAt`/`sourceExternalId`/userinfo-stripping/non-stageable tests green.
    Hold 100% V8 (both arms of the new precedence branch tested); no new `v8 ignore`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/staging/payload.test.ts && pnpm run depcruise && pnpm run knip</automated>
  </verify>
  <done>
    payload.ts derives replayTimestamp as epoch → filename → listing; payload.test.ts proves all four
    precedence rungs (epoch-wins incl. over a present filename+listing, filename-fallback,
    listing-fallback, absent) plus the `derived:`/non-epoch fallthrough; depcruise + knip green; audit
    fields unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip the golden-oracle replay_timestamp assertion to the epoch-derived UTC value</name>
  <read_first>
    - src/run/golden-e2e.integration.test.ts — lines 174-225 (the per-row assertion loop; the
      StagingRow type at 29-48 does NOT yet select `replay_timestamp`; the discoveredAt comment block
      at 212-224 claims "replay_timestamp stays filename-derived" — now FALSE).
    - src/run/fixtures/golden/manifest.json — `expectedExternalIds` are all numeric epochs (90/90);
      `source_replay_id` for each staged corpus row IS that epoch.
    - .agents/skills/solidstats-fetcher-ts-tests/SKILL.md (oracle strength; UPDATE not loosen).
  </read_first>
  <files>src/run/golden-e2e.integration.test.ts</files>
  <behavior>
    - Run 1: every staged row's replay_timestamp equals new Date(Number(source_replay_id)*1000).toISOString()
      (epoch-derived, since all corpus ids are in-range numeric epochs).
    - The discoveredAt audit assertion still holds (discoveredAt unchanged).
    - Run 2 idempotency assertions unchanged.
  </behavior>
  <action>
    Add `replay_timestamp` to the `select` [src: src/run/golden-e2e.integration.test.ts#L176] and to
    the `StagingRow` type (a `string` column, NOT inside promotion_evidence). In the per-row loop,
    ASSERT (do not loosen) that `row.replay_timestamp` equals the epoch-derived UTC for that row:
    `new Date(Number(row.source_replay_id) * 1000).toISOString()` — this is deterministic because every
    corpus id is an in-range numeric epoch [src: src/run/fixtures/golden/manifest.json#expectedExternalIds].
    This is the behavior-preservation-gate UPDATE for THIS intentional change — a concrete value match,
    not a shape regex. REPLACE the stale comment block [src: src/run/golden-e2e.integration.test.ts#L212-216]
    that asserts "replay_timestamp stays filename-derived across this corpus" with one stating
    replay_timestamp is now epoch-derived (true UTC), and that the filename/listing fallbacks are proven
    by the payload unit test (not exercisable here — the corpus has no non-epoch id). KEEP the
    `discoveredAt` audit assertions [src: src/run/golden-e2e.integration.test.ts#L217-224] intact —
    discoveredAt is unchanged. Do NOT change run-2 idempotency assertions. Do NOT decode replay bytes.
    The golden corpus IS present locally, so this test runs (not skipped) under test:integration.
  </action>
  <verify>
    <automated>pnpm run test:integration -- src/run/golden-e2e.integration.test.ts</automated>
  </verify>
  <done>
    The golden oracle selects and asserts the concrete epoch-derived replay_timestamp per row, the
    stale "filename-derived" comment is corrected, discoveredAt + idempotency assertions still pass, and
    the integration test is GREEN (updated, not loosened).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external source → fetcher | `externalId` (`/replays/{id}`) is attacker-influenceable source data parsed into a `timestamptz` written to a cross-service field. |
| fetcher staging → server-2 | `replayTimestamp` value is read by `server-2`; a bogus value corrupts displayed game times. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qj5-01 | Tampering | `epochToUtcIso` parsing an untrusted source id | mitigate | Strict integer-string guard + epoch range window (2015..2035); non-numeric/out-of-range/coercion-artifact input → undefined → falls through to fallbacks. Never throws (no DoS via parse exception). |
| T-qj5-02 | Information disclosure | replay byte contents | accept | No code path decodes bytes; only the source id string is read — no-parsing invariant intact (depcruise `no-replay-parser` fence already enforces). |
| T-qj5-03 | Tampering | staging schema / replay_timestamp field | accept | Value-only change, same field/type — NOT a schema/DDL change; no cross-app schema protocol triggered. server-2 owns reads + any backfill (out of scope). |
</threat_model>

<verification>
- `pnpm run verify` exits 0 (format → lint → typecheck → unit → 100% V8 coverage → build → depcruise → knip).
- `pnpm run test:integration` golden end-to-end oracle GREEN with the flipped, concrete epoch-derived
  `replay_timestamp` assertion (the behavior-preservation gate, UPDATED for this intentional change).
- No new `/* v8 ignore */` added anywhere; both arms of every new guard/branch tested.
- depcruise green: `staging/` → `time/` is a downward cross-cutting import; no new fence violation.
- No-parsing invariant intact: no module imports an OCAP parser or decodes replay bytes.
</verification>

<success_criteria>
- Staged `replayTimestamp` is the `externalId` epoch-derived ISO UTC when the id is an in-range epoch,
  and falls through filename → listing only for non-epoch/id-less candidates; epoch parser never throws.
- `promotion_evidence.discoveredAt` and `sourceExternalId` audit fields are byte-for-byte unchanged.
- Full unit coverage of the epoch matrix and the four precedence rungs; golden oracle asserts the
  concrete epoch UTC value per corpus row.
- `pnpm run verify` and `pnpm run test:integration` both green; worktree committed clean.
</success_criteria>

<output>
Create `.planning/quick/260623-qj5-fix-replay-timestamp-source-use-external/260623-qj5-SUMMARY.md` when done.
</output>
