---
phase: 26-test-quality-pass-correctness-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/watch.ts
  - src/commands/run-once.ts
  - src/commands/discover.ts
  - src/errors/invariant-violation-error.ts
  - src/config.ts
  - src/config.test.ts
  - src/run/run-once-summary.ts
autonomous: true
requirements: [CORR-01]
must_haves:
  truths:
    - "No raw `new Error(` is thrown from the three composition guards (watch/run-once/discover requireStagingRepository + stageRawEvidence) — each raises a typed AppError subclass on its unreachable branch"
    - "config.ts sourceTransportOrUndefined no longer uses a blind `as SourceTransport` cast — the returned value is membership-checked against the SourceTransport union"
    - "An invalid source transport string is rejected (returns undefined / fails Zod) rather than silently typed as a valid SourceTransport"
    - "The two evidence-write swallow sites in run-once-summary.ts log the error object under the pino `err` key (§AA traceback preserved), not just an event discriminator"
    - "No RESEARCH-rejected false-positive site is modified (config.ts:25 Zod transform, source/retry.ts toAbortError, source-client.ts Cloudflare cause-chain, postgres `as DatabaseError` narrowing, object-key value-object guards, s3-raw-storage swallow all left as-is)"
  artifacts:
    - path: "src/errors/invariant-violation-error.ts"
      provides: "Typed AppError subclass for provably-unreachable composition invariants (isOperational:false → exit 1)"
      contains: "extends AppError"
    - path: "src/config.ts"
      provides: "Membership-checked SourceTransport resolution (no blind cast)"
    - path: "src/run/run-once-summary.ts"
      provides: "§AA-compliant evidence-write swallow logging with { err }"
  key_links:
    - from: "src/commands/watch.ts"
      to: "src/errors/invariant-violation-error.ts"
      via: "requireStagingRepository throws the typed invariant error instead of raw Error"
      pattern: "InvariantViolationError|new (ConfigValidationError|AppError)"
    - from: "src/config.ts"
      to: "src/types/source-transport.ts"
      via: "runtime membership check against the SourceTransport union literals"
      pattern: "=== \"direct\"|=== \"ssh\"|includes\\("
---

<objective>
CORR-01: починить только **живо-подтверждённые** correctness-находки, ничего из RESEARCH-rejected false-positives не трогая. Три класса правок:

1. **W-02 (класс из 3 сайтов)** — raw `new Error(` на v8-ignored unreachable composition-guard'ах в `commands/{watch,run-once,discover}.ts` → типизированный `AppError`-подкласс. Чинится весь класс, не один сайт (CLAUDE.md «fix the class, not the line»).
2. **config.ts:197** — blind `value as SourceTransport` cast → runtime membership-check против union (`solidstats-shared-ts-standards §B` — no unexplained `as`; security V5 Tampering).
3. **§AA traceback** — два evidence-write swallow'а в `run-once-summary.ts` логируют без `{ err }` → передать error-объект (`[std: correctness §AA]` Traceback preserved; security V7).

Plus I-01 doc-only note (см. task 1).

Purpose: повысить корректность источника с zero false-positive churn; ни одна правка не расширяет ingest write-scope и не меняет staging-контракт.
Output: типизированные guard'ы, валидированный transport, диагностируемые swallow-логи. Behavior-preserving — golden oracle и 100% coverage держатся.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-test-quality-pass-correctness-hygiene/26-RESEARCH.md

Skill rules this plan must honor (read in full before editing source):
@.agents/skills/solidstats-shared-backend-ts-standards/SKILL.md
@.agents/skills/solidstats-shared-backend-ts-standards/references/correctness-and-quality.md
@.agents/skills/solidstats-shared-ts-standards/SKILL.md
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md
</context>

<premises>
premises:
  - claim: W-02 is a 3-site class — raw `new Error(` on v8-ignored unreachable guards in watch.ts:15, run-once.ts:53, discover.ts:100
    src: 26-RESEARCH.md#Research-Question-2
    verify: "grep -rn 'new Error(' src/commands/{watch,run-once,discover}.ts"
  - claim: all three guards are genuinely unreachable — stagingRepository is undefined iff shouldStage===false, and all three callers pass { shouldStage: true }
    src: src/commands/store-raw-resources.ts#L57
    verify: "grep -n 'shouldStage: true' src/commands/{watch,run-once,discover}.ts"
  - claim: stagingRepository optionality is structurally tied to a runtime boolean (shouldStage), so pure type-narrowing that deletes the guard would require restructuring the shared StoreRawResources contract — typed-invariant fallback is the proportionate fix
    src: src/commands/store-raw-resources.ts#L76
    verify: "grep -n 'stagingRepository' src/commands/store-raw-resources.ts src/commands/shared.ts"
  - claim: config.ts:197 returns `value as SourceTransport` with no membership check; the value is only validated later by the downstream z.enum at safeParse time, so the helper's return type is a lie until then
    src: src/config.ts#L197
    verify: "grep -n 'as SourceTransport' src/config.ts"
  - claim: SourceTransport is the closed union \"direct\" | \"ssh\"
    src: src/types/source-transport.ts#L1
    verify: "cat src/types/source-transport.ts"
  - claim: run-once-summary.ts has two evidence-write swallow blocks (bare `catch {`) that log a warn WITHOUT the error object
    src: src/run/run-once-summary.ts#L170
    verify: "grep -n 'catch {' src/run/run-once-summary.ts"
  - claim: s3-raw-storage.ts swallow at the catch has NO logger in scope — adding a log would require injecting a logger (scope creep), so it stays as-is (A2 resolved to leave)
    src: src/storage/s3-raw-storage.ts#L132
    verify: "grep -c 'log\\|Logger' src/storage/s3-raw-storage.ts"
  - claim: object-key value-object guards, config.ts:25 Zod transform, source/retry.ts toAbortError, source-client Cloudflare cause-chain are RESEARCH false-positives — DO NOT modify
    src: 26-RESEARCH.md#Correctness-Hygiene-Verified-Findings
    verify: "grep -rn 'new Error(' src/storage/object-key.ts src/source/retry.ts src/config.ts"

carried_forward:
  - claim: W-02 + I-01 deferred from Phase 20 review; W-02 line numbers predate Phase 22 splits and were re-verified live (watch:15, run-once:53, discover:100)
    src: 26-RESEARCH.md#Research-Question-2
  - claim: convention-audit semantic tier ~50% false-positive (Haiku-verified) — only REAL verdicts from RESEARCH tables get committed
    src: .planning/STATE.md#v3.1-Open-Gates

graph_note:
  - claim: CORR-01 touches graph communities 14 (Configuration Management), 1 (CLI Command Registration), 2/31 (Run Summary Assembly) — no downstream cross-band dependents; blast radius is contained and behavior-preserving
    src: graph:Configuration Management
  - claim: .planning/graphs/graph.json is STALE (built 2026-06-20, before Phase 22/24/25 splits) — treat community membership as approximate, line numbers re-verified live above
    src: .planning/graphs/graph.json
</premises>

<tasks>

<task type="auto">
  <name>Task 1: Add typed invariant-violation error + fix the W-02 guard class (3 sites) + I-01 doc note</name>
  <read_first>
    - src/commands/watch.ts (full — requireStagingRepository at lines 10-19; flushLogger-in-try at line 129 for I-01)
    - src/commands/run-once.ts (lines 48-57 — requireStagingRepository)
    - src/commands/discover.ts (lines 93-104 — stageRawEvidence guard)
    - src/commands/store-raw-resources.ts (full — confirms stagingRepository undefined iff shouldStage===false)
    - src/errors/app-error.ts (full — AppError<Code> abstract base: constructor signature, isOperational, details)
    - src/errors/config-validation-error.ts (full — reference subclass shape: constructor → super(code, message, options))
    - src/errors/checkpoint-conflict-error.ts (full — second reference subclass)
    - .agents/skills/solidstats-shared-backend-ts-standards/SKILL.md §B (typed error system)
  </read_first>
  <action>
    Create `src/errors/invariant-violation-error.ts`: a typed `AppError`-подкласс для provably-unreachable composition invariants. Class `InvariantViolationError` extends `AppError<"invariant_violation">`; constructor takes `details?: Readonly<Record<string, unknown>>` and passes `super("invariant_violation", <static message>, { details, isOperational: false })`. `isOperational: false` маркирует programmer-bug → CLI boundary мапит на exit 1 (НЕ exit 2 — это не config-ошибка) `[src: 26-RESEARCH.md#Typed-Error-Hierarchy]`. `details` — только identifiers (никаких bytes/secrets), per app-error.ts doc `[std: §B]`. Follow the exact subclass shape of `config-validation-error.ts` (same file layout, same `extends AppError<...>` generic, same doc-comment style).

    Replace the raw `throw new Error(...)` in ALL THREE guards with `throw new InvariantViolationError({ guard: "<name>" })`:
    - `src/commands/watch.ts:15` (requireStagingRepository) — `{ guard: "requireStagingRepository", command: "watch" }`
    - `src/commands/run-once.ts:53` (requireStagingRepository) — `{ guard: "requireStagingRepository", command: "run-once" }`
    - `src/commands/discover.ts:100` (stageRawEvidence) — `{ guard: "stageRawEvidence", command: "discover" }`

    KEEP the existing `/* v8 ignore next ... -- ... */` comment on each guard's `if (repository === undefined)` branch UNCHANGED — the branch is still structurally unreachable (RESEARCH premise: shouldStage===true for all callers), so the ignore is correct and is NOT a new suppression (TEST-05 forbids only NEW ignores). Do NOT attempt to delete the guard via type-narrowing: `stagingRepository` optionality is tied to the runtime `shouldStage` boolean on the shared `StoreRawResources` contract `[src: src/commands/store-raw-resources.ts#L76]`; narrowing it away would require a discriminated return type on the composition root — out of scope and behavior-touching.

    I-01 (doc-only, same file): above the `try { ... await flushLogger(rootLogger); ... } finally { await resources.dispose(); ... }` block in `watch.ts` (around line 99), add a one-line WHY comment documenting that `flushLogger` runs inside the `try` deliberately so a flush rejection still runs `dispose()` in `finally` then propagates uncaught to the CLI boundary (mirrors run-once) — to prevent a future silent-swallow regression. English only, explains WHY not WHAT `[std: correctness §Comments]`. Do NOT change any behavior.

    Update `src/index.ts` only if it re-exports the public error surface and the new error belongs there (check existing pattern; if errors are not re-exported from index, do not add).
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/cli.test.ts src/errors && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'new Error(' src/commands/{watch,run-once,discover}.ts` returns ZERO matches.
    - `grep -rn 'InvariantViolationError' src/commands/{watch,run-once,discover}.ts` returns exactly 3 throw sites.
    - `src/errors/invariant-violation-error.ts` exists and contains `extends AppError<"invariant_violation">` and `isOperational: false`.
    - The three `/* v8 ignore` comments on the guard branches are still present (count unchanged): `grep -c 'v8 ignore' src/commands/watch.ts` == 1, run-once.ts == 1 (the guard one), discover.ts == 1 (the guard one).
    - `pnpm run test:coverage` exits 0 with 100% (statements/branches/functions/lines).
    - A WHY comment about flushLogger-in-try is present near watch.ts:99-130.
  </acceptance_criteria>
  <done>The W-02 class is fully fixed with one typed error; no raw Error in the three guards; v8-ignores preserved (not net-new); coverage 100%; I-01 documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace config.ts:197 blind cast with SourceTransport membership check + invalid-input test</name>
  <read_first>
    - src/config.ts (lines 1-10, 76-135 schema incl. `sourceTransport: z.enum(["direct","ssh"])` at 92; lines 180-230 — stringOrUndefined + sourceTransportOrUndefined + readSourceConfigInput)
    - src/types/source-transport.ts (full — `type SourceTransport = "direct" | "ssh"`)
    - src/config.test.ts (lines 40-75, 250-275, 400-405 — existing sourceTransport direct/ssh/empty cases the fix must keep green)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §B (no unexplained `as`)
  </read_first>
  <behavior>
    - Test: a known transport string ("direct") → sourceTransportOrUndefined returns "direct" (unchanged behavior).
    - Test: a known transport string ("ssh") → returns "ssh".
    - Test: an empty/whitespace string → returns undefined (unchanged).
    - Test: an UNKNOWN string (e.g. "ftp") → returns undefined (NEW behavior: invalid value is not asserted as a valid SourceTransport). Assert via loadSourceConfig: an unknown source transport env value is rejected by the downstream z.enum and surfaces a ConfigValidationError, OR sourceTransportOrUndefined returns undefined and the z.enum default("direct") applies — match whichever the chosen implementation produces, and assert it explicitly (no blind-pass).
  </behavior>
  <action>
    In `src/config.ts`, rewrite `sourceTransportOrUndefined` (lines 190-198) to remove the `value as SourceTransport` cast `[src: src/config.ts#L197]`. Validate union membership at runtime before returning the narrowed type. Two acceptable forms (Claude's discretion):
    (a) compare against the `SourceTransport` union literals directly — `if (value === "direct" || value === "ssh") return value;` then `return undefined` — TypeScript narrows `value` to `SourceTransport` inside the guard with NO cast; OR
    (b) derive the allowed set from a single `as const` tuple of the union members and use `.includes()` with a type predicate helper.
    Prefer (a) for two members — minimal, no new const, no `as`. The function signature stays `(value: boolean | string | undefined) => SourceTransport | undefined`. An unknown non-empty string now returns `undefined` instead of being mis-asserted, so the downstream `z.enum(["direct","ssh"]).default("direct")` at line 92 governs the final value honestly. Do NOT change the schema, the redaction helper, or any other config code.

    Add the membership literals as the source of truth for the comparison — if form (a), no new symbol; if form (b), a single `const SOURCE_TRANSPORTS = ["direct", "ssh"] as const` co-located in config.ts. Either way: zero `as SourceTransport` remains.

    In `src/config.test.ts`, add an invalid-transport case proving the new behavior (an unknown transport string does not pass through as a valid SourceTransport). Use the existing test style in this file (it already uses `test.each` for reject-cases at lines 150/188 — match that idiom if adding multiple invalid values). Keep all existing sourceTransport tests green.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/config.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'as SourceTransport' src/config.ts` returns ZERO matches.
    - `src/config.test.ts` has a case asserting an unknown/invalid source transport string is NOT returned as a valid SourceTransport (rejected or defaulted, explicitly asserted).
    - All pre-existing sourceTransport tests (direct/ssh/empty → default) still pass.
    - `pnpm run test:coverage` exits 0 at 100%.
  </acceptance_criteria>
  <done>config.ts validates SourceTransport membership instead of blind-casting; invalid input is provably rejected/defaulted; coverage 100%.</done>
</task>

<task type="auto">
  <name>Task 3: §AA traceback fix — pass { err } in the two run-once-summary evidence-write swallows</name>
  <read_first>
    - src/run/run-once-summary.ts (lines 154-195 — writeEvidence with two `catch {` blocks at ~170 and ~188)
    - src/run/run-once.test.ts (the `evidence_write_failed` cases — the only test file covering these paths)
    - src/commands/store-raw-resources.ts (lines 26-37 — reference pattern: `catch (error) { log.warn({ err: error }, "...") }` — the exact §AA idiom already used in this repo)
    - .agents/skills/solidstats-shared-backend-ts-standards/references/correctness-and-quality.md §AA (Traceback preserved; Swallowed errors)
  </read_first>
  <action>
    In `src/run/run-once-summary.ts`, both evidence-write swallow blocks currently read `} catch { input.log?.warn({ event: "evidence_write_failed", runId: input.runId }, "...") }` `[src: src/run/run-once-summary.ts#L170]`. Change each to bind the caught error and pass it under the pino `err` key: `} catch (error) { input.log?.warn({ err: error, event: "evidence_write_failed", runId: input.runId }, "<same message>") }`. Use catch-binding name `error` (the repo's unicorn/catch-error-name convention — see store-raw-resources.ts) and log key `err` (pino serializes the stack). Keep BOTH messages, the `event` discriminator, the `runId`, and the swallow-and-continue behavior byte-identical otherwise — this is a §AA Traceback-preserved fix, NOT a behavior change (the run still does not fail on evidence-write error). Apply to BOTH catch blocks (the S3 evidenceStore write and the dev-only file write) — fix the class, not one site.

    Do NOT touch any other swallow in the repo (checkpoint parseJsonOrUndefined, html URL-resolve, contract-check isJson, payload URL credential-strip, s3-raw-storage:132) — all RESEARCH false-positives or no-logger-in-scope `[src: 26-RESEARCH.md#Correctness-Hygiene-Verified-Findings]`.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/run/run-once.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - Both `catch` blocks in `writeEvidence` bind `(error)` and include `err: error` in the warn payload: `grep -c 'err: error' src/run/run-once-summary.ts` >= 2.
    - Zero bare `} catch {` remain in `writeEvidence`: the two evidence-write catches are now `catch (error)`.
    - The `evidence_write_failed` event discriminator, messages, and runId are unchanged; the run still does not fail on an evidence-write rejection.
    - `pnpm test -- src/run/run-once.test.ts` passes; `pnpm run test:coverage` exits 0 at 100%.
  </acceptance_criteria>
  <done>Both evidence-write swallows preserve the traceback (§AA); no other swallow touched; behavior and coverage unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | Untrusted env strings parsed into the validated AppConfig (SourceTransport) |
| internal composition guard | Programmer-invariant boundary (staging repository presence) — not external input |
| error details / logs → operator | Error `details` and log fields surface to the operator/run summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | config.ts sourceTransportOrUndefined | mitigate | Replace blind `as SourceTransport` with runtime union-membership check so an invalid transport cannot masquerade as valid (ASVS V5) — Task 2 |
| T-26-02 | Information disclosure | InvariantViolationError.details / run-once-summary warn `{ err }` | mitigate | `details` carries identifiers only (guard/command names, runId) — no bytes, no secrets, per app-error.ts doc; `{ err }` serializes a stack of an internal/evidence error, no payload bytes (ASVS V7) — Tasks 1, 3 |
| T-26-03 | (n/a — no endpoints, no write-scope change) | — | accept | Phase adds no endpoints, no S3/staging write-path change; attack surface unchanged |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — phase works with the existing toolchain (RESEARCH § Standard Stack: "Установка пакетов НЕ требуется") |
</threat_model>

<verification>
After EACH task commit (per-change behavior-preservation gate):
- `pnpm test` (unit) green
- `pnpm run test:coverage` — 100% statements/branches/functions/lines, NO new `v8 ignore` added
After the plan (wave-merge gate, before phase close):
- `pnpm run verify` (format:check → lint → typecheck → test → coverage → build → depcruise → knip) exits 0
- `pnpm run test:integration` — golden run-once oracle (`src/run/golden-e2e.integration.test.ts`) green, byte-stable (no behavior change)
</verification>

<success_criteria>
- Zero raw `new Error(` in the three composition guards; one typed `InvariantViolationError` (exit-1 semantics) used instead.
- Zero `as SourceTransport` in config.ts; invalid transport provably rejected/defaulted.
- Both evidence-write swallows preserve the traceback via `{ err }`.
- No RESEARCH-rejected false-positive modified; no new `v8 ignore`.
- golden oracle + 100% coverage + depcruise + knip green.
</success_criteria>

<output>
Create `.planning/phases/26-test-quality-pass-correctness-hygiene/26-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New public symbols created by this plan:
- `InvariantViolationError` (class) — `src/errors/invariant-violation-error.ts`, code literal `"invariant_violation"`, `isOperational: false` (exit 1). Used by the three command-band composition guards.
- (form (b) only, optional) `SOURCE_TRANSPORTS` const tuple in `src/config.ts` — internal, only if the executor picks the `.includes()` membership form; form (a) adds no symbol.

No other public symbols are added or renamed. All remaining work is in-place edits to existing functions.
