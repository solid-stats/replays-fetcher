---
phase: 26-test-quality-pass-correctness-hygiene
plan: 04
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/run/ingest-page.test.ts
  - src/run/run-once.test.ts
autonomous: true
requirements: [TEST-04, TEST-05, TEST-01]
must_haves:
  truths:
    - "No real wall-clock `setTimeout`-as-sleep remains in ingest-page.test.ts or run-once.test.ts — out-of-order async completion is forced deterministically (manual promise ordering or vi fake timers), not by a 10ms wall-clock delay"
    - "The out-of-order ordering tests still prove the same ordering behavior they proved before — the determinism change preserves the assertion"
    - "Duplicated inline literals in these two suites (checksum/sourceFilename/object_key) are factored into shared builders/constants (TEST-01) where they repeat 3+ times"
    - "The v8-ignore reachability sweep confirms all surviving ignore sites are structurally unreachable with a reason; NO new `v8 ignore` is added; the three W-02 guard ignores remain (fixed-but-still-unreachable from plan 26-01)"
  artifacts:
    - path: "src/run/ingest-page.test.ts"
      provides: "deterministic out-of-order ordering tests with no wall-clock sleep"
      contains: "test"
  key_links:
    - from: "src/run/ingest-page.test.ts"
      to: "src/run/ingest-page.ts"
      via: "ordering behavior asserted via deterministic completion control"
      pattern: "ingestPage|out-of-order"
---

<objective>
TEST-04 + TEST-05 + TEST-01 на `run/ingest-page.test.ts` и `run/run-once.test.ts`:

1. **TEST-04** — реальные wall-clock `setTimeout(resolve, OUT_OF_ORDER_DELAY_MS=10)` / `setTimeout(resolve, 1)`, форсирующие out-of-order async completion, → детерминированный механизм упорядочивания (manual deferred promises ИЛИ `vi.useFakeTimers()` + `advanceTimersByTime`) `[src: 26-RESEARCH.md#TEST-04]`. Никаких wall-clock sleeps `[std: §G deterministic time]`.
2. **TEST-01** — дублированные inline-литералы (checksum/sourceFilename/object_key) в этих двух suite → shared builders/constants где повторяются 3+ раз (rule of three) `[std: §G typed builders]`.
3. **TEST-05** — ревизия v8-ignore reachability: подтвердить leave-вердикты по 21 unreachable-сайту, убедиться что НИ ОДНОГО нового ignore не добавлено рефактором, и что три W-02 guard-ignore остались (plan 26-01 их сохранил — ветка всё ещё unreachable) `[src: 26-RESEARCH.md#v8-ignore-Reachability-Inventory]`.

КРИТИЧНО (Pitfall 3): `run/watch-loop.test.ts` УЖЕ использует injected `sleep` seam (детерминированно) — НЕ переписывать его на fake-timers. TEST-04 целит только реальные wall-clock setTimeout в ingest-page/run-once.
КРИТИЧНО (Pitfall 4): coverage уже 100% — SC#3 = ревизия существующих ignore, НЕ выдумывание net-new покрытия.

Purpose: убрать недетерминизм/flakiness из ordering-тестов и подтвердить корректность ignore-сайтов без нового suppression.
Output: deterministic ordering tests, builder-deduped literals, verified ignore inventory; coverage 100% held.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-test-quality-pass-correctness-hygiene/26-RESEARCH.md

Skill rules this plan must honor (read in full before editing tests):
@.agents/skills/solidstats-fetcher-ts-tests/SKILL.md
@.agents/skills/solidstats-shared-ts-standards/SKILL.md
</context>

<premises>
premises:
  - claim: ingest-page.test.ts uses real wall-clock setTimeout with OUT_OF_ORDER_DELAY_MS=10 at lines 122/125 and 243 to force out-of-order completion
    src: src/run/ingest-page.test.ts#L14
    verify: "grep -n 'setTimeout\\|OUT_OF_ORDER_DELAY_MS' src/run/ingest-page.test.ts"
  - claim: run-once.test.ts uses real wall-clock setTimeout (OUT_OF_ORDER_DELAY_MS=10 at 1424; setTimeout(resolve,1) at 1520)
    src: src/run/run-once.test.ts#L30
    verify: "grep -n 'setTimeout\\|OUT_OF_ORDER_DELAY_MS' src/run/run-once.test.ts"
  - claim: watch-loop.test.ts ALREADY uses an injected fake sleep seam (sleepCalls) — TEST-04 compliant; must NOT be rewritten to vi.useFakeTimers
    src: src/run/watch-loop.test.ts
    verify: "grep -c 'sleepCalls\\|sleep:' src/run/watch-loop.test.ts"
  - claim: coverage is already 100% (gate in vitest.config.ts) — there are NO net-new uncovered reachable branches; SC#3 is a revision of existing v8-ignore sites
    src: 26-RESEARCH.md#v8-ignore-Reachability-Inventory
    verify: "grep -n 'thresholds\\|100' vitest.config.ts"
  - claim: 24 v8-ignore sites enumerated; 21 are genuinely-unreachable defensive guards / production default seams that must stay; the 3 W-02 guard ignores stay (branch still unreachable after the 26-01 typed-error fix)
    src: 26-RESEARCH.md#v8-ignore-Reachability-Inventory
    verify: "grep -rcn 'v8 ignore' src --include='*.ts' | grep -v ':0'"
  - claim: cli.test.ts Date.now() in tmp-path and replay-byte-client.test.ts new Promise(reject) are NOT sleeps — false-positives for TEST-04, do not touch
    src: 26-RESEARCH.md#TEST-04
    verify: "grep -n 'Date.now' src/cli.test.ts | head"

carried_forward:
  - claim: TEST-05 forbids NEW v8 ignore; if a refactor opens a branch, write a test — never add an ignore (Pitfall 5)
    src: 26-RESEARCH.md#Common-Pitfalls
  - claim: plan 26-01 keeps the three W-02 guard v8-ignores in place (typed error swapped in, branch still structurally unreachable) — this plan must confirm they survived, not flag them as new
    src: 26-RESEARCH.md#Research-Question-2
</premises>

<!-- planner-discipline-allow: setTimeout -->
<!-- planner-discipline-allow: OUT_OF_ORDER_DELAY_MS -->

<tasks>

<task type="auto">
  <name>Task 1: Replace wall-clock out-of-order sleeps with deterministic ordering (TEST-04)</name>
  <read_first>
    - src/run/ingest-page.test.ts (full — OUT_OF_ORDER_DELAY_MS const at 14; the two out-of-order tests using setTimeout at 122-126 and 242-243)
    - src/run/run-once.test.ts (lines 25-35 for the const; lines ~1415-1430 and ~1515-1525 for the two setTimeout sites)
    - src/run/watch-loop.test.ts (the injected-sleep-seam pattern — reference for how this repo does deterministic timing; do NOT edit this file)
    - src/run/ingest-page.ts (ingestPage signature — to understand how completion order is observed, so the deterministic mechanism asserts the same thing)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §G (deterministic time: vi.useFakeTimers / no real sleep)
  </read_first>
  <action>
    Remove the real wall-clock delays that force out-of-order async completion in both files `[src: src/run/ingest-page.test.ts#L14]` `[src: src/run/run-once.test.ts#L30]`. Replace with a deterministic ordering mechanism. Two acceptable forms (Claude's discretion — pick the one that keeps the test most legible):
    (a) **Manual deferred promises** — create explicit `Deferred` handles (`{ promise, resolve }`) per in-flight item and resolve them in the exact order the test wants to assert, so completion order is controlled with zero timers; OR
    (b) **`vi.useFakeTimers()`** in the test, keep `setTimeout` but advance with `vi.advanceTimersByTimeAsync(...)` to flush completions in a controlled order, with `vi.useRealTimers()` in teardown.

    Prefer (a) where the test only needs "B finishes before A" (no real timer semantics under test) — it is the strictest determinism and removes the 10ms wall-clock entirely. Delete the now-unused `OUT_OF_ORDER_DELAY_MS` consts. The tests must still assert the SAME ordering behavior (out-of-order completion handled correctly by ingestPage / run-once) — the mechanism changes, the asserted behavior does not.

    Do NOT touch `watch-loop.test.ts` (already deterministic via injected sleep seam — Pitfall 3). Do NOT touch `cli.test.ts` Date.now() tmp-paths or `replay-byte-client.test.ts` reject-promises (TEST-04 false-positives). Do NOT modify production code.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/run/ingest-page.test.ts src/run/run-once.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setTimeout' src/run/ingest-page.test.ts` == 0 (or, if form (b) chosen, every remaining setTimeout is paired with `vi.useFakeTimers`/`advanceTimersByTime` — no bare real-clock sleep).
    - `grep -c 'setTimeout' src/run/run-once.test.ts` reduced to 0 for the out-of-order sites (same form-(b) exception applies).
    - `grep -c 'OUT_OF_ORDER_DELAY_MS' src/run/ingest-page.test.ts src/run/run-once.test.ts` == 0 if the const is removed (form (a)).
    - `watch-loop.test.ts` is unchanged: `git diff --name-only -- src/run/watch-loop.test.ts | wc -l` == 0.
    - The out-of-order ordering behavior is still asserted; `pnpm test` for both files passes; `pnpm run test:coverage` 100%.
  </acceptance_criteria>
  <done>No real wall-clock out-of-order sleeps in ingest-page/run-once tests; ordering asserted deterministically; watch-loop untouched; coverage 100%.</done>
</task>

<task type="auto">
  <name>Task 2: Factor duplicated literals into shared builders/constants (TEST-01)</name>
  <read_first>
    - src/run/ingest-page.test.ts (full — existing candidate(...)/rawStored(...) helpers; the deps object-literals duplicated between tests)
    - src/run/run-once.test.ts (full — rawStored() helper; repeated checksum/sourceFilename/object_key literals, 5+ inline)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md §G (typed builders; rule-of-three for extraction)
    - .agents/skills/solidstats-backend... DRY rule-of-three (correctness-and-quality.md §DRY)
  </read_first>
  <action>
    Apply the rule-of-three: where a literal (checksum, sourceFilename, object_key) or a deps object-literal repeats 3+ times across tests in a file, extract it into a named constant or extend the existing typed builder (`candidate`/`rawStored`) with overrides `[src: 26-RESEARCH.md#Test-Quality-Inventory]`. Reuse/extend the helpers already present in each file rather than introducing a parallel one. A literal that appears only once or twice stays inline (DRY is rule-of-three, not rule-of-one). Keep every assertion byte-identical — only the source of the value changes. Do NOT cross files (each file keeps its own builders; no new shared test-infra module needed for this scope). Do NOT modify production code.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- src/run/ingest-page.test.ts src/run/run-once.test.ts && pnpm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - No literal value (checksum / sourceFilename / object_key) appears inline 3+ times in either file where a builder/constant could hold it — repeated literals are factored.
    - Existing `candidate`/`rawStored` helpers are reused/extended, not duplicated by a parallel helper.
    - `git diff --name-only` shows NO production-code change.
    - Both test files pass; `pnpm run test:coverage` 100%; assertions unchanged.
  </acceptance_criteria>
  <done>3+-repeat literals/deps in ingest-page/run-once tests are builder/constant-backed; assertions preserved; coverage 100%.</done>
</task>

<task type="auto">
  <name>Task 3: v8-ignore reachability sweep — confirm no new ignore, all survivors justified (TEST-05)</name>
  <read_first>
    - 26-RESEARCH.md § v8-ignore Reachability Inventory (the 24-site table with reachable/unreachable verdicts)
    - src/commands/watch.ts, src/commands/run-once.ts, src/commands/discover.ts (the 3 W-02 guard ignores — confirm plan 26-01 kept them)
    - vitest.config.ts (coverage thresholds + exclude list)
    - .agents/skills/solidstats-fetcher-ts-tests/SKILL.md § Coverage gate + Coverage-suppression mechanism (reason-after-`--`, never on reachable branch, cluster rule)
  </read_first>
  <action>
    This is a verification-and-audit task, not a coverage-padding task (Pitfall 4 — coverage is already 100%). Enumerate every `/* v8 ignore` site in `src/` and confirm against the RESEARCH inventory `[src: 26-RESEARCH.md#v8-ignore-Reachability-Inventory]`:
    1. The total count of `v8 ignore` sites did NOT increase versus pre-phase baseline (no new suppression added by plans 26-01/02/03/04). If any test refactor in this phase opened a previously-ignored branch as now-reachable, write a test for it — do NOT add a new ignore (Pitfall 5 / TEST-05).
    2. The three W-02 guard ignores (watch.ts, run-once.ts, discover.ts) are still present and still carry a reason after `--` — plan 26-01 kept them because the branch stays structurally unreachable after the typed-error swap. Confirm, do not remove.
    3. Every surviving ignore carries a `-- <reason>` (no bare `/* v8 ignore next */`).
    No source edits are expected in this task unless (1) surfaces a now-reachable branch needing a real test. If everything checks out, the deliverable is the recorded confirmation in the SUMMARY (the inventory verdicts hold; substantial shrink achieved = almost no test-on-branch work, as RESEARCH predicted).
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm run test:coverage && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcn 'v8 ignore' src --include='*.ts'` total is <= the pre-phase baseline count (no NEW ignore introduced across the whole phase).
    - The 3 W-02 guard ignores are present in watch.ts/run-once.ts/discover.ts, each with a `--` reason.
    - Zero bare `/* v8 ignore next */` without a `--` reason anywhere in `src/`.
    - `pnpm run test:coverage` exits 0 at 100%; `pnpm run lint` green.
  </acceptance_criteria>
  <done>v8-ignore inventory confirmed: no new ignore, all survivors reason-tagged and unreachable, W-02 guard ignores intact; coverage 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only change) | No production trust boundary crossed — edits two `*.test.ts` files; production source untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | determinism refactor weakening the ordering assertion | mitigate | Form (a)/(b) preserves the asserted out-of-order behavior; coverage 100% + the test itself fail if ordering regresses — Task 1 |
| T-26-04-02 | Tampering | hidden new v8-ignore masking an opened branch | mitigate | Task 3 audits total ignore count vs baseline; an opened branch gets a real test, never a new ignore (TEST-05) |
| T-26-04-SC | Tampering | npm installs | accept | No package installs — existing Vitest toolchain only |

Phase adds no endpoints, no write-scope change, no production edit in this plan — security-light.
</threat_model>

<verification>
After EACH task commit:
- `pnpm test -- src/run/ingest-page.test.ts src/run/run-once.test.ts` green (no flakiness — re-run twice to confirm determinism on the ordering tests)
- `pnpm run test:coverage` — 100% maintained, total `v8 ignore` count not increased
After the plan (wave-merge gate, phase close):
- `pnpm run verify` exits 0
- `pnpm run test:integration` — golden run-once oracle green, byte-stable
</verification>

<success_criteria>
- Zero real wall-clock out-of-order sleeps in ingest-page/run-once tests; ordering asserted deterministically.
- 3+-repeat literals factored; assertions preserved.
- No new `v8 ignore`; all survivors reason-tagged + unreachable; W-02 guard ignores intact.
- watch-loop.test.ts untouched; coverage + golden oracle green.
</success_criteria>

<output>
Create `.planning/phases/26-test-quality-pass-correctness-hygiene/26-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New test-infra symbols only:
- Possible `Deferred`-style helper or extended `candidate`/`rawStored` builders in `src/run/ingest-page.test.ts` / `src/run/run-once.test.ts` (test-infra, not production).

No production symbols are created or renamed by this plan. The v8-ignore sweep (Task 3) is an audit — it removes the three W-02 guard ignores ONLY if plan 26-01 already did (it does not), otherwise it leaves the inventory unchanged.
