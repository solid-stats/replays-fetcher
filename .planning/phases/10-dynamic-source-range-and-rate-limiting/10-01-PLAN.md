---
phase: 10-dynamic-source-range-and-rate-limiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/config.test.ts
  - README.md
autonomous: true
requirements: [RANGE-04, RANGE-01]
must_haves:
  truths:
    - "REPLAY_SOURCE_CONCURRENCY parses to a bounded int (default 8, min 1, max 32); out-of-range or non-numeric env rejected before any S3/PG mutation"
    - "REPLAY_SOURCE_REQUEST_SPACING_MS parses to a bounded int (default 250, min 0, max 5000); out-of-range rejected"
    - "REPLAY_SOURCE_MAX_PAGES is optional (no default): unset => undefined (unbounded loop downstream); set => a positive int cap"
    - "Both new knobs are non-secret and appear in redactConfig output pass-through (no leak, no redaction)"
  artifacts:
    - path: "src/config.ts"
      provides: "sourceConcurrency + sourceRequestSpacingMs Zod fields; optional sourceMaxPages; new env-var reads"
      contains: "sourceConcurrency"
    - path: "src/config.test.ts"
      provides: "bounds + optionality + redaction coverage for the new knobs"
      contains: "REPLAY_SOURCE_CONCURRENCY"
  key_links:
    - from: "src/config.ts loadSourceConfig/loadConfig"
      to: "process.env REPLAY_SOURCE_CONCURRENCY / REPLAY_SOURCE_REQUEST_SPACING_MS / REPLAY_SOURCE_MAX_PAGES"
      via: "readSourceConfigInput"
      pattern: "REPLAY_SOURCE_CONCURRENCY"
---

<objective>
Add the two Phase 10 pacing/concurrency config knobs and make `sourceMaxPages` an optional
safety-valve cap (no default), with Zod-bounded validation that fails before any S3/PostgreSQL
mutation. This is the foundation every Wave-2 consumer reads (RANGE-04 bounds; RANGE-01 optional cap).

Purpose: A hostile or typo'd env value must not drive unbounded concurrency or starve pacing; the
optional cap removes the hardcoded `default(1)` page bound that made the full corpus an overnight job.
Output: `sourceConcurrency`, `sourceRequestSpacingMs`, optional `sourceMaxPages` on `SourceConfig`/`AppConfig`;
README env-var table updated; new `config.test.ts` cases green.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dynamic-source-range-and-rate-limiting/10-RESEARCH.md
@.planning/phases/10-dynamic-source-range-and-rate-limiting/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces (phase-wide artifact ledger, this plan's slice):
- Env vars: `REPLAY_SOURCE_CONCURRENCY` (default 8, min 1, max 32), `REPLAY_SOURCE_REQUEST_SPACING_MS` (default 250, min 0, max 5000).
- Config keys: `sourceConcurrency: number`, `sourceRequestSpacingMs: number`, `sourceMaxPages?: number` (now optional).
- Module constants in `src/config.ts`: `MIN_CONCURRENCY=1`, `MAX_CONCURRENCY=32`, `defaultSourceConcurrency=8`, `MAX_SPACING_MS=5000`, `defaultSourceRequestSpacingMs=250` (named to satisfy `no-magic-numbers`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add concurrency + spacing Zod knobs and make sourceMaxPages optional</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts (the file being modified — `sourceConfigSchema` lines 30-60, `readSourceConfigInput` lines 167-187, `defaultSourceTimeoutMs` line 27, `redactConfig` lines 152-165)
    - .planning/phases/10-dynamic-source-range-and-rate-limiting/10-PATTERNS.md (§`src/config.ts (MODIFY)` — exact Zod entry shape to copy, hoisted-constant requirement)
  </read_first>
  <behavior>
    - Default: with no env vars, `loadSourceConfig({})` yields `sourceConcurrency === 8`, `sourceRequestSpacingMs === 250`, and `sourceMaxPages === undefined`.
    - Bounds (reject): `REPLAY_SOURCE_CONCURRENCY=0` and `=33` throw `ConfigError`; `REPLAY_SOURCE_REQUEST_SPACING_MS=-1` and `=5001` throw `ConfigError`; non-numeric values throw.
    - Bounds (accept edges): `REPLAY_SOURCE_CONCURRENCY=1` and `=32` accepted; `REPLAY_SOURCE_REQUEST_SPACING_MS=0` and `=5000` accepted.
    - Optional cap: `REPLAY_SOURCE_MAX_PAGES` unset => `sourceMaxPages === undefined`; `=5` => `5`; `=0`/negative throws (still `.int().positive()`).
  </behavior>
  <action>
    Hoist named module constants beside `defaultSourceTimeoutMs` (line 27): `MIN_CONCURRENCY = 1`, `MAX_CONCURRENCY = 32`, `defaultSourceConcurrency = 8`, `MAX_SPACING_MS = 5000`, `defaultSourceRequestSpacingMs = 250` (the `.min(...)/.max(...)` args are NOT exempt from `no-magic-numbers`, unlike `.default(...)`). In `sourceConfigSchema` (line 30) add `sourceConcurrency: z.coerce.number().int().min(MIN_CONCURRENCY).max(MAX_CONCURRENCY).default(defaultSourceConcurrency)` and `sourceRequestSpacingMs: z.coerce.number().int().min(0).max(MAX_SPACING_MS).default(defaultSourceRequestSpacingMs)`. Change `sourceMaxPages: z.coerce.number().int().positive().default(1)` (line 32) to `z.coerce.number().int().positive().optional()` (drop the default; this is the RANGE-01 optional safety-valve cap — unset means unbounded, the Wave-2 loop substitutes `Number.POSITIVE_INFINITY`). In `readSourceConfigInput` (lines 167-187) thread two new reads `sourceConcurrency: source["REPLAY_SOURCE_CONCURRENCY"]` and `sourceRequestSpacingMs: source["REPLAY_SOURCE_REQUEST_SPACING_MS"]`, and widen its return type signature with the two new `string | boolean | undefined` members (alphabetical readonly ordering, matching existing). Do NOT touch `redactConfig` (152-165): both new knobs are non-secret and ride the existing `...config` spread unchanged. Per RANGE-04 (Zod-validated min/max) and RANGE-01 (optional cap).
  </action>
  <verify>
    <automated>pnpm test src/config.test.ts</automated>
  </verify>
  <done>`SourceConfig`/`AppConfig` expose `sourceConcurrency: number`, `sourceRequestSpacingMs: number`, `sourceMaxPages?: number`; default/bounds/optional behaviors above hold; `pnpm run typecheck` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend config.test.ts + README env-var table</name>
  <files>src/config.test.ts, README.md</files>
  <read_first>
    - src/config.test.ts (the file being extended — existing bounded-int and redaction cases as the analog for the new assertions)
    - src/config.ts (post-Task-1, the contract under test)
    - README.md (the env-var documentation table for source/pacing knobs — locate the existing `REPLAY_SOURCE_*` rows)
    - .planning/phases/10-dynamic-source-range-and-rate-limiting/10-RESEARCH.md (§Runtime State Inventory — the `REPLAY_SOURCE_MAX_PAGES` semantics-change note operators must read)
  </read_first>
  <behavior>
    - Test: default values (8 / 250 / undefined) when env unset.
    - Test: each min/max boundary accept + each out-of-range reject for both new knobs (parameterized table preferred).
    - Test: `sourceMaxPages` unset => undefined; set => positive int; zero/negative reject.
    - Test: `redactConfig` output contains the literal `sourceConcurrency` and `sourceRequestSpacingMs` values unchanged (non-secret pass-through), while existing secret redactions are unaffected.
  </behavior>
  <action>
    Add colocated Vitest cases in `src/config.test.ts` covering every `<behavior>` bullet; wrap raw numeric literals as `Number("8")`/`Number("32")` etc. to dodge `no-magic-numbers` in tests (existing test convention). Use a parameterized table over `[env, expected|throws]` for the bounds matrix to keep the 100% V8 gate green without branch gaps. In `README.md`, add `REPLAY_SOURCE_CONCURRENCY` and `REPLAY_SOURCE_REQUEST_SPACING_MS` rows to the env-var table with their default/min/max, and update the `REPLAY_SOURCE_MAX_PAGES` row to state it is now an OPTIONAL safety-valve cap (unset => unbounded stop-on-empty; any operator/scheduler env that relied on the old `default(1)` to fetch only page 1 must now set it explicitly) — per the RESEARCH Runtime State Inventory note.
  </action>
  <verify>
    <automated>pnpm test src/config.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>New config cases green; coverage on `config.ts` stays 100%; README documents both new knobs and the `REPLAY_SOURCE_MAX_PAGES` semantics change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → config loader | Untrusted env strings cross into the Zod validator before any S3/PostgreSQL mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | `REPLAY_SOURCE_CONCURRENCY` | mitigate | Zod `.int().min(1).max(32)`; an absurdly high value fails validation (throws `ConfigError`) before a single request is dispatched — caps client-side fan-out against the Cloudflare-fronted source. |
| T-10-02 | Denial of Service | `REPLAY_SOURCE_REQUEST_SPACING_MS` | mitigate | Zod `.int().min(0).max(5000)`; bounds prevent both a pacing-disabling negative and a worker-pinning oversized spacing. |
| T-10-03 | Information Disclosure | new knobs in `redactConfig` | accept | Both are non-secret operational integers (concurrency, spacing-ms); pass-through visibility is intended for operator audit — no secret/byte/HTML content. |
</threat_model>

<verification>
- `pnpm test src/config.test.ts` green (bounds, optionality, redaction).
- `pnpm run typecheck` green (new keys typed; no `any`/`as`).
- `pnpm run lint` clean (named constants satisfy `no-magic-numbers`).
</verification>

<success_criteria>
- `sourceConcurrency` (8/1/32) and `sourceRequestSpacingMs` (250/0/5000) are Zod-bounded config; invalid env rejected before mutation (RANGE-04).
- `sourceMaxPages` is optional (no default) — the RANGE-01 safety-valve cap.
- Both new knobs visible (non-secret) in `redactConfig`; README documents them and the cap semantics change.
</success_criteria>

<output>
Create `.planning/phases/10-dynamic-source-range-and-rate-limiting/10-01-SUMMARY.md` when done.
</output>
