---
phase: 08-source-failure-diagnostics-and-retry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/config.test.ts
  - src/source/classify-failure.ts
  - src/source/classify-failure.test.ts
  - src/source/backoff.ts
  - src/source/backoff.test.ts
  - src/source/retry.ts
  - src/source/retry.test.ts
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
  - DIAG-04
user_setup: []

must_haves:
  truths:
    - "classifyFailure routes transient network/TLS codes, HTTP 429/5xx, and Cloudflare challenge bodies (incl. status-200 traps) to retry, and non-CF 4xx/404/410 + malformed body to no-retry (DIAG-02)."
    - "AggregateError causes from dual-stack happy-eyeballs are unwrapped before classification so the inner cause.code is read (DIAG-02)."
    - "fullJitterDelay with an injected random produces a deterministic, cap-bounded delay; parseRetryAfter handles both delta-seconds and HTTP-date with injected now (DIAG-03)."
    - "withRetry retries transient/rate_limited up to the configured attempts and never retries permanent; sleep and random are injected so tests are deterministic (DIAG-03)."
    - "The classification struct carries identifiers only (kind, cloudflareMarker, status, causeCode, causeMessage) and never a response body, bytes, or secret — a unit test asserts no body marker leaks (DIAG-04)."
    - "sourceRetryAttempts is a Zod-validated, env-overridable, non-redacted config field defaulting to 3 (DIAG-03)."
  artifacts:
    - path: "src/source/classify-failure.ts"
      provides: "Shared tri-state failure classifier consumed by both source adapters"
      exports: ["classifyFailure", "FailureKind", "FailureClassification", "ClassifyInput"]
    - path: "src/source/backoff.ts"
      provides: "Pure full-jitter delay + Retry-After parsing"
      exports: ["fullJitterDelay", "parseRetryAfter"]
    - path: "src/source/retry.ts"
      provides: "Generic bounded retry wrapper with injected sleep/random and threaded AbortSignal"
      exports: ["withRetry", "RetrySourceReadOptions", "RetryAttemptEvent", "SourceReadPhase"]
    - path: "src/config.ts"
      provides: "sourceRetryAttempts config field + env override"
      contains: "sourceRetryAttempts"
  key_links:
    - from: "src/source/retry.ts"
      to: "src/source/classify-failure.ts"
      via: "classify callback (injected, not a hard import) re-classifies each thrown error"
      pattern: "classify"
    - from: "src/source/retry.ts"
      to: "src/source/backoff.ts"
      via: "fullJitterDelay + parseRetryAfter compute each round's delay"
      pattern: "fullJitterDelay|parseRetryAfter"
---

<objective>
Build the three shared, pure, transport-agnostic primitives that Phase 8 needs before any adapter is touched: the single shared failure classifier (DIAG-02), the full-jitter backoff + Retry-After math (DIAG-03), and the generic bounded retry wrapper with injected `sleep`/`random` and a threaded `AbortSignal` (DIAG-03). Also add the operator-configurable `sourceRetryAttempts` config field (DIAG-03) and the no-body-leak guarantee on the classification struct (DIAG-04).

Purpose: These are the dependency root of the phase. Both source adapters (Plans 02 and 03) and the orchestrator (Plan 04) consume them. Landing them first, fully tested in isolation, lets the adapter plans run in parallel against a fixed contract instead of exploring the codebase.

Output: `src/source/classify-failure.ts`, `src/source/backoff.ts`, `src/source/retry.ts` with colocated tests, plus the `sourceRetryAttempts` Zod field + env override in `src/config.ts`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-CONTEXT.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-RESEARCH.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following NEW symbols and files (the rest of the phase consumes them):

| Symbol / file | Kind | Where |
|---------------|------|-------|
| `src/source/classify-failure.ts` | new file | shared classifier module |
| `classifyFailure(input: ClassifyInput): FailureClassification` | new fn | DIAG-02 |
| `FailureKind = "transient" \| "rate_limited" \| "permanent"` | new type | DIAG-02 |
| `ClassifyInput` (`error?`, `httpStatus?`, `cfChallenge?`, `malformedBody?`) | new type | DIAG-02 |
| `FailureClassification` (`kind`, `cfChallenge`, `httpStatus?`, `causeCode?`, `causeMessage?`) | new type | DIAG-02/04 |
| `src/source/backoff.ts` | new file | backoff math |
| `fullJitterDelay(round, random, base?, cap?): number` | new fn | DIAG-03 |
| `parseRetryAfter(value: string \| null, now: () => number): number \| undefined` | new fn | DIAG-03 |
| `src/source/retry.ts` | new file | retry wrapper |
| `withRetry<T>(opts: RetrySourceReadOptions<T>): Promise<T>` | new fn | DIAG-03 |
| `RetrySourceReadOptions<T>` | new type | DIAG-03 |
| `RetryAttemptEvent` (`phase`, `page?`, `attempt`, `delayMs`, `causeCode?`) | new type | DIAG-01/03 |
| `SourceReadPhase = "list" \| "detail" \| "bytes"` | new type | DIAG-01 |
| `sourceRetryAttempts` config field + `REPLAY_SOURCE_RETRY_ATTEMPTS` env override + `defaultSourceRetryAttempts = 3` | new config | DIAG-03 |

(Adapter-side symbols — widened `ReplayByteFetchError` union, enriched `SourceFetchError` details, new `DiscoveryDiagnostic` fields, `onRetry` wiring — are produced by Plans 02/03/04.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sourceRetryAttempts config field + env override (DIAG-03)</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts (the file being modified — note `defaultSourceTimeoutMs` at line 27, `sourceConfigSchema` fields at lines 31-40, `readSourceConfigInput` return type + body at lines 159-177, `redactConfig` at 144-157, `RedactedAppConfig` omit list at 74-86)
    - src/config.test.ts (analog: the `REPLAY_SOURCE_TIMEOUT_MS` override test at lines 30, 45, 57-61 and the `REPLAY_SOURCE_MAX_PAGES` test at line 66)
    - 08-PATTERNS.md → "src/config.ts (MODIFY)" section (Zod field + env-override + redaction-posture guidance: attempt count is NOT a secret, must stay out of redactConfig)
  </read_first>
  <behavior>
    - Default: with no `REPLAY_SOURCE_RETRY_ATTEMPTS` set, `loadSourceConfig`/`loadConfig` yield `sourceRetryAttempts === 3`.
    - Override: `REPLAY_SOURCE_RETRY_ATTEMPTS: "5"` yields `sourceRetryAttempts === 5`.
    - Validation: a negative or non-integer value (e.g. `"-1"`, `"abc"`) makes `loadConfig` throw `ConfigError` whose issues mention `sourceRetryAttempts`.
    - Redaction: `redactConfig` output still contains the literal `sourceRetryAttempts` number (it is NOT a secret and must NOT be redacted).
  </behavior>
  <action>
    Add a `const defaultSourceRetryAttempts = 3;` next to `defaultSourceTimeoutMs` (per DIAG-03 locked default of 3 retries). Add `sourceRetryAttempts: z.coerce.number().int().nonnegative().default(defaultSourceRetryAttempts)` to `sourceConfigSchema` (nonnegative so an operator can disable retry with 0; mirror the coerce/int discipline of the existing `sourceTimeoutMs` field). Extend `readSourceConfigInput`'s return-type shape and returned object with `sourceRetryAttempts: source["REPLAY_SOURCE_RETRY_ATTEMPTS"]`. Do NOT add `sourceRetryAttempts` to `redactConfig` or the `RedactedAppConfig` omit list — keeping the redaction posture intact means leaving this non-secret integer visible (per 08-PATTERNS.md). Export `defaultSourceRetryAttempts` so the config test can assert the default without a magic number (ESLint no-magic-numbers). In `src/config.test.ts`, add tests for the four behaviors above, copying the existing `REPLAY_SOURCE_TIMEOUT_MS` override test shape.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- config 2>&1 | tail -20</automated>
  </verify>
  <done>`sourceRetryAttempts` defaults to 3, is env-overridable via `REPLAY_SOURCE_RETRY_ATTEMPTS`, rejects negative/non-integer values with a `ConfigError` citing the field, and is present (un-redacted) in `redactConfig` output; config tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared failure classifier with AggregateError unwrap + CF detection + no-body-leak (DIAG-02, DIAG-04)</name>
  <files>src/source/classify-failure.ts, src/source/classify-failure.test.ts</files>
  <read_first>
    - src/discovery/source-client.ts (lines 140-156: `classifySshFailure` — the explicit seed to generalize; line 15 `httpTooManyRequestsStatus = 429`; lines 63-67 the 429→rate_limited mapping)
    - src/errors/app-error.ts (lines 9-16 doc comment: identifiers-only `details` discipline; lines 34-38: how `cause` is preserved)
    - src/discovery/source-client.test.ts (lines 1-45: `vi.stubGlobal("fetch", ...)`, `afterEach` cleanup, `rejects.toMatchObject({ code, name })` assertion style)
    - 08-RESEARCH.md → "Failure Classification Taxonomy" (full signal→kind table), "Распаковка AggregateError" (`unwrapCauseCode` algorithm), Pattern 1, Pattern 3 (`CF_BODY_MARKERS`, `detectCloudflareChallenge`), "Common Pitfalls" 1/2/4
    - 08-PATTERNS.md → "src/source/classify-failure.ts (NEW)" (classifier contract + seed)
  </read_first>
  <behavior>
    - HTTP 429 (no CF markers) → `kind: "rate_limited"`.
    - HTTP 500/502/503/504 → `kind: "transient"`.
    - HTTP 404 / 410 / non-CF 400 / 401 / 403 → `kind: "permanent"`.
    - `cfChallenge: true` (status 200 OR 4xx/5xx with cf-ray + body marker) → `kind: "transient"`, `cfChallenge: true`.
    - cause.code `ECONNRESET` / `ENOTFOUND` / `EAI_AGAIN` / `ETIMEDOUT` → `kind: "transient"`, `causeCode` set.
    - cause.code starting `UND_ERR_` (e.g. `UND_ERR_CONNECT_TIMEOUT`) → `transient`.
    - TLS codes (`ERR_TLS_*` / `CERT_*` / `EPROTO`) → `transient` (bounded by caller attempts).
    - `AggregateError` wrapped in a `TypeError("fetch failed").cause` → unwrapped; the first inner error's `.code` becomes `causeCode` and drives classification.
    - `malformedBody: true` → `permanent`.
    - Unknown / unrecognized error with no usable code or status → `permanent` (safe default; never blind-retry).
    - DIAG-04: given an `error` whose `.message`/inner contains a large/secret body marker (e.g. `"SECRET_BODY_zzz"` and 5KB of HTML), the returned struct serialized via `JSON.stringify` does NOT contain that marker or the HTML — only `kind`, `cfChallenge`, optional `httpStatus`/`causeCode`/`causeMessage` (and `causeMessage` is the short library message, never the response body).
  </behavior>
  <action>
    Create `src/source/classify-failure.ts` exporting `FailureKind`, `ClassifyInput`, `FailureClassification`, and `classifyFailure(input)`. Generalize the line-140 `classifySshFailure` from a binary message-substring check into a tri-state classifier driven by stable signals. Implement an internal `unwrapCauseCode(error)` following the 08-RESEARCH.md algorithm: peel `TypeError.cause`, then if the result is an `AggregateError` pick the first inner error carrying a string `.code` (happy-eyeballs dual-stack), and return `{ code?, message? }` where `message` is the short Error message only. Precedence order: cfChallenge → 429 rate_limited / 5xx transient → 404/410/non-CF 4xx permanent / malformedBody permanent → network/TLS/`UND_ERR_` transient → default permanent. Keep `UND_ERR_` as a `startsWith` prefix match so future undici codes are covered. Define `causeMessage` as a short string sliced/guarded so a hostile long message cannot smuggle a body (cap length defensively, e.g. keep only a bounded prefix — store the cap as a named const, no magic number). Use `consistent-type-definitions` (interface for object types), `readonly` fields, no `any`/`as` (narrow with `instanceof` + `in` guards as the seed does). Do NOT perform any I/O and do NOT read `Response` bodies here — the adapter pre-computes `cfChallenge`/`malformedBody` and passes them in. Create `src/source/classify-failure.test.ts` covering every behavior row above plus the explicit no-body-leak assertion (DIAG-04), mirroring the `source-client.test.ts` style; no fenced response bodies belong in `details`.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- classify-failure 2>&1 | tail -25</automated>
  </verify>
  <done>Every taxonomy row classifies correctly; AggregateError is unwrapped to the inner code; status-200 CF challenge → transient + `cfChallenge:true`; unknown → permanent; the no-body-leak test proves the serialized classification contains no response body / secret marker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full-jitter backoff + Retry-After parsing, and the bounded retry wrapper with injected sleep/random + AbortSignal (DIAG-03)</name>
  <files>src/source/backoff.ts, src/source/backoff.test.ts, src/source/retry.ts, src/source/retry.test.ts</files>
  <read_first>
    - src/discovery/discover.ts (lines 125-143: injectable `sleep` seam in `createPacedSourceClient`; lines 559-564: `defaultSleep` adapter with its `/* v8 ignore */` note)
    - src/discovery/source-client.ts (lines 37-50: `create*(config, options)` defaulted-dependency factory shape; lines 55-61: AbortController + setTimeout(abort) timeout pattern)
    - src/source/classify-failure.ts (the `FailureClassification`/`FailureKind` contract from Task 2 — the retry wrapper takes a `classify` callback returning this)
    - 08-RESEARCH.md → "Backoff with Full Jitter" (formula + `fullJitterDelay`), "Retry-After parsing" (`parseRetryAfter`, max(backoff, retryAfter)), "Retry Composition + AbortSignal Threading" (RESOLVED: per-round timeout, caller signal threads through all rounds), "Bounded retry loop skeleton", "Common Pitfalls" 3/5
    - 08-PATTERNS.md → "src/source/retry.ts (NEW)" (retry helper contract + injectable seam to mirror)
  </read_first>
  <behavior>
    - `fullJitterDelay(0, () => 0)` → 0; `fullJitterDelay(0, () => 0.5, 500, 30000)` → 250; round grows `base * 2 ** round` but is capped at 30000 (e.g. round 7 with `random=()=>0.999...` ≈ just under cap, never above).
    - `parseRetryAfter("120", now)` → 120000; `parseRetryAfter("Wed, 21 Oct 2026 07:28:00 GMT", () => fixedEpoch)` → `dateMs - fixedEpoch` clamped to ≥0; `parseRetryAfter(null, now)` and `parseRetryAfter("garbage", now)` → undefined.
    - `withRetry` with a read that always throws a transient error retries exactly `attempts` times (total `attempts + 1` reads), calling injected `sleep` `attempts` times, then rethrows.
    - `withRetry` with a read that throws a permanent error performs exactly 1 read, calls `sleep` 0 times, and rethrows immediately.
    - `withRetry` with a read that succeeds on the 2nd try (transient then ok) returns the value after exactly 1 `sleep`.
    - For a `rate_limited` classification whose error carries `Retry-After`, the chosen delay is `max(fullJitterDelay(round, random), parsedRetryAfter)`.
    - The injected `signal` (caller AbortSignal) is passed into every `read(signal)` round; if the caller aborts, the chain stops (the wrapper does not swallow the abort).
    - `onRetry` is invoked once per retry round with `{ phase, page?, attempt, delayMs, causeCode? }` before the sleep.
  </behavior>
  <action>
    Create `src/source/backoff.ts` exporting `fullJitterDelay(round, random, base = 500, cap = 30_000)` (formula `Math.floor(random() * Math.min(cap, base * 2 ** round))`, constants named — no magic numbers) and `parseRetryAfter(value, now)` (delta-seconds via `/^\d+$/`, HTTP-date via `Date.parse`, invalid → undefined, `Math.max(0, …)` for past dates), per 08-RESEARCH.md. Create `src/source/retry.ts` exporting `SourceReadPhase = "list" | "detail" | "bytes"`, `RetryAttemptEvent`, `RetrySourceReadOptions<T>` (fields: `read(signal): Promise<T>`, `phase`, `page?`, `url`, `attempts`, `signal`, `classify`, `sleep?`, `random?`, `now?`, `onRetry?`), and `withRetry<T>(opts)`. Implement the bounded loop from the 08-RESEARCH.md skeleton: re-classify each thrown error via `opts.classify`; stop immediately on `permanent` or when `round >= attempts` (rethrow, after invoking the existing error-enrichment is left to the adapters — here just rethrow the original error so callers attach details); otherwise compute `delay = max(fullJitterDelay(round, random), retryAfter ?? 0)`, call `onRetry`, then `await sleep(delay)`. Default `sleep` to a copy of `discover.ts:defaultSleep` (same `/* v8 ignore */` note) and `random` to `Math.random`, `now` to `Date.now` — all injectable so tests are deterministic (mirror the `sleep` seam exactly, add `random`/`now` the same way). Thread `opts.signal` into each `read(signal)` call (per the RESOLVED per-round-timeout decision: the adapter owns the per-round fetch timeout; the wrapper passes the caller signal through every round and aborts the whole chain on external cancel). Keep the two intentional `no-await-in-loop` eslint-disable comments with rationale (sequential retry rounds / backoff between rounds). No pino import here — the wrapper only calls the injected `onRetry` callback (logger lives in the orchestrator, Plan 04). Create `src/source/backoff.test.ts` and `src/source/retry.test.ts` covering every behavior above using injected `random`/`sleep`/`now` and a fake `classify`; drive timing only through the injected `sleep` (never real timers — mirror `discover.test.ts`).
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- "src/source/backoff" "src/source/retry" 2>&1 | tail -25</automated>
  </verify>
  <done>`fullJitterDelay`/`parseRetryAfter` produce exact deterministic values with injected `random`/`now` and respect the 30s cap; `withRetry` retries transient/rate_limited exactly `attempts` times via injected `sleep`, never retries permanent, honors `max(backoff, Retry-After)`, threads the caller `signal` into every round, and emits one `onRetry` per round; all tests pass with zero real-timer use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external source → adapter → classifier | Untrusted HTTP/SSH error objects, status codes, `Retry-After` headers, and challenge bodies cross into the classifier as `ClassifyInput`. |
| classifier/retry → diagnostics/logs | Classification struct and retry events flow toward operator-visible diagnostics and stderr logs. |

## STRIDE Threat Register (ASVS L2)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `classifyFailure` `causeMessage` | mitigate | Allowlist struct fields (kind, cfChallenge, status, causeCode, causeMessage); `causeMessage` is the short library message only, length-capped via a named const; no response body / bytes / secret ever copied in. Verified by the no-body-leak unit test (DIAG-04). |
| T-08-02 | Denial of Service | `withRetry` loop on self timeout-abort | mitigate | Hard bounded `attempts` from Zod config (default 3), full jitter, cap 30s; per-round timeout cannot reset the round counter (Pitfall 3). |
| T-08-03 | Denial of Service | `parseRetryAfter` hostile/huge `Retry-After` | mitigate | Bounded `attempts` caps total rounds; invalid/garbage values parse to `undefined` (ignored), so a malformed header cannot force a giant wait beyond the attempt budget. |
| T-08-04 | Tampering | log injection via server-controlled `causeMessage` | mitigate | `causeMessage` is emitted only as a structured JSON value via the injected `onRetry`/pino path (Plan 04), never interpolated into a format string. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (08-RESEARCH.md "Package Legitimacy Audit: Не применимо"); all primitives are Node built-ins + already-installed deps. |
</threat_model>

<verification>
- `pnpm test -- config classify-failure src/source/backoff src/source/retry` all green.
- `pnpm exec tsc --noEmit` passes (strict; no `any`/`as`).
- `pnpm exec eslint src/config.ts src/source` passes (ESLint `all`: no-magic-numbers, consistent-type-definitions, no-ternary, import hygiene, Unicorn).
- No-body-leak test present in `classify-failure.test.ts` and passing (DIAG-04).
- No real timers used in `retry.test.ts`/`backoff.test.ts` (injected `sleep`/`random`/`now` only).
</verification>

<success_criteria>
- DIAG-02 classifier is a single shared pure module with correct transient/permanent routing and AggregateError unwrap.
- DIAG-03 backoff (full jitter, base 500ms, cap 30s), Retry-After honoring, bounded operator-configurable attempts, and deterministic injected seams exist and are tested.
- DIAG-04 no-body-leak guarantee is encoded in the classification struct shape and asserted by a unit test.
- `sourceRetryAttempts` config field (default 3, env override, non-redacted) is added and tested.
- 100% reachable coverage maintained for the new modules.
</success_criteria>

<output>
Create `.planning/phases/08-source-failure-diagnostics-and-retry/08-01-SUMMARY.md` when done.
</output>
