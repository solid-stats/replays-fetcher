---
phase: 08-source-failure-diagnostics-and-retry
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/storage/replay-byte-client.ts
  - src/storage/replay-byte-client.test.ts
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-04
user_setup: []

must_haves:
  truths:
    - "ReplayByteFetchError's code union is widened ADDITIVELY to include a rate_limited member while keeping fetch_failed, so existing consumers in store-raw-replay.ts / run/summary.ts / run/types.ts keep compiling (DIAG-02, closes Phase 7 WR-03)."
    - "Both direct and SSH byte reads route their failures through the SAME shared classifyFailure and are wrapped in withRetry — transient/rate_limited retried, permanent failed immediately (DIAG-02, DIAG-03)."
    - "A failed byte read carries phase:'bytes', httpStatus (when a Response existed), causeCode, causeMessage, url, attempts, and cfChallenge in ReplayByteFetchError.details — identifiers only, no bytes/body/secret (DIAG-01, DIAG-04)."
    - "instanceof ReplayByteFetchError and the AppError base are preserved; only the generic union parameter changes (DIAG-02)."
  artifacts:
    - path: "src/storage/replay-byte-client.ts"
      provides: "Bytes adapter wired to shared classifier + retry with widened union + enriched details"
      contains: "classifyFailure"
  key_links:
    - from: "src/storage/replay-byte-client.ts"
      to: "src/source/classify-failure.ts"
      via: "classifyFailure consumed for direct + SSH byte failures"
      pattern: "classifyFailure"
    - from: "src/storage/replay-byte-client.ts"
      to: "src/source/retry.ts"
      via: "withRetry wraps each fetchBytes read"
      pattern: "withRetry"
---

<objective>
Apply the SAME shared classifier and retry wrapper to the bytes adapter (`replay-byte-client.ts`), widen `ReplayByteFetchError`'s code union ADDITIVELY (keep `fetch_failed`, add `rate_limited`) to close Phase 7 WR-03, and enrich `ReplayByteFetchError.details` with identifiers-only evidence (DIAG-01/04). This is the bytes half of the "one shared classifier reused by source-client AND replay-byte-client" locked decision.

Purpose: Unify the bytes path's resilience and diagnostics with the list/detail path so transient byte fetch failures retry and surface rich evidence, while permanent failures still flow into the existing `fetch_failed` failure category that `store-raw-replay.ts` / `run/summary.ts` depend on.

Output: widened `ReplayByteFetchError` union, both direct + SSH byte reads routed through `classifyFailure` + `withRetry`, and enriched identifiers-only `details`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-CONTEXT.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-RESEARCH.md
@.planning/phases/08-source-failure-diagnostics-and-retry/08-PATTERNS.md
@src/source/classify-failure.ts
@src/source/retry.ts
@src/storage/store-raw-replay.ts
</context>

<artifacts_this_phase_produces>
NEW / changed symbols introduced by this plan:

| Symbol / file | Kind | Where |
|---------------|------|-------|
| `ReplayByteFetchError extends AppError<"fetch_failed" \| "rate_limited">` | changed type (additive widen) | src/storage/replay-byte-client.ts (DIAG-02, WR-03) |
| Enriched `ReplayByteFetchError` thrown with identifiers-only `details` (phase:'bytes', httpStatus, causeCode, causeMessage, url, attempts, cfChallenge) | changed behavior | src/storage/replay-byte-client.ts (DIAG-01/04) |
| Both direct + SSH `fetchBytes` reads wrapped in `withRetry` + classified via `classifyFailure` | changed behavior | src/storage/replay-byte-client.ts (DIAG-02/03) |
| `ReplayByteClient.fetchBytes(url, opts?)` accepts optional read-options seam (attempts?, signal?, page?, onRetry?) | changed interface | src/storage/replay-byte-client.ts (DIAG-03) |

Consumes from Plan 01: `classifyFailure`, `withRetry`, `RetryAttemptEvent`, `SourceReadPhase`.
Boundary note: `fetch_failed` is KEPT because `store-raw-replay.ts:13,57`, `run/summary.ts:140-141`, and `run/types.ts:10` consume it (verified by grep — 10 non-test references); removing it would regress those consumers. The union only grows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen ReplayByteFetchError union additively + route byte reads through shared classifier + retry (DIAG-02, DIAG-03, WR-03)</name>
  <files>src/storage/replay-byte-client.ts, src/storage/replay-byte-client.test.ts</files>
  <read_first>
    - src/storage/replay-byte-client.ts (the file being modified — `ReplayByteFetchError` union line 19; direct adapter 52-85 incl. AbortController/timeout 55-58 and catch/wrap 71-79; SSH adapter 87-119 incl. catch/wrap 107-116; `ReplayByteClient` interface 15-17; getSshHost 121-130)
    - src/storage/store-raw-replay.ts (lines 12-19, 52-64: the `fetch_failed` consumer — `failureCategory: "fetch_failed"` MUST keep compiling; this is why the widen is additive)
    - src/source/classify-failure.ts + src/source/retry.ts (Plan 01 contracts)
    - src/discovery/source-client.ts (its structural twin AFTER Plan 02 — mirror the same withRetry + classifyFailure + enriched-details shape; if Plan 02 already merged, read its final form)
    - 08-RESEARCH.md → "Widen ReplayByteFetchError (WR-03)" (RESOLVED: keep fetch_failed, add rate_limited additively), "Diagnostic Payload Shape", Pitfalls 3/4
    - 08-PATTERNS.md → "src/storage/replay-byte-client.ts (MODIFY)" (mirror twin guidance, remove duplicated SSH scaffold per IN-02 where practical)
  </read_first>
  <behavior>
    - `ReplayByteFetchError` accepts `code: "fetch_failed" | "rate_limited"`; `new ReplayByteFetchError("fetch_failed", ...)` still constructs and `error instanceof ReplayByteFetchError` and `error instanceof AppError` both remain true.
    - Direct byte read, fetch rejects with cause.code `ETIMEDOUT`: retried up to `attempts`, then throws with enriched `details` (`phase: "bytes"`, `causeCode: "ETIMEDOUT"`, `url`, `attempts`, `cfChallenge: false`).
    - Direct byte read, HTTP 429: classified `rate_limited` → code `rate_limited`, retried, `details.httpStatus === 429`.
    - Direct byte read, HTTP 404: classified permanent → 1 try, no retry, thrown with `details.httpStatus === 404` and code `fetch_failed`.
    - SSH byte read failure: routed through the SAME shared `classifyFailure` + `withRetry`, enriched details (no httpStatus for SSH).
    - Permanent byte failure still surfaces as `failureCategory: "fetch_failed"` through `store-raw-replay.ts` (consumer test still green).
    - No-body-leak: a failing response whose bytes/body contain a marker (e.g. `"SECRET_BYTES_zzz"`) does NOT appear in serialized `ReplayByteFetchError.details` (DIAG-04).
  </behavior>
  <action>
    Widen `ReplayByteFetchError extends AppError<"fetch_failed">` to `AppError<"fetch_failed" | "rate_limited">` — ADDITIVE only; do NOT drop `fetch_failed` (it has real consumers: `store-raw-replay.ts:13,57`, `run/summary.ts:140-141`, `run/types.ts:10`). Keep the public constructor + the existing `no-useless-constructor` eslint-disable rationale. Mirror the Plan 02 source-client refactor: move the real `fetch` (direct) / `execFile` (SSH) into a `read(signal)` closure passed to `withRetry`, keeping the per-round `AbortController` + `setTimeout(abort, config.sourceTimeoutMs)` inside the direct closure (per-round-timeout RESOLVED) and threading the optional caller `signal`. On failure paths build a `ClassifyInput` (`error`, `httpStatus` when a Response existed, `cfChallenge` if you reuse the source-client CF detector for byte responses — bytes come from the JSON data endpoint so a CF challenge there is also possible), call the shared `classifyFailure`, map `FailureKind` → code (`rate_limited` → `rate_limited`; transient/permanent/unknown → `fetch_failed` to preserve the existing failure category), and throw `ReplayByteFetchError` with `cause` preserved and identifiers-only `details` (phase:'bytes', httpStatus, causeCode, causeMessage, url, attempts, cfChallenge) — NEVER raw bytes/body/secret (DIAG-04). Add the optional read-options seam to `ReplayByteClient.fetchBytes(url, options?)` (attempts?, page?, signal?, onRetry?), defaulting to a single no-retry try when absent so existing callers (`store-raw-replay.ts`) keep working. Remove duplicated SSH catch/wrap scaffolding where the shared classifier now covers it (IN-02), keeping behavior. Extend `replay-byte-client.test.ts` with the behaviors above (widen-union construct + instanceof, transient retry via injected sleep, 429 rate_limited, 404 permanent-no-retry, SSH path, no-body-leak) using `vi.stubGlobal("fetch")` / injected execFile; drive retry timing only through injected `sleep`.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/replays-fetcher && pnpm test -- replay-byte-client store-raw-replay 2>&1 | tail -25</automated>
  </verify>
  <done>`ReplayByteFetchError` union is widened additively (fetch_failed kept, rate_limited added, instanceof + AppError preserved); both byte transports classify via the shared `classifyFailure` and retry transient/rate_limited via `withRetry` while never retrying permanent; details are identifiers-only (no byte/body leak); `store-raw-replay` consumer tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external source (JSON data endpoint) → replay-byte-client.fetchBytes | Untrusted HTTP responses (status, headers, raw bytes) and SSH stdout/transport errors cross into the bytes adapter. |
| replay-byte-client → ReplayByteFetchError.details → failure evidence | Adapter-built error details flow toward store-raw-replay failure evidence, run summary, and stderr logs. |
| operator config → base64 SSH positional arg | Raw byte URL passed to SSH as a base64-encoded positional argument, never interpolated into the shell command. |

## STRIDE Threat Register (ASVS L2)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `ReplayByteFetchError.details` + raw replay bytes | mitigate | Identifiers-only allowlist (phase, httpStatus, causeCode, causeMessage, url, attempts, cfChallenge); raw bytes and response body are NEVER copied into details. No-body-leak unit test (DIAG-04). |
| T-08-02 | Elevation of Privilege / Injection | SSH byte transport URL handling | mitigate | URL stays base64-encoded into the existing positional `$1` arg (`base64 -d`), unchanged from v1; no shell interpolation of the URL — injection surface unchanged. |
| T-08-03 | Denial of Service | byte read retry loop | mitigate | Bounded `attempts` threaded through `withRetry`; per-round `sourceTimeoutMs` timeout; full jitter + 30s cap (Plan 01). |
| T-08-04 | Tampering | additive union widening breaking consumers | mitigate | `fetch_failed` retained (grep-verified consumers); consumer tests (`store-raw-replay`, `summary`) run in verify; additive-only change preserves `instanceof`/AppError. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (08-RESEARCH.md audit N/A); only existing modules + Plan 01 primitives. |
</threat_model>

<verification>
- `pnpm test -- replay-byte-client store-raw-replay` green (incl. widen-union, retry, no-body-leak; consumer tests unbroken).
- `pnpm exec tsc --noEmit` passes (additive union; `fetch_failed` consumers compile).
- `pnpm exec eslint src/storage/replay-byte-client.ts` passes (ESLint `all`).
- `fetch_failed` still present in `replay-byte-client.ts` (`grep -v '^\s*//' src/storage/replay-byte-client.ts | grep -c '"fetch_failed"'` ≥ 1).
- Retry timing driven via injected `sleep` only.
</verification>

<success_criteria>
- WR-03 closed: `ReplayByteFetchError` distinguishes transient/rate_limited while keeping `fetch_failed`.
- DIAG-02: bytes path uses the single shared classifier; permanent byte failures not retried.
- DIAG-03: byte reads wrapped in `withRetry` with bounded operator-configurable attempts + threaded AbortSignal.
- DIAG-01/04: enriched identifiers-only details with a no-body-leak test.
- `store-raw-replay.ts` / `run/summary.ts` consumers remain green; 100% reachable coverage maintained.
</success_criteria>

<output>
Create `.planning/phases/08-source-failure-diagnostics-and-retry/08-03-SUMMARY.md` when done.
</output>
