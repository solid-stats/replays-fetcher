---
phase: 16-oxlint-migration-import-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .oxlintrc.json
  - eslint.config.js
autonomous: true
requirements: [LNT-01, LNT-02, LNT-04]
must_haves:
  truths:
    - "`pnpm lint` runs oxlint (not eslint) and exits without crashing"
    - "Ни один eslint-пакет не остаётся в package.json"
    - "`.oxlintrc.json` расширяет shared preset по относительному node_modules-пути"
    - "`pnpm run lint:types` существует как отдельный скрипт и НЕ входит в verify"
    - "`unicorn/no-null` и `no-await-in-loop` отключены в эффективной конфигурации"
  artifacts:
    - path: ".oxlintrc.json"
      provides: "Repo oxlint config: extends shared preset + repo-local overrides"
      contains: "node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json"
    - path: "package.json"
      provides: "lint=oxlint, lint:types=oxlint --type-aware, oxlint@1.69.0 pinned, eslint deps removed"
      contains: "\"oxlint\": \"1.69.0\""
  key_links:
    - from: ".oxlintrc.json"
      to: "node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json"
      via: "extends relative path"
      pattern: "node_modules/@solid-stats/ts-toolchain/oxlint/base"
    - from: "package.json"
      to: ".oxlintrc.json"
      via: "lint script invokes oxlint --config .oxlintrc.json"
      pattern: "oxlint.*--config .oxlintrc.json"
---

<objective>
Поменять линтер: удалить ESLint и все его плагины, поставить `oxlint@1.69.0`, создать
`.oxlintrc.json` (extends shared preset + repo-local overrides с портированными ОПЦИЯМИ),
переписать скрипты `lint`/`lint:types`, удалить `eslint.config.js`.

Это первая волна связанной замены. После неё `pnpm lint` запускает oxlint. Ожидается, что
oxlint при первом прогоне выдаст РЕАЛЬНЫЕ convention-нарушения (func-style и др.) — их
исправляют планы 16-02/16-03. В этом плане «green lint» НЕ требуется; требуется чтобы oxlint
корректно запускался на этой конфигурации и список ошибок совпадал с эмпирикой spike 001.

Purpose: закрыть LNT-01 (swap линтера), LNT-02 (порт опций, no `js.configs.all`,
`unicorn/no-null`+`no-await-in-loop` off) и часть LNT-04 (наличие `lint:types` вне verify).
Output: `.oxlintrc.json`, обновлённый `package.json`, удалённый `eslint.config.js`,
обновлённый `pnpm-lock.yaml`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-RESEARCH.md

# Эмпирические источники (читать перед написанием конфига)
@.planning/spikes/001-oxlint-preset-port/oxlintrc.candidate.json
@.planning/spikes/001-oxlint-preset-port/dropped.tsv

# Текущее состояние, которое заменяем
@eslint.config.js
@package.json
@node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать .oxlintrc.json (extends shared preset + repo-local overrides)</name>
  <files>.oxlintrc.json</files>
  <read_first>
    - `eslint.config.js` — источник портируемых ОПЦИЙ (строки 25-93: import-x/order,
      require-await off, capitalized-comments off, func-style off→в preset error,
      no-magic-numbers с ignore, prevent-abbreviations allowList cli/env/s3, и т.д.)
    - `node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json` — verified
      состояния: `$schema`=строка 2, `plugins`=["typescript","unicorn","import","oxc"]
      (строка 3), `no-await-in-loop:error` (8), `func-style:error` (63),
      `typescript/no-magic-numbers:warn` (202), `typescript/require-await:error` (263),
      `unicorn/no-null:off` (318), `unicorn/prevent-abbreviations:error` (390).
    - `.planning/spikes/001-oxlint-preset-port/oxlintrc.candidate.json` — структурный образец.
  </read_first>
  <action>
    Создать `.oxlintrc.json` в корне репо. Структура:
    (1) `$schema`: "./node_modules/oxlint/configuration_schema.json" (повторить на repo-уровне —
    при extends относительный $schema из preset резолвится неверно, см. RESEARCH Assumption про
    $schema).
    (2) `extends`: МАССИВ с ОДНИМ относительным путём
    "./node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json". НЕ bare specifier
    (GitHub #15538 — oxlint не резолвит package specifier в extends). НЕ добавлять `plugins`
    повторно (наследуется из preset).
    (3) `rules` — repo-local overrides ПОВЕРХ preset, портируя ОПЦИИ из eslint.config.js
    (severity-only порт даёт 1336 false positives — Pitfall 3):
      - "no-await-in-loop": "off" (preset держит error; fetcher делает sequential I/O в
        retry-циклах — eslint.config.js это разрешал через disable-комментарии);
      - "typescript/require-await": "off" (eslint.config.js строка 47 имел require-await off;
        preset держит typescript/require-await error — выровнять);
      - "unicorn/prevent-abbreviations": ["error", { "allowList": { "cli": true, "env": true,
        "s3": true } }] (порт allowList из eslint.config.js строки 84-92);
      - "typescript/no-magic-numbers": ["warn", { "ignore": [-2, 0, 1, 2, 4],
        "ignoreArrayIndexes": true, "ignoreDefaultValues": true, "ignoreEnums": true,
        "ignoreReadonlyClassProperties": true }] (порт no-magic-numbers ignore-листа из
        eslint.config.js строки 64-71; снимает 363 severity-only ложных срабатывания).
    (4) `ignorePatterns`: ["dist/**", "coverage/**", ".agents/**", ".claude/**", ".planning/**",
    "node_modules/**"] (порт ignores из eslint.config.js строки 105-112, плюс node_modules).
    НЕ добавлять `max-lines-per-function`/`max-statements` overrides — preset держит
    max-lines-per-function off, а опции skipBlankLines/skipComments oxlint не поддерживает
    (Assumption A1); потеря документируется в плане 16-04 (RULE-DELTA.md). НЕ включать
    `js.configs.all`-эквивалент. `unicorn/no-null` уже off в preset — НЕ дублировать.
  </action>
  <verify>
    <automated>node -e "const c=require('./.oxlintrc.json'); const a=c.extends.join(','); if(!a.includes('node_modules/@solid-stats/ts-toolchain/oxlint/base.oxlintrc.json')) throw new Error('extends path wrong: '+a); if(c.rules['no-await-in-loop']!=='off') throw new Error('no-await-in-loop must be off'); if(c.rules['typescript/require-await']!=='off') throw new Error('require-await must be off'); if(!Array.isArray(c.rules['unicorn/prevent-abbreviations'])) throw new Error('prevent-abbreviations allowList missing'); console.log('oxlintrc OK')"</automated>
  </verify>
  <done>
    `.oxlintrc.json` существует; валидный JSON; extends — относительный node_modules путь к
    shared preset; repo overrides содержат no-await-in-loop off, typescript/require-await off,
    prevent-abbreviations с allowList, typescript/no-magic-numbers с ignore-листом; нет
    bare-specifier extends; нет `js.configs.all`.
  </done>
</task>

<task type="auto">
  <name>Task 2: package.json — удалить eslint deps, добавить oxlint@1.69.0, переписать lint/lint:types</name>
  <files>package.json</files>
  <read_first>
    - `package.json` строки 30-46 (devDependencies) и 15-20 (scripts lint/verify).
    - RESEARCH «Package Legitimacy Audit»: oxlint@1.69.0 — официальный OXC пакет, pin без `^`.
    - RESEARCH «Удаляемые пакеты»: eslint, @eslint/js, typescript-eslint,
      eslint-plugin-unicorn, eslint-plugin-import-x, eslint-import-resolver-typescript.
  </read_first>
  <action>
    В `package.json` devDependencies УДАЛИТЬ строки: `eslint`, `@eslint/js`,
    `typescript-eslint`, `eslint-plugin-unicorn`, `eslint-plugin-import-x`,
    `eslint-import-resolver-typescript`. ДОБАВИТЬ `"oxlint": "1.69.0"` (точный pin, БЕЗ `^` —
    LNT-01 фиксирует версию). НЕ добавлять `oxlint-tsgolint` в devDependencies/lockfile —
    он ставится изолированно в dev-окружении (RESEARCH LNT-04; не попадает в pnpm install/verify).
    В scripts: заменить `"lint": "eslint ."` на `"lint": "oxlint --config .oxlintrc.json src"`;
    добавить `"lint:types": "oxlint --type-aware --config .oxlintrc.json src"` (отдельный скрипт).
    НЕ трогать `verify` в этом плане (depcruise/knip/lint:types-порядок — план 16-06);
    `verify` продолжит вызывать `pnpm run lint`, который теперь = oxlint.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies; for(const k of ['eslint','@eslint/js','typescript-eslint','eslint-plugin-unicorn','eslint-plugin-import-x','eslint-import-resolver-typescript']){ if(k in d) throw new Error('eslint dep still present: '+k);} if(d.oxlint!=='1.69.0') throw new Error('oxlint not pinned 1.69.0: '+d.oxlint); if(!/^oxlint .*--config .oxlintrc.json src$/.test(p.scripts.lint)) throw new Error('lint script wrong: '+p.scripts.lint); if(!/--type-aware/.test(p.scripts['lint:types'])) throw new Error('lint:types missing --type-aware'); if(/lint:types/.test(p.scripts.verify)) throw new Error('lint:types must NOT be in verify'); console.log('package.json OK')"</automated>
  </verify>
  <done>
    Шесть eslint-пакетов отсутствуют в devDependencies; `oxlint` запинен на `1.69.0`;
    `lint`=`oxlint --config .oxlintrc.json src`; `lint:types`=`oxlint --type-aware ...`
    существует и НЕ упоминается в `verify`; `oxlint-tsgolint` НЕ в lockfile.
  </done>
</task>

<task type="auto">
  <name>Task 3: Удалить eslint.config.js, обновить lockfile, прогнать oxlint и сверить ошибки со spike</name>
  <files>eslint.config.js, pnpm-lock.yaml</files>
  <read_first>
    - `eslint.config.js` (удаляется целиком, 114 строк).
    - `.planning/spikes/001-oxlint-preset-port/run-supported.txt` — эталонный список нарушений:
      func-style (~330), method-signature-style (20), explicit-member-accessibility (11),
      consistent-type-specifier-style (41), custom-error-definition (6), id-length (9),
      no-useless-assignment (1). Это РЕАЛЬНЫЕ нарушения — НЕ подавлять, чинятся в 16-02/16-03.
  </read_first>
  <action>
    Удалить файл `eslint.config.js`. Выполнить `pnpm install` чтобы oxlint встал и из
    `pnpm-lock.yaml` ушли eslint-зависимости. Прогнать `pnpm run lint` ОДИН раз для baseline:
    ожидается НЕнулевой exit (реальные convention-нарушения из spike 001). Зафиксировать в
    SUMMARY: что oxlint запустился, какие категории правил сработали, и что 363 no-magic-numbers
    + 236 no-use-before-define из severity-only spike-прогона ИСЧЕЗЛИ благодаря портированным
    опциям (no-magic-numbers ignore-лист) и тому, что `no-use-before-define` в eslint-варианте
    off, а typescript-вариант допускает функции (functions:false-эквивалент). Если опции
    сработали правильно — остаются ТОЛЬКО style-fix категории (func-style, method-signature-style,
    explicit-member-accessibility, consistent-type-specifier-style, custom-error-definition,
    id-length, no-useless-assignment). НЕ чинить код здесь — это задача 16-02/16-03. НЕ добавлять
    suppressions чтобы скрыть ошибки.
  </action>
  <verify>
    <automated>test ! -f eslint.config.js && ! grep -q '"eslint"' pnpm-lock.yaml && node_modules/.bin/oxlint --version | grep -q '1.69.0' && echo "swap OK"</automated>
  </verify>
  <done>
    `eslint.config.js` удалён; `pnpm-lock.yaml` не содержит eslint-пакетов; `oxlint --version`
    = 1.69.0; `pnpm run lint` запускается через oxlint (ненулевой exit от реальных convention
    findings допустим и зафиксирован в SUMMARY как baseline для 16-02/16-03); НЕ no-magic-numbers
    / no-use-before-define массив (опции сняли его).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Установка нового dev-инструмента (oxlint) пересекает supply-chain границу |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | oxlint@1.69.0 install | mitigate | Точный pin `1.69.0` (не `^`); официальный OXC пакет (RESEARCH Package Legitimacy Audit — Approved); lockfile коммитится |
| T-16-02 | Tampering | удаление 6 eslint devDeps | accept | Удаление зависимостей снижает поверхность; lockfile фиксирует факт |
| T-16-SC | Tampering | npm installs (pnpm) | mitigate | Все три инструмента фазы (oxlint/depcruise/knip) проверены в RESEARCH audit как [OK]; нет [ASSUMED]/[SUS] пакетов в verify-пути — checkpoint не требуется |
</threat_model>

<verification>
- `pnpm run lint` исполняет oxlint (не eslint).
- `.oxlintrc.json` расширяет shared preset по относительному пути; опции портированы.
- Шесть eslint-пакетов удалены из package.json и lockfile.
- `lint:types` существует, не входит в verify.
- Baseline-список oxlint-нарушений совпадает по категориям со spike 001 (style-fix only).
</verification>

<success_criteria>
- LNT-01: eslint удалён, oxlint — единственный линтер, `pnpm lint`=oxlint.
- LNT-02: опции портированы (не severity-only), нет `js.configs.all`, `unicorn/no-null` +
  `no-await-in-loop` off.
- LNT-04 (часть): `lint:types` существует как non-blocking скрипт вне verify.
- `eslint.config.js` удалён; lockfile чист от eslint.
</success_criteria>

<output>
Create `.planning/phases/16-oxlint-migration-import-hygiene/16-01-SUMMARY.md` when done.
Зафиксировать в SUMMARY baseline-вывод первого `pnpm run lint` (категории нарушений) — он нужен
планам 16-02/16-03 как контрольный список code-fix.
</output>
