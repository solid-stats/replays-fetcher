---
phase: 16-oxlint-migration-import-hygiene
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - RULE-DELTA.md
autonomous: true
requirements: [LNT-03, LNT-04]
must_haves:
  truths:
    - "`RULE-DELTA.md` существует и перечисляет все 32 dropped ESLint-правила"
    - "Каждое dropped правило имеет disposition: accepted-lost или covered-by (tsc|depcruise|knip|tsgolint)"
    - "`import/order` зафиксирован как accepted loss (per DFT-02 deferred)"
    - "`pnpm run lint:types` запускается без краша на этом репо (LNT-04 ревалидация)"
    - "`lint:types` НЕ входит в blocking verify-цепочку"
  artifacts:
    - path: "RULE-DELTA.md"
      provides: "Before/after rule-delta документ; каждое dropped правило явно принято/покрыто"
      contains: "import/order"
  key_links:
    - from: "RULE-DELTA.md"
      to: ".planning/spikes/001-oxlint-preset-port/dropped.tsv"
      via: "таблица 32 правил с dispositions из dropped.tsv"
      pattern: "naming-convention"
---

<objective>
Создать `RULE-DELTA.md` — committed before/after rule-delta артефакт (LNT-03). Для каждого из 32
правил из spike `dropped.tsv` указать disposition: либо `accepted-lost` (с rationale), либо
`covered-by` инструментом (tsc / dependency-cruiser / knip / tsgolint). `import/order` —
единственный genuine orphan — фиксируется как accepted loss per DFT-02 (deferred). Плюс
ревалидировать LNT-04: `pnpm run lint:types` (type-aware oxlint/tsgolint) запускается чисто на
этом репо и остаётся вне verify.

Purpose: закрыть LNT-03 (rule-delta артефакт) и финализировать LNT-04 (type-aware ревалидация,
non-blocking).
Output: `RULE-DELTA.md` в корне репо; зафиксированный в SUMMARY результат прогона `lint:types`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-oxlint-migration-import-hygiene/16-RESEARCH.md

# Источник списка dropped правил + готовый анализ dispositions в RESEARCH «LNT-02/LNT-03» таблице
@.planning/spikes/001-oxlint-preset-port/dropped.tsv
@.planning/spikes/001-oxlint-preset-port/oxlintrc.supported.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать RULE-DELTA.md (32 dropped правила с dispositions)</name>
  <files>RULE-DELTA.md</files>
  <read_first>
    - `.planning/spikes/001-oxlint-preset-port/dropped.tsv` — 32 строки (plugin TAB rule).
    - RESEARCH раздел «Рецепт LNT-02/LNT-03» — таблица с уже проанализированными dispositions
      (например: import/no-unresolved→tsc, import/no-unused-modules→knip,
      import/no-extraneous-dependencies→knip, import/no-deprecated→typescript/no-deprecated в
      preset, typescript/naming-convention→accepted ЗНАЧИМАЯ потеря, import/order→orphan).
    - RESEARCH примечание: `unicorn/prevent-abbreviations` фигурирует в dropped.tsv, НО
      присутствует в supported.json с allowList — НЕ потеря (документировать как «restored via
      repo override»).
  </read_first>
  <action>
    Создать `RULE-DELTA.md` в КОРНЕ репо. Структура:
    (1) Заголовок + контекст: «Phase 16 ESLint→Oxlint rule delta; before = eslint.config.js
    (js.configs.all + strictTypeChecked + stylisticTypeChecked + import-x + unicorn recommended);
    after = oxlint 1.69.0 + shared preset @solid-stats/ts-toolchain + repo overrides».
    (2) Таблица «Dropped rules» из всех 32 строк dropped.tsv, колонки: Plugin | Rule | Disposition
    (accepted-lost / covered-by:<tool> / restored-via-override) | Rationale.
    Использовать готовый анализ из RESEARCH:
      - covered-by:tsc — eslint(no-octal), no-octal-escape, no-undef-init, import/no-unresolved;
      - covered-by:knip — import/no-extraneous-dependencies, import/no-unused-modules;
      - covered-by:preset — import/no-deprecated (→ typescript/no-deprecated);
      - covered-by:oxfmt — unicorn/template-indent;
      - restored-via-override — unicorn/prevent-abbreviations (allowList в repo override);
      - accepted-lost — остальные (consistent-this, no-unreachable-loop, require-atomic-updates*,
        import/no-import-module-exports, no-relative-packages, no-useless-path-segments,
        typescript/member-ordering, typescript/naming-convention [ЗНАЧИМАЯ потеря],
        typescript/prefer-destructuring, unicorn/better-regex, consistent-destructuring,
        expiring-todo-comments, no-array-push-push, no-for-loop, no-keyword-prefix,
        no-named-default, no-unnecessary-polyfills, no-unused-properties, prefer-export-from,
        prefer-json-parse-buffer, prefer-switch, string-content) с краткой rationale каждой.
    (3) Отдельная секция «import/order — accepted loss»: единственный genuine orphan; нет Oxlint
    1.69.0 эквивалента; принято per DFT-02 deferred (residual simple-import-sort добавляется
    только если depcruise/knip оставят ordering непокрытым — здесь не добавляем).
    (4) Секция «Eslint.config.js option losses»: max-lines-per-function skipBlankLines/skipComments
    (preset держит off — Assumption A1), max-statements (не в preset) — задокументировать как
    accepted.
    (5) Секция «discover.ts fence #2 boundary»: depcruise-правило commands→storage/staging будет
    severity:warn (не error), т.к. discover.ts использует DI (BuildCliDependencies), реального
    нарушения нет — документировать здесь как known/allowed (готовит почву для плана 16-05).
    КАЖДОЕ из 32 правил должно иметь явный disposition (нет пустых строк).
  </action>
  <verify>
    <automated>test -f RULE-DELTA.md && c=$(grep -v '^#' RULE-DELTA.md | grep -cE 'naming-convention|no-unresolved|no-unused-modules|import/order|prevent-abbreviations'); test "$c" -ge 5 && grep -q 'import/order' RULE-DELTA.md && grep -qiE 'DFT-02|accepted' RULE-DELTA.md && echo "RULE-DELTA OK"</automated>
  </verify>
  <done>
    `RULE-DELTA.md` существует в корне; перечислены все 32 dropped правила, каждое с disposition;
    import/order помечен accepted loss per DFT-02; option-losses и fence-#2 секции присутствуют.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ревалидировать lint:types (type-aware oxlint/tsgolint), подтвердить non-blocking</name>
  <files>package.json</files>
  <read_first>
    - RESEARCH «Рецепт LNT-04»: spike 001 доказал — tsgolint alpha запускается на всех файлах без
      краша (~+160мс), 0 срабатываний на чистом src. Установка tsgolint — изолированная (вне
      pnpm lockfile): `npm install oxlint-tsgolint` во временной директории, копирование
      `oxlint-tsgolint` + `@oxlint-tsgolint` в node_modules, симлинк в .bin/tsgolint.
    - `package.json` scripts: `lint:types` = `oxlint --type-aware --config .oxlintrc.json src`
      (создан в 16-01).
  </read_first>
  <action>
    Установить tsgolint ИЗОЛИРОВАННО (RESEARCH процедура: временная директория →
    `npm install oxlint-tsgolint` → копировать `node_modules/oxlint-tsgolint` и
    `node_modules/@oxlint-tsgolint` в node_modules этого репо → симлинк
    `.bin/tsgolint` → `../oxlint-tsgolint/bin/tsgolint.js`). Это НЕ попадает в pnpm-lock.yaml
    (dev-only, не в verify). Запустить `pnpm run lint:types` — должен завершиться без
    краша/паники. На чистом src ожидается 0 срабатываний (код прошёл strictTypeChecked).
    Подтвердить, что `lint:types` НЕ упоминается в `verify` (grep). Зафиксировать в SUMMARY:
    команду установки, факт что lint:types ран без краша, что он вне verify (LNT-04 policy:
    non-blocking до per-repo валидации). Если tsgolint-пакет недоступен/изменил процедуру
    (Assumption A4) — зафиксировать как known limitation; `lint:types` скрипт всё равно
    присутствует (его наличие закрывает LNT-04 surface), а блокирующего требования к verify нет.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!/--type-aware/.test(p.scripts['lint:types'])) throw new Error('lint:types missing'); if(/lint:types/.test(p.scripts.verify)) throw new Error('lint:types must be OUT of verify'); console.log('lint:types non-blocking OK')"</automated>
  </verify>
  <done>
    `lint:types` скрипт присутствует и отсутствует в `verify`; прогон `pnpm run lint:types`
    задокументирован в SUMMARY (ран без краша ИЛИ зафиксирован known limitation по доступности
    tsgolint); LNT-04 policy non-blocking подтверждён.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → isolated dev install | tsgolint ставится изолированно вне lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | oxlint-tsgolint (alpha, isolated install) | accept | Не в pnpm lockfile, не в verify-цепочке (non-blocking); RESEARCH audit пометил [ASSUMED] но изолированная установка не влияет на CI/prod surface |
| T-16-06 | Information disclosure | RULE-DELTA.md (документ) | accept | Документ описывает toolchain dispositions; не содержит секретов |
</threat_model>

<verification>
- `RULE-DELTA.md` существует, все 32 dropped правила с dispositions.
- import/order = accepted loss per DFT-02.
- `lint:types` присутствует, вне verify; прогон задокументирован.
</verification>

<success_criteria>
- LNT-03: rule-delta задокументирован, каждое dropped правило явно принято/покрыто.
- LNT-04: type-aware oxlint ревалидирован, non-blocking вне verify.
</success_criteria>

<output>
Create `.planning/phases/16-oxlint-migration-import-hygiene/16-04-SUMMARY.md` when done.
Зафиксировать результат прогона `pnpm run lint:types` (ран без краша / known limitation) и
команду изолированной установки tsgolint.
</output>
