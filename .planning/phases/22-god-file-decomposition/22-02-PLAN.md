---
phase: 22-god-file-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discovery/discover.ts
  - src/discovery/discover-candidate.ts
  - src/discovery/discover-diagnostics.ts
  - src/discovery/discover-dedup.ts
autonomous: true
requirements: [SPLIT-02]
must_haves:
  truths:
    - "src/discovery/discover.ts is under 300 lines with the oxlint-disable max-lines line removed [src: 22-RESEARCH.md#SPLIT-02]"
    - "Every new discovery/ sibling is under 300 lines and carries no oxlint-disable max-lines [src: 22-CONTEXT.md#decisions]"
    - "discoverReplaysDryRun and toRawReplayUrl stay importable from discover.js (no caller edited) [src: 22-RESEARCH.md#SPLIT-02]"
    - "pnpm run verify is green after EACH extraction commit, not only at the end [src: 22-CONTEXT.md#domain]"
    - "The golden run-once oracle stays green after the split [src: 22-VALIDATION.md]"
    - "Zero logic/identifier/signature change — pure structural move [src: 22-CONTEXT.md#decisions]"
  artifacts:
    - path: "src/discovery/discover-candidate.ts"
      provides: "fixture/html candidate assembly + dedup/registry helpers extracted from discover"
    - path: "src/discovery/discover-diagnostics.ts"
      provides: "diagnostic-evidence/attach helpers + report builder + source-failure diagnostic extracted from discover"
    - path: "src/discovery/discover.ts"
      provides: "discoverReplaysDryRun entry, toRawReplayUrl (public), paced-source-client wiring"
      contains: "export const discoverReplaysDryRun"
  key_links:
    - from: "src/discovery/discover.ts"
      to: "src/discovery/discover-candidate.ts"
      via: "imports discoverPageCandidates / parseSourceFixture"
    - from: "src/discovery/discover.ts"
      to: "src/discovery/discover-diagnostics.ts"
      via: "imports buildReport / buildSourceFailureDiagnostic / buildReadOptions"
  prohibitions:
    - "No split crosses a band or lands in a shared adapters/ dir (all siblings live in src/discovery/) [src: 22-CONTEXT.md#decisions]"
    - "No NEW upward/cross-band import is introduced (Phase 23 fences stay a no-op) [src: 22-RESEARCH.md#Import-Graph-Safety]"
    - "The oxlint-disable max-lines line is removed only on the FINAL extraction, once the parent is under 300 [src: 22-RESEARCH.md#Per-Extraction-Gate]"
    - "discover.ts imports SourceFetchError from ./source-client.js (the parent re-export path) — it MUST NOT switch to importing from source-client-error.js, or the SPLIT-02/SPLIT-03 file sets overlap [src: 22-RESEARCH.md#Parallel-Safety-Verdict]"
    - "src/commands/shared.ts (296/300) is untouched [src: 22-RESEARCH.md#shared.ts-Watch]"
    - "Test files are NOT split in this phase; discover.test.ts imports stay on discover.js / source-client.js [src: 22-RESEARCH.md#Pitfall-3]"
---

<objective>
Split `src/discovery/discover.ts` (701L) into the parent plus two cohesive same-band siblings in `src/discovery/`, and remove its `oxlint-disable max-lines` suppression. Pure structural move: no logic, identifier, or signature change. Implements SPLIT-02 [src: 22-RESEARCH.md#SPLIT-02].

Purpose: drop the parent and every sibling under the 300-line `max-lines` threshold with the suppression gone. The public exports `discoverReplaysDryRun` (consumed by commands/shared.ts) and `toRawReplayUrl` (consumed by contract-check.ts) stay in the parent so no caller is edited and the SPLIT-02 file set stays disjoint from the other three splits.

Output: `src/discovery/discover.ts` (~140L), `src/discovery/discover-candidate.ts` (~310L; split a 3rd sibling if it lands over 300), `src/discovery/discover-diagnostics.ts` (~180L).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-god-file-decomposition/22-RESEARCH.md
@src/discovery/discover.ts
@src/discovery/discover.test.ts
</context>

<premises_ledger>
| # | Claim | Source | Verify |
|---|-------|--------|--------|
| P1 | discover.ts is 701L and carries exactly one max-lines suppression | [src: 22-CONTEXT.md#decisions] | `wc -l src/discovery/discover.ts` is 701; `grep -c 'oxlint-disable max-lines' src/discovery/discover.ts` is 1 |
| P2 | Public exports consumed by callers are discoverReplaysDryRun (commands/shared.ts) and toRawReplayUrl (contract-check.ts) | [src: 22-RESEARCH.md#SPLIT-02] | `grep -rn 'discoverReplaysDryRun\|toRawReplayUrl' src/commands src/contract-check` |
| P3 | discover.ts imports SourceFetchError from ./source-client.js and does NOT edit source-client.ts | [src: 22-RESEARCH.md#Parallel-Safety-Verdict] | `grep -n 'from "./source-client.js"' src/discovery/discover.ts` |
| P4 | No test imports an internal (non-public) symbol from discover.ts | [src: 22-RESEARCH.md#Pitfall-3] | `grep -rn 'from "./discover.js"' src/discovery/*.test.ts` shows only discoverReplaysDryRun |
</premises_ledger>

<artifacts_this_phase_produces>
- `src/discovery/discover-candidate.ts` (new) — fixture parsing + candidate assembly + dedup/registry
- `src/discovery/discover-diagnostics.ts` (new) — diagnostic-evidence/attach helpers + report builder + source-failure diagnostic
- (conditional) `src/discovery/discover-dedup.ts` (new) — only if `discover-candidate.ts` lands over 300
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract diagnostics/report helpers into discover-diagnostics.ts</name>
  <read_first>src/discovery/discover.ts (attachNumber/attachString/attachPhase/attachCfChallenge 517-561, withSourceFailureEvidence 563-580, detailUrlOrSource 582-590, buildSourceFailureDiagnostic 592-613, diagnosticEvidence 268-289, withOptionalDiagnosticEvidence 291-317, buildReadOptions 615-631, buildReport 492-515, BuildReportOptions 60-65)</read_first>
  <files>src/discovery/discover-diagnostics.ts, src/discovery/discover.ts</files>
  <action>Create `src/discovery/discover-diagnostics.ts` and MOVE verbatim: attachNumber, attachString, attachPhase, attachCfChallenge, withSourceFailureEvidence, detailUrlOrSource, buildSourceFailureDiagnostic, diagnosticEvidence, withOptionalDiagnosticEvidence, buildReadOptions, buildReport, BuildReportOptions, and the ReadOptions type (consumed by buildReadOptions and discover-candidate). Export buildReport, buildSourceFailureDiagnostic, buildReadOptions, diagnosticEvidence, withOptionalDiagnosticEvidence, the ReadOptions type, and the BuildReportOptions type — whatever the parent and the candidate sibling still consume. The diagnostics sibling imports DiscoveryDiagnostic/DiscoveryReport/ReplayCandidate from ./types.js, SourceFetchError from ./source-client.js (a TYPE/VALUE used by detailUrlOrSource/buildSourceFailureDiagnostic — keep this import on ./source-client.js, the parent re-export path), and DiscoverReplaysDryRunOptions from the parent (or lift that shared options type into a small same-band discover-types.ts if it creates a circular boundary; planner discretion, keep no-circular green). Add the matching import in discover.ts. Move each symbol's leading JSDoc verbatim. Keep the max-lines suppression line in discover.ts for now (the parent is still over 300). No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>discover-diagnostics.ts exists with the moved symbols exported; discover.ts imports them; `pnpm run verify` exits 0; no-circular green; the commit reads as a pure move.</done>
</task>

<task type="auto">
  <name>Task 2: Extract candidate assembly into discover-candidate.ts, drop the suppression, confirm under 300</name>
  <read_first>src/discovery/discover.ts (fixture/source types 23-85, parseSourceFixture 125-139, isValidFixtureUrl 112, toReplayCandidate 141-217, toReplayCandidateFromHtmlRow 219-248, hasChangedMetadata 250-266, collectCandidateDiagnostics 319-380, discoverRowCandidate 382-405, collectFixtureCandidates 407-430, discoverPageCandidates 432-490)</read_first>
  <files>src/discovery/discover-candidate.ts, src/discovery/discover.ts</files>
  <action>Create `src/discovery/discover-candidate.ts` and MOVE verbatim: SourceCandidateFixture, SourceFixture, MutableReplayMetadata, MutableReplaySource, CandidateFixtureResult, CandidateRegistryEntry, DiscoverPageCandidatesResult, parseSourceFixture, isValidFixtureUrl, toReplayCandidate, toReplayCandidateFromHtmlRow, hasChangedMetadata, collectCandidateDiagnostics, discoverRowCandidate, collectFixtureCandidates, discoverPageCandidates. Export parseSourceFixture, discoverPageCandidates, and the DiscoverPageCandidatesResult type (the parent's discoverReplaysDryRun calls parseSourceFixture and discoverPageCandidates). This sibling imports extractFilenameFromDetailHtml/extractReplayRows from ./html.js, the discovery types from ./types.js, toRawReplayUrl from the parent (./discover.js — it stays the public export site) or, if that creates a circular boundary, keep toRawReplayUrl in the parent and import it; the candidate sibling also imports diagnosticEvidence/withOptionalDiagnosticEvidence/ReadOptions from ./discover-diagnostics.js — all same-band. After the move, measure discover.ts and discover-candidate.ts with wc -l. If discover-candidate.ts is 300 or more, relocate the dedup/registry helpers (hasChangedMetadata, collectCandidateDiagnostics, CandidateRegistryEntry) into a 3rd sibling src/discovery/discover-dedup.ts so both land under 300 [src: 22-RESEARCH.md#SPLIT-02]. Once discover.ts is confirmed under 300, REMOVE the max-lines suppression line — this is the FINAL extraction; never remove it while the parent is 300 or more [src: 22-RESEARCH.md#Per-Extraction-Gate]. The parent discover.ts then retains: DiscoverReplaysDryRunOptions, toRawReplayUrl (public), toPageUrl, defaultRequestDelayMs, defaultSleep, createPacedSourceClient, and the discoverReplaysDryRun entry. No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify && test "$(grep -c 'oxlint-disable max-lines' src/discovery/discover.ts)" -eq 0 && test "$(wc -l < src/discovery/discover.ts)" -lt 300 && test "$(wc -l < src/discovery/discover-candidate.ts)" -lt 300 && test "$(wc -l < src/discovery/discover-diagnostics.ts)" -lt 300 && pnpm run test:integration</automated>
  </verify>
  <done>discover.ts is under 300 with the suppression gone; every discovery/ sibling is under 300; discoverReplaysDryRun and toRawReplayUrl still export from discover.js (no caller edited); `pnpm run verify` exits 0; the golden oracle is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure structural in-band code move; no input, auth, secret, schema, key-layout, or external-surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-NOOP | n/a | discover split | accept | No new threats: compile-time-only moves of pure helpers. The identifiers-only diagnostic builders (DIAG-01/04, threat T-08-01) move verbatim with their guard JSDoc; existing unit tests + the golden oracle pin behavior [src: 22-RESEARCH.md#Security-Domain]. |
| T-22-02-SC | Tampering | npm/pnpm installs | mitigate | No package installs in this plan (structural move only); no `[ASSUMED]`/`[SUS]` package introduced. |
</threat_model>

<verification>
- `pnpm run verify` exits 0 after EACH extraction commit (lint + typecheck + test + 100% V8 coverage + build + depcruise + knip).
- `pnpm run test:integration` (golden run-once oracle) green after the final extraction.
- `grep -c 'oxlint-disable max-lines' src/discovery/discover.ts` is 0.
- `wc -l` confirms discover.ts and every new discovery/ sibling are under 300.
- `grep -n 'from "./source-client.js"' src/discovery/discover.ts` still resolves SourceFetchError from the parent path (no overlap with SPLIT-03).
- `wc -l src/commands/shared.ts` is unchanged (296).
</verification>

<success_criteria>
SPLIT-02 is satisfied: discover.ts is split within the discovery/ band into cohesive siblings, the `oxlint-disable max-lines` line is removed, every file is under 300 lines, no caller is edited, and the golden oracle plus 100% coverage stay green.
</success_criteria>

<output>
Create `.planning/phases/22-god-file-decomposition/22-02-SUMMARY.md` when done.
</output>
