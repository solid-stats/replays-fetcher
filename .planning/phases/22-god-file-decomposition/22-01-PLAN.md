---
phase: 22-god-file-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/run/run-once.ts
  - src/run/run-once-checkpoint.ts
  - src/run/run-once-summary.ts
  - src/run/run-once-page.ts
  - src/run/run-once-types.ts
autonomous: true
requirements: [SPLIT-01]
must_haves:
  truths:
    - "src/run/run-once.ts is under 300 lines with the oxlint-disable max-lines line removed [src: 22-RESEARCH.md#SPLIT-01]"
    - "Every new run/ sibling is under 300 lines and carries no oxlint-disable max-lines [src: 22-CONTEXT.md#decisions]"
    - "runOnce, RunOnceResult, derivePagesPerMinute stay importable from run-once.js (no caller edited) [src: 22-RESEARCH.md#SPLIT-01]"
    - "pnpm run verify is green after EACH extraction commit, not only at the end [src: 22-CONTEXT.md#domain]"
    - "The golden run-once oracle stays green after the split [src: 22-VALIDATION.md]"
    - "Zero logic/identifier/signature change — pure structural move [src: 22-CONTEXT.md#decisions]"
  artifacts:
    - path: "src/run/run-once-checkpoint.ts"
      provides: "resume/checkpoint helpers extracted from run-once"
    - path: "src/run/run-once-summary.ts"
      provides: "rate/emit/assemble + derivePagesPerMinute extracted from run-once"
    - path: "src/run/run-once-page.ts"
      provides: "page-loop + throttle + page-completion extracted from run-once"
    - path: "src/run/run-once.ts"
      provides: "runOnce entry, RunOnceInput/Result, sanitizeSourceUrl; re-exports derivePagesPerMinute"
      contains: "export const runOnce"
  key_links:
    - from: "src/run/run-once.ts"
      to: "src/run/run-once-page.ts"
      via: "imports runPageLoop / buildRunRuntime / buildLoopState"
    - from: "src/run/run-once.ts"
      to: "src/run/run-once-summary.ts"
      via: "imports assembleResult + re-exports derivePagesPerMinute"
  prohibitions:
    - "No split crosses a band or lands in a shared adapters/ dir (all siblings live in src/run/) [src: 22-CONTEXT.md#decisions]"
    - "No NEW upward/cross-band import is introduced (Phase 23 fences stay a no-op) [src: 22-RESEARCH.md#Import-Graph-Safety]"
    - "The oxlint-disable max-lines line is removed only on the FINAL extraction, once the parent is under 300 [src: 22-RESEARCH.md#Per-Extraction-Gate]"
    - "src/commands/shared.ts (296/300) is untouched [src: 22-RESEARCH.md#shared.ts-Watch]"
    - "Test files are NOT split in this phase; existing run-once.test.ts imports stay on run-once.js [src: 22-RESEARCH.md#Pitfall-3]"
---

<objective>
Split `src/run/run-once.ts` (1043L, the highest-risk god-file — the ingest orchestration core) into the parent plus three cohesive same-band siblings in `src/run/`, and remove its `oxlint-disable max-lines` suppression. Pure structural move: no logic, identifier, or signature change. Implements SPLIT-01 [src: 22-RESEARCH.md#SPLIT-01].

Purpose: drop the parent and every sibling under the 300-line `max-lines` threshold with the suppression gone, so Phase 23's band fences land as a no-op. A dropped branch here keeps 100% coverage but the golden run-once oracle catches it — hence the per-extraction oracle run is mandatory.

Output: `src/run/run-once.ts` (~120L), `src/run/run-once-checkpoint.ts` (~230L), `src/run/run-once-summary.ts` (~260L), `src/run/run-once-page.ts` (~330L; split a 4th sibling if it lands over 300).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-god-file-decomposition/22-RESEARCH.md
@src/run/run-once.ts
@src/run/run-once.test.ts
</context>

<premises_ledger>
| # | Claim | Source | Verify |
|---|-------|--------|--------|
| P1 | run-once.ts is 1043L and carries exactly one max-lines suppression | [src: 22-CONTEXT.md#decisions] | `wc -l src/run/run-once.ts` is 1043; `grep -c 'oxlint-disable max-lines' src/run/run-once.ts` is 1 |
| P2 | Callers consume only `runOnce` (commands/shared.ts, run-once.test.ts, no-leak.test.ts) and `derivePagesPerMinute` (run-once.test.ts) | [src: 22-RESEARCH.md#SPLIT-01] | `grep -rn 'from "./run-once' src/run/*.test.ts src/commands/shared.ts` |
| P3 | All moved helpers import only downward — no new upward import | [src: 22-RESEARCH.md#Import-Graph-Safety] | `pnpm run depcruise` green after each move |
| P4 | No test imports an internal (non-public) symbol from run-once.ts | [src: 22-RESEARCH.md#Pitfall-3] | grep in P2 shows only `runOnce` / `derivePagesPerMinute` |
</premises_ledger>

<artifacts_this_phase_produces>
- `src/run/run-once-checkpoint.ts` (new) — resume-state + checkpoint builders
- `src/run/run-once-summary.ts` (new) — rate derivation + per-page/run emit + result assembly
- `src/run/run-once-page.ts` (new) — page-loop driver + throttle + page completion
- (conditional) `src/run/run-once-page-rate.ts` (new) — only if `run-once-page.ts` lands over 300
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract resume/checkpoint helpers into run-once-checkpoint.ts</name>
  <read_first>src/run/run-once.ts (resume/checkpoint cluster: ResumeState 355-359, startFresh/resumeFrom 177-195, resolveResumeState 361-398, LoopState/buildLoopState 400-427, buildCheckpoint 429-458, writePageCheckpoint 460-500, writeFinalCheckpoint 502-533, writeInput 240-250, aggregatePageCounts 252-267, the three Option helpers 197-238); src/run/run-once.test.ts</read_first>
  <files>src/run/run-once-checkpoint.ts, src/run/run-once.ts</files>
  <action>Create `src/run/run-once-checkpoint.ts` and MOVE these symbols verbatim from `run-once.ts`: ResumeState, startFresh, resumeFrom, resolveResumeState, LoopState, buildLoopState, BuildCheckpointInput, buildCheckpoint, WritePageCheckpointInput, writePageCheckpoint, writeFinalCheckpoint, writeInput, aggregatePageCounts, discoveredRangeOption, sourceFailureOption, resumeInvocationOption, RESUME_INVOCATION. They reference RunOnceInput, AssembleResultInput, MutableDiscoveryReport, emptyDiscoveryReport, FIRST_PAGE: to give the sibling a clean import boundary, prefer exporting these shared private types from their home file and importing them in the sibling, over duplicating. If a circular boundary appears (parent imports the sibling and the sibling imports the parent for a value), lift the shared private types and FIRST_PAGE into a small same-band run-once-types.ts and import from there (planner discretion; the goal is no-circular staying green). Export every moved symbol the parent or other siblings still consume: buildLoopState, resolveResumeState, writePageCheckpoint, writeFinalCheckpoint, discoveredRangeOption, sourceFailureOption, resumeInvocationOption, and the ResumeState/LoopState/BuildCheckpointInput/WritePageCheckpointInput types. Add the matching import in run-once.ts. Move each symbol's leading JSDoc and the inline comments verbatim — no rewording. Do NOT change any identifier, signature, or body. Keep the max-lines suppression line in run-once.ts for now (the parent is still over 300). The max-lines threshold is 300 [src: 22-CONTEXT.md#decisions].</action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>run-once-checkpoint.ts exists with the moved symbols exported; run-once.ts imports them; `pnpm run verify` exits 0 (lint + typecheck + test + coverage + build + depcruise + knip green); no-circular green; the commit reads as a pure move.</done>
</task>

<task type="auto">
  <name>Task 2: Extract rate/emit/assemble into run-once-summary.ts; re-export derivePagesPerMinute from parent</name>
  <read_first>src/run/run-once.ts (derivePagesPerMinute 154-175, deriveCandidatesPerMinute 316-328, emit helpers 587-660, writeEvidence 881-922, AssembleResultInput/assembleResult 924-1007, deriveDiscoveredLastPage 573-585, constants MS_PER_MINUTE/LAST_TIMESTAMP_INDEX 102-103); src/run/run-once.test.ts line 21</read_first>
  <files>src/run/run-once-summary.ts, src/run/run-once.ts</files>
  <action>Create `src/run/run-once-summary.ts` and MOVE verbatim: derivePagesPerMinute, deriveCandidatesPerMinute, EmitPageRateLineInput, emitPageRateLine, derivePageFailureEventName, derivePageFailureMessage, emitPageFailureEvent, AssembleResultInput, assembleResult, writeEvidence, deriveDiscoveredLastPage, and the constants MS_PER_MINUTE and LAST_TIMESTAMP_INDEX. Export derivePagesPerMinute (public), assembleResult, emitPageRateLine, emitPageFailureEvent, deriveDiscoveredLastPage, and the AssembleResultInput type for the parent and the page sibling. In run-once.ts ADD a re-export statement (not a wrapper) that re-exports derivePagesPerMinute from the summary sibling, so that importing derivePagesPerMinute from run-once.js in run-once.test.ts resolves unchanged [src: 22-RESEARCH.md#SPLIT-01]. Move each symbol's leading JSDoc verbatim, including the v8 ignore comment on deriveCandidatesPerMinute. The summary sibling imports buildRunSummary/deriveRunStatus/deriveSourceFailure/runExitCode from ./summary.js and discoveredRangeOption/resumeInvocationOption/sourceFailureOption/writeFinalCheckpoint from ./run-once-checkpoint.js as needed — all same-band, no upward import. Do NOT fold these into the existing run/summary.ts (it would risk pushing summary.ts over 300) [src: 22-RESEARCH.md#Open-Questions]. Keep the max-lines suppression line in run-once.ts. No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>run-once-summary.ts exists; derivePagesPerMinute is re-exported from run-once.ts (the run-once.test.ts import path is unchanged); knip reports no orphan/unused re-export; `pnpm run verify` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Extract the page loop into run-once-page.ts, drop the suppression, confirm under 300</name>
  <read_first>src/run/run-once.ts (constants/types 100-152, runtime/discovery helpers 269-353, processPage 535-571, throttle + completeOkPage + runPageLoop 662-879)</read_first>
  <files>src/run/run-once-page.ts, src/run/run-once.ts</files>
  <action>Create `src/run/run-once-page.ts` and MOVE verbatim: RunRuntime, buildRunRuntime, defaultPacer, CONCURRENCY_FLOOR, MutablePageCounts, ProcessPageInput, processPage, applyRateLimitThrottle, CompleteOkPageInput, CompleteOkPageResult, completeOkPage, PageLoopContext, runPageLoop, MutableDiscoveryReport, emptyDiscoveryReport, appendDiscoveryReport, buildDiscoverInput, toPageUrl, FIRST_PAGE. Move the two require-atomic-updates inline disable comments on state.etag and state.lastCompletedPage verbatim with the loop. Export buildRunRuntime, runPageLoop, MutableDiscoveryReport, emptyDiscoveryReport, FIRST_PAGE, and any type the checkpoint/summary siblings still import (MutablePageCounts). The page sibling imports emitPageRateLine/emitPageFailureEvent from ./run-once-summary.js, ingestPage from ./ingest-page.js, and writePageCheckpoint from ./run-once-checkpoint.js — all same-band/downward. After the move, measure run-once.ts and run-once-page.ts with wc -l. If run-once-page.ts is 300 or more, relocate applyRateLimitThrottle plus the page-rate emit helpers into a 4th sibling src/run/run-once-page-rate.ts so both land under 300 [src: 22-RESEARCH.md#Pitfall-5]. Once run-once.ts is confirmed under 300, REMOVE the max-lines suppression line from run-once.ts — this is the FINAL extraction; never remove it while the parent is 300 or more [src: 22-RESEARCH.md#Per-Extraction-Gate]. The parent run-once.ts then retains only: the RunOnceInput type, RunOnceResult (exported), sanitizeSourceUrl, the runOnce entry, and the derivePagesPerMinute re-export. No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify && test "$(grep -c 'oxlint-disable max-lines' src/run/run-once.ts)" -eq 0 && test "$(wc -l < src/run/run-once.ts)" -lt 300 && test "$(wc -l < src/run/run-once-checkpoint.ts)" -lt 300 && test "$(wc -l < src/run/run-once-summary.ts)" -lt 300 && test "$(wc -l < src/run/run-once-page.ts)" -lt 300 && pnpm run test:integration</automated>
  </verify>
  <done>run-once.ts is under 300 with the suppression line gone; every run/ sibling is under 300; `pnpm run verify` exits 0; the golden run-once oracle (test:integration) is green; commands/shared.ts line count is unchanged (296).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure structural in-band code move; no input, auth, secret, schema, key-layout, or external-surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-NOOP | n/a | run-once split | accept | No new threats: compile-time-only moves of pure helpers. `sanitizeSourceUrl` (WR-02 userinfo strip) and identifiers-only diagnostic builders move verbatim with their guard comments; the golden oracle + existing unit tests pin their behavior [src: 22-RESEARCH.md#Security-Domain]. |
| T-22-01-SC | Tampering | npm/pnpm installs | mitigate | No package installs in this plan (structural move only); no `[ASSUMED]`/`[SUS]` package introduced. |
</threat_model>

<verification>
- `pnpm run verify` exits 0 after EACH of the three extraction commits (lint + typecheck + test + 100% V8 coverage + build + depcruise + knip).
- `pnpm run test:integration` (golden run-once oracle) green after the final extraction.
- `grep -c 'oxlint-disable max-lines' src/run/run-once.ts` is 0.
- `wc -l` confirms run-once.ts and every new run/ sibling are under 300.
- `wc -l src/commands/shared.ts` is unchanged (296).
</verification>

<success_criteria>
SPLIT-01 is satisfied: run-once.ts is split within the run/ band into cohesive siblings, the `oxlint-disable max-lines` line is removed, every file is under 300 lines, no caller is edited, and the golden oracle plus 100% coverage stay green.
</success_criteria>

<output>
Create `.planning/phases/22-god-file-decomposition/22-01-SUMMARY.md` when done.
</output>