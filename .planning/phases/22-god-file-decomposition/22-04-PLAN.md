---
phase: 22-god-file-decomposition
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/replay-byte-client.ts
  - src/storage/replay-byte-client-error.ts
  - src/storage/replay-byte-client-retry.ts
autonomous: true
requirements: [SPLIT-04]
must_haves:
  truths:
    - "src/storage/replay-byte-client.ts is under 300 lines with the oxlint-disable max-lines line removed [src: 22-RESEARCH.md#SPLIT-04]"
    - "Every new storage/ sibling is under 300 lines and carries no oxlint-disable max-lines [src: 22-CONTEXT.md#decisions]"
    - "ReplayByteFetchError physically lives in replay-byte-client-error.ts and is re-exported from replay-byte-client.ts, so ./replay-byte-client.js stays the import path for store-raw-replay.ts and the run/ consumers (no caller edited) [src: 22-RESEARCH.md#Pitfall-2]"
    - "createReplayByteClient, ReplayByteClient, ByteFetchOptions stay importable from replay-byte-client.js [src: 22-RESEARCH.md#SPLIT-04]"
    - "no-circular stays green after the error class moves into the error sibling [src: 22-RESEARCH.md#Pitfall-2]"
    - "pnpm run verify is green after EACH extraction commit, not only at the end [src: 22-CONTEXT.md#domain]"
    - "Zero logic/identifier/signature change — pure structural move [src: 22-CONTEXT.md#decisions]"
  artifacts:
    - path: "src/storage/replay-byte-client-error.ts"
      provides: "ReplayByteFetchError class (moved here) + error/classify builders"
      contains: "export class ReplayByteFetchError"
    - path: "src/storage/replay-byte-client-retry.ts"
      provides: "RetryWiring + runWithRetry + try-count helpers extracted from replay-byte-client"
    - path: "src/storage/replay-byte-client.ts"
      provides: "createReplayByteClient entry, ReplayByteClient/ByteFetchOptions types, direct + SSH adapter factories, re-exports ReplayByteFetchError"
      contains: "export const createReplayByteClient"
  key_links:
    - from: "src/storage/replay-byte-client.ts"
      to: "src/storage/replay-byte-client-error.ts"
      via: "imports the error builders + re-exports ReplayByteFetchError (parent depends on sibling; no back-edge)"
    - from: "src/storage/replay-byte-client.ts"
      to: "src/storage/replay-byte-client-retry.ts"
      via: "imports runWithRetry"
  prohibitions:
    - "No split crosses a band or lands in a shared adapters/ dir (all siblings live in src/storage/) [src: 22-CONTEXT.md#decisions]"
    - "No NEW upward/cross-band import is introduced (Phase 23 fences stay a no-op) [src: 22-RESEARCH.md#Import-Graph-Safety]"
    - "The error sibling MUST NOT import ReplayByteFetchError from the parent — the class moves INTO the sibling and the parent re-exports it (else no-circular fails) [src: 22-RESEARCH.md#Pitfall-2]"
    - "The oxlint-disable max-lines line is removed only on the FINAL extraction, once the parent is under 300 [src: 22-RESEARCH.md#Per-Extraction-Gate]"
    - "src/commands/shared.ts (296/300) is untouched [src: 22-RESEARCH.md#shared.ts-Watch]"
    - "Test files are NOT split; replay-byte-client.test.ts and store-raw-replay.test.ts keep importing from ./replay-byte-client.js [src: 22-RESEARCH.md#Pitfall-3]"
---

<objective>
Split `src/storage/replay-byte-client.ts` (489L) into the parent plus two cohesive same-band siblings in `src/storage/`, and remove its `oxlint-disable max-lines` suppression. Pure structural move: no logic, identifier, or signature change. Implements SPLIT-04 [src: 22-RESEARCH.md#SPLIT-04].

Purpose: this file is a near-exact mirror of `source-client.ts`, including the same circular-import hazard. The error builders reference `ReplayByteFetchError` and the parent's adapter factories reference the error builders. Mitigation (Pitfall 2): physically move `ReplayByteFetchError` INTO `replay-byte-client-error.ts` and re-export it from the parent, so the dependency is strictly parent to sibling with no back-edge, and `./replay-byte-client.js` stays the unchanged import path for `store-raw-replay.ts`, `run/run-once.ts`, `run/ingest-page.ts`, and `run/watch-loop.ts`.

Output: `src/storage/replay-byte-client.ts` (~210L), `src/storage/replay-byte-client-error.ts` (~230L), `src/storage/replay-byte-client-retry.ts` (~70L).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/replays-fetcher/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-god-file-decomposition/22-RESEARCH.md
@src/storage/replay-byte-client.ts
@src/storage/replay-byte-client.test.ts
</context>

<premises_ledger>
| # | Claim | Source | Verify |
|---|-------|--------|--------|
| P1 | replay-byte-client.ts is 489L and carries exactly one max-lines suppression | [src: 22-CONTEXT.md#decisions] | `wc -l src/storage/replay-byte-client.ts` is 489; `grep -c 'oxlint-disable max-lines' src/storage/replay-byte-client.ts` is 1 |
| P2 | Public exports consumed by callers: ReplayByteFetchError (store-raw-replay.ts), ReplayByteClient (store-raw-replay.ts, run/*), ByteFetchOptions, createReplayByteClient (commands/shared.ts) | [src: 22-RESEARCH.md#SPLIT-04] | `grep -rn 'replay-byte-client' src/storage src/run src/commands` |
| P3 | Moving ReplayByteFetchError into the error sibling + re-exporting from the parent breaks the cycle (parent to sibling only) | [src: 22-RESEARCH.md#Pitfall-2] | `pnpm run depcruise` (no-circular) green after the move |
| P4 | replay-byte-client.test.ts and store-raw-replay.test.ts import only the public surface from ./replay-byte-client.js | [src: 22-RESEARCH.md#Pitfall-3] | `grep -rn 'from "./replay-byte-client.js"' src/storage/*.test.ts` |
</premises_ledger>

<artifacts_this_phase_produces>
- `src/storage/replay-byte-client-error.ts` (new) — ReplayByteFetchError (moved here) + error/classify builders
- `src/storage/replay-byte-client-retry.ts` (new) — RetryWiring + runWithRetry + try-count helpers
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the retry wiring into replay-byte-client-retry.ts</name>
  <read_first>src/storage/replay-byte-client.ts (bytesPhase/noRetryAttempts/initialTry 87-89, totalTries 91-96, RetryWiring 113-121, runWithRetry 123-170)</read_first>
  <files>src/storage/replay-byte-client-retry.ts, src/storage/replay-byte-client.ts</files>
  <action>Create `src/storage/replay-byte-client-retry.ts` and MOVE verbatim: RetryWiring, runWithRetry, totalTries, noRetryAttempts, initialTry, and the bytesPhase constant. Export runWithRetry, totalTries, and bytesPhase (the parent's adapter factories call runWithRetry; the error builders call totalTries and reference bytesPhase). The retry sibling imports withRetry + RetrySourceReadOptions + SourceReadPhase from ../source/retry.js, FailureClassification from ../source/classify-failure.js, and ByteFetchOptions from the parent (./replay-byte-client.js — it stays the public export site); if importing ByteFetchOptions from the parent creates a circular boundary, lift ByteFetchOptions and ReplayByteClient into a small same-band replay-byte-client-types.ts and import from there (planner discretion; keep no-circular green). Add the matching import in replay-byte-client.ts. Move each symbol's leading JSDoc verbatim. Keep the max-lines suppression line in replay-byte-client.ts for now. No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>replay-byte-client-retry.ts exists with runWithRetry/totalTries/bytesPhase exported; replay-byte-client.ts imports them; `pnpm run verify` exits 0; no-circular green; the commit reads as a pure move.</done>
</task>

<task type="auto">
  <name>Task 2: Move ReplayByteFetchError + error builders into replay-byte-client-error.ts, re-export from parent, drop the suppression</name>
  <read_first>src/storage/replay-byte-client.ts (ReplayByteFetchError class 66-81, toByteCode 105-111, BuildErrorInput 172-179, buildByteFetchError 190-228, httpHeaderRetryAfter 230, DirectHttpErrorInput 232-235, buildDirectHttpError 242-263, directRetryAfter 270-285, reclassifyDirect 287-298, classifyDirect 300-306, classifySsh 308-309, DirectByteErrorInput 311-315, buildPageInput 317-327, toDirectByteError 329-354, SshByteErrorInput 401-405, toSshByteError 407-418)</read_first>
  <files>src/storage/replay-byte-client-error.ts, src/storage/replay-byte-client.ts</files>
  <action>Create `src/storage/replay-byte-client-error.ts` and physically MOVE the ReplayByteFetchError class into it verbatim (with its constructor, the additive-union JSDoc, and the no-useless-constructor inline disable comment), PLUS these helpers verbatim: toByteCode, BuildErrorInput, buildByteFetchError, httpHeaderRetryAfter, DirectHttpErrorInput, buildDirectHttpError, directRetryAfter, reclassifyDirect, classifyDirect, classifySsh, DirectByteErrorInput, buildPageInput, toDirectByteError, SshByteErrorInput, toSshByteError. The error sibling imports AppError from ../errors/app-error.js, classifyFailure + ClassifyInput/FailureClassification/FailureKind from ../source/classify-failure.js, parseRetryAfter from ../source/backoff.js, ByteFetchOptions from the parent or the lifted types module, and totalTries + bytesPhase from ./replay-byte-client-retry.js — all downward/same-band. It MUST NOT import ReplayByteFetchError from the parent (the class now lives here) [src: 22-RESEARCH.md#Pitfall-2]. Export ReplayByteFetchError, toDirectByteError, toSshByteError, buildDirectHttpError, classifyDirect, classifySsh, directRetryAfter, and any type the parent's adapter factories still reference. In replay-byte-client.ts ADD a re-export statement that re-exports ReplayByteFetchError from ./replay-byte-client-error.js, so importing ReplayByteFetchError from ./replay-byte-client.js in store-raw-replay.ts resolves unchanged. Also update getSshHost in the parent (it constructs a ReplayByteFetchError) to import the class from ./replay-byte-client-error.js. After the move, measure replay-byte-client.ts, replay-byte-client-error.ts, replay-byte-client-retry.ts with wc -l. Once replay-byte-client.ts is confirmed under 300, REMOVE the max-lines suppression line — FINAL extraction; never remove it while the parent is 300 or more [src: 22-RESEARCH.md#Per-Extraction-Gate]. The parent replay-byte-client.ts then retains: ReplayByteClient + ByteFetchOptions types (or the re-export if lifted), the re-exported ReplayByteFetchError, ExecFile/ExecFileOptions, defaultExecFile, CreateReplayByteClientOptions, getSshHost, createDirectReplayByteClient, createSshReplayByteClient, and the createReplayByteClient entry. No identifier/signature/body change.</action>
  <verify>
    <automated>pnpm run verify && test "$(grep -c 'oxlint-disable max-lines' src/storage/replay-byte-client.ts)" -eq 0 && test "$(wc -l < src/storage/replay-byte-client.ts)" -lt 300 && test "$(wc -l < src/storage/replay-byte-client-error.ts)" -lt 300 && test "$(wc -l < src/storage/replay-byte-client-retry.ts)" -lt 300 && test "$(grep -c 'export class ReplayByteFetchError' src/storage/replay-byte-client-error.ts)" -eq 1 && pnpm run test:integration</automated>
  </verify>
  <done>ReplayByteFetchError physically lives in replay-byte-client-error.ts and is re-exported from replay-byte-client.ts; store-raw-replay.ts and run/ import paths are unchanged; no-circular green; replay-byte-client.ts is under 300 with the suppression gone; every sibling under 300; `pnpm run verify` exits 0; the golden oracle is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure structural in-band code move; no input, auth, secret, schema, key-layout, or external-surface change. The HTTP/SSH replay-byte-fetch adapter surface is byte-identical after the move. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-NOOP | n/a | replay-byte-client split | accept | No new threats: compile-time-only moves. The identifiers-only error builder (no replay bytes/body/secret copied — DIAG-04, threat T-08-01) and the SSH base64 byte-fetch command wiring move verbatim with their guard JSDoc; existing unit tests + the golden oracle pin behavior [src: 22-RESEARCH.md#Security-Domain]. |
| T-22-04-SC | Tampering | npm/pnpm installs | mitigate | No package installs in this plan (structural move only); no `[ASSUMED]`/`[SUS]` package introduced. |
</threat_model>

<verification>
- `pnpm run verify` exits 0 after EACH extraction commit (lint + typecheck + test + 100% V8 coverage + build + depcruise + knip).
- `pnpm run test:integration` (golden run-once oracle) green after the final extraction.
- `grep -c 'export class ReplayByteFetchError' src/storage/replay-byte-client-error.ts` is 1 (class physically moved).
- `grep -n 'ReplayByteFetchError' src/storage/replay-byte-client.ts` shows only a re-export (no class body).
- `pnpm run depcruise` no-circular green.
- `grep -c 'oxlint-disable max-lines' src/storage/replay-byte-client.ts` is 0; `wc -l` confirms the parent and both siblings are under 300.
- `wc -l src/commands/shared.ts` is unchanged (296).
</verification>

<success_criteria>
SPLIT-04 is satisfied: replay-byte-client.ts is split within the storage/ band into cohesive siblings, ReplayByteFetchError lives in the error sibling and re-exports from the parent (no caller edited, no-circular green), the `oxlint-disable max-lines` line is removed, every file is under 300 lines, and the golden oracle plus 100% coverage stay green.
</success_criteria>

<output>
Create `.planning/phases/22-god-file-decomposition/22-04-SUMMARY.md` when done.
</output>
