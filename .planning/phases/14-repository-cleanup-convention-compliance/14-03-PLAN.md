---
phase: 14-repository-cleanup-convention-compliance
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/types/run-summary.ts
  - src/run/types.ts
  - src/evidence/s3-evidence-store.ts
autonomous: true
requirements: [CLN-04]
must_haves:
  truths:
    - "The cross-band RunSummary contract lives in cross-cutting src/types/, not in orchestration run/"
    - "The evidence adapter imports RunSummary downward from src/types/, never upward from run/"
    - "All existing importers of run/types keep compiling (no broken imports)"
    - "pnpm verify stays green at 100% coverage; behavior unchanged"
  artifacts:
    - path: "src/types/run-summary.ts"
      provides: "Cross-cutting RunSummary + related run-summary shape types (moved from run/types.ts)"
      contains: "RunSummary"
    - path: "src/run/types.ts"
      provides: "Barrel re-export of the moved types so existing run/* importers are unbroken"
      contains: "export"
  key_links:
    - from: "src/evidence/s3-evidence-store.ts"
      to: "src/types/run-summary.ts"
      via: "import type { RunSummary } from \"../types/run-summary.js\" (downward, fixes fence #1)"
      pattern: "types/run-summary"
    - from: "src/run/types.ts"
      to: "src/types/run-summary.ts"
      via: "re-export barrel keeps run/* and test importers working"
      pattern: "export .* from"
---

<objective>
CLN-04c: устранить нарушение boundary-fence #1 — адаптер `evidence/s3-evidence-store.ts` импортирует тип `RunSummary` ВВЕРХ из orchestration `run/types.js`. Перенести cross-band data-contract типы в cross-cutting `src/types/` (skill §A adjustment 5: «moving the *type* to `types/` fixes it while the *builder* (`run/summary.ts`) stays in orchestration»).

Purpose: Привести направление импортов к downward-only фенсу до того, как Phase 16 depcruise начнёт его проверять — Phase 16 проверяет правила, которые УЖЕ должны выполняться. Поведение и публичный контракт не меняются; это перемещение объявлений типов.
Output: Новый `src/types/run-summary.ts`; `src/run/types.ts` становится barrel-реэкспортом; `evidence/s3-evidence-store.ts` импортирует downward из `../types/`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-repository-cleanup-convention-compliance/14-RESEARCH.md

# Skill — планка соответствия
@.agents/skills/solidstats-fetcher-ts-conventions/SKILL.md

# Источники, которые задание правит / на которые опирается
@src/run/types.ts
@src/evidence/s3-evidence-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLN-04c — перенести RunSummary contract в src/types/, оставить run/types.ts barrel</name>
  <files>src/types/run-summary.ts, src/run/types.ts</files>
  <read_first>
    - src/run/types.ts (полный набор объявлений: `RunFailureCategory`, `SourceFailureClassification`, `RunStatus`, `RunSourceFailure`, `RunSummaryCounts`, `RunSummary`, `CompactRunSummary`, `RunConfigFailureSummary`, `RunExitCode` + его upward-импорты типов из discovery/source/staging/storage)
    - 14-RESEARCH.md §CLN-04 DEV-04 + Pitfall 4 (после создания types/ сделать run/types.ts реэкспортером — существующие импорты продолжат работать)
    - .agents/skills/solidstats-fetcher-ts-conventions §A band-table (Cross-cutting `types/` = «cross-band data contracts … the type lives here so a lower band never imports it upward from run/») + fence #1
  </read_first>
  <action>
    Создать `src/types/run-summary.ts` и перенести в него ВСЕ объявления типов/интерфейсов, сейчас живущие в `src/run/types.ts`: `RunFailureCategory`, `SourceFailureClassification`, `RunStatus`, `RunSourceFailure`, `RunSummaryCounts`, `RunSummary`, `CompactRunSummary`, `RunConfigFailureSummary`, `RunExitCode`. Сохранить их `import type` зависимости (`DiagnosticCode`/`DiscoveryDiagnostic`/`ReplayCandidate` из `../discovery/types.js`, `SourceReadPhase` из `../source/retry.js`, `IngestStagingResult` из `../staging/types.js`, `StoreRawReplayResult` из `../storage/store-raw-replay.js`) — это импорты из capability/cross-cutting бэндов, которые types/ вправе тянуть как контрактные зависимости; пути относительные к новому расположению (`../discovery/...` и т.д. остаются теми же, т.к. `src/types/` на той же глубине, что `src/run/`). НЕ менять формы типов — это перемещение, не редизайн.
    Затем сделать `src/run/types.ts` чистым barrel-реэкспортом: `export type { RunFailureCategory, SourceFailureClassification, RunStatus, RunSourceFailure, RunSummaryCounts, RunSummary, CompactRunSummary, RunConfigFailureSummary, RunExitCode } from "../types/run-summary.js";` (per Pitfall 4: barrel сохраняет все существующие `../run/types.js` импорты рабочими — `run/summary.ts`, `run/run-once.ts`, `cli.test.ts`, `checkpoint/checkpoint.test.ts`, `evidence/*.test.ts`/`.fixtures.ts` продолжают компилироваться без правок). НЕ удалять `src/run/types.ts` (это сломало бы coverage include и десяток импортов). Builder `run/summary.ts` остаётся в orchestration — его НЕ трогать.
  </action>
  <verify>
    <automated>test -f src/types/run-summary.ts && grep -q "export interface RunSummary" src/types/run-summary.ts && grep -Eq "export (type )?\{" src/run/types.ts && pnpm run typecheck || echo "FAIL: types not moved or run/types barrel missing or typecheck red"</automated>
  </verify>
  <done>`src/types/run-summary.ts` содержит все девять перенесённых объявлений (включая `RunSummary`); `src/run/types.ts` — barrel, реэкспортирующий их из `../types/run-summary.js`; `pnpm run typecheck` зелёный; формы типов не изменены.</done>
</task>

<task type="auto">
  <name>Task 2: CLN-04c — переключить evidence-адаптер на downward-импорт из src/types/</name>
  <files>src/evidence/s3-evidence-store.ts</files>
  <read_first>
    - src/evidence/s3-evidence-store.ts:25 (`import type { RunSummary } from "../run/types.js";` — это и есть upward layer violation fence #1)
    - .agents/skills/solidstats-fetcher-ts-conventions fence #1 (lower band Adapter никогда не импортирует из upper band Orchestration)
  </read_first>
  <action>
    В `src/evidence/s3-evidence-store.ts` заменить `import type { RunSummary } from "../run/types.js";` на `import type { RunSummary } from "../types/run-summary.js";` — теперь адаптер тянет контракт downward из cross-cutting `types/`, а не upward из `run/`, закрывая fence #1. Это единственная правка файла; никакой логики store/serialize не менять (RunSummary сериализуется как передан — STATE Phase 11 D-08/D-12). Тестовые/fixture-файлы evidence (`s3-evidence-store.integration.test.ts`, `s3-evidence-store.fixtures.ts`) импортируют из `../run/types.js` через barrel — их МОЖНО оставить на barrel (они тесты, не production adapter band), но если хочется чистоты, допускается переключить и их на `../types/run-summary.js`; production-фенс закрывается уже одной правкой s3-evidence-store.ts.
  </action>
  <verify>
    <automated>grep -q 'types/run-summary.js' src/evidence/s3-evidence-store.ts && ! grep -q 'run/types.js' src/evidence/s3-evidence-store.ts && pnpm run typecheck || echo "FAIL: evidence adapter still imports run/types upward or typecheck red"</automated>
  </verify>
  <done>`src/evidence/s3-evidence-store.ts` импортирует `RunSummary` из `../types/run-summary.js` и НЕ импортирует из `../run/types.js`; fence #1 закрыт для production adapter band; `pnpm run typecheck` зелёный; логика store/serialize не изменена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Перемещение объявлений типов между модулями; новой внешней поверхности атаки нет. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | сломанные импорты после перемещения типа (compile-time регрессия) | mitigate | `run/types.ts` остаётся barrel-реэкспортом (Pitfall 4) → все существующие импортеры компилируются; `pnpm run typecheck` + `pnpm verify` зелёные. Severity: low. |
</threat_model>

<verification>
- `test -f src/types/run-summary.ts` и `grep "export interface RunSummary" src/types/run-summary.ts`.
- `src/evidence/s3-evidence-store.ts` импортирует из `../types/run-summary.js`, не из `../run/types.js`.
- `src/run/types.ts` — barrel-реэкспорт; все прежние `../run/types.js` импортеры компилируются.
- Полный гейт волны: `pnpm run verify` зелёный, coverage 100%, файловый набор НЕ сокращён (добавлен `src/types/run-summary.ts`; `run/types.ts` сохранён).
</verification>

<success_criteria>
CLN-04c закрыт: cross-band контракт `RunSummary` живёт в `src/types/run-summary.ts`; production evidence-адаптер импортирует его downward (fence #1 закрыт); `run/types.ts` barrel сохраняет совместимость импортов; builder `run/summary.ts` остался в orchestration; `pnpm verify` зелёный при 100% coverage; поведение байт-идентично.
</success_criteria>

<output>
Create `.planning/phases/14-repository-cleanup-convention-compliance/14-03-SUMMARY.md` when done
</output>