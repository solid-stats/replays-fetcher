---
phase: 04-staging-and-promotion-handoff
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - README.md
  - docs/integration-contract.md
  - src/cli.test.ts
autonomous: true
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03
  - STAGE-04
  - STAGE-05
  - TEST-04
must_haves:
  truths:
    - "README documents the Phase 4 command path, staging table name, required PostgreSQL setting, idempotency behavior, and boundaries."
    - "Integration contract documents `server-2` as owner of promotion, canonical replays, parse jobs, RabbitMQ, and duplicate conflict handling."
    - "Tests prove Phase 4 does not write forbidden business tables."
  artifacts:
    - path: "README.md"
      provides: "Operator docs for Phase 4 staging handoff"
    - path: "docs/integration-contract.md"
      provides: "Updated staging compatibility contract"
---

<objective>
Document and verify Phase 4 boundaries.

Purpose: The staging handoff changes cross-application behavior, so operator docs and compatibility docs must be explicit.
Output: README/contract updates, final boundary tests, and Phase 4 verification evidence.
</objective>

<context>
@.planning/phases/04-staging-and-promotion-handoff/04-CONTEXT.md
@.planning/phases/04-staging-and-promotion-handoff/04-03-SUMMARY.md
@docs/integration-contract.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator and compatibility docs</name>
  <files>README.md, docs/integration-contract.md</files>
  <action>Document the Phase 4 command path, `DATABASE_URL`, `ingest_staging_records`, `pending` staging status, idempotency behavior, source identity rules, and explicit boundaries: no parser, no parser artifacts, no canonical `replays`, no `parse_jobs`, no RabbitMQ publish, no scheduled `run-once` until Phase 5.</action>
  <verify>
    <automated>grep -n "ingest_staging_records" README.md docs/integration-contract.md && grep -n "does not create canonical" README.md docs/integration-contract.md</automated>
  </verify>
  <done>Docs match implemented Phase 4 behavior and server-2 compatibility evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Final Phase 4 verification</name>
  <files>src/cli.test.ts</files>
  <action>Add or update final static boundary guards to scan Phase 4 staging path files for forbidden writes to `replays`, `parse_jobs`, parser results/events, stats, identity, roles, requests, moderation, parser artifacts, local replay-list files, and `run-once` implementation.</action>
  <verify>
    <automated>pnpm run verify</automated>
  </verify>
  <done>Full verify passes with colocated tests and 100% coverage.</done>
</task>

</tasks>

<verification>
Run `pnpm run verify`.
</verification>

<output>
After completion, create `.planning/phases/04-staging-and-promotion-handoff/04-04-SUMMARY.md` and then `.planning/phases/04-staging-and-promotion-handoff/04-VERIFICATION.md`.
</output>
