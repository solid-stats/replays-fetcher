---
phase: 04-staging-and-promotion-handoff
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/staging/postgres-staging-repository.ts
  - src/staging/postgres-staging-repository.test.ts
autonomous: true
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03
  - STAGE-04
  - STAGE-05
  - TEST-04
must_haves:
  truths:
    - "Repository writes only `ingest_staging_records`."
    - "Repository does not write `replays`, `parse_jobs`, parser result/event tables, stats, identity, roles, requests, or moderation tables."
    - "Repeated matching staging payloads are idempotent."
    - "Mismatched source identity or checksum/object key conflicts return structured conflict evidence without canonical writes."
  artifacts:
    - path: "src/staging/postgres-staging-repository.ts"
      provides: "PostgreSQL repository for ingest_staging_records writes"
      exports: ["createPostgresStagingRepository", "PostgresStagingRepository"]
  key_links:
    - from: "src/staging/postgres-staging-repository.ts"
      to: "/home/afgan0r/Projects/SolidGames/server-2/src/infra/db/migrations/0001_v1_domain_schema.sql"
      via: "inserts into ingest_staging_records"
      pattern: "ingest_staging_records"
---

<objective>
Implement the staging database write boundary.

Purpose: Phase 4 needs idempotent PostgreSQL writes that match `server-2`'s existing staging schema while avoiding forbidden business tables.
Output: Fake-query tested staging repository with structured outcomes.
</objective>

<context>
@.planning/phases/04-staging-and-promotion-handoff/04-CONTEXT.md
@.planning/phases/04-staging-and-promotion-handoff/04-01-SUMMARY.md
@/home/afgan0r/Projects/SolidGames/server-2/src/infra/db/migrations/0001_v1_domain_schema.sql
@/home/afgan0r/Projects/SolidGames/server-2/src/modules/ingest/repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify repository SQL and idempotency</name>
  <files>src/staging/postgres-staging-repository.test.ts</files>
  <behavior>
    - Test 1: repository inserts pending rows into `ingest_staging_records` with exact payload fields.
    - Test 2: matching existing row returns `already_staged`.
    - Test 3: source identity conflict returns `conflict`.
    - Test 4: checksum/object key conflict returns `conflict`.
    - Test 5: database failure returns structured `failed` evidence.
    - Test 6: static SQL guard rejects forbidden table writes.
  </behavior>
  <action>Create failing colocated tests around an injected minimal query client. Avoid live PostgreSQL unless a later implementation needs it.</action>
  <verify>
    <automated>pnpm test -- src/staging/postgres-staging-repository.test.ts</automated>
  </verify>
  <done>Tests define observable repository behavior and forbidden write guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staging repository</name>
  <files>src/staging/postgres-staging-repository.ts, src/staging/postgres-staging-repository.test.ts</files>
  <behavior>
    - Test 1: successful insert returns `staged` with staging row identity/evidence.
    - Test 2: unique/idempotent conflicts are inspected and classified through safe SELECTs.
    - Test 3: only `ingest_staging_records` is mutated.
  </behavior>
  <action>Implement repository with an injectable query client or `pg` pool factory. Prefer explicit SQL over an ORM. Handle unique violations by reading existing staging evidence and returning structured outcomes. Do not update terminal/promoted rows unless the plan explicitly proves it is safe; prefer reporting conflicts for `server-2` operators.</action>
  <verify>
    <automated>pnpm test -- src/staging/postgres-staging-repository.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>Repository behavior is fully fake-tested and boundary-safe.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/staging/postgres-staging-repository.test.ts && pnpm run typecheck`.
</verification>

<output>
After completion, create `.planning/phases/04-staging-and-promotion-handoff/04-02-SUMMARY.md`.
</output>
