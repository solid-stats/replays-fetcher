---
phase: 04-staging-and-promotion-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/staging/types.ts
  - src/staging/payload.ts
  - src/staging/payload.test.ts
autonomous: true
requirements:
  - STAGE-01
  - STAGE-03
  - STAGE-04
must_haves:
  truths:
    - "Fetcher can transform Phase 3 raw storage evidence into a server-2-compatible staging payload."
    - "Payload uses server-2 table columns: source_system, source_replay_id, object_key, checksum, size_bytes, replay_timestamp, status, promotion_evidence, conflict_details."
    - "Source URL, external ID, filename, discovered/fetched timestamps, bucket, object key, checksum, byte size, and raw storage status are preserved as promotion evidence."
  artifacts:
    - path: "src/staging/types.ts"
      provides: "Staging payload and result contracts"
      exports: ["IngestStagingPayload", "IngestStagingResult"]
    - path: "src/staging/payload.ts"
      provides: "Raw storage evidence to staging payload mapper"
      exports: ["toIngestStagingPayload"]
  key_links:
    - from: "src/staging/payload.ts"
      to: "src/storage/types.ts"
      via: "accepts RawReplayStorageEvidence"
      pattern: "RawReplayStorageEvidence"
    - from: "src/staging/types.ts"
      to: "server-2/src/infra/db/migrations/0001_v1_domain_schema.sql"
      via: "models ingest_staging_records columns"
      pattern: "source_system"
---

<objective>
Define the Phase 4 staging payload contract.

Purpose: The fetcher needs a small, auditable conversion from raw storage evidence into `server-2` staging rows before adding PostgreSQL writes.
Output: Colocated tests and TypeScript types for staging payload generation.
</objective>

<context>
@.planning/phases/04-staging-and-promotion-handoff/04-CONTEXT.md
@.planning/phases/03-raw-replay-storage/03-VERIFICATION.md
@server-2/src/infra/db/migrations/0001_v1_domain_schema.sql
@server-2/src/modules/ingest/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify staging payload behavior</name>
  <files>src/staging/payload.test.ts</files>
  <behavior>
    - Test 1: stored/skipped raw storage evidence maps to `status: "pending"` staging payload.
    - Test 2: `source_system` defaults to `sg-zone`.
    - Test 3: `source_replay_id` uses external source ID when present.
    - Test 4: missing external ID falls back to a deterministic source identity that does not parse replay bytes.
    - Test 5: promotion evidence preserves source URL, external ID, source filename, bucket, object key, checksum, byte size, fetchedAt, and raw storage status.
  </behavior>
  <action>Create failing colocated tests for `toIngestStagingPayload` using Phase 3 raw evidence fixtures. Tests must not use PostgreSQL, parser artifacts, `replays`, `parse_jobs`, or scheduled `run-once`.</action>
  <verify>
    <automated>pnpm test -- src/staging/payload.test.ts</automated>
  </verify>
  <done>Tests fail until staging payload types and mapper exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staging payload mapper</name>
  <files>src/staging/types.ts, src/staging/payload.ts, src/staging/payload.test.ts</files>
  <behavior>
    - Test 1: payload field names line up with `server-2` staging schema.
    - Test 2: payload contains only staging-safe evidence and no canonical replay/parse job fields.
    - Test 3: conflict/failed raw storage evidence is rejected or represented as non-stageable before database write.
  </behavior>
  <action>Implement staging contracts and `toIngestStagingPayload`. Keep the mapper pure, deterministic, and explicit about non-stageable raw storage statuses. Preserve enough evidence for `server-2` dedupe/manual review without making product dedupe decisions in the fetcher.</action>
  <verify>
    <automated>pnpm test -- src/staging/payload.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>Payload mapper is tested and ready for repository insertion.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/staging/payload.test.ts && pnpm run typecheck`.
</verification>

<output>
After completion, create `.planning/phases/04-staging-and-promotion-handoff/04-01-SUMMARY.md`.
</output>
