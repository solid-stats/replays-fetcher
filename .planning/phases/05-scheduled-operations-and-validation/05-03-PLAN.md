---
phase: 05-scheduled-operations-and-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/cli.ts
  - src/cli.test.ts
autonomous: true
requirements:
  - RUN-02
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
must_haves:
  truths:
    - "CLI `run-once` executes the scheduled one-shot cycle instead of throwing planned-phase error."
    - "CLI writes one structured JSON summary to stdout."
    - "CLI sets exit code 0 for success and 2 for expected operational failures."
  artifacts:
    - path: "src/cli.ts"
      provides: "run-once command wiring"
---

<objective>
Expose scheduled run-once through the CLI.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify CLI run-once behavior</name>
  <files>src/cli.test.ts</files>
  <action>Replace planned-error tests with success/failure CLI tests using injected `runOnce`. Verify structured JSON, exit codes, and no secret leakage.</action>
  <verify><automated>pnpm test -- src/cli.test.ts</automated></verify>
  <done>CLI tests fail until run-once wiring exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire CLI run-once</name>
  <files>src/cli.ts, src/cli.test.ts</files>
  <action>Load full config, create source/byte/storage/staging dependencies, call `runOnce`, write summary JSON, and set process exit code from the result. Keep dry-run/store-raw commands intact.</action>
  <verify><automated>pnpm test -- src/cli.test.ts && pnpm run typecheck</automated></verify>
  <done>run-once is available for cron/container scheduling.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-scheduled-operations-and-validation/05-03-SUMMARY.md`.
</output>
