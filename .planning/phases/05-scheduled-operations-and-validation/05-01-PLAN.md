---
phase: 05-scheduled-operations-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/run/types.ts
  - src/run/summary.ts
  - src/run/summary.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - TEST-01
must_haves:
  truths:
    - "Run summaries have stable counts for discovery, raw storage, staging, conflicts, skipped, and failed outcomes."
    - "Failure taxonomy distinguishes config_invalid, source_unavailable, fetch_failed, storage_failed, storage_conflict, staging_failed, staging_conflict, and not_stageable."
    - "Summary evidence includes run ID and omits secrets/raw bytes."
  artifacts:
    - path: "src/run/types.ts"
      provides: "Run summary and failure taxonomy contracts"
    - path: "src/run/summary.ts"
      provides: "Pure summary/count helpers"
---

<objective>
Define run summary and failure taxonomy contracts.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify run summary behavior</name>
  <files>src/run/summary.test.ts</files>
  <action>Create colocated tests for count aggregation, failure category mapping, exit-code decision, and secret-free summary shape.</action>
  <verify><automated>pnpm test -- src/run/summary.test.ts</automated></verify>
  <done>Tests define the summary contract before run-once wiring.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement summary helpers</name>
  <files>src/run/types.ts, src/run/summary.ts, src/run/summary.test.ts</files>
  <action>Implement pure helpers for run IDs/timestamps passed in by caller, count aggregation from discovery/raw/staging results, failure category derivation, and exit code decision.</action>
  <verify><automated>pnpm test -- src/run/summary.test.ts && pnpm run typecheck</automated></verify>
  <done>Summary helpers are deterministic and fully tested.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-scheduled-operations-and-validation/05-01-SUMMARY.md`.
</output>
