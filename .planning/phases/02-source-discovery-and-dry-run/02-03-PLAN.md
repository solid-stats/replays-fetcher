---
phase: 02
plan: 03
type: execute
wave: 4
depends_on:
  - 02-02
files_modified:
  - README.md
  - tests/cli.test.ts
  - tests/discovery.test.ts
  - src/cli.ts
autonomous: true
requirements:
  - RUN-03
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
  - TEST-05
---

# Plan 02-03: No-Mutation Guarantees, Docs, and Final Gates

<objective>
As an operator, I want dry-run behavior documented and covered by final no-mutation checks, so that Phase 2 can hand off a trustworthy candidate report contract to raw storage work without hidden side effects.
</objective>

<threat_model>
## Threat Model

- T-02-01: A future edit wires dry-run into storage, staging, parser, or local replay-list output. Mitigation: add tests and grep-based verification for forbidden mutation surfaces.
- T-02-05: Operators misunderstand dry-run as a full ingest cycle. Mitigation: update README with explicit Phase 2 command scope and deferred Phase 3/4/5 behavior.
</threat_model>

<tasks>
<task id="02-03-01" type="execute">
<read_first>
- `tests/cli.test.ts`
- `tests/discovery.test.ts`
- `src/cli.ts`
- `src/discovery/discover.ts`
- `src/config.ts`
</read_first>
<action>
Add final no-mutation guard coverage:
- Tests must prove `discover --dry-run` does not call or import S3 clients, PostgreSQL clients, parser artifact writers, raw replay byte download helpers, local replay-list file writes, or `run-once`.
- If no such modules exist yet, enforce this through grep-verifiable source boundaries and tests that only inject a `SourceClient`.
- Keep `run-once` throwing `run-once is planned for Phase 5`.
- Ensure `discover` without `--dry-run` remains blocked until Phase 3.
</action>
<acceptance_criteria>
- `tests/cli.test.ts` contains `run-once is planned for Phase 5`.
- `tests/cli.test.ts` contains `discover requires --dry-run until Phase 3`.
- `rg -n "S3Client|Pool\\(|writeFile|parse.completed|parse.failed|parse_jobs|replaysList" src tests` returns no dry-run mutation implementation matches, excluding planning text if any.
- `pnpm test` exits 0.
</acceptance_criteria>
<verify>
pnpm test
rg -n "S3Client|Pool\\(|writeFile|parse.completed|parse.failed|parse_jobs|replaysList" src tests
</verify>
</task>

<task id="02-03-02" type="execute">
<read_first>
- `README.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/phases/02-source-discovery-and-dry-run/02-CONTEXT.md`
</read_first>
<action>
Update `README.md` to describe the implemented Phase 2 command:
- Include `pnpm exec tsx src/cli.ts discover --dry-run`.
- State that dry-run reads the configured source and emits JSON to stdout.
- State that dry-run does not write S3 objects, staging rows, parser artifacts, local replay-list files, or `server-2` business tables.
- Document the top-level report fields: `ok`, `mode`, `sourceUrl`, `generatedAt`, `counts`, `candidates`, and `diagnostics`.
- Keep README's AI agents plus GSD-only workflow statement.
</action>
<acceptance_criteria>
- `README.md` contains `discover --dry-run`.
- `README.md` contains `does not write S3`.
- `README.md` contains `staging rows`.
- `README.md` contains `AI agents`.
- `pnpm run verify` exits 0.
</acceptance_criteria>
<verify>
pnpm run verify
</verify>
</task>
</tasks>

<verification>
- `pnpm run verify`
- `git diff --check`
</verification>

<success_criteria>
- README and tests describe the actual dry-run command and non-mutating boundary.
- Full project verification passes.
- Phase 2 requirement IDs are all covered by completed plans.
</success_criteria>

<must_haves>
- Covers RUN-03, SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, and TEST-05.
- Leaves S3 storage for Phase 3, staging/outbox for Phase 4, and run-once scheduling for Phase 5.
- Provides final proof that dry-run mode is non-mutating.
</must_haves>
