---
phase: 06-close-v1-audit-gaps-connectivity-checks-and-discovered-times
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/config.ts
  - src/config.test.ts
  - src/cli.ts
  - src/cli.test.ts
autonomous: true
requirements:
  - RUN-04
  - OPS-02
must_haves:
  truths:
    - "D-01/D-06: `replays-fetcher check` reports real source, S3, and staging connectivity statuses when full config is present."
    - "D-05: expected check failures emit structured JSON and set exit code 2."
    - "D-12/D-13/D-14: check JSON is part of the operational log surface, redacts S3 credentials, database URLs, and SSH command secrets, and does not add a separate logger."
  artifacts:
    - path: "src/config.ts"
      provides: "redactConfig support for staging.databaseUrl and sourceSshCommand"
    - path: "src/config.test.ts"
      provides: "redaction target tests for database URL and SSH command"
    - path: "src/cli.ts"
      provides: "async check command wiring for config plus source/S3/PostgreSQL probes"
    - path: "src/cli.test.ts"
      provides: "CLI check success/failure/leakage/boundary tests"
  key_links:
    - from: "src/cli.ts"
      to: "src/check/source-connectivity.ts"
      via: "checkSourceConnectivity"
      pattern: "checkSourceConnectivity"
    - from: "src/cli.ts"
      to: "src/check/s3-connectivity.ts"
      via: "checkS3Connectivity with createS3ConnectivitySenderFromConfig"
      pattern: "checkS3Connectivity"
    - from: "src/cli.ts"
      to: "src/check/postgres-connectivity.ts"
      via: "checkPostgresConnectivityFromDatabaseUrl"
      pattern: "checkPostgresConnectivityFromDatabaseUrl"
---

<objective>
Wire the read-only connectivity helpers into `replays-fetcher check`.

Purpose: Replace the audit-blocking check output with real structured preflight behavior while preserving existing config redaction and exit-code conventions.
Output: `check` command JSON with concrete source, S3, and staging connectivity check results.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-RESEARCH.md
@.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md
@src/cli.ts
@src/cli.test.ts
@src/config.ts
@src/check/connectivity.ts
@src/check/source-connectivity.ts
@src/check/s3-connectivity.ts
@src/check/postgres-connectivity.ts

<interfaces>
Expected check JSON shape:

```typescript
{
  ok: boolean;
  checks: {
    config: { status: "passed" | "failed" };
    sourceConnectivity?: ConnectivityCheck;
    s3Connectivity?: ConnectivityCheck;
    stagingConnectivity?: ConnectivityCheck;
  };
  config?: ReturnType<typeof redactConfig>;
  issues?: readonly string[];
}
```

Required `redactConfig` target values before any check output uses config:

```typescript
redacted.sourceSshCommand === "[redacted-source-ssh-command]";
redacted.staging.databaseUrl === "[redacted-database-url]";
```

Dependency injection additions to `BuildCliDependencies`:

```typescript
readonly checkSourceConnectivity?: typeof checkSourceConnectivity;
readonly checkS3Connectivity?: typeof checkS3Connectivity;
readonly checkPostgresConnectivityFromDatabaseUrl?: typeof checkPostgresConnectivityFromDatabaseUrl;
readonly createS3ConnectivitySenderFromConfig?: typeof createS3ConnectivitySenderFromConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace incomplete check output with real probes</name>
  <files>src/config.ts, src/config.test.ts, src/cli.ts, src/cli.test.ts</files>
  <read_first>
    <file>src/cli.ts</file>
    <file>src/cli.test.ts</file>
    <file>src/config.ts</file>
    <file>src/config.test.ts</file>
    <file>src/check/connectivity.ts</file>
    <file>src/check/source-connectivity.ts</file>
    <file>src/check/s3-connectivity.ts</file>
    <file>src/check/postgres-connectivity.ts</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: valid full config plus successful injected probes emits `ok: true`, `checks.config.status: "passed"`, and all three connectivity checks with `status: "passed"`.
    - Test 2: the output JSON contains no `not-implemented` value when full config is valid.
    - Test 3: a failed injected S3 or PostgreSQL check emits `ok: false` and sets `process.exitCode` to `2`.
    - Test 4: config validation failure still emits structured JSON with `checks.config.status: "failed"` and exit code `2` without running connectivity probes.
    - Test 5: `redactConfig` returns `sourceSshCommand: "[redacted-source-ssh-command]"` and `staging.databaseUrl: "[redacted-database-url]"`, and serialized check output contains neither `postgres://user:password@` nor `sshpass`.
  </behavior>
  <action>First update `src/config.ts` `redactConfig` and its return type so it redacts `sourceSshCommand` to exactly `"[redacted-source-ssh-command]"` and `staging.databaseUrl` to exactly `"[redacted-database-url]"` in addition to existing S3 access key and secret key redaction. Add `src/config.test.ts` coverage for those exact target values using an SSH command that contains `sshpass -p source-secret` and a database URL that contains `postgres://user:password@`. Then update `src/cli.ts` imports and `BuildCliDependencies` so `check` can use `checkSourceConnectivity`, `checkS3Connectivity`, `checkPostgresConnectivityFromDatabaseUrl`, and `createS3ConnectivitySenderFromConfig`. Change `registerCheckCommand` to an async action. On valid config, create the source client with `dependencies.createSourceClient(config)`, create the S3 connectivity sender with `dependencies.createS3ConnectivitySenderFromConfig(config.s3)`, run the three probes, aggregate with `connectivityOk`, and write JSON containing `{ ok, checks: { config: { status: "passed" }, sourceConnectivity, s3Connectivity, stagingConnectivity }, config: redactConfig(config) }`. If any probe status is `failed`, set `process.exitCode = 2`. On `ConfigError`, write `{ ok: false, checks: { config: { status: "failed" } }, issues: error.issues }`, set exit code `2`, and do not create source/S3/PostgreSQL clients. Preserve rethrow behavior for unexpected programmer errors. Per D-06, remove the `sourceConnectivity`, `s3Connectivity`, and `stagingConnectivity` `not-implemented` values.</action>
  <verify>
    <automated>pnpm test -- src/config.test.ts src/cli.test.ts src/check/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\[redacted-database-url\\]|\\[redacted-source-ssh-command\\]" src/config.ts src/config.test.ts src/cli.test.ts` finds redaction target values and assertions.
    - `rg -n "postgres://user:password@|sshpass" src/config.test.ts src/cli.test.ts` finds explicit negative leakage fixtures/assertions.
    - `rg -n "not-implemented" src/cli.ts src/cli.test.ts` returns no matches.
    - `rg -n "checkSourceConnectivity|checkS3Connectivity|checkPostgresConnectivityFromDatabaseUrl|createS3ConnectivitySenderFromConfig" src/cli.ts` finds all helper integrations.
    - `rg -n "checks:\\s*\\{\\s*config:\\s*\\{\\s*status" src/cli.ts src/cli.test.ts` finds structured config check output.
    - `pnpm test -- src/config.test.ts src/cli.test.ts src/check/*.test.ts` passes.
  </acceptance_criteria>
  <done>The CLI check command performs real injected source/S3/PostgreSQL probes and reports structured pass/fail status with exit code 2 for expected failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add check output leakage and boundary tests</name>
  <files>src/cli.test.ts</files>
  <read_first>
    <file>src/cli.test.ts</file>
    <file>src/cli.ts</file>
    <file>src/config.ts</file>
    <file>docs/integration-contract.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-CONTEXT.md</file>
    <file>.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test 1: check output redacts `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, and credentials inside `DATABASE_URL`.
    - Test 2: expected source/S3/PostgreSQL failures do not serialize raw error objects or source body text.
    - Test 3: check command source contains no parser artifact writes, RabbitMQ publication, or canonical server-2 business table writes.
  </behavior>
  <action>Extend `src/cli.test.ts` check-command tests with explicit negative assertions over `JSON.stringify(output)` for `secret-key`, `access-key`, `postgres://user:password@`, `sshpass`, `raw-replay-bytes`, `parser_artifact`, `parse_jobs`, `parse_results`, `insert into replays`, `identity`, `roles`, `requests`, and `moderation_actions`. Also assert check output includes the safe redaction target values `"[redacted-database-url]"` and `"[redacted-source-ssh-command]"` when the loaded config contains database credentials and an SSH command secret. Add tests using injected failed probe functions to prove failed check JSON contains controlled `failureCategory` values but not raw thrown error objects. Preserve existing static boundary guard style; if new guard tokens are needed, construct them at runtime as current tests do so grep gates do not self-trigger. Do not add a separate per-item logger.</action>
  <verify>
    <automated>pnpm test -- src/cli.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "secret-key|postgres://user:password|sshpass|raw-replay-bytes|parser_artifact|\\[redacted-database-url\\]|\\[redacted-source-ssh-command\\]" src/cli.test.ts` finds explicit leakage assertions and redacted targets.
    - `rg -n "sourceConnectivity|s3Connectivity|stagingConnectivity" src/cli.test.ts` finds all three check outputs under test.
    - `rg -n "pino|logger|per-item" src/cli.ts src/cli.test.ts` returns no new logger implementation matches.
    - `pnpm test -- src/cli.test.ts` passes.
  </acceptance_criteria>
  <done>Check output tests prove real connectivity status, redaction, expected failure behavior, and boundary safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator environment -> CLI config | Secrets enter process through env-backed config. |
| CLI check -> stdout | Structured JSON becomes operator-visible logs. |
| CLI check -> source/S3/PostgreSQL | Network and database failures become operational status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Information Disclosure | `src/config.ts` and `src/cli.ts` check output | mitigate | Extend `redactConfig(config)` to replace `staging.databaseUrl` with `"[redacted-database-url]"` and `sourceSshCommand` with `"[redacted-source-ssh-command]"`; CLI tests assert credential substrings are absent. |
| T-06-15 | Repudiation | `src/cli.ts` check output | mitigate | Emit explicit pass/fail per source/S3/staging probe and exit code 2 on expected failures. |
| T-06-16 | Denial of Service | `src/cli.ts` check flow | mitigate | Preserve async command behavior and dependency helper resource cleanup from Plan 01. |
| T-06-17 | Elevation of Privilege | `src/cli.test.ts` boundary guard | mitigate | Static tests keep check wiring out of parser/server/web responsibilities and canonical business tables. |
</threat_model>

<verification>
Run `pnpm test -- src/config.test.ts src/cli.test.ts src/check/*.test.ts`. Then run `rg -n "not-implemented" src/cli.ts src/cli.test.ts` and require no matches.
</verification>

<success_criteria>
- Valid full config check output has concrete connectivity statuses, never `not-implemented`.
- Expected failed probes set exit code 2 and return structured JSON.
- Config failures do not run connectivity probes.
- Check output does not leak S3 credentials, database credentials, SSH command secrets, raw bytes, parser artifacts, or server-2 business records.
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps-connectivity-checks-and-discovered-times/06-04-SUMMARY.md`
</output>
