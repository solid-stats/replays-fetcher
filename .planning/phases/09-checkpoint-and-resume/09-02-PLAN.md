---
phase: 09-checkpoint-and-resume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/staging/payload.ts
  - src/staging/payload.test.ts
  - src/staging/types.ts
  - src/staging/postgres-staging-repository.integration.test.ts
  - src/config.ts
  - src/config.test.ts
autonomous: true
requirements: [RESUME-04]
must_haves:
  truths:
    - "Every staged row's promotion_evidence jsonb carries a run_id key (snake_case — the cross-app contract key per REQUIREMENTS RESUME-04) alongside the existing discoveredAt, written through the existing ingest_staging_records column."
    - "No new staging columns, no new tables, no server-2 schema change — run_id is purely additive into the existing jsonb object."
    - "A real-Postgres integration test proves promotion_evidence->>'run_id' equals the stamped run id after insertStaging."
    - "A checkpoint S3 prefix is operator-configurable (default checkpoints) and validated by the config Zod schema."
  artifacts:
    - path: "src/staging/payload.ts"
      provides: "toIngestStagingPayload stamps run_id into promotionEvidence"
      contains: "run_id"
    - path: "src/config.ts"
      provides: "checkpointPrefix config field with default"
      contains: "checkpointPrefix"
  key_links:
    - from: "src/staging/payload.ts"
      to: "promotion_evidence jsonb"
      via: "additive run_id key in promotionEvidence object"
      pattern: "run_id"
    - from: "src/config.ts"
      to: "S3_CHECKPOINT_PREFIX env"
      via: "Zod default-backed env mapping in loadConfig"
      pattern: "checkpointPrefix"
---

<objective>
Make run/resume status visible to `server-2` through the existing staging contract — stamp `run_id` into the already-written `promotion_evidence` jsonb of `ingest_staging_records` — and add the operator-configurable checkpoint S3 prefix to config. Both are additive, contract-safe changes with disjoint files from the other Wave 1 plans.

Purpose: RESUME-04 (run identity as the `run_id` jsonb key in existing jsonb, NO schema change) and the config substrate the S3 checkpoint store (Plan 03) needs for the `checkpoints/<slug>/latest.json` key.
Output: `src/staging/payload.ts` + `src/staging/types.ts` (`run_id`), a real-Postgres integration assertion in `src/staging/postgres-staging-repository.integration.test.ts`, `src/config.ts` (checkpointPrefix), with colocated tests.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-checkpoint-and-resume/09-CONTEXT.md
@.planning/phases/09-checkpoint-and-resume/09-RESEARCH.md
@.planning/phases/09-checkpoint-and-resume/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Plan 02 produces:
- **run_id in promotion_evidence** — `src/staging/payload.ts` (`toIngestStagingPayload` stamps the jsonb key `run_id`) + `src/staging/types.ts` (`IngestStagingPayload.promotionEvidence.run_id?`). The TS option/variable name stays camelCase (`runId`); only the STAMPED jsonb key string is `run_id`.
- **run_id integration coverage** — `src/staging/postgres-staging-repository.integration.test.ts` asserts `promotion_evidence->>'run_id'` equals the stamped run id after a real `insertStaging` (Testcontainers Postgres).
- **checkpoint config prefix** — `src/config.ts` (`checkpointPrefix`, env `S3_CHECKPOINT_PREFIX`, default `checkpoints`).
Cross-app fact (NOT a code task, and NOT a human gate): server-2 was directly inspected by the orchestrator (`src/modules/ingest/repository/repository.ts:184,699,707`, `routes.ts:83`) and currently has NO reader of any run id in `promotion_evidence` — it merges `promotionEvidence` as an opaque JsonObject. The fetcher therefore establishes the convention; `run_id` is safe and a future server-2 correlation will read `promotion_evidence->>'run_id'`. See 09-RESEARCH Open Questions (RESOLVED): key = run_id, server-2-verified. No blocking human-verify item.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp run_id into promotion_evidence (additive, no schema change)</name>
  <files>src/staging/payload.ts, src/staging/payload.test.ts, src/staging/types.ts</files>
  <read_first>
    - src/staging/payload.ts (`toIngestStagingPayload` / `toPayload` lines 16-73 — note the conditional-spread pattern for `discoveredAt` lines 55-60 and `sourceExternalId` lines 62-69)
    - src/staging/types.ts (`IngestStagingPayload.promotionEvidence` lines 16-27 — note `discoveredAt?: string` stays camelCase, do NOT rename it)
    - eslint.config.js (line 7: `js.configs.all` enables the `camelcase` rule, which flags a snake_case property — a scoped disable on exactly the `run_id` assignment/type member is the accepted fix for a cross-service contract key)
    - 09-PATTERNS.md section "src/staging/payload.ts (MODIFY — transform)"
  </read_first>
  <behavior>
    - Test: `toIngestStagingPayload(storedEvidence, { runId: "run-X" })` returns a payload whose `promotionEvidence.run_id === "run-X"` (snake_case jsonb key), alongside the existing `discoveredAt`/`sourceUrl` keys.
    - Test: `toIngestStagingPayload(storedEvidence, {})` (no runId) omits the key entirely (no `run_id: undefined`, no `run_id` substring) — backward compatible; the existing `toStrictEqual` test (payload.test.ts:30) must still pass unchanged.
    - Test: existing assertions (discoveredAt present, sourceExternalId conditional, replayTimestamp) still pass — additive only.
    - Test: the serialized `promotionEvidence` contains no new column-shaped fields beyond `run_id` (boundary: jsonb-only).
  </behavior>
  <action>
    Add an optional `runId?: string` to `ToIngestStagingPayloadOptions` (payload.ts:12-14) — the TS option name stays camelCase (`runId`), it is an ordinary variable, not the jsonb key. In `toPayload` (payload.ts:40-73), thread the `runId` through and stamp it into the base `promotionEvidence` object next to `discoveredAt`, using the SAME conditional-spread style already used for `discoveredAt` (payload.ts:55-60): only add the key when defined (no `run_id: undefined`). The STAMPED jsonb key string MUST be `run_id` (snake_case) — this is the cross-service contract key per REQUIREMENTS RESUME-04 + CONTEXT, and matches 09-RESEARCH Open Questions (RESOLVED): key = run_id, server-2-verified (server-2 has no existing reader; the fetcher sets the convention and a future correlation reads `promotion_evidence->>'run_id'`). Because `js.configs.all` (eslint.config.js:7) enables the `camelcase` rule, which flags a snake_case property, add a scoped `// eslint-disable-next-line camelcase` on exactly the `run_id` property assignment (the accepted reason: it is an external cross-service jsonb contract key, not a local identifier). Do NOT rename or touch the existing `discoveredAt` key. Update `IngestStagingPayload.promotionEvidence` in types.ts to add `readonly run_id?: string` with a scoped `// eslint-disable-next-line camelcase` on that type member if the rule fires there. The staging SQL already serializes the whole object as `$8::jsonb` (postgres-staging-repository.ts:95,106) — no SQL change, NO new column, NO new table — RESUME-04 locked scope. Pass `options.runId` from `toIngestStagingPayload` into `toPayload`. No `any`/`as`; preserve `readonly` style.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/staging/payload.test.ts</automated>
  </verify>
  <done>promotionEvidence carries the jsonb key `run_id` when provided and omits it when not; types updated additively with a scoped camelcase disable; existing `toStrictEqual` test unchanged-passing; no SQL/schema change; all staging unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operator-configurable checkpoint S3 prefix in config</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts (s3 sub-schema lines 62-74; env mapping in loadConfig lines 106-121; `.default(...)`/`z.coerce` patterns e.g. sourceMaxPages line 32 / sourceRetryAttempts lines 42-46)
    - 09-PATTERNS.md section "src/config.ts (MODIFY — config)"
  </read_first>
  <behavior>
    - Test: `loadConfig` with no `S3_CHECKPOINT_PREFIX` set yields `config.s3.checkpointPrefix === "checkpoints"` (default).
    - Test: `loadConfig` with `S3_CHECKPOINT_PREFIX=cp` yields `config.s3.checkpointPrefix === "cp"`.
    - Test: an empty `S3_CHECKPOINT_PREFIX` (`""`) fails validation (min length 1) → ConfigError.
    - Test: `redactConfig` leaves `checkpointPrefix` visible (it is not a secret).
  </behavior>
  <action>
    Add `checkpointPrefix: z.string().min(1).default("checkpoints")` to the `s3` Zod object (config.ts:62-74), following the `.default(...)` pattern of `sourceMaxPages` (config.ts:32). Map the env var in `loadConfig`'s `s3` object (config.ts:110-117): `checkpointPrefix: source["S3_CHECKPOINT_PREFIX"]`. The default is the existing S3 bucket + a `checkpoints/` prefix (09-CONTEXT line 56) — the bucket is reused, only the prefix is new. The prefix is not a secret, so `redactConfig` (config.ts:150-163) does not need to redact it (confirm with a test). Do not introduce a new top-level config object; keep it under `s3` for cohesion with the bucket it shares. No `any`/`as`; no magic strings beyond the default literal.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/config.test.ts</automated>
  </verify>
  <done>checkpointPrefix defaults to "checkpoints", honors S3_CHECKPOINT_PREFIX, rejects empty, is not redacted; config tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Real-Postgres integration — run_id persisted in promotion_evidence jsonb</name>
  <files>src/staging/postgres-staging-repository.integration.test.ts</files>
  <read_first>
    - src/staging/postgres-staging-repository.integration.test.ts (existing Testcontainers Postgres test: `StagingEvidenceRow` interface lines 11-16, `payload` fixture lines 21-41, the `promotion_evidence` select + `discoveredAt` assertion lines 73-89, `applyStagingSchema` lines 93-116)
    - src/staging/payload.ts (`toIngestStagingPayload` with the `runId` option from Task 1 — use it to build the staged payload so the test exercises the real stamping path, not a hand-built literal)
  </read_first>
  <behavior>
    - Test: after `repository.stage(payload)` for a payload built via `toIngestStagingPayload(evidence, { runId })`, the persisted `promotion_evidence->>'run_id'` (queried from real Postgres) equals the stamped `runId`.
    - Test: the existing `discoveredAt` assertion still passes (additive — `run_id` does not displace it).
  </behavior>
  <action>
    Extend the existing Testcontainers Postgres integration test (no new container/harness — reuse `PostgreSqlContainer` + `applyStagingSchema`). Build the staged payload through `toIngestStagingPayload(storedEvidence, { runId: "run-2026-..." })` (import from `./payload.js`) rather than the hand-built `payload` literal so the test proves the real Task 1 stamping path end-to-end into jsonb. Add `run_id` (snake_case) to the `StagingEvidenceRow.promotion_evidence` shape with a scoped `// eslint-disable-next-line camelcase` on that property (cross-service jsonb key, mirrors Task 1). Either select `promotion_evidence->>'run_id'` directly in SQL or assert `rows.rows[0]?.promotion_evidence.run_id` equals the stamped run id. Keep the existing `discoveredAt`/`replay_timestamp`/idempotent-`already_staged` assertions intact. No `any`/`as`; reuse the existing fixtures, constants, and cleanup hooks.
  </action>
  <verify>
    <automated>pnpm run test:integration</automated>
  </verify>
  <done>real-Postgres test proves `promotion_evidence->>'run_id'` equals the stamped run id after insertStaging; existing discoveredAt/idempotency assertions still pass; VALIDATION row 9-02-02 now maps to a real test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetcher → server-2 (staging jsonb) | `promotion_evidence` is the cross-app contract surface; the additive `run_id` key must not break server-2's reader or expand the schema. |
| env → config | `S3_CHECKPOINT_PREFIX` is operator-supplied; validated by Zod before use in S3 key construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | staging schema / cross-app contract | mitigate | `run_id` is added ONLY to the existing `promotion_evidence` jsonb via conditional spread; no new column, table, or SQL change (RESUME-04 locked scope). Unit test asserts the staging payload shape is otherwise unchanged; the real-Postgres integration test (Task 3) asserts `promotion_evidence->>'run_id'` persists. Cross-app verified: server-2 has NO existing run-id reader (orchestrator inspected repository.ts:184,699,707 / routes.ts:83 — opaque JsonObject merge), so `run_id` safely establishes the convention; a future server-2 correlation reads `promotion_evidence->>'run_id'` (09-RESEARCH Open Questions RESOLVED; ASVS L2 V1.11 — documented inter-service contract). |
| T-09-04 | Information Disclosure | run_id value | accept | The run id is a non-secret correlation id (`run-<iso>-<uuid>`, cli.ts:378-380); safe to persist in jsonb and logs. No PII/secret content. |
| T-09-05 | Tampering | S3_CHECKPOINT_PREFIX → S3 key | mitigate | Zod `min(1)` validation before the prefix reaches key construction; the slug builder (Plan 03) further constrains the full key to an S3-safe charset (ASVS L2 V5.1). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (config + payload + integration-test edits only). |
</threat_model>

<verification>
- `pnpm exec vitest run src/staging/payload.test.ts src/config.test.ts` green.
- `pnpm run test:integration` green — including the new `promotion_evidence->>'run_id'` real-Postgres assertion (VALIDATION row 9-02-02).
- `pnpm run typecheck` and `pnpm run lint` clean — the scoped `// eslint-disable-next-line camelcase` on the `run_id` key keeps `js.configs.all` happy.
- Cross-app: VERIFIED, no human gate. server-2 has no existing run-id reader; `run_id` is the established convention (09-RESEARCH Open Questions RESOLVED). A future server-2 correlation will read `promotion_evidence->>'run_id'`.
</verification>

<success_criteria>
- promotion_evidence carries the jsonb key `run_id` when provided, omitted otherwise; no schema/SQL change (RESUME-04).
- Real-Postgres integration test proves `promotion_evidence->>'run_id'` persists (VALIDATION 9-02-02 maps to a real test).
- `checkpointPrefix` config field validated and defaulted; honors `S3_CHECKPOINT_PREFIX`.
- All existing staging/config tests still pass (additive-only); `toStrictEqual` payload test unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-checkpoint-and-resume/09-02-SUMMARY.md` when done.
</output>
