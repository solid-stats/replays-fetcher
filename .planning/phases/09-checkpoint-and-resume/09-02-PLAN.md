---
phase: 09-checkpoint-and-resume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/staging/payload.ts
  - src/staging/payload.test.ts
  - src/staging/types.ts
  - src/config.ts
  - src/config.test.ts
autonomous: true
requirements: [RESUME-04]
must_haves:
  truths:
    - "Every staged row's promotion_evidence jsonb carries a runId key alongside the existing discoveredAt, written through the existing ingest_staging_records column."
    - "No new staging columns, no new tables, no server-2 schema change — runId is purely additive into the existing jsonb object."
    - "A checkpoint S3 prefix is operator-configurable (default checkpoints) and validated by the config Zod schema."
  artifacts:
    - path: "src/staging/payload.ts"
      provides: "toIngestStagingPayload stamps runId into promotionEvidence"
      contains: "runId"
    - path: "src/config.ts"
      provides: "checkpointPrefix config field with default"
      contains: "checkpointPrefix"
  key_links:
    - from: "src/staging/payload.ts"
      to: "promotion_evidence jsonb"
      via: "additive runId key in promotionEvidence object"
      pattern: "runId"
    - from: "src/config.ts"
      to: "S3_CHECKPOINT_PREFIX env"
      via: "Zod default-backed env mapping in loadConfig"
      pattern: "checkpointPrefix"
---

<objective>
Make run/resume status visible to `server-2` through the existing staging contract — stamp `runId` into the already-written `promotion_evidence` jsonb of `ingest_staging_records` — and add the operator-configurable checkpoint S3 prefix to config. Both are additive, contract-safe changes with disjoint files from the other Wave 1 plans.

Purpose: RESUME-04 (run identity in existing jsonb, NO schema change) and the config substrate the S3 checkpoint store (Plan 03) needs for the `checkpoints/<slug>/latest.json` key.
Output: `src/staging/payload.ts` + `src/staging/types.ts` (runId), `src/config.ts` (checkpointPrefix), with colocated tests.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-checkpoint-and-resume/09-CONTEXT.md
@.planning/phases/09-checkpoint-and-resume/09-RESEARCH.md
@.planning/phases/09-checkpoint-and-resume/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Plan 02 produces:
- **runId in promotion_evidence** — `src/staging/payload.ts` (`toIngestStagingPayload` stamps `runId`) + `src/staging/types.ts` (`IngestStagingPayload.promotionEvidence.runId?`).
- **checkpoint config prefix** — `src/config.ts` (`checkpointPrefix`, env `S3_CHECKPOINT_PREFIX`, default `checkpoints`).
Cross-app note (NOT a code task): server-2 must read the run identifier as `promotion_evidence->>'runId'` (camelCase, per 09-RESEARCH Open Question RESOLVED). Record in VALIDATION.md as a manual human-verify item.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp runId into promotion_evidence (additive, no schema change)</name>
  <files>src/staging/payload.ts, src/staging/payload.test.ts, src/staging/types.ts</files>
  <read_first>
    - src/staging/payload.ts (`toIngestStagingPayload` / `toPayload` lines 16-73 — note the conditional-spread pattern for `discoveredAt` lines 55-60 and `sourceExternalId` lines 62-69)
    - src/staging/types.ts (`IngestStagingPayload.promotionEvidence` lines 16-27)
    - 09-PATTERNS.md section "src/staging/payload.ts (MODIFY — transform)"
  </read_first>
  <behavior>
    - Test: `toIngestStagingPayload(storedEvidence, { runId: "run-X" })` returns a payload whose `promotionEvidence.runId === "run-X"`, alongside the existing `discoveredAt`/`sourceUrl` keys.
    - Test: `toIngestStagingPayload(storedEvidence, {})` (no runId) omits the key entirely (no `runId: undefined`) — backward compatible with existing callers.
    - Test: existing assertions (discoveredAt present, sourceExternalId conditional, replayTimestamp) still pass — additive only.
    - Test: the serialized `promotionEvidence` contains no new column-shaped fields beyond `runId` (boundary: jsonb-only).
  </behavior>
  <action>
    Add an optional `runId?: string` to `ToIngestStagingPayloadOptions` (payload.ts:12-14). In `toPayload` (payload.ts:40-73), thread the `runId` through and stamp it into the base `promotionEvidence` object next to `discoveredAt`, using the SAME conditional-spread style already used for `discoveredAt` (payload.ts:55-60): only add the `runId` key when defined (no `runId: undefined`). Use camelCase `runId` (per 09-RESEARCH Open Question RESOLVED — consistency with the existing `discoveredAt` key in the same jsonb object). Update `IngestStagingPayload.promotionEvidence` in types.ts to add `readonly runId?: string` (purely additive; the staging SQL already serializes the whole object as `$8::jsonb`, postgres-staging-repository.ts:95,106 — no SQL change, NO new column, NO new table — RESUME-04 locked scope). Pass `options.runId` from `toIngestStagingPayload` into `toPayload`. No `any`/`as`; preserve `readonly` style.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/staging/payload.test.ts</automated>
  </verify>
  <done>promotionEvidence carries runId when provided and omits it when not; types updated additively; no SQL/schema change; all staging tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operator-configurable checkpoint S3 prefix in config</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts (s3 sub-schema lines 62-74; env mapping in loadConfig lines 106-121; `.default(...)`/`z.coerce` patterns e.g. sourceMaxPages line 32 / sourceRetryAttempts lines 42-46)
    - 09-PATTERNS.md section "src/config.ts (MODIFY — config)"
  </read_first>
  <behavior>
    - Test: `loadConfig` with no `S3_CHECKPOINT_PREFIX` set yields `config.s3.checkpointPrefix === "checkpoints"` (default).
    - Test: `loadConfig` with `S3_CHECKPOINT_PREFIX=cp` yields `config.s3.checkpointPrefix === "cp"`.
    - Test: an empty `S3_CHECKPOINT_PREFIX` (`""`) fails validation (min length 1) → ConfigError.
    - Test: `redactConfig` leaves `checkpointPrefix` visible (it is not a secret).
  </behavior>
  <action>
    Add `checkpointPrefix: z.string().min(1).default("checkpoints")` to the `s3` Zod object (config.ts:62-74), following the `.default(...)` pattern of `sourceMaxPages` (config.ts:32). Map the env var in `loadConfig`'s `s3` object (config.ts:110-117): `checkpointPrefix: source["S3_CHECKPOINT_PREFIX"]`. The default is the existing S3 bucket + a `checkpoints/` prefix (09-CONTEXT line 56) — the bucket is reused, only the prefix is new. The prefix is not a secret, so `redactConfig` (config.ts:150-163) does not need to redact it (confirm with a test). Do not introduce a new top-level config object; keep it under `s3` for cohesion with the bucket it shares. No `any`/`as`; no magic strings beyond the default literal.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/config.test.ts</automated>
  </verify>
  <done>checkpointPrefix defaults to "checkpoints", honors S3_CHECKPOINT_PREFIX, rejects empty, is not redacted; config tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetcher → server-2 (staging jsonb) | `promotion_evidence` is the cross-app contract surface; an additive key must not break server-2's reader or expand the schema. |
| env → config | `S3_CHECKPOINT_PREFIX` is operator-supplied; validated by Zod before use in S3 key construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | staging schema / cross-app contract | mitigate | `runId` is added ONLY to the existing `promotion_evidence` jsonb via conditional spread; no new column, table, or SQL change (RESUME-04 locked scope). Test asserts the staging SQL/payload shape is otherwise unchanged. Cross-app verification item (server-2 reads `promotion_evidence->>'runId'`) recorded in VALIDATION.md (ASVS L2 V1.11 — documented inter-service contract). |
| T-09-04 | Information Disclosure | runId value | accept | `runId` is a non-secret correlation id (`run-<iso>-<uuid>`, cli.ts:378-380); safe to persist in jsonb and logs. No PII/secret content. |
| T-09-05 | Tampering | S3_CHECKPOINT_PREFIX → S3 key | mitigate | Zod `min(1)` validation before the prefix reaches key construction; the slug builder (Plan 03) further constrains the full key to an S3-safe charset (ASVS L2 V5.1). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (config + payload edits only). |
</threat_model>

<verification>
- `pnpm exec vitest run src/staging/payload.test.ts src/config.test.ts` green.
- `pnpm run typecheck` clean.
- Manual cross-app item to log in VALIDATION.md: server-2 correlates staged rows via `promotion_evidence->>'runId'` (camelCase). If server-2 expects snake_case `run_id`, switch the single key.
</verification>

<success_criteria>
- promotion_evidence carries `runId` when provided, omitted otherwise; no schema/SQL change (RESUME-04).
- `checkpointPrefix` config field validated and defaulted; honors `S3_CHECKPOINT_PREFIX`.
- All existing staging/config tests still pass (additive-only).
</success_criteria>

<output>
Create `.planning/phases/09-checkpoint-and-resume/09-02-SUMMARY.md` when done.
</output>
