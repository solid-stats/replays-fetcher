---
phase: 03-raw-replay-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/storage/checksum.ts
  - src/storage/checksum.test.ts
  - src/storage/object-key.ts
  - src/storage/object-key.test.ts
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - TEST-02
must_haves:
  truths:
    - "Fetcher derives a SHA-256 checksum from raw replay bytes before choosing a final object key."
    - "Fetcher uses raw object keys exactly shaped as raw/sha256/<sha256>.ocap."
    - "Storage evidence can represent stored, skipped, conflict, and failed outcomes without staging or parser work."
  artifacts:
    - path: "src/storage/checksum.ts"
      provides: "SHA-256 checksum helper for raw replay bytes"
      exports: ["calculateSha256"]
    - path: "src/storage/object-key.ts"
      provides: "Deterministic raw object key generation"
      exports: ["toRawReplayObjectKey"]
    - path: "src/storage/types.ts"
      provides: "Raw storage request/result evidence contracts"
      exports: ["RawReplayStorageStatus", "RawReplayStorageEvidence", "RawReplayObjectIdentity"]
  key_links:
    - from: "src/storage/object-key.ts"
      to: "src/storage/checksum.ts"
      via: "object key accepts a precomputed checksum"
      pattern: "raw/sha256/"
    - from: "src/storage/types.ts"
      to: "src/discovery/types.ts"
      via: "storage evidence preserves ReplayCandidate source identity fields"
      pattern: "ReplayCandidate"
---

<objective>
Create the pure raw-storage contracts for Phase 3.

Purpose: Phase 3 decisions require checksum-first identity and deterministic raw object keys before any S3 mutation logic exists.
Output: Focused TypeScript helpers and colocated tests for checksum, object key generation, and storage evidence result shapes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@docs/integration-contract.md
@.planning/phases/03-raw-replay-storage/03-CONTEXT.md
@.planning/phases/02-source-discovery-and-dry-run/02-VERIFICATION.md

<interfaces>
From `src/discovery/types.ts`:
```typescript
export interface ReplayCandidate {
  readonly identity: { readonly filename: string };
  readonly metadata?: {
    readonly discoveredAt?: string;
    readonly missionText?: string;
    readonly serverId?: number;
    readonly world?: string;
  };
  readonly source: {
    readonly externalId?: string;
    readonly page?: number;
    readonly url: string;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify checksum and raw key behavior</name>
  <files>src/storage/checksum.test.ts, src/storage/object-key.test.ts</files>
  <behavior>
    - Test 1: `calculateSha256(new TextEncoder().encode("solid-stats"))` returns the Node crypto SHA-256 hex digest.
    - Test 2: `toRawReplayObjectKey("<64 lowercase hex chars>")` returns `raw/sha256/<sha256>.ocap` exactly, per Phase 3 locked decision.
    - Test 3: invalid checksum strings are rejected before an object key can be produced.
  </behavior>
  <action>Create failing colocated Vitest tests for checksum calculation and object-key generation. Tests must use deterministic in-memory bytes only and must not introduce S3, PostgreSQL, parser, staging, local replay-list, or run-once behavior.</action>
  <verify>
    <automated>pnpm test -- src/storage/checksum.test.ts src/storage/object-key.test.ts</automated>
  </verify>
  <done>Tests fail because the storage helpers do not exist yet, and every expected behavior references the locked Phase 3 object-key rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement storage identity contracts</name>
  <files>src/storage/types.ts, src/storage/checksum.ts, src/storage/object-key.ts, src/storage/checksum.test.ts, src/storage/object-key.test.ts</files>
  <behavior>
    - Test 1: checksum helper returns lowercase SHA-256 hex for Uint8Array input.
    - Test 2: key helper returns `raw/sha256/<sha256>.ocap` only after checksum validation.
    - Test 3: exported storage result types can represent `stored`, `skipped`, `conflict`, and `failed` statuses with checksum, objectKey, bucket, byteSize, fetchedAt, and source identity evidence.
  </behavior>
  <action>Implement `calculateSha256(bytes: Uint8Array): string` using Node `crypto`, `toRawReplayObjectKey(sha256: string): string`, and `src/storage/types.ts` contracts for raw storage evidence. The contracts must preserve `ReplayCandidate` source evidence and must not include staging rows, parse jobs, parser artifacts, product deduplication, or scheduled run summaries.</action>
  <verify>
    <automated>pnpm test -- src/storage/checksum.test.ts src/storage/object-key.test.ts && pnpm run typecheck</automated>
  </verify>
  <done>Checksum/key tests pass, TypeScript exports are available for downstream plans, and no code path can generate a non-`raw/sha256/<sha256>.ocap` key.</done>
</task>

</tasks>

<source_audit>
## Coverage

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Store raw replay files with checksum-backed idempotency | Plans 03-01 through 03-04 |
| REQ | STOR-01 raw prefix | Task 2 key helper |
| REQ | STOR-02 checksum | Task 2 checksum helper |
| REQ | STOR-03 evidence fields | Task 2 storage types |
| REQ | STOR-04 non-destructive identity rules | Task 2 status contracts, Plan 03-02 adapter |
| REQ | STOR-05 structured failures | Task 2 `failed` status contract, Plans 03-02/03-03 |
| REQ | TEST-02 local/mocked storage tests | Plan 03-02 |
| CONTEXT | raw/sha256/<sha256>.ocap | Task 2 |
| CONTEXT | checksum before final key | Task 2 |
| CONTEXT | colocated tests | Tasks 1-2 |
| CONTEXT | deferred staging/outbox/parser/run-once | Explicitly excluded in task actions |
| RESEARCH | S3 raw object prefix and checksum evidence | Task 2 contracts |
| RESEARCH | avoid unsafe deduplication | Status contracts only preserve evidence; product dedupe remains out of scope |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay bytes -> checksum helper | Untrusted replay bytes enter local hashing logic. |
| checksum -> object key | Computed identity becomes storage path material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `toRawReplayObjectKey` | mitigate | Validate checksum as exactly 64 lowercase hex characters before forming `raw/sha256/<sha256>.ocap`. |
| T-03-02 | Information disclosure | storage evidence types | mitigate | Store only source URL/ID, checksum, object key, size, timestamps, and status; do not add secrets or raw bytes to evidence objects. |
| T-03-03 | Elevation of privilege | Phase boundary | mitigate | Keep contracts free of staging/business-table/parser/job fields so downstream code cannot use these types as authority to promote records. |
</threat_model>

<verification>
Run `pnpm test -- src/storage/checksum.test.ts src/storage/object-key.test.ts && pnpm run typecheck`.
</verification>

<success_criteria>
Raw replay identity is deterministic, checksum-backed, covered by colocated tests, and ready for an idempotent S3 adapter.
</success_criteria>

<output>
After completion, create `.planning/phases/03-raw-replay-storage/03-01-SUMMARY.md`.
</output>
